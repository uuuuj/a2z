# 2026-04-13 — 프로젝트 초기 셋업

## 주제
기존 a2z-HYI 프로젝트에 **로직 흐름 문서화** + **git 원격 연결** + **개발 추적 체계** 도입.

## 배경
- 프로젝트에 문서가 전혀 없어 신규 참여자/QA/담당자가 코드를 직접 읽어야 함
- 개발 진행 이력, 담당자 피드백, 완료 여부를 체계적으로 관리할 필요
- 원격 저장소 연결 + 앞으로의 모든 개발을 기록하는 구조 필요

## 한 일

### 1. 로직 흐름 문서 72개 작성 (`docs/`)
- 48개 핸들러(버튼/이벤트) 전수 문서화
- 표준 11개 섹션 (개요/트리거/사전조건/흐름/분기/예외/상태변화/후행/링크/이력)
- 카테고리별 `_index.md` + 코드 레퍼런스 9개 + 최상위 가이드 5개
- 멀티 에이전트 토론(아키텍트/QA/기술문서 전문가)으로 구조 설계

### 2. Git 원격 연결
- 기존 원격 `HYI` 브랜치 → `X_HYI`로 rename (GitHub 웹 UI로 아카이브)
- 로컬 `git init` + `origin` 추가
- 초기 커밋 후 신규 `HYI` 브랜치로 푸시 (97 파일)

### 3. 개발 추적 체계 구축 (`docs/tracking/`)
- 4축 구조: FEEDBACK(입력) / TASKS(처리) / CHANGELOG(출력) / sessions(맥락)
- ID 체계: FB-xxx, T-xxx 상호 참조
- 담당자 피드백 원문 보존 원칙

### 4. 자동화 도구
- `CLAUDE.md` — 7개 작업 규칙 (R1~R7)
- `/commit` 슬래시 커맨드 — docs 동기화 + CHANGELOG/TASKS 갱신 + 커밋
- `.gitignore` 보강

## 영향 범위
- 코드 변경: 없음 (문서·설정만)
- 추가 파일: ~80개 (docs 72, tracking 5, CLAUDE.md, commit 커맨드, .gitignore 수정)

## 다음 작업 제안
- 첫 실제 기능 개발/버그 수정 시 `/commit` 플로우 테스트
- 담당자에게 저장소 링크 공유 → 첫 FB-001 수신
- 필요 시 `/push` 커맨드 추가 (현재 push는 수동)
- Stop 훅으로 세션 요약 자동 생성 도입 검토

## 참고 링크
- 저장소: https://github.com/uuuuj/a2z/tree/HYI
- 문서 진입점: [docs/README.md](../../README.md)
- 파이프라인: [docs/_pipeline.md](../../_pipeline.md)
