# 2026-07-03 — 가공도 EA 스틴트 죽은 코드 정리 + 문서 동기화

## 주제
2026-06-30~07-02 가공도 EA 작업(24커밋, 시행착오 다수)이 남긴 폐기 접근 잔재를 청소하고, 삭제로 어긋난 문서를 동기화한 마무리 세션.

## 배경 / 진행 방식
- 시작: 다중 에이전트 워크플로(파인더 2 = MfgDrawing·Dimensions+Pose / 후보별 3-렌즈 적대 검증 = 참조·실행경로·보존의도)로 죽은 코드 탐지.
- 1차 실행은 **fable ultracode**로 돌려 82에이전트·237만 토큰 소모 중 세션 한도 도달(검증 단계 일부 중단). 사용자 요청으로 **Opus로 전환**, 대규모 재실행 없이 확정 결과만 이어받아 수술적으로 마감.
- 원칙: **만장일치(3-렌즈) 확정 7건만 제거**. 검증이 중단된 후보는 손대지 않음(정밀도 우선).

## 한 일 (커밋 `a7031cb`)
제거한 죽은 코드 7건:
1. `PushMfgDimTextOutside`(가공도 치수 텍스트 역추정 수동 배치) — 호출부 0. `dc410d4` revert가 호출만 제거하고 본체를 남긴 잔재 + 깨진 XML doc.
2. `GenerateMfgDrawing2DAll`(옛 8×3 그리드 일괄 출력) + 3. `RenderMfgViewForDrawing`(셀 렌더 어댑터) — `cac4454` 재배선 후 전이적 dead. 현 경로 = `GenerateMfgDrawingManual`→`RenderMfgRowToViewArea`.
4. `DrawDimension`의 `mfgTextOutwardCanvas`/`mfgCanvasScale` 파라미터 + `[DimTextOut]` 블록 — 호출부가 인자 안 넘겨 가드 항상 false.
5. `MfgViewPose.MirrorAxis` — `40c60ec`가 유일 reader 제거해 write-only.
6. `MfgViewPose.RequiresPostSelectionRotation` — 배선된 적 없음.
7. 관련 stale 주석 + 삭제 메서드 참조 주석 정리.

문서 동기화:
- `docs/code-reference/form1-mfg-drawing.md` 전면 재작성 — 삭제 메서드 2개 제거, 신규 `ProbeAndRollLandscape`·`DrawMfgDimsAtScale` 추가, 앵커 라인 전수 갱신(대량 삭제로 드리프트), 라인수 2,455.
- `docs/기능/가공도/가공도 시트.md` — EA 두 뷰 가로화·상하 스왑·미러 반영, last_updated·이력 추가.
- `docs/tracking/CHANGELOG.md` — 정리 항목.

## 영향 범위
- 코드: `A2Z/Form1.MfgDrawing.cs`(-대량), `Form1.Dimensions.cs`, `Form1.DrawingSheets.cs`, `Form1.cs`, `Models/MfgViewPose.cs`. 107 삽입 / 495 삭제.
- **사용자 대면 동작 무변경** (순수 죽은 코드). MSBuild Debug 통과(에러 0), push 완료.

## 확정된 핵심 지식 ⭐
- **보존한 살아있는 코드**(삭제 금지): `ApplyParallelTextShift`(Dimensions.cs 정의, 제작도 DrawingSheets.cs가 사용) / `MirrorVertical`(2D 미러 적용) / `ShapeDrawingIds`(CaptureMfgSceneToViewArea가 소비) / `isEA·isEA3d·isEAUse180`(재활성 예정) / DiagLog 인프라.
- 삭제 방식: 대형 연속 블록(PushMfgDimTextOutside + 두 구형 메서드 ~400줄)은 앵커 기반 PowerShell(`[System.IO.File]` raw text, UTF-8 no-BOM·CRLF 보존)로 제거해 331줄 붙여넣기 회피. 소규모는 Edit. CR 2455/2455·git diff --check 클린으로 인코딩 보존 확인.

## 이어갈 지점 ⭐
1. **가공도 EA 두 뷰 실기 검증 대기** — 가로화·상하 스왑·미러·보조선·치수 텍스트가 사내 PDF에서 의도대로 나오는지. 커밋 `d6bdfa6`~`40c60ec` + 이번 문서 반영분.
2. **미검증 죽은 코드 후보 — 2차에서 grep 검증 완료** (2026-07-03, Opus 직접):
   - **추가 제거**: `mfgTotalOff`(비-_m)+maxTotalDist 루프(2번 할당·읽기 0, `_m`은 live), `BuildMfgSceneCore`·`BuildEaSecondaryScene`의 `availW`/`availH` 파라미터(호출부 값 전달하나 내부 참조 0), stale 주석 4곳(`MfgScreenRightPositive`·`ApplyAbsoluteCameraRoll`=미존재 심볼, '세로=오른쪽 조사 중'=왼쪽으로 종결, `btnMfgDrawing_Click`=삭제 버튼).
   - **보존(근거 확인)**: `isAboveWider`/`isLShape`(읽힘=live, isAboveWider는 항상 false라 단순화 여지만), `UsedMinusCamera`(스냅샷 판정에 읽힘), `CameraData` 스냅샷 블록·필드(T-036 재사용 계획), `OrientationAxis`/`OrientationAngle`(8필드 설계 모델), `alignExtToBaseline`(활성 실험 폴백), `struName`(진행 중 페이지 빌더 — STRU 슬롯 여지), `zoomRatio`(내부 사용).
3. **C-Mfg 페이지 배치 본진** (STATUS.md 기존 계획) — 5행 페이지 helper 갱신부터. 이번 정리와 독립.

## 참고 링크
- 커밋: `a7031cb` (죽은 코드 7건 + 문서)
- 코드 레퍼런스: `docs/code-reference/form1-mfg-drawing.md` (재작성)
- 직전 세션: `docs/tracking/sessions/2026-06-30-가공도-EA-치수배치.md`
