---
title: 가공도 수동 우선 재배선 계획 (v7)
status: PROPOSED (Codex 1·2·3·4·5·6차 검토 모두 반영, 사용자 컨펌 + Codex 7차 대기)
created: 2026-05-22
revised: 2026-07-21 (`ALL` 전용 출력 경로 삭제 반영)
parent: docs/리팩토링/가공도-템플릿-설계.md (v2.1 — SUPERSEDED)
브랜치: refactor/dead-code
관련: T-064 (STRU 일괄 출력), feedback_mfg_balloon_2026-05-19, feedback_isea_preservation
P1 commit: d8682b4 (2026-05-23, push 완료)

# 가공도 수동 우선 재배선 (Step C-Mfg-Rewire)

## 0. 변경 이력

### 2026-07-21 후속

`ALL` 버튼과 `ExportAllSheetsToPdfCore`가 삭제되어 자동 가공도 재배선 대상은 `ProcessSingleStruFull` 한 곳만 남았다.

### v6 → v7 (Codex 6차 검토 — 결과 객체 패턴 도입)

| # | 영역 | v6 | v7 |
|---|---|---|---|
| 🟡 NG → OK | 중복 MessageBox | **함수 내부 모든 MessageBox 제거 + `MfgDrawingResult` 객체 반환**. 호출부가 단일 통합 MessageBox |
| 🟡 try/finally 범위 (Codex 6차 #1) | 진입부 변경부가 finally 밖 → 예외 시 BOM·UI·캔버스 복원 X | **try 진입을 함수 최상단(템플릿 확인 직후)으로 이동** |
| 🟡 반환값 (Codex 6차 #2) | `int successPdfs`만 | **`MfgDrawingResult` 클래스 신설**: SuccessPdfs / InsufficientBomPdfs / TemplateMissing / BomRows / ExpectedBomRows / Warnings |
| 🟡 호출부 단일 메시지 (Codex 6차 #3) | 함수와 호출부 각자 알림 | **수동**: 결과 받아 단일 MessageBox (PDF 개수 + BOM 부족 + Warnings 합산). **자동**: DiagLog만 |
| 🟢 surplus snapshot (Codex 6차 #4) | 부족만 잡고 초과는 무시 | `bomSnapshot.Count != expectedBomRows` WARN + 초과 시 첫 15행만 사용 |
| 🟢 P4a 호출 위치 (Codex 6차 #5) | 모호 | **`ProcessSingleStruFull §8`의 일반 시트 루프 후 1회** (`ALL` 전용 경로는 2026-07-21 삭제) |
| 🟢 interactive 매개변수 | v6 신설 | **삭제** (결과 객체로 대체) |

### v5 → v6 (요약, 본문 통합됨)
interactive 매개변수, BOM snapshot 무결성, UI 잠금, P4 체크리스트 정정, 완료 메시지 BOM 부족 표시.

### v1~v5 누적 (요약)
v1 초안 → v2 (Codex 1차 10건) → v3 (Codex 재차 차단 1 + 보강 6) → v4 (사용자 BOM 사양 + Codex 3차 보강 4) → v5 (Codex 4차 차단 1 + 보강 5).

---

## 1. 배경

사용자 명시 방향 (2026-05-22, 2026-05-23):

> "자동 가공도 로직은 일단 폐기하고 수동으로 가공도 그리고 수동이 잘 나오면 그 수동 로직 함수 그대로 자동에서 호출"
> "작업/데이터 탭에서의 버튼들 다 없애줘"
> "BOM표량 도면정보는 제작도와 같이 붙여넣기만 하면돼"
> WARN 정책 = "Shape·Note·Measure 각각 try/catch (일부 실패어도 PDF 만듬)"
> BOM 부족 알림 = B (로그 + 출력 끝나고 요약 알림)
> UI 잠금 = 추가 (출력 중 시트 목록 비활성화)
> v7 = 결과 객체 도입

→ **5단계 순차 진행** (P1 완료, P2~P5 예정).

---

## 2. 의존성 분석

### 2.1 P1 완료 상태 (commit d8682b4, 2026-05-23)
모든 자동·수동 진입점 hard skip 완료, 호출자 grep 0건. CHANGELOG.md 2026-05-23 참조.

### 2.2 폐기 대상 (P4b 일괄 삭제)
- `GenerateMfgDrawing2DAll(List)` — Form1.MfgDrawing.cs:911, 호출자 0건
- `RenderMfgViewForDrawing(...)` — Form1.MfgDrawing.cs:1105, 호출자 0건

### 2.3 유지·재사용 함수
| 함수 | 위치 | 사용처 |
|---|---|---|
| `BuildMfgSceneCore` | Form1.MfgDrawing.cs:152 | ExecuteMfgDrawing + 수동 새 함수 재사용 |
| `MfgViewPose` | Models/MfgViewPose.cs | 위 |
| `_lastMfgViewPose` | Form1.cs:131 | LvDrawingSheet 시트 미리보기 (수동 새 함수 write 금지) |
| `ExecuteMfgDrawing` | Form1.MfgDrawing.cs:817 | LvDrawingSheet 시트 미리보기 |
| `RestoreAllPartsVisibility` | Form1.MfgDrawing.cs:37 | Drawing2D, GlobalViews, **GenerateMfgDrawingManual finally** |
| `CollectBOMInfo(bool, DrawingSheetData)` | Form1.Clash.cs:~24 | 제작도, **수동 새 함수 재사용** |
| `lvDrawingBOMInfo` ListView | 좌측 패널 | 제작도, **수동 새 함수가 snapshot으로 1회 복사** |
| `SanitizeFileName` | Form1.DrawingSheets.cs:1291 | 자동·수동 파일명 공용 |

### 2.4 신규 (P2-helpers에서 활용)
| 함수 | 역할 |
|---|---|
| `MfgPage` (nested) | rowsPerPage 5 |
| `SplitMfgIntoPages` | rowsPerPage 5 갱신 |
| `BuildMfgPageData` | `List<string[]> bomSnapshot` 매개변수 |
| `SnapshotBomRows` | `lvDrawingBOMInfo` 1회 복사 |
| `EnsureViewAreasCache` | View 영역 1회 캐시 (local dict 검증 후 대입) |
| `ResetCanvasForMfgPage` | 페이지 진입 초기화 |
| `GetDefaultDrawingSaveDir` | `Application.StartupPath/Drawings` |
| `MakeUniquePdfPath` | 충돌·sanitize·MAX_PATH |
| **`MfgDrawingResult`** (v7) | 결과 객체 |
| `GenerateMfgDrawingManual` (v7) | 통합 본체 (PDF 소유, 결과 객체 반환) |
| `RenderMfgRowToViewArea` | 행 1개 렌더 |

---

## 3. Phase별 commit 계획

### P1 ✅ 완료 (commit `d8682b4`)
CHANGELOG.md 2026-05-23 참조.

### P2. 수동 새 함수 작성 (3 commit)

#### P2-helpers (~40분)

**`SplitMfgIntoPages`** (rowsPerPage 4 → 5):
```csharp
private List<MfgPage> SplitMfgIntoPages(List<DrawingSheetData> mfgSheets, int rowsPerPage = 5)
{
    // 기존 로직 그대로
}
```

**`SnapshotBomRows`** (UI race 차단):
```csharp
private List<string[]> SnapshotBomRows()
{
    var rows = new List<string[]>();
    if (lvDrawingBOMInfo.Items.Count <= 1) return rows;

    for (int i = 1; i < lvDrawingBOMInfo.Items.Count; i++)
    {
        ListViewItem item = lvDrawingBOMInfo.Items[i];
        rows.Add(new string[]
        {
            item.Text,                      // [0] NO
            SafeSubItem(item, 1),           // [1] ITEM
            SafeSubItem(item, 2),           // [2] MATERIAL
            SafeSubItem(item, 3),           // [3] SIZE
            SafeSubItem(item, 4),           // [4] Q'TY
            SafeSubItem(item, 5),           // [5] T/W
            SafeSubItem(item, 6),           // [6] MA
            SafeSubItem(item, 7),           // [7] FA
        });
    }
    return rows;
}
```

**`BuildMfgPageData`** (제작도 방식 + snapshot 매개변수):
```csharp
private Dictionary<int, string> BuildMfgPageData(
    MfgPage page, int totalPages, string struName, List<string[]> bomSnapshot)
{
    var data = new Dictionary<int, string>();

    // 빈 슬롯 선초기화 (미치환 {Input_N} 노출 방지)
    for (int k = 1; k <= 129; k++)
        data[k] = "";

    // 도면정보
    data[1] = "CEDAR FLNG";
    data[2] = "SN2688";
    data[3] = totalPages > 1
        ? $"가공도 ({page.PageIdx}/{totalPages})"
        : "가공도";

    // 좌측 5행 BOM 이름 (Input_5~9)
    for (int i = 0; i < page.Rows.Count && i < 5; i++)
    {
        var sheet = page.Rows[i];
        if (sheet.MemberIndices.Count == 0) continue;
        var bom = bomList.FirstOrDefault(b => b.Index == sheet.MemberIndices[0]);
        if (bom == null) continue;
        data[5 + i] = bom.Name ?? "";
    }

    // 우측 BOM 표 8컬럼 × 15행 (Input_10~129, snapshot 사용)
    int bomMapped = 0;
    if (bomSnapshot != null)
    {
        int n = Math.Min(bomSnapshot.Count, 15);
        for (int i = 0; i < n; i++)
        {
            string[] row = bomSnapshot[i];
            data[10 + i]  = row[0];   // NO
            data[25 + i]  = row[1];   // ITEM
            data[40 + i]  = row[2];   // MATERIAL
            data[55 + i]  = row[3];   // SIZE
            data[70 + i]  = row[4];   // Q'TY
            data[85 + i]  = row[5];   // T/W
            data[100 + i] = row[6];   // MA
            data[115 + i] = row[7];   // FA
        }
        bomMapped = n;
    }
    DiagLog($"[BuildMfgPageData] p{page.PageIdx}/{totalPages} BOM 매핑 {bomMapped}행");

    return data;
}
```

**`EnsureViewAreasCache`** (local dict 검증 후 대입):
```csharp
private void EnsureViewAreasCache(
    ref Dictionary<int, VIZCore3D.NET.Data.TemplateViewArea> cache,
    string xlsxPath)
{
    if (cache != null) return;

    var list = vizcore3d.Drawing2D.Template.GetViewAreasFromExcel(xlsxPath);
    if (list == null || list.Count == 0)
        throw new InvalidOperationException($"가공도 템플릿 View 영역 없음: {xlsxPath}");

    var dict = list.ToDictionary(a => a.Index, a => a);
    for (int i = 1; i <= 5; i++)
        if (!dict.ContainsKey(i))
            throw new InvalidOperationException($"가공도 템플릿 View_{i} 누락: {xlsxPath}");

    cache = dict;
    DiagLog($"[EnsureViewAreasCache] View_1~View_5 캐시 완료");
}
```

**`ResetCanvasForMfgPage`** / **`GetDefaultDrawingSaveDir`** / **`MakeUniquePdfPath`** — v5~v6와 동일.

**v7 신설: `MfgDrawingResult` 클래스** (Codex 6차 권고):
```csharp
/// <summary>
/// 가공도 출력 결과 객체. 호출부가 받아 단일 MessageBox 또는 자동 요약 생성.
/// </summary>
private sealed class MfgDrawingResult
{
    public int SuccessPdfs;               // 성공 저장 PDF 수
    public int InsufficientBomPdfs;       // BOM 부족 상태에서 저장된 PDF 수
    public bool TemplateMissing;          // 가공도 엑셀 템플릿 누락 → PDF 0개
    public int BomRows;                   // 실제 snapshot BOM 행 수
    public int ExpectedBomRows;           // 예상 BOM 행 수 = Min(부재수, 15)
    public List<string> Warnings = new List<string>();   // 사용자에게 보일 경고 텍스트

    public bool HasIssues => Warnings.Count > 0 || InsufficientBomPdfs > 0 || TemplateMissing;
}
```

**P2-helpers 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] 호출자 0건 (P2-integrate 전)

#### P2-row — `RenderMfgRowToViewArea` (~45분)

(v5~v6와 동일: `DeleteObjectBy2DView` cleanup + `newScale` 가드 + Shape/Note/Measure 각각 try/catch + row clear/finally. 변경 없음.)

핵심 코드 패턴 (v5 §3 P2-row 본문 참조):
- 진입 cleanup (Clear Note/Measure/ShapeDrawing)
- `BuildMfgSceneCore` → pose 지역변수
- area·objW/H·curScale·newScale 모두 가드 (NaN/Infinity 포함)
- Shape/Note/Measure 각각 try/catch WARN (사용자 사양: 일부 실패해도 row 성공)
- finally: `!success && objId >= 0` 시 `DeleteObjectBy2DView(objId)`
- finally: Note/Measure/ShapeDrawing 종료 cleanup

**P2-row 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] sdk-verifier로 `DeleteObjectBy2DView` 시그니처 확인 (VIZCore3D.NET.xml:30372 확인됨)

#### P2-integrate — `GenerateMfgDrawingManual` + 버튼 재배선 (~50분)

**v7 본체**:
```csharp
/// <summary>
/// 가공도 수동 통합 함수 v7. PDF 소유. 결과 객체 반환.
/// 호출자:
///   - 수동: btnMfgDrawingSheet_Click → 단일 MessageBox로 결과 표시
///   - 자동: ProcessSingleStruFull §8 → DiagLog만, 상위 요약은 호출자 책임
/// 함수 내부 MessageBox 없음 (Codex 6차 권고).
/// </summary>
private MfgDrawingResult GenerateMfgDrawingManual(
    List<DrawingSheetData> mfgSheets,
    string saveDir,
    string struName,
    int struIndex = 0)
{
    var result = new MfgDrawingResult();
    if (mfgSheets == null || mfgSheets.Count == 0) return result;

    string xlsxPath = Path.Combine(GetSolutionPath(), "사용자템플릿_엑셀_가공도.xlsx");
    if (!File.Exists(xlsxPath))
    {
        DiagLog($"[GenMfgManual] 템플릿 누락: {xlsxPath}");
        result.TemplateMissing = true;
        result.Warnings.Add($"가공도 엑셀 템플릿 누락: {xlsxPath}");
        return result;
    }

    // v7 Codex 6차: try 진입 최상단 — 모든 mutable 작업 보호
    DrawingSheetData previousSelectedSheet = null;
    bool prevLvEnabled = lvDrawingSheet.Enabled;

    try
    {
        // UI 잠금 (가장 먼저)
        lvDrawingSheet.Enabled = false;

        if (lvDrawingSheet.SelectedItems.Count > 0)
            previousSelectedSheet = lvDrawingSheet.SelectedItems[0].Tag as DrawingSheetData;

        // ── 진입부 강제 초기화 8단계 ──
        vizcore3d.Review.Note.Clear();
        vizcore3d.Review.Measure.Clear();
        vizcore3d.ShapeDrawing.Clear();
        Clear2DView();
        if (vizcore3d.View.XRay.Enable) vizcore3d.View.XRay.Enable = false;
        vizcore3d.Object3D.Show(VIZCore3D.NET.Data.Object3DKind.ALL, true);
        vizcore3d.Object3D.Select(VIZCore3D.NET.Data.Object3dSelectionModes.DESELECT_ALL);
        vizcore3d.View.SetRenderMode(VIZCore3D.NET.Data.RenderModes.DASH_LINE);

        // ── BOM 표 1회 채우기 (가공도 전체 부재, 모든 페이지 동일) ──
        var allMfgBomIndices = mfgSheets
            .Where(s => s.MemberIndices.Count > 0)
            .Select(s => s.MemberIndices[0])
            .Distinct()
            .ToList();

        if (allMfgBomIndices.Count > 15)
        {
            string msg = $"가공도 부재 {allMfgBomIndices.Count}개 — BOM 표 15행 초과, 16번째 이후 PDF 미표시";
            DiagLog($"[GenMfgManual] WARN {msg}");
            result.Warnings.Add(msg);
        }

        var syntheticSheet = new DrawingSheetData
        {
            BaseMemberIndex = -3,
            BaseMemberName = "가공도_묶음",
            MemberIndices = allMfgBomIndices,
            MfgDrawingNo = 0
        };
        CollectBOMInfo(false, syntheticSheet);

        var bomSnapshot = SnapshotBomRows();
        int expectedBomRows = Math.Min(allMfgBomIndices.Count, 15);
        result.BomRows = bomSnapshot.Count;
        result.ExpectedBomRows = expectedBomRows;

        // v7 Codex 6차 #4: surplus·부족 모두 검출
        if (bomSnapshot.Count != expectedBomRows)
        {
            DiagLog($"[GenMfgManual] WARN BOM snapshot mismatch: {bomSnapshot.Count} vs 예상 {expectedBomRows}");
            if (bomSnapshot.Count > expectedBomRows)
                result.Warnings.Add($"BOM snapshot 초과 ({bomSnapshot.Count}행, 예상 {expectedBomRows}) — 첫 15행만 사용");
        }

        bool bomSnapshotInsufficient = bomSnapshot.Count < expectedBomRows;
        if (bomSnapshotInsufficient)
            DiagLog($"[GenMfgManual] WARN BOM 부족: {bomSnapshot.Count} < {expectedBomRows} (PDF 계속 생성)");

        var pages = SplitMfgIntoPages(mfgSheets, 5);
        Dictionary<int, VIZCore3D.NET.Data.TemplateViewArea> viewAreasCache = null;

        foreach (var page in pages)
        {
            int failedRows = 0;
            int successRows = 0;
            try
            {
                ResetCanvasForMfgPage();
                var data = BuildMfgPageData(page, pages.Count, struName, bomSnapshot);
                vizcore3d.Drawing2D.Template.ImportExcelWithData(xlsxPath, data);
                EnsureViewAreasCache(ref viewAreasCache, xlsxPath);

                for (int i = 0; i < page.Rows.Count; i++)
                {
                    var sheet = page.Rows[i];
                    if (sheet.MemberIndices.Count == 0) { failedRows++; continue; }
                    var bom = bomList.FirstOrDefault(b => b.Index == sheet.MemberIndices[0]);
                    if (bom == null) { failedRows++; continue; }

                    var area = viewAreasCache[i + 1];
                    if (RenderMfgRowToViewArea(i + 1, bom, area)) successRows++;
                    else failedRows++;
                }

                if (failedRows > 0)
                    DiagLog($"[GenMfgManual] p{page.PageIdx} WARN failed={failedRows} success={successRows}");

                if (successRows == 0)
                {
                    DiagLog($"[GenMfgManual] p{page.PageIdx} SKIP — 모든 row 실패");
                    result.Warnings.Add($"p{page.PageIdx} 페이지 모든 row 실패, PDF 미저장");
                    continue;
                }

                vizcore3d.Drawing2D.Render();
                vizcore3d.Drawing2D.Object2D.UnselectAllObjectBy2DView();
                vizcore3d.Drawing2D.Object2D.UnselectCurrentWorkObjectBy2DView();

                string pdfPath = MakeUniquePdfPath(saveDir, struName, page.PageIdx, pages.Count, struIndex);
                vizcore3d.Drawing2D.Object2D.Export2PDFBy2DView(pdfPath);
                result.SuccessPdfs++;
                if (bomSnapshotInsufficient) result.InsufficientBomPdfs++;

                DiagLog($"[GenMfgManual] p{page.PageIdx}/{pages.Count} 저장: {pdfPath}");
            }
            catch (Exception ex)
            {
                DiagLog($"[GenMfgManual] p{page.PageIdx} ERROR: {ex.Message}");
                result.Warnings.Add($"p{page.PageIdx} 페이지 ERROR: {ex.Message}");
            }
        }
    }
    catch (Exception ex)
    {
        DiagLog($"[GenMfgManual] FATAL: {ex.Message}");
        result.Warnings.Add($"가공도 출력 FATAL: {ex.Message}");
    }
    finally
    {
        // BOM UI 복원
        try
        {
            if (previousSelectedSheet != null)
            {
                CollectBOMInfo(false, previousSelectedSheet);
                DiagLog($"[GenMfgManual] BOM UI 복원: '{previousSelectedSheet.BaseMemberName}'");
            }
        }
        catch (Exception ex)
        {
            DiagLog($"[GenMfgManual] BOM UI 복원 실패: {ex.Message}");
        }
        // UI 잠금 해제
        lvDrawingSheet.Enabled = prevLvEnabled;
        // 전체 부재 가시성 복원
        try { RestoreAllPartsVisibility(); } catch { }
    }

    DiagLog($"[GenMfgManual] 완료 — Success={result.SuccessPdfs} BomShort={result.InsufficientBomPdfs} Warnings={result.Warnings.Count}");
    return result;
}
```

**`btnMfgDrawingSheet_Click` 재배선** (v7 — 단일 MessageBox 통합):
```csharp
private void btnMfgDrawingSheet_Click(object sender, EventArgs e)
{
    var mfgSheets = new List<DrawingSheetData>();
    foreach (ListViewItem lvi in lvDrawingSheet.Items)
        if (lvi.Text.StartsWith("가공도"))
        {
            var s = lvi.Tag as DrawingSheetData;
            if (s != null && s.MemberIndices.Count > 0) mfgSheets.Add(s);
        }

    if (mfgSheets.Count == 0)
    {
        MessageBox.Show("가공도 시트가 없습니다.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Warning);
        return;
    }

    string saveDir = GetDefaultDrawingSaveDir();
    var result = GenerateMfgDrawingManual(mfgSheets, saveDir, "manual", struIndex: 0);

    // v7 단일 MessageBox 통합 (Codex 6차 권고)
    if (result.TemplateMissing)
    {
        MessageBox.Show(
            $"가공도 엑셀 템플릿 누락:\n{Path.Combine(GetSolutionPath(), "사용자템플릿_엑셀_가공도.xlsx")}\n\nPDF 생성 안 됨.",
            "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
        return;
    }

    var sb = new System.Text.StringBuilder();
    sb.AppendLine($"가공도 PDF {result.SuccessPdfs}개 저장:");
    sb.AppendLine(saveDir);

    if (result.InsufficientBomPdfs > 0)
    {
        sb.AppendLine();
        sb.AppendLine($"⚠️ BOM 부족 PDF: {result.InsufficientBomPdfs}개");
        sb.AppendLine($"  (snapshot {result.BomRows}행 < 예상 {result.ExpectedBomRows}행)");
    }

    if (result.Warnings.Count > 0)
    {
        sb.AppendLine();
        sb.AppendLine("⚠️ 경고:");
        foreach (var w in result.Warnings)
            sb.AppendLine($"  · {w}");
    }

    var icon = result.HasIssues ? MessageBoxIcon.Warning : MessageBoxIcon.Information;
    MessageBox.Show(sb.ToString(), "가공도 출력 완료", MessageBoxButtons.OK, icon);
}
```

**P2-integrate 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] `rg "GenerateMfgDrawingManual" A2Z/` → 호출 1건 (btnMfgDrawingSheet_Click)
- [ ] **참고**: P4a 후엔 호출자가 3~4건

### P3. 사내 검증 (사용자 작업)

#### 합격 기준 (14개)
- 5행/페이지 레이아웃 / 도면정보·BOM 표 / 모든 페이지 BOM 동일 / 치수·풍선 / 페이지 분할 / 페이지·row·partial 잔재 없음 / 파일명 충돌 없음 / 저장 경로 / EA single-view / 자동 P1 hard skip 유지 / 가공도 16개 이상 시 경고 / BOM 복원 / Shape/Note/Measure 일부 실패해도 PDF 만듬 / 예외 0건

#### v7 추가 확인 항목
- 메시지박스가 한 번만 뜸 (BOM 부족이든 일반이든 통합 메시지박스 1회)
- 출력 도중 도면 시트 목록 비활성 (클릭 안 됨)
- BOM 부족 시 메시지박스에 "BOM 부족 PDF X개" 한 줄 + 경고 목록

### P4. 자동 재배선 + 옛 dead 코드 일괄 삭제 (2 commit)

#### P4a — 자동 진입점 재배선

**`ProcessSingleStruFull` §8** (일반 시트 루프 후 1회 호출):
```csharp
// P4a — 가공도 묶음 처리 (v7 결과 객체)
var mfgSheets = new List<DrawingSheetData>();
foreach (ListViewItem lvi in lvDrawingSheet.Items)
    if (lvi.Text.StartsWith("가공도"))
    {
        var s = lvi.Tag as DrawingSheetData;
        if (s != null && s.MemberIndices.Count > 0) mfgSheets.Add(s);
    }

if (mfgSheets.Count > 0)
{
    var mfgResult = GenerateMfgDrawingManual(mfgSheets, struSubDir, struNode.NodeName, struIndex);
    pdfCount += mfgResult.SuccessPdfs;
    DiagLog($"[P4a STRU] '{struNode.NodeName}' 가공도 Success={mfgResult.SuccessPdfs} BomShort={mfgResult.InsufficientBomPdfs} Warnings={mfgResult.Warnings.Count}");

    // 자동 경로: MessageBox 없음. 상위 STRU 일괄 호출자가 종합 요약을 책임
    // (사용자가 자동 출력 후 결과 폴더에서 가공도 PDF 개수 확인 + 로그 검토)
}
```

#### P4b — 옛 dead 코드 일괄 삭제
- `GenerateMfgDrawing2DAll(List)` 본체 제거 (~174줄)
- `RenderMfgViewForDrawing(...)` 본체 제거 (~132줄)

**P4 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] `rg "GenerateMfgDrawing2DAll|RenderMfgViewForDrawing" A2Z/` → 0건
- [ ] `rg "GenerateMfgDrawingManual" A2Z/` → 호출 **2건** (수동 + STRU)
- [ ] 자동·수동 동일 PDF 결과 (G2-final)

### P5. EA 부재 두 뷰 — 구현 완료, 실기 검증 대기

- `RenderMfgRowToViewArea`가 EA View 영역을 위·아래로 분할한다.
- 첫 번째 뷰는 최장축 치수를 생략하고, 두 번째 독립 카메라가 최장축 치수를 표시한다.
- 최장축 Z는 `X_MINUS + 화면축 90도`, 그 외는 `Z_MINUS`를 사용한다.
- 과거 T자형 출력 원인이던 두 번째 뷰의 추가 정렬 회전은 복원하지 않았다.
- EA 실물 모델 PDF에서 상하 뷰 방향과 길이 치수 위치 확인이 남아 있다.

---

## 4. 위험 + 롤백

| 위험 | 완화 |
|---|---|
| 🔴 P1 no-op 회피 (Codex 1차 #1) | **P1 완료됨** — Export2PDFBy2DView까지 hard skip |
| 🔴 row 간 누적 (Codex 1차 #4) | row try/finally Clear |
| 🔴 v3 빨간불 (POSSTART/POSEND) | **v4 폐기로 소멸** |
| 🔴 v4 차단 (DeleteObject SDK 메서드명) | **v5 `DeleteObjectBy2DView`로 교체** |
| 🟡 중복 MessageBox (Codex 6차 NG) | **v7 `MfgDrawingResult` 결과 객체 + 호출부 단일 메시지박스** |
| 🟡 try/finally 범위 (Codex 6차 #1) | **v7 try 진입 최상단** |
| 🟡 자동 일괄 MessageBox 차단 | **v6 interactive (v7 결과 객체로 대체)** |
| 🟡 BOM UI race | `SnapshotBomRows` 분리 |
| 🟡 출력 후 BOM UI 잔존 | finally 선택 시트 BOM 복원 |
| 🟡 출력 중 시트 클릭 race | UI 잠금 (`Enabled = false`) |
| 🟡 가공도 16개 이상 정보 손실 | Warnings 추가 + 호출부 메시지 |
| 🟡 BOM snapshot 무결성 | `expected` 비교 + surplus WARN (v7) |
| 🟡 `newScale` overflow | newScale 별도 가드 |
| 🟡 Shape/Note/Measure 일부 실패 | 각각 try/catch WARN (사용자 사양) |
| 🟡 미치환 `{Input_N}` | `data[1..129] = ""` 선초기화 |
| 🟡 partial 실패 잔재 | finally `DeleteObjectBy2DView` |
| 🟡 selection 이벤트 트리거 | P1 lvi.Selected 이전 skip |
| 🟡 ViewArea 누락 | `EnsureViewAreasCache` throw |
| 🟡 PDF 파일명 충돌 | `MakeUniquePdfPath` |
| 🟡 EA baseline 누락 | P5 전 G2-b, 후 G2-c |
| 🟢 LvDrawingSheet 미리보기 회귀 | `_lastMfgViewPose` write 금지 + finally `RestoreAllPartsVisibility` |
| 🟢 P4b dead 참조 잔존 | P4 grep 재검증 |
| ⏸️ `BeginUpdate/EndUpdate` 예외 | 별 REQ로 분리 |

**롤백**: 각 P commit 독립 → `git revert <hash>`. P1은 push 완료.

---

## 5. Codex 검토 사이클

| 라운드 | 상태 | 결과 |
|---|---|---|
| 1차 (v1 → v2) | 완료 | 수렴 6 + 지적 10 |
| 2·재차 (v2 → v3) | 완료 | 차단 1 + 보강 9 |
| 3차 (v3 → v4) | 완료 | 차단 1(POSSTART/POSEND, v4에서 자동 소멸) + 보강 4 |
| 4차 (v4 → v5) | 완료 | 차단 1(DeleteObject) + 보강 5 + 추가 4 |
| 5차 (v5 → v6) | 완료 | 차단 0 + 보강 2 + 사소 정정 2 |
| 6차 (v6 → v7) | 완료 | 차단 0 + 보강 3 + NG 1 (중복 MessageBox) → 결과 객체 패턴 도입 |
| 7차 (v7 검토) | **대기** | v7가 6차 권고 모두 해결했는지 확인 |

---

## 6. 작업 순서 (요약)

> **수동 진입점: 도면정보 탭의 큰 "가공도 출력" 버튼.**
> 작업/데이터 탭의 가공도 버튼은 P1에서 폐기 완료 (d8682b4).

```
[2026-05-23] P1 ✅ 완료 (d8682b4)
   ↓ (Codex 7차 + 사용자 컨펌)
P2-helpers    — 헬퍼 함수 + MfgDrawingResult 클래스
P2-row        — RenderMfgRowToViewArea
P2-integrate  — GenerateMfgDrawingManual (결과 객체 반환) + 수동 버튼 재배선 (단일 메시지박스)
   ↓ push
P3 사내 검증 (사용자, 14개 + v7 확인 3개)
   ↓
P4a — 자동 재배선 (ProcessSingleStruFull §8에서 가공도 모음 1회 호출, 결과 객체 받아 DiagLog만)
P4b — dead 코드 일괄 삭제
   ↓ push
[G2-final]
   ↓
G2-b → P5a → G2-c
```

총 commit (P5 제외, P1 완료): **5개 남음**.

---

## 7. 변경 이력

| 날짜 | 변경 | 작성 |
|---|---|---|
| 2026-05-22 | v1 초안 | HYI + Claude |
| 2026-05-22 | v2 — Codex 1차 10건 | HYI + Claude + Codex 1차 |
| 2026-05-23 | v3 — Codex 2·재차 차단 1/보강 6/추가 3 | HYI + Claude + Codex 재차 |
| 2026-05-23 | v4 — 사용자 BOM 사양 변경 + Codex 3차 보강 4 + P1 완료 | HYI + Claude + Codex 3차 |
| 2026-05-23 | v5 — Codex 4차 차단 1(DeleteObject→DeleteObjectBy2DView) + 보강 5 | HYI + Claude + Codex 4차 |
| 2026-05-23 | v6 — Codex 5차 보강 2(interactive + BOM 무결성) + 사용자 결정 (UI 잠금 + BOM 경고 방식) | HYI + Claude + Codex 5차 |
| 2026-05-23 | **v7 — Codex 6차 권고: `MfgDrawingResult` 결과 객체 도입 + try/finally 범위 최상단 + 호출부 단일 MessageBox 통합 + surplus snapshot WARN + P4 호출 위치 명시. v6의 `interactive` 매개변수 삭제 (결과 객체로 대체)** | HYI + Claude + Codex 6차 |
