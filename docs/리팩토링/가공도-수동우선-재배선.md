---
title: 가공도 수동 우선 재배선 계획 (v3)
status: PROPOSED (Codex 1·2·재차 검토 모두 반영, 사용자 컨펌 대기)
created: 2026-05-22
revised: 2026-05-23 (v3 — Codex 재검토 차단 1건 + 보강 6건 + 추가 발견 3건 모두 반영)
parent: docs/리팩토링/가공도-템플릿-설계.md (v2.1)
브랜치: refactor/dead-code (C-Mfg 후속, 새 흐름)
관련: T-064 (STRU 일괄 출력), feedback_mfg_balloon_2026-05-19, feedback_isea_preservation

# 가공도 수동 우선 재배선 (Step C-Mfg-Rewire)

## 0. 변경 이력

### v2 → v3 (Codex 재검토 반영)
| # | 영역 | v2 | v3 |
|---|---|---|---|
| 🔴 A1 | `TemplateViewArea` 키 타입 (차단급) | 본문에서 `Dictionary<int>` 선언 + `["View_1"]` 문자열 키 접근 모순 | **전부 `Dictionary<int, TemplateViewArea>` + `cache.ContainsKey(i)` + `cache[i+1]` 접근**. SDK 정식 `TemplateViewArea.Index = int` (XML 문서 확인) |
| 🟡 A2 | `saveDir` 자동·수동 정책 일치 | `Path.Combine(GetSolutionPath(), "Drawings")` | **`Path.Combine(Application.StartupPath, "Drawings")`** (자동 STRU 일괄 `Form1.Stru.cs:362` 패턴 일치) + 공통 helper `GetDefaultDrawingSaveDir()` 신설 |
| 🟡 A3 | `ParseSprefForMfgRow` UDA 캐싱 | 매 호출마다 `UDA.Keys` 조회 | **시그니처 `ParseSprefForMfgRow(int bomIdx, List<string> udaKeyList = null)`**, null이면 함수 진입 시 1회 조회 후 reuse |
| 🟡 (신규) | `struName` sanitize 누락 | raw `struName` 사용 | **`SanitizeFileName(struName)`** (Form1.DrawingSheets.cs:1291 helper 재사용) + 길이 clamp + Windows MAX_PATH 260자 진단 로그 |
| 🟡 D1 | `RenderMfgRowToViewArea` 반환값 처리 | bool 반환했으나 호출부 무시 | **`failedRows` 누적 + 페이지 정책 명시**. row 1개라도 실패 시 page-warning DiagLog. 페이지의 모든 row 실패면 export skip (성공 PDF로 카운트 X) |
| 🟡 D2 | EA G2-b/G2-c baseline 게이트 | P5에 명시 없음 | **§3 P3·P5 합격 기준에 명시** — "EA는 P5 전 single-view fallback까지만", "P5 진입 전 G2-b baseline, P5 후 G2-c 검증" |
| 🟡 B1 | commit 분할 입도 | P2a/P2b/P2c/P2d 4개 | **P2 통합** (P2a+P2b 합치고 P2c/P2d 분리 → 3개로 축소): `P2-helpers` + `P2-row` + `P2-integrate` |
| 🟢 C1 | §6 진입점 명시 | 다이어그램만 | **§6 헤더에 "수동 진입점: 도면정보 탭 큰 '가공도 출력' 버튼. 작업/데이터 탭 가공도 버튼은 폐기."** 한 줄 추가 |
| 🟢 C2 | §0 패치 표 내부 모순 | v2 §0 3건 모순 | **v2 패치 표 정정** (line 20 타입·line 22 경로·line 25 반환값 의미 설명 통일) |
| 🟢 C3 | "더블클릭" 용어 부정확 | "시트 더블클릭" | **"시트 선택"** (실제 `SelectedIndexChanged` 이벤트, Form1.cs:192) |
| 🟢 (신규) | commit 빌드 green 명시 | 없음 | **각 commit 끝 build green + grep 검증 체크리스트 명시** (P1, P2-row, P2-integrate, P4b) |
| 🟡 A4 | `RescaleObject` guard 범위 | `objW/H`, `curScale`만 | **`area.Width/Height`, `fitRatio` NaN/Infinity도 가드** |

### v1 → v2 (Codex 1차 10건, 본문 §3·§4에 통합 반영됨)
요약: P1 hard skip, row try/finally cleanup, ViewArea dict 접근, SPREF helper, saveDir T-064 정책, 파일명 unique, RescaleObject guard, 반환값 = 성공 PDF 수, btnMfgDrawingSheet P1 명확화.

---

## 1. 배경

사용자 명시 방향 (2026-05-22):

> "자동 가공도 로직은 일단 폐기하고 수동으로 가공도 그리고 수동이 잘 나오면 그 수동 로직 함수 그대로 자동에서 호출해서 사용하는걸 원해 — 그럼 자동 수동 따로 수정할 필요가 없잖아"

> "작업/데이터 탭에서의 버튼들 다 없애줘"

→ v2.1 원안의 **C-Mfg-5에서 자동·수동 통합 + PDF 소유 이전을 한 commit에서 처리**(위험 "높음")를 **순차 5단계로 분산** + **자동을 먼저 임시 비활성**해 수동 검증을 거친 뒤 자동 재배선.

### v2.1과 비교
| 영역 | v2.1 (parent) | v3 (본 계획) |
|---|---|---|
| 통합 시점 | C-Mfg-5 한 commit에서 자동·수동 동시 전환 | P1 (자동 비활성) → P2 (수동 작성) → P3 (검증) → P4 (자동 재배선) 순차 |
| 옛 자동 코드 | C-Mfg-5에서 일괄 제거 | P1에서 hard skip, P4에서 일괄 삭제 |
| 사내 검증 범위 | G2-a (일반) + G2-b (EA baseline) + G2-c (EA) | P3 (수동만) + P4 후 자동·수동 일치 + P5 EA 앞뒤 G2-b/G2-c |
| 작업/데이터 탭 UI | 미언급 | **btnMfgDrawing 버튼·핸들러 폐기** |
| Codex 견제 | 1·2차 수렴 | 1차(10건) + 2차(9건) + 재차(7건+추가 3건) 모두 반영 |

---

## 2. 의존성 분석

### 2.1 작업/데이터 탭 (`tabPageWork`) 가공도 관련

| 컨트롤 | 위치 (Designer.cs) | 핸들러 | 의존 함수 |
|---|---|---|---|
| `btnMfgDrawing` ("가공도 출력") | 76, 594, 602-611, 1445 | `btnMfgDrawing_Click` (Form1.MfgDrawing.cs:19-31) | `ExecuteMfgDrawing(bom.Index)` |

같은 패널의 `btnBalloonAdjust` ("풍선 위치 조정")는 가공도 무관 → 유지. **P1에서 Location/TabIndex 재정렬 포함**.

### 2.2 도면정보 탭 (`tabPageDrawing`) 가공도 관련

| 컨트롤 | 위치 (Designer.cs) | 핸들러 | 의존 함수 |
|---|---|---|---|
| `btnMfgDrawingSheet` ("가공도 출력") | 119, 1008, 1071-1079, 1459 | `btnMfgDrawingSheet_Click` (Form1.MfgDrawing.cs:909-929) | `GenerateMfgDrawing2DAll(mfgSheets)` |

이 버튼이 **수동 새 함수의 진입점**으로 유지됨 (사용자 결정).

### 2.3 자동 진입점 (가공도 묶음 호출)

| 위치 | 행동 | P1 처리 |
|---|---|---|
| `ProcessSingleStruFull` §8 (Form1.Stru.cs:740-790) | STRU 일괄 → 가공도 묶음 → PDF | **export 이전 hard skip + DiagLog (pdfCount 증가 X)** |
| `ExportAllSheetsToPdfCore` group=false (Form1.DrawingSheets.cs:1149-1218) | 가공도 시트도 시트별 분리 PDF | **`lvi.Selected = true` 트리거 이전 skip** + 아래 레거시 조건 제거 |
| `ExportAllSheetsToPdfCore` group=true (Form1.DrawingSheets.cs:1220-1273) | 가공도 묶음 1 PDF | **블록 전체 hard skip + DiagLog** |

### 2.4 LvDrawingSheet 시트 선택 미리보기 (유지)

가공도 시트(`BaseMemberIndex == -3`) **선택** 시 (`SelectedIndexChanged` 이벤트, Form1.cs:192 / Form1.DrawingSheets.cs:611):
- `ExecuteMfgDrawing(sheet.MemberIndices[0])` 호출 (3D 뷰 + 치수)
- `_lastMfgViewPose` 회전 재적용 (Form1.DrawingSheets.cs:675-696)

→ 이 흐름은 **유지** (P1·P4와 별개).

⚠️ **수동 새 함수(`GenerateMfgDrawingManual`)는 `_lastMfgViewPose`에 write 금지** — 시트 미리보기 흐름과 충돌 방지. `BuildMfgSceneCore` 호출 시 반환 pose는 지역변수만 사용.

### 2.5 폐기 대상 함수 (P1: dead 격리, P4: 일괄 삭제)

| 함수 | 위치 | 현재 호출자 | P1 후 호출자 |
|---|---|---|---|
| `GenerateMfgDrawing2DAll(List)` | Form1.MfgDrawing.cs:935 | btnMfgDrawingSheet_Click, ProcessSingleStruFull §8, ExportAllSheetsToPdfCore × 2 | **0건** |
| `RenderMfgViewForDrawing(row, col, bomIdx)` | Form1.MfgDrawing.cs:1129 | GenerateMfgDrawing2DAll | **0건** |

### 2.6 유지 함수

| 함수 | 위치 | 사용처 | 운명 |
|---|---|---|---|
| `BuildMfgSceneCore(bomIdx) → MfgViewPose` | Form1.MfgDrawing.cs:152 | ExecuteMfgDrawing, **수동 새 함수도 재사용** | **유지** |
| `MfgViewPose` 모델 | Models/MfgViewPose.cs | 위 | **유지** |
| `_lastMfgViewPose` 필드 | Form1.cs:131 | LvDrawingSheet 시트 선택 미리보기 회전 재적용 | **유지** (수동 새 함수 write 금지) |
| `ExecuteMfgDrawing(bomIdx)` | Form1.MfgDrawing.cs:831 | LvDrawingSheet 시트 선택 미리보기 | **유지** |
| `RestoreAllPartsVisibility()` | Form1.MfgDrawing.cs:37 | Drawing2D, GlobalViews, **GenerateMfgDrawingManual 종료 시 호출** | **유지** |
| `MfgDrawingNo` (BOMData) | Models.cs:138 | 시트 라벨 ("가공도_N") | **유지** |
| `SanitizeFileName(string)` | Form1.DrawingSheets.cs:1291 | 자동 파일명, **수동 MakeUniquePdfPath에서도 재사용** | **유지** |

### 2.7 신규 helper (현재 PoC, 호출자 0건)

| 함수 | 위치 | 운명 |
|---|---|---|
| `MfgPage` (nested) | Form1.MfgDrawing.cs:59 | **P2-helpers 활용** (rowsPerPage 5로 갱신) |
| `SplitMfgIntoPages` | Form1.MfgDrawing.cs:69 | **P2-helpers 활용** |
| `BuildMfgPageData` | Form1.MfgDrawing.cs:94 | **P2-helpers 활용** (PoC 슬롯 → v2.1 사양 + SPREF helper 통합) |

---

## 3. Phase별 commit 계획

### P1. 폐기 + 자동 임시 hard skip (1 commit)

**Designer.cs 변경**:
- `btnMfgDrawing` 컨트롤 4곳(선언·Controls.Add·정의 블록·필드) 제거
- `panelBOMButtons`에서 `btnBalloonAdjust`만 남음 → **Location/TabIndex 재정렬 포함**

**Form1.MfgDrawing.cs 변경**:
- `btnMfgDrawing_Click` 핸들러 메서드 제거 (line 19-31)
- `btnMfgDrawingSheet_Click` (line 909-929) 본문 교체:
  ```csharp
  private void btnMfgDrawingSheet_Click(object sender, EventArgs e)
  {
      DiagLog("[P1 no-op] btnMfgDrawingSheet_Click — 수동 새 함수 작성 중 (P2-integrate에서 활성화)");
      MessageBox.Show(
          "가공도 출력 흐름은 재설계 중입니다.\n" +
          "P2-integrate 완료 후 새 함수로 활성화됩니다.\n" +
          "(refactor/dead-code 브랜치 — 자동 STRU 일괄에서도 임시 누락)",
          "임시 비활성", MessageBoxButtons.OK, MessageBoxIcon.Information);
  }
  ```

**Form1.Stru.cs §8 (line 740-790)** 전체 블록 교체:
```csharp
// ─── 8) 가공도 묶음 처리 — P1 hard skip (refactor/dead-code) ───
// 옛 GenerateMfgDrawing2DAll + Export2PDFBy2DView 호출 제거.
// 수동 새 함수(GenerateMfgDrawingManual) 작성·검증 후 P4에서 재배선.
int mfgSkipCount = 0;
foreach (ListViewItem lvi in lvDrawingSheet.Items)
    if (lvi.Text.StartsWith("가공도")) mfgSkipCount++;
if (mfgSkipCount > 0)
    DiagLog($"[P1 no-op] STRU '{struNode.NodeName}' 가공도 묶음 {mfgSkipCount}건 hard skip (P4에서 재활성)");
// pdfCount 증가 X
```

**Form1.DrawingSheets.cs 변경**:

group=false (line 1149-1218) — `lvi.Selected = true` **이전**에 skip + 레거시 조건 제거:
```csharp
for (int i = 0; i < lvDrawingSheet.Items.Count; i++)
{
    ListViewItem lvi = lvDrawingSheet.Items[i];
    DrawingSheetData sheet = lvi.Tag as DrawingSheetData;
    if (sheet == null || sheet.MemberIndices.Count == 0) continue;

    string sheetLabel = lvi.Text;

    // P1 hard skip — 가공도는 selection 트리거 + export 모두 우회
    if (sheetLabel.StartsWith("가공도"))
    {
        DiagLog($"[P1 no-op] ALL PDF 가공도 시트 hard skip: {sheetLabel}");
        continue;
    }
    // 옛 `if (groupMfgSheets && sheetLabel.StartsWith("가공도")) continue;` 제거 — 위 hard skip이 흡수
    
    // ... 이하 기존 일반 시트 처리 (lvi.Selected = true부터)
```

group=true (line 1220-1273) 전체 블록 교체:
```csharp
if (groupMfgSheets)
{
    int mfgSkipCount = 0;
    foreach (ListViewItem lvi in lvDrawingSheet.Items)
        if (lvi.Text.StartsWith("가공도")) mfgSkipCount++;
    if (mfgSkipCount > 0)
        DiagLog($"[P1 no-op] ALL PDF 가공도 묶음 {mfgSkipCount}건 hard skip (P4에서 재활성)");
}
```

**P1 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] `rg -n "btnMfgDrawing[^S]" A2Z/` → 0건 (Sheet 가공도 버튼은 SUM)
- [ ] `rg -n "GenerateMfgDrawing2DAll" A2Z/` → 정의만 (호출 0건)
- [ ] `rg -n "RenderMfgViewForDrawing" A2Z/` → 정의만 (호출 0건)
- [ ] Form1 열기 → 작업/데이터 탭 가공도 버튼 사라짐, 풍선 위치 조정만 보임

**위험**: 낮음. **롤백**: `git revert <hash>`.

### P2. 수동 새 함수 작성 (3 commit)

#### P2-helpers (P2a+P2b 통합) (~40분)

신규/갱신 helper 모두 한 commit:

**`SplitMfgIntoPages` 갱신**:
```csharp
private List<MfgPage> SplitMfgIntoPages(List<DrawingSheetData> mfgSheets, int rowsPerPage = 5)
{
    // rowsPerPage 기본값 4 → 5 (v2.1 사양)
    // ... (기존 로직 유지)
}
```

**`BuildMfgPageData` 갱신** (v2.1 사양 슬롯 + SPREF helper):
```csharp
private Dictionary<int, string> BuildMfgPageData(MfgPage page, int totalPages, string struName)
{
    var data = new Dictionary<int, string>();
    data[1] = "CEDAR FLNG";
    data[2] = "SN2688";
    data[3] = "가공도";
    data[4] = totalPages > 1 ? $"{page.PageIdx}/{totalPages}" : "1/1";

    // UDA 키 1회 캐시 (Codex 재검토 A3)
    List<string> udaKeyList = null;
    try
    {
        var keys = vizcore3d.Object3D.UDA.Keys;
        if (keys != null && keys.Count > 0) udaKeyList = new List<string>(keys);
    }
    catch { }

    for (int i = 0; i < page.Rows.Count && i < 5; i++)
    {
        var sheet = page.Rows[i];
        if (sheet.MemberIndices.Count == 0) continue;
        int bomIdx = sheet.MemberIndices[0];
        var bom = bomList.FirstOrDefault(b => b.Index == bomIdx);
        if (bom == null) continue;

        var (item, material, size) = ParseSprefForMfgRow(bomIdx, udaKeyList);
        data[5 + i]  = bom.Name ?? "";   // Row1~5 BOM 이름
        data[10 + i] = item;              // Row1~5 ITEM
        data[15 + i] = material;          // Row1~5 MATERIAL
        data[20 + i] = size;              // Row1~5 SIZE
        data[25 + i] = "1";                // Row1~5 Q'TY (가공도는 1)
    }
    return data;
}
```

**`ParseSprefForMfgRow` 신설** (Codex 재검토 A3):
```csharp
/// <summary>
/// CollectBOMInfo (Form1.Clash.cs:86~) SPREF/MATREF/POSSTART/POSEND 파싱 로직 추출.
/// 가공도 BOM 표 한 행을 위한 (ITEM, MATERIAL, SIZE) 반환.
/// </summary>
/// <param name="bomIdx">BOMData.Index</param>
/// <param name="udaKeyList">호출자가 1회 캐시한 UDA 키 목록. null이면 함수가 1회 조회.</param>
private (string item, string material, string size) ParseSprefForMfgRow(
    int bomIdx, List<string> udaKeyList = null)
{
    if (udaKeyList == null)
    {
        try
        {
            var keys = vizcore3d.Object3D.UDA.Keys;
            if (keys != null && keys.Count > 0) udaKeyList = new List<string>(keys);
        }
        catch { }
    }
    if (udaKeyList == null) return ("", "", "");

    string sprefVal = "", matrefVal = "", posStartVal = "", posEndVal = "";
    int currentIdx = bomIdx;
    for (int depth = 0; depth < 10; depth++)
    {
        if (currentIdx < 0) break;
        foreach (string key in udaKeyList)
        {
            string keyUpper = key.Trim().ToUpper();
            try
            {
                var val = vizcore3d.Object3D.UDA.FromIndex(currentIdx, key);
                string valStr = (val != null) ? val.ToString().Trim() : "";
                if (keyUpper == "SPREF" && string.IsNullOrEmpty(sprefVal) && !string.IsNullOrEmpty(valStr)) sprefVal = valStr;
                else if (keyUpper == "MATREF" && string.IsNullOrEmpty(matrefVal) && !string.IsNullOrEmpty(valStr)) matrefVal = valStr;
                else if (keyUpper == "POSSTART" && string.IsNullOrEmpty(posStartVal) && !string.IsNullOrEmpty(valStr)) posStartVal = valStr;
                else if (keyUpper == "POSEND" && string.IsNullOrEmpty(posEndVal) && !string.IsNullOrEmpty(valStr)) posEndVal = valStr;
            }
            catch { }
        }
        if (!string.IsNullOrEmpty(sprefVal) && !string.IsNullOrEmpty(matrefVal)
            && !string.IsNullOrEmpty(posStartVal) && !string.IsNullOrEmpty(posEndVal)) break;
        try
        {
            var parentNode = vizcore3d.Object3D.FromIndex(currentIdx);
            if (parentNode == null || parentNode.ParentIndex == currentIdx) break;
            currentIdx = parentNode.ParentIndex;
        }
        catch { break; }
    }

    // SPREF 파싱 (CollectBOMInfo T-061 로직 그대로)
    string itemVal = "", sizeVal = "";
    if (!string.IsNullOrEmpty(sprefVal))
    {
        string sprefClean = sprefVal.StartsWith("/") ? sprefVal.Substring(1) : sprefVal;
        int slashIdx = sprefClean.IndexOf('/');
        int colonIdx = sprefClean.IndexOf(':');
        int splitIdx;
        if (slashIdx < 0 && colonIdx < 0) splitIdx = sprefClean.Length;
        else if (slashIdx < 0) splitIdx = colonIdx;
        else if (colonIdx < 0) splitIdx = slashIdx;
        else splitIdx = Math.Min(slashIdx, colonIdx);
        itemVal = sprefClean.Substring(0, splitIdx).Trim();
        if (splitIdx < sprefClean.Length) sizeVal = sprefClean.Substring(splitIdx + 1).Trim();
    }
    // POSSTART/POSEND 3D 거리 → SIZE 뒤에 "xLENGTH" (CollectBOMInfo 동일 로직)
    // ... (생략, 실제 구현 시 CollectBOMInfo line 168~ 복제)

    return (itemVal, matrefVal, sizeVal);
}
```

**`EnsureViewAreasCache` 신설** (Codex 재검토 A1 — int 키 통일):
```csharp
/// <summary>
/// 엑셀 템플릿에서 View_1~View_5 영역을 1회 캐시.
/// SDK 정식: TemplateViewArea.Index = int (1..5).
/// </summary>
private void EnsureViewAreasCache(
    ref Dictionary<int, VIZCore3D.NET.Data.TemplateViewArea> cache,
    string xlsxPath)
{
    if (cache != null) return;
    var list = vizcore3d.Drawing2D.Template.GetViewAreasFromExcel(xlsxPath);
    cache = list.ToDictionary(a => a.Index, a => a);
    for (int i = 1; i <= 5; i++)
        if (!cache.ContainsKey(i))
            throw new InvalidOperationException($"가공도 템플릿 누락: View_{i}");
}
```

**`ResetCanvasForMfgPage` 신설**:
```csharp
private void ResetCanvasForMfgPage()
{
    Clear2DView();
    vizcore3d.Drawing2D.View.SetCanvasSize(297, 210);
    vizcore3d.Drawing2D.View.SetSelectCanvas(1);
}
```

**`GetDefaultDrawingSaveDir` 신설** (Codex 재검토 A2):
```csharp
/// <summary>
/// 자동·수동 공통 PDF 저장 폴더. T-064 정책: Application.StartupPath/Drawings.
/// </summary>
private string GetDefaultDrawingSaveDir()
{
    string dir = Path.Combine(Application.StartupPath, "Drawings");
    if (!Directory.Exists(dir)) Directory.CreateDirectory(dir);
    return dir;
}
```

**`MakeUniquePdfPath` 신설** (Codex 재검토 신규 + A4):
```csharp
/// <summary>
/// PDF 파일명 충돌 방지 + sanitize + 길이 clamp + MAX_PATH 진단.
/// Form1.DrawingSheets.cs:1291 SanitizeFileName 재사용.
/// </summary>
private string MakeUniquePdfPath(string saveDir, string struName, int pageIdx, int totalPages, int struIndex)
{
    string safeName = SanitizeFileName(struName ?? "manual");
    // 길이 clamp — STRU 이름 너무 길면 자름 (Windows MAX_PATH 260자 대비)
    if (safeName.Length > 40) safeName = safeName.Substring(0, 40);

    string ts = DateTime.Now.ToString("yyyyMMdd_HHmmss_fff");
    string baseName = $"{safeName}_가공도_p{pageIdx}of{totalPages}_S{struIndex}_{ts}.pdf";
    string path = Path.Combine(saveDir, baseName);

    int n = 1;
    while (File.Exists(path))
    {
        string alt = $"{Path.GetFileNameWithoutExtension(baseName)}_{n}.pdf";
        path = Path.Combine(saveDir, alt);
        n++;
    }
    // Windows MAX_PATH 260자 진단
    if (path.Length > 240)
        DiagLog($"[MakeUniquePdfPath] WARN path 길이 {path.Length} (Windows MAX_PATH 260 임박): {path}");
    return path;
}
```

**P2-helpers 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] 호출자 0건 (P2-integrate 전)

#### P2-row — RenderMfgRowToViewArea 일반 부재 단일 뷰 (~40분)

(v2 §3 P2c와 동일, 단 Codex 재검토 A4 반영: `area.W/H`, `fitRatio` NaN/Infinity guard 추가)

```csharp
private bool RenderMfgRowToViewArea(int rowIdx, BOMData bom,
    VIZCore3D.NET.Data.TemplateViewArea area)
{
    // ── row 진입 cleanup ──
    vizcore3d.Review.Note.Clear();
    vizcore3d.Review.Measure.Clear();
    vizcore3d.ShapeDrawing.Clear();

    int objId = -1;
    try
    {
        // area 자체 가드 (Codex 재검토 A4)
        if (area.Width <= 0 || area.Height <= 0
            || float.IsNaN(area.Width) || float.IsNaN(area.Height)
            || float.IsInfinity(area.Width) || float.IsInfinity(area.Height))
        {
            DiagLog($"[RenderMfgRow] row={rowIdx} area 비정상 W={area.Width} H={area.Height}");
            return false;
        }

        var pose = BuildMfgSceneCore(bom.Index);

        vizcore3d.View.SetRenderMode(VIZCore3D.NET.Data.RenderModes.DASH_LINE);
        vizcore3d.View.SilhouetteEdge = true;
        vizcore3d.View.SilhouetteEdgeColor = Color.Green;
        vizcore3d.View.FlyToObject3d(new List<int> { bom.Index }, 1.25f);
        if (pose.ApplyZ90)
        {
            vizcore3d.View.ScreenAxisRotation.LockZAxis = false;
            vizcore3d.View.RotateCameraByScreenAxis(0, 0, 90);
            vizcore3d.View.FlyToObject3d(new List<int> { bom.Index }, 1.25f);
        }
        if (pose.ApplyR180)
        {
            vizcore3d.View.ScreenAxisRotation.LockZAxis = false;
            vizcore3d.View.RotateCameraByScreenAxis(0, 0, 180);
            vizcore3d.View.FlyToObject3d(new List<int> { bom.Index }, 1.25f);
        }

        // EA 마커 (P5까지 single-view fallback)
        if (IsAngleFromSpref(bom.Index))
            DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} EA 부재 — P5 전 single-view fallback");

        vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemLineWidth(2.0f);
        objId = vizcore3d.Drawing2D.Object2D.Create2DViewObjectWithModelHiddenLineAtCanvasOrigin(
            VIZCore3D.NET.Data.Drawing2D_ModelViewKind.CURRENT);
        if (objId < 0)
        {
            DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} 2D 캡처 실패 (objId<0)");
            return false;
        }

        // fit (objW/H, curScale, fitRatio guard — Codex 재검토 A4 확장)
        float objW = 0f, objH = 0f;
        vizcore3d.Drawing2D.Object2D.GetObjectSize(objId, ref objW, ref objH);
        if (objW <= 0 || objH <= 0 || float.IsNaN(objW) || float.IsNaN(objH))
        {
            DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} objSize 비정상 W={objW} H={objH}");
            return false;
        }
        float fitRatio = Math.Min(area.Width / objW, area.Height / objH);
        if (fitRatio <= 0 || float.IsNaN(fitRatio) || float.IsInfinity(fitRatio))
        {
            DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} fitRatio 비정상={fitRatio}");
            return false;
        }
        float curScale = vizcore3d.Drawing2D.Object2D.GetObjectScale(objId);
        if (curScale <= 0 || float.IsNaN(curScale))
        {
            DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} curScale 비정상={curScale}");
            return false;
        }
        vizcore3d.Drawing2D.Object2D.RescaleObject(objId, curScale * fitRatio);

        float cx = area.X + area.Width / 2f;
        float cy = area.Y + area.Height / 2f;
        vizcore3d.Drawing2D.Object2D.MoveObjectTo(objId, cx, cy);

        // ShapeDrawing/Note/Measure → 2D (v2 §3 P2c와 동일)
        if (pose.ShapeDrawingIds != null && pose.ShapeDrawingIds.Count > 0)
        {
            vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemLineWidth(0.1f);
            vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemLineType(VIZCore3D.NET.Data.Object2D_LineTypes.SOLID);
            vizcore3d.Drawing2D.Object2D.Add2DObjectFromShapeDrawing(pose.ShapeDrawingIds);
        }

        var noteIds = vizcore3d.Review.Note.Items.Select(n => n.ID).ToList();
        if (noteIds.Count > 0)
        {
            vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemTextHeight(3.5f);
            vizcore3d.Drawing2D.View.Add2DNoteFrom3DNote(noteIds.ToArray());
            vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemTextHeight(7f);
        }

        var measureIds = vizcore3d.Review.Measure.Items
            .Where(m => m.Visible).Select(m => m.ID).ToList();
        if (measureIds.Count > 0)
        {
            vizcore3d.Drawing2D.Object2D.Set2DViewCreateObjectItemMeasureLineWidth(0.5f);
            ApplyParallelTextShift(pose.ViewDirection,
                vizcore3d.Drawing2D.Object2D.GetObjectScale(objId),
                vizcore3d.Review.Measure.Items);
            vizcore3d.Drawing2D.Measure.Add2DMeasureFrom3DMeasure(measureIds.ToArray());
        }

        DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} OK objId={objId} fitRatio={fitRatio:F3}");
        return true;
    }
    catch (Exception ex)
    {
        DiagLog($"[RenderMfgRow] row={rowIdx} bom={bom.Index} ERROR: {ex.Message}");
        return false;
    }
    finally
    {
        // ── row 종료 cleanup ──
        vizcore3d.Review.Note.Clear();
        vizcore3d.Review.Measure.Clear();
        vizcore3d.ShapeDrawing.Clear();
    }
}
```

**P2-row 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] 호출자 0건 (P2-integrate 전)

#### P2-integrate — GenerateMfgDrawingManual + 버튼 재배선 (~40분)

```csharp
/// <summary>
/// 가공도 수동 통합 함수. PDF 소유.
/// 호출자: btnMfgDrawingSheet_Click (P2-integrate), ProcessSingleStruFull §8 (P4a), ExportAllSheetsToPdfCore (P4a).
/// 반환: 성공 저장 PDF 수.
/// row 정책 (Codex 재검토 D1):
///   - 행 1개라도 실패 시 page-warning DiagLog
///   - 페이지 모든 row 실패 시 export skip (성공 PDF 카운트 X)
/// </summary>
private int GenerateMfgDrawingManual(
    List<DrawingSheetData> mfgSheets,
    string saveDir,
    string struName,
    int struIndex = 0)
{
    if (mfgSheets == null || mfgSheets.Count == 0) return 0;

    string xlsxPath = Path.Combine(GetSolutionPath(), "사용자템플릿_엑셀_가공도.xlsx");
    if (!File.Exists(xlsxPath))
    {
        DiagLog($"[GenMfgManual] 템플릿 누락: {xlsxPath}");
        MessageBox.Show($"가공도 엑셀 템플릿 누락:\n{xlsxPath}", "오류");
        return 0;
    }

    // ── 진입부 강제 초기화 8단계 ──
    vizcore3d.Review.Note.Clear();
    vizcore3d.Review.Measure.Clear();
    vizcore3d.ShapeDrawing.Clear();
    Clear2DView();
    if (vizcore3d.View.XRay.Enable) vizcore3d.View.XRay.Enable = false;
    vizcore3d.Object3D.Show(VIZCore3D.NET.Data.Object3DKind.ALL, true);
    vizcore3d.Object3D.Select(VIZCore3D.NET.Data.Object3dSelectionModes.DESELECT_ALL);
    vizcore3d.View.SetRenderMode(VIZCore3D.NET.Data.RenderModes.DASH_LINE);

    var pages = SplitMfgIntoPages(mfgSheets, 5);
    Dictionary<int, VIZCore3D.NET.Data.TemplateViewArea> viewAreasCache = null;
    int successPdfs = 0;

    try
    {
        foreach (var page in pages)
        {
            int failedRows = 0;
            int successRows = 0;
            try
            {
                ResetCanvasForMfgPage();
                var data = BuildMfgPageData(page, pages.Count, struName);
                vizcore3d.Drawing2D.Template.ImportExcelWithData(xlsxPath, data);
                EnsureViewAreasCache(ref viewAreasCache, xlsxPath);

                for (int i = 0; i < page.Rows.Count; i++)
                {
                    var sheet = page.Rows[i];
                    if (sheet.MemberIndices.Count == 0) { failedRows++; continue; }
                    var bom = bomList.FirstOrDefault(b => b.Index == sheet.MemberIndices[0]);
                    if (bom == null) { failedRows++; continue; }

                    var area = viewAreasCache[i + 1];  // Codex 재검토 A1: int 키
                    if (RenderMfgRowToViewArea(i + 1, bom, area)) successRows++;
                    else failedRows++;
                }

                if (failedRows > 0)
                    DiagLog($"[GenMfgManual] p{page.PageIdx} WARN failed={failedRows} success={successRows}");

                // 페이지 모든 row 실패 시 export skip (Codex 재검토 D1)
                if (successRows == 0)
                {
                    DiagLog($"[GenMfgManual] p{page.PageIdx} SKIP — 모든 row 실패, PDF 미저장");
                    continue;
                }

                vizcore3d.Drawing2D.Render();
                vizcore3d.Drawing2D.Object2D.UnselectAllObjectBy2DView();
                vizcore3d.Drawing2D.Object2D.UnselectCurrentWorkObjectBy2DView();

                string pdfPath = MakeUniquePdfPath(saveDir, struName, page.PageIdx, pages.Count, struIndex);
                vizcore3d.Drawing2D.Object2D.Export2PDFBy2DView(pdfPath);
                successPdfs++;
                DiagLog($"[GenMfgManual] p{page.PageIdx}/{pages.Count} 저장: {pdfPath}");
            }
            catch (Exception ex)
            {
                DiagLog($"[GenMfgManual] p{page.PageIdx} ERROR: {ex.Message}");
            }
        }
    }
    finally
    {
        // ── 종료 cleanup (Codex 재검토 추가): 전체 부재 가시성 복원 ──
        RestoreAllPartsVisibility();
    }

    return successPdfs;
}
```

**`btnMfgDrawingSheet_Click` 재배선**:
```csharp
private void btnMfgDrawingSheet_Click(object sender, EventArgs e)
{
    var mfgSheets = new List<DrawingSheetData>();
    foreach (ListViewItem lvi in lvDrawingSheet.Items)
        if (lvi.Text.StartsWith("가공도"))
        {
            var s = lvi.Tag as DrawingSheetData;
            if (s != null && s.MemberIndices.Count > 0) mfgSheets.Add(s);
        }

    if (mfgSheets.Count == 0)
    {
        MessageBox.Show("가공도 시트가 없습니다.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Warning);
        return;
    }

    string saveDir = GetDefaultDrawingSaveDir();  // Codex 재검토 A2: 자동 정책 일치
    int pdfs = GenerateMfgDrawingManual(mfgSheets, saveDir, "manual", struIndex: 0);
    MessageBox.Show($"가공도 PDF {pdfs}개 저장:\n{saveDir}", "완료",
        MessageBoxButtons.OK, MessageBoxIcon.Information);
}
```

**P2-integrate 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] `rg -n "GenerateMfgDrawingManual" A2Z/` → 1 호출 (btnMfgDrawingSheet_Click)
- [ ] 사용자가 수동 클릭 가능 상태

### P3. 사내 검증 (사용자 작업)

#### 시나리오
1. 모델 열기 → 치수 추출 → STRU 체크 (자동 일괄은 P1에서 가공도 hard skip 상태)
2. 도면 시트 목록에서 가공도 시트 5개 이상 생성됐는지 확인
3. 도면정보 탭 **"가공도 출력" 버튼** (큰 버튼) 클릭
4. PDF 저장 폴더 확인: `{StartupPath}/Drawings/`

#### 합격 기준 (10개 — Codex 재검토 B 보강)
- [ ] 5행/페이지 레이아웃 (행별: BOM이름 25% : 모델 75% = 1:3)
- [ ] 우측에 BOM 표 + 도면정보 + 페이지 N/M (ITEM·MATERIAL·SIZE 모두 채워짐)
- [ ] 모델별 가로·세로 치수 + 홀/슬롯 풍선 표시
- [ ] 5부재 초과 시 페이지 분할 (예상 PDF 개수 = `ceil(N/5)`)
- [ ] 페이지 잔재 없음 (이전 페이지 모델 안 보임)
- [ ] row 간 잔재 없음 (이전 부재 치수·풍선 안 보임)
- [ ] PDF 파일명 충돌 없음 (연속 클릭 안전)
- [ ] 저장 경로 = `{StartupPath}/Drawings/{safeName}_가공도_p1of3_S0_{ts}.pdf`
- [ ] **EA 부재는 single-view fallback** (P5 전, 두 뷰 안 나옴 — DiagLog에서 EA marker 확인)
- [ ] 자동 STRU 일괄 출력 → 가공도 누락 (P1 hard skip 상태) + DiagLog "가공도 묶음 N건 hard skip"
- [ ] partial PDF 없음 (성공 PDF 수 = ceil(N/5)에서 실패 row 페이지 제외)
- [ ] 예외 0건

#### 불합격 시
- 수정 사항을 새 commit (P2 후속)로 추가
- P3 재검증

### P4. 자동 재배선 + 옛 dead 코드 일괄 삭제 (2 commit)

#### P4a — 자동 진입점 재배선

`ProcessSingleStruFull` §8: hard skip 제거 →
```csharp
// ─── 8) 가공도 묶음 처리 — P4a 재배선 ───
var mfgSheets = new List<DrawingSheetData>();
foreach (ListViewItem lvi in lvDrawingSheet.Items)
    if (lvi.Text.StartsWith("가공도"))
    {
        var s = lvi.Tag as DrawingSheetData;
        if (s != null && s.MemberIndices.Count > 0) mfgSheets.Add(s);
    }
if (mfgSheets.Count > 0)
{
    int mfgPdfs = GenerateMfgDrawingManual(mfgSheets, struSubDir, struNode.NodeName, struIndex);
    pdfCount += mfgPdfs;
}
```

`ExportAllSheetsToPdfCore` 가공도 분기:
- group=true: 묶음 처리 블록을 `GenerateMfgDrawingManual` 호출로 교체
- group=false: 가공도 1개씩 single-sheet 묶음(`List<DrawingSheetData> { sheet }`)으로 `GenerateMfgDrawingManual` 호출

#### P4b — 옛 dead 코드 삭제

- `GenerateMfgDrawing2DAll(List)` 본체 제거 (Form1.MfgDrawing.cs:935~1108)
- `RenderMfgViewForDrawing(row, col, bomIdx)` 본체 제거 (Form1.MfgDrawing.cs:1129~1261)
- 관련 주석/DiagLog 정리

**P4 빌드 green 체크리스트**:
- [ ] MSBuild Debug green
- [ ] `rg -n "GenerateMfgDrawing2DAll|RenderMfgViewForDrawing" A2Z/` → 0건
- [ ] 자동·수동 모두 동일 PDF 결과 (G2-final)

### P5. (선택) EA 부재 두 뷰

**G2-b (P5 진입 전 baseline)**: 일반 부재 + EA single-view fallback 상태 PDF 보존 (참조 기준).

**P5a — EA 두 뷰 (isEA 재활성)**: v2.1 §4.6 패턴.

**G2-c (P5 후 검증)**: EA 두 뷰 정확성 + 일반 부재 회귀 없음.

**P5b — ExecuteMfgDrawing/_lastMfgViewPose 정리 (YAGNI)**: 그대로 둠.

---

## 4. 위험 + 롤백

| 위험 | 완화 |
|---|---|
| 🔴 **A1 ViewArea 키 타입 오류** (Codex 재검토 차단급) | v3에서 `Dictionary<int, TemplateViewArea>` + `cache[i+1]`로 통일. SDK 정식 `TemplateViewArea.Index = int` |
| 🔴 **P1 no-op 회피로 엉뚱한 캔버스가 가공도 PDF로 저장** (Codex 1차 #1) | Export2PDFBy2DView 호출 줄 자체를 우회 (hard skip), pdfCount 증가 X |
| 🔴 **row 간 Note/Measure/ShapeDrawing 누적** (Codex 1차 #4) | RenderMfgRowToViewArea 진입·종료마다 Clear (try/finally) |
| 🟡 **D1 row 실패 무시 회귀** (Codex 재검토 D1) | failedRows 누적 + 페이지 정책 (모든 row 실패 시 export skip, partial PDF 카운트 X) |
| 🟡 saveDir 자동·수동 불일치 (Codex 재검토 A2) | `GetDefaultDrawingSaveDir()` 공통 helper (`Application.StartupPath/Drawings`) |
| 🟡 `struName` 파일명 특수문자·길이 (Codex 재검토 신규) | `SanitizeFileName` + 40자 clamp + MAX_PATH 240자 임박 WARN 로그 |
| 🟡 selection 이벤트가 ExecuteMfgDrawing 트리거 (Codex 1차 #2) | `lvi.Selected = true` 이전에 skip + 옛 레거시 조건 제거 |
| 🟡 ViewArea 누락 (View_1~5 일부 없음) | `EnsureViewAreasCache`에서 즉시 throw + DiagLog |
| 🟡 SPREF helper 누락으로 BOM 표 빈칸 | P2-helpers에 `ParseSprefForMfgRow` 필수 포함 |
| 🟡 UDA 키 호출 N번 누수 (Codex 재검토 A3) | `ParseSprefForMfgRow(bomIdx, udaKeyList = null)` 옵션 + `BuildMfgPageData`에서 1회 캐시 |
| 🟡 EA baseline 게이트 누락 (Codex 재검토 D2) | P5 진입 전 G2-b baseline, P5 후 G2-c 명시 |
| 🟡 PDF 파일명 충돌 (Codex 1차 #8) | `yyyyMMdd_HHmmss_fff` + struIndex + suffix |
| 🟡 RescaleObject NaN/0 + area NaN (Codex 1차 #9 + 재검토 A4) | objW/H, curScale, area.W/H, fitRatio 모두 가드 |
| 🟢 페이지 간 잔재 | 페이지마다 `ResetCanvasForMfgPage` |
| 🟢 ImportExcelWithData/GetViewAreasFromExcel SDK 시그니처 미스매치 | P2-helpers 진입 전 sdk-verifier 호출 |
| 🟢 LvDrawingSheet 시트 선택 미리보기 회귀 | `_lastMfgViewPose` write 금지 계약 + `GenerateMfgDrawingManual` 종료 시 `RestoreAllPartsVisibility()` |
| 🟢 옛 dead 코드 P4b 삭제 시 참조 잔존 | P4 진입 전 grep 재검증 |

**롤백 일반**: 각 P commit 독립 → `git revert <hash>` 한 줄.

---

## 5. Codex 검토 사이클

| 라운드 | 상태 | 결과 |
|---|---|---|
| 1차 (v1 → v2) | 완료 | 수렴 6건, 지적 10건, "v2 필요" |
| 2차 (v2 → v3) | 완료 | 수렴 6건, 지적 4건 + 신규 5건, "v3 필요" |
| 재차 (v2 fresh) | 완료 | 차단급 1건 + 보강 6건 + 추가 3건 → v3 patch |
| 3차 (v3 검토) | 대기 | v3가 모든 지적을 해결했는지 최종 검토 후 사용자 컨펌 |

---

## 6. 작업 순서 (요약)

> **수동 진입점: 도면정보 탭의 큰 "가공도 출력" 버튼.**
> 작업/데이터 탭의 가공도 버튼(`btnMfgDrawing`)은 P1에서 폐기.

```
[현재] refactor/dead-code @ 0edcbbc
   ↓ (Codex 3차 검토 + 사용자 컨펌)
P1            (1 commit) — UI 폐기 + 자동·수동 진입점 hard skip
   ↓ build green + grep 0건 확인
P2-helpers    (1 commit) — Split/BuildPageData(5행) + ParseSpref + ViewAreaCache(int) + ResetCanvas + GetDefaultSaveDir + MakeUniquePdfPath
   ↓ build green
P2-row        (1 commit) — RenderMfgRowToViewArea (row clear/finally, guard 확장)
   ↓ build green
P2-integrate  (1 commit) — GenerateMfgDrawingManual + btnMfgDrawingSheet_Click 재배선
   ↓ push
P3 사내 검증 (사용자, 12개 항목)
   ↓ 합격 후
P4a (1 commit) — 자동 진입점 재배선
P4b (1 commit) — 옛 dead 코드 (GenerateMfgDrawing2DAll + RenderMfgViewForDrawing) 일괄 삭제
   ↓ push
[G2-final] 자동·수동 일치 확인
   ↓
[G2-b baseline] — P5 진입 전 일반 + EA single-view PDF 보존
P5a (별 commit) — EA 두 뷰 (위험 매우 높음)
[G2-c] — EA 두 뷰 정확성 검증
P5b (YAGNI) — ExecuteMfgDrawing 그대로 둠
```

총 commit 수 (P5 제외): **6개** (P1, P2-helpers, P2-row, P2-integrate, P4a, P4b).
예상 작업량: 약 6~9시간 + 사내 검증.

---

## 7. 변경 이력

| 날짜 | 변경 | 작성 |
|---|---|---|
| 2026-05-22 | v1 초안 — 사용자 방향 반영 | HYI + Claude |
| 2026-05-22 | v2 — Codex 1차 10건 반영 | HYI + Claude + Codex 1차 |
| 2026-05-23 | v3 — Codex 2차 9건 + 재검토 차단 1건/보강 6건/추가 3건 모두 반영. `Application.StartupPath/Drawings` saveDir 통일, ViewArea int 키, SanitizeFileName + 길이 clamp, failedRows 정책, EA G2-b/G2-c, commit 분할 3개로 축소, 빌드 green 체크리스트, "더블클릭" → "시트 선택" 용어 정정 | HYI + Claude + Codex 재차 |
