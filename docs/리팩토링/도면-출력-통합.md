---
title: 도면 출력 경로 통합 리팩토링 계획 (v3.1)
status: APPROVED (Codex "수렴" 선언, 사용자 컨펌 대기)
created: 2026-05-18
revised: 2026-05-18 (v3.1 — Codex 1·2·3·4·5차 검토 모두 반영, 5차 "수렴")
author: HYI + Claude (Opus 4.7) + reviewer_codex (OpenAI Codex CLI, 5-round)
관련: T-064 (STRU 일괄 출력) / T-028 (치수 4경로 통합) / T-032 (Osnap 캐시) / T-036 (가공도 카메라 회전 보존)
브랜치: refactor/dead-code (Phase A 완료, 이어서 진행)
---

# 도면 출력 경로 통합 리팩토링 계획 (v3)

## 0.1. v3 → v3.1 추가 patch (Codex 4·5차 반영)

| # | 영역 | v3 | v3.1 |
|---|---|---|---|
| P1 | D1 commit 순도 | 단독 commit | **D1 전 별도 hygiene commit** — `.gitignore`에 `docs/리팩토링/_baseline/` 추가 |
| P2 | Step B 분할 | 3-commit (B1·B2·B3) | **4-commit** — B1a(skeleton compile, `MfgViewPose` + `BuildMfgSceneCore` 빈 스텁) → B1b(추출) → B2(Execute 어댑터) → B3(Render 어댑터) |
| P3 | `_lastMfgViewPose` write scope | 명시 안 함 | **`ExecuteMfgDrawing`만 갱신** — `RenderMfgViewForDrawing`는 read X, 지역변수만 |
| P4 | G2-G3 사이 smoke | 누락 | **smoke 검증 추가** — 가공도 일괄 직후 일반/설치도 시트 출력 → RenderMode·XRay·SilhouetteEdge·Note/Measure/Shape 잔존 오염 확인 |
| P5 | Pre-0 메타데이터 | 6종 | **+4종** — `lvDrawingBOMInfo` row count/hash, `chainDimensionList` count/hash, 선택 sheet 번호/base/member count, 2D canvas size/object count |
| P6 | hygiene commit 분리 | 미명시 | **기능 commit과 분리** — Designer 라벨, 매뉴얼, STATUS.md, `.gitignore`는 별도 |
| P7 | Pre-0 캡처 메타 (5차) | 메타데이터만 | **+ 재현 정보** — 입력 파일, 산출물 경로, PDF/이미지명, 날짜, 브랜치/커밋 해시 |
| P8 | D1 사전 점검 (5차) | 미명시 | **비정적 참조 확인** — reflection / designer event / CAD callback / serialization name binding으로 삭제 대상이 묶여있지 않은지 1회 |

---

## 0. v2 → v3 변경 요약

Codex 2차·3차 검토에서 v2의 6개 빈틈 + 세부 설계 6개 답변을 받아 모두 반영.

| 변경 영역 | v2 | v3 |
|---|---|---|
| **Step 순서** | A → B → C → D → E → F | **Pre-0 → D1(BOM sheetOverride) → E1(Osnap 명시) → A → B1·B2·B3(Mfg 3-commit) → C1·C2(Excel/Legacy 분리) → F** |
| **CameraSnapshot** | 단순 카메라 상태 | **`MfgViewPose`** (8필드: CameraData, ViewDirection, LongestAxis, CameraDirection enum, UsedMinusCamera, ApplyZ90, ApplyR180, **OrientationAngle**) |
| **공통 코어 시그니처** | `static MfgScene.Build(vc, bomIndex, bom, style)` | **`DrawingContext` 주입** + `BuildMfgSceneCore`는 1차 Form1 internal 메서드, 2차에서 별도 클래스로 승격 |
| **CollectBOMInfo 변경** | 새 sheetOverride 인자 추가 | **이미 존재** (`Form1.Clash.cs:20`) — 호출처 3곳(652/1376/1710)만 sheet 전달로 수정 |
| **Step C** | 단일 통합 | **`SheetTemplateMode` enum 인자** + `LegacyGridRenderer` / `ExcelTemplateRenderer` 분리 (치수·BOM·pose는 공유, 배치만 분리) |
| **.csproj** | 누락 | **명시 Compile Include 필수** — 폴더 신설 commit에 포함 |
| **R12 검증 게이트** | 매 Step | **3 게이트로 묶음**: BOM+Osnap 안정화 / Mfg 코어 완료 / Sheet 코어 완료 |
| **Pre-0 캡처** | PDF + CSV | **+ 메타데이터**: DiagLog, bomList.Count, xraySelectedNodeIndices.Count, Note/Measure/ShapeDrawing.Count, RenderMode, XRay.Enable |
| **namespace 분리** | 초기 `A2Z.Drawing` | **`A2Z` 유지** — namespace 분리는 최종 단계 |
| **legacy 경로** | 즉시 제거 후보 | **fallback 가치 유지** — 즉시 제거 금지 (Codex Q4 확신 없음) |

---

## 1. 목표 (불변)

> "수동으로 디버깅, 자동으로 실무" — 동일 입력 → 양쪽 동일 결과 (PDF + 치수 + 화면)

원칙: DRY + SRP. 위험 회피: 베이스라인 회귀 비교 + Step별 commit + Codex 게이트.

---

## 2. Codex 발견 + Claude 검증 사실

| # | 사실 | 위치 | 검증 |
|---|---|---|---|
| F1 | 자동 가공도는 `RenderMfgViewForDrawing` 호출 (Execute 아님) | Form1.MfgDrawing.cs:739 | ✅ |
| F2 | `noteIds.Clear()`로 PDF 풍선 비활성화 | Form1.MfgDrawing.cs:1584 | ✅ |
| F3 | 수동 `SMOOTH` 파란 vs 자동 `DASH_LINE` Cyan | :155/:322-326 vs :907/:1071-1076 | ✅ |
| F4 | `_mfgDrawingZ90Applied` 등 전역 필드 부작용 | Form1.cs / Form1.DrawingSheets.cs:663-679 | ✅ |
| F5 | `CollectBOMInfo(false)`가 `lvDrawingSheet.SelectedItems[0]` 의존 | Form1.Clash.cs:40-43 | ✅ |
| F6 | `CollectBOMInfo` 시그니처에 `sheetOverride` **이미 존재** | Form1.Clash.cs:20 | ✅ (Claude 분석 정정) |
| F7 | `UseExcelTemplate = true`가 기본값, Excel 경로가 실질 본진 | Form1.DrawingSheets.cs:1291, :1300 | ✅ |
| F8 | csproj는 명시 `<Compile Include=...>` 방식 (SDK style 아님) | A2Z.csproj:55~ | ✅ |
| F9 | `CameraData`는 `ScreenAxisRotation` 미포함 — pose에 별도 필드 필요 | Form1.cs:129 (Codex) | ✅ |

---

## 3. 단계 구조 (Codex 권고 순서)

### Pre-0 — 베이스라인 캡처 + 보강 (코드 변경 0)

**산출물**:
- 동일 STRU·동일 부재로 수동·자동 양쪽 캡처
  - PDF (가공도·제작도·조립도·설치도)
  - lvDimension CSV
  - 3D 뷰 스크린샷
  - **추가 메타데이터**: DiagLog 전문, `bomList.Count`, `xraySelectedNodeIndices.Count`, `Note/Measure/ShapeDrawing.Count`, `RenderMode`, `XRay.Enable` 상태 스냅샷
- 저장: `docs/리팩토링/_baseline/2026-05-18/` (**`.gitignore`에 추가**)

**검증 기준**: 각 Step 후 같은 시나리오 재캡처 → 픽셀 diff + 메타데이터 diff

### Step D1 — `CollectBOMInfo` 호출처 명시화 (Step A 선행 조건)
**현황**: 3곳에서 `CollectBOMInfo(false)` — `lvDrawingSheet.SelectedItems[0]` 의존
- `Form1.DrawingSheets.cs:652` (LvDrawingSheet_SelectedIndexChanged)
- `Form1.DrawingSheets.cs:1376` (GenerateSheetDrawing2D legacy)
- `Form1.DrawingSheets.cs:1710` (GenerateSheetDrawing2D Excel)

**변경**: 각 함수 인자/지역 변수 `sheet`를 `CollectBOMInfo(false, sheet)`로 명시 전달

**영향 파일**: Form1.DrawingSheets.cs (3곳)
**T-028 충돌**: 없음 (`chainDimensionList` 알고리즘 불변, scope만 명확화)
**검증**: 수동 경로 BOM 정보 변화 없음, 자동 경로 BOM 정확성 확보

### Step E1 — Osnap 캐시 명시 전달 (Step C 전제)
**현황**: `_lastCollectedNodeOsnapMap` 캐시가 `CompleteMainDimensionPostClash`(:428)에서만 사용. 시트 선택(:614)·2D 출력(:1358)·Excel(:1692)는 `preBuiltMap=null` → 시트마다 재수집

**변경**:
- 3곳에서 `_lastCollectedNodeOsnapMap` 전달
- 캐시 유효성 검사 — STRU/세대 카운터 도입 (만료 시 null fallback)

**영향 파일**: Form1.DrawingSheets.cs (3곳), Form1.cs (세대 카운터 필드)
**효과**: H3 성능 핫스팟 해소 (시트당 1~3초 단축)

### Step A — `ApplySheetSelection(sheet)` 추출
**전제**: D1·E1 완료 (BOM·캐시가 명시 인자 흐름)

**변경**:
- `LvDrawingSheet_SelectedIndexChanged` (Form1.DrawingSheets.cs:517~692) 본체를 `public void ApplySheetSelection(DrawingSheetData sheet)`로 추출
- 핸들러는 thin wrapper (1줄)
- `ProcessSingleStruFull` 루프(Form1.Stru.cs:643~648): `lvi.Selected=true` + `Thread.Sleep(200)` 삭제 → `ApplySheetSelection(sheet)` 직접 호출
- lvDrawingSheet UI 선택 표시는 별도 (단순 highlight)

**위험**: `sender`/`EventArgs` 의존 검증 (현재 미사용 추정)
**검증**: 수동 경로 동작 동일 + 자동 경로 시간 단축 (시트 N개 × 200ms 누적 제거)

### Step B1 — `MfgViewPose` + `BuildMfgSceneCore` 추출 (commit 1/3)

**`MfgViewPose` 정의** (Codex Q1 답변 반영, 8필드):
```csharp
internal sealed class MfgViewPose
{
    public VIZCore3D.NET.Data.CameraData CameraData { get; set; }
    public string ViewDirection { get; set; }              // "X"/"Y"/"Z"
    public string LongestAxis { get; set; }
    public VIZCore3D.NET.Data.CameraDirection CameraDirection { get; set; }  // X_PLUS, X_MINUS 등
    public bool UsedMinusCamera { get; set; }
    public bool ApplyZ90 { get; set; }
    public bool ApplyR180 { get; set; }
    public float OrientationAngle { get; set; }            // ApplyOrientationRotation 보존

    public bool RequiresPostSelectionRotation
    {
        get { return ApplyZ90 || ApplyR180; }              // 파생 property
    }
}
```

**변경**:
- `Models.cs` 또는 신규 `Models/MfgViewPose.cs`에 정의
- `BuildMfgSceneCore(int bomIndex)` — Form1 internal 메서드로 추출 (1차는 별도 클래스 X)
  - 부재 격리·BBox·축 판별·Osnap 수집·치수 계산·풍선·EA 분기(현재 isEA=false라 진입 X)
  - **return**: `MfgViewPose` + 부수 효과 명세 (Note/Measure/ShapeDrawing 추가됨, 외부에서 Clear 책임)
- `ExecuteMfgDrawing`, `RenderMfgViewForDrawing` 본문 변경 없음 (다음 commit에서 어댑터로 축소)

**T-036 흡수**: `_mfgDrawingZ90Applied` 등 전역 필드 → `_lastMfgViewPose` 1개 필드로 대체 (수동 경로용)
**검증**: 기존 PDF/3D 결과 동일 (`viewDirection`, `longestAxis`, `UsedMinusCamera`, `ApplyZ90/R180`, 치수 count 로그 비교)

### Step B2 — `ExecuteMfgDrawing` 어댑터로 축소 (commit 2/3)
**변경**:
- `ExecuteMfgDrawing(bomIndex)` = `BuildMfgSceneCore(bomIndex)` 호출 + 3D 뷰 유지 (수동 경로)
- 반환된 `MfgViewPose`를 `_lastMfgViewPose`에 저장
- `LvDrawingSheet_SelectedIndexChanged`의 후처리 회전(:663~679)이 `_lastMfgViewPose`를 사용

**검증**: PAD/PLATE·X/Y/Z·ORIENTATION 45도·Z90 케이스 PDF·3D 뷰 비교

### Step B3 — `RenderMfgViewForDrawing` 어댑터로 축소 (commit 3/3)
**변경**:
- `RenderMfgViewForDrawing(row, col, bomIndex)` = `BuildMfgSceneCore(bomIndex)` 호출 + 2D 캡처·그리드 배치
- 자동 경로 가공도 분기와 수동 가공도 분기가 **동일한 3D 장면 생성** 보장
- 풍선 정책(F2 `noteIds.Clear()`) — 사용자 결정 후 옵션화

**검증**: 가공도 전체 출력 PDF 각 셀 방향·치수 개수·2D fit·텍스트 시프트 비교

**R12 게이트 #2**: 사내 PC 실기 검증

### Step C1 — `SheetTemplateMode` enum + Excel/Legacy renderer 분리
**변경**:
```csharp
public enum SheetTemplateMode { Excel, LegacyGrid }

private void GenerateSheetDrawing2D(
    DrawingSheetData sheet,
    SheetTemplateMode mode = SheetTemplateMode.Excel)
{ ... }
```
- `UseExcelTemplate` 전역 bool 제거, 호출자가 명시
- 두 경로(`Excel`/`LegacyGrid`) 모두 보존 (legacy는 fallback)
- 분리: `RenderViaExcelTemplate(sheet)` / `RenderViaLegacyGrid(sheet)`

**영향 파일**: Form1.DrawingSheets.cs

### Step C2 — `SheetScene` 추출
- `SheetScene.Build(sheet)` — 3D 장면 (가시성·XRay·SilhouetteEdge·하이라이트·치수)
- `SheetCanvas.Render*` — 2D 캡처 (Excel/Legacy 두 renderer 호출)
- `ApplySheetSelection`는 `SheetScene.Build` 호출로 단순화

**R12 게이트 #3**: 사내 PC 실기 검증

### Step F — `SheetKind` enum (OCP, 후순위)
- `Models.cs`에 `enum SheetKind { Sheet1, Normal, Installation, Manufacturing }`
- `DrawingSheetData`에 프로퍼티 추가 + `BaseMemberIndex` 호환 유지
- 분기 10곳+ 일괄 수정

---

## 4. `DrawingContext` + 의존성 인터페이스 (Codex Q2 반영)

**Step B2/B3 이후 추출** (1차는 Form1 internal 헬퍼, 2차 승격):
```csharp
internal interface IUdaReader
{
    string GetValue(int nodeIndex, string keyName);
    string GetSpref(int nodeIndex);
    Tuple<string, float> ParseOrientation(int nodeIndex);
}

internal interface IDimensionEngine
{
    List<Vector3D> MergeCoordinates(
        List<(Vertex3D point, string nodeName)> points, float tolerance);

    List<ChainDimensionData> AddChainDimensionByAxis(
        List<Vector3D> points, string axis, float tolerance,
        string viewDirection = null);
}

internal sealed class DrawingContext
{
    public VIZCore3DControl Vc { get; private set; }
    public IList<BOMData> BomList { get; private set; }
    public IUdaReader Uda { get; private set; }
    public IDimensionEngine Dimensions { get; private set; }
    public Action<string> Log { get; private set; }

    public DrawingContext(VIZCore3DControl vc, IList<BOMData> bomList,
        IUdaReader uda, IDimensionEngine dimensions, Action<string> log)
    { /* assign */ }
}
```

**생성 시점**: `CreateDrawingContext()` factory로 호출 시점마다 생성 (bomList 변동성). Form1이 필드로 들고 다니지 않음.

---

## 5. .csproj 변경 패턴 (Codex Q3 + 검증)

**현재 패턴** (A2Z.csproj:55~):
```xml
<Compile Include="Form1.cs">
  <SubType>Form</SubType>
</Compile>
<Compile Include="Form1.BOM.cs">
  <DependentUpon>Form1.cs</DependentUpon>
  <SubType>Form</SubType>
</Compile>
...
<Compile Include="Models.cs" />
```

**신규 독립 클래스 추가 패턴**:
```xml
<Compile Include="Models\MfgViewPose.cs" />
<Compile Include="Drawing\MfgScene.cs" />
<Compile Include="Drawing\MfgCanvas.cs" />
```

**위치**: `Form1.ExcelTemplate.cs` 뒤, `Form1.Designer.cs` 앞 (Codex 권고)
**중요**: `DependentUpon`/`SubType`은 **partial Form1만**. 독립 클래스는 없음.
**변경 순서**: 파일 생성 + csproj 수정 → 로컬 빌드 → green 확인 → commit

---

## 6. R12 검증 게이트 (Codex 권고 — 3개로 묶음)

매 commit 후 push + 로컬 빌드는 필수이지만, **사내 PC 실기 검증 요청은 3 게이트에만**:

| 게이트 | 시점 | 검증 내용 |
|---|---|---|
| **G1** | Step D1 + E1 완료 후 | BOM 정확성, Osnap 캐시 적용 후 시트 출력 결과 동일 |
| **G2** | Step B3 (Mfg 코어 분할) 완료 후 | 가공도 PDF·3D 동일, T-036 회전 보존 |
| **G3** | Step C2 (Sheet 코어) 완료 후 | 일반 시트 PDF·3D 동일, Excel/Legacy 모두 |

각 게이트 통과 후 다음 묶음 진입.

---

## 7. 파일 구조 재편안 (수정)

### 최종 목표
```
A2Z/
  Form1.*.cs              (얇은 UI 어댑터만, ~300줄/파일)
  Models.cs               + Models/MfgViewPose.cs, CameraSnapshot.cs
  Drawing/
    MfgScene.cs / MfgCanvas.cs / MfgStyle.cs
    SheetScene.cs / SheetCanvas.cs
    LegacyGridRenderer.cs / ExcelTemplateRenderer.cs
    DimensionEngine.cs
  Services/
    BomCollector.cs / OsnapCache.cs / ClashRunner.cs
```

### 단계별 도입
1. Step B1: `Models/MfgViewPose.cs` 신설 (가장 작은 첫 파일 — csproj 추가 패턴 검증)
2. Step B3 후: `Drawing/MfgScene.cs`, `Drawing/MfgCanvas.cs` 추출 (2차 승격)
3. Step C2 후: `Drawing/SheetScene.cs` 등
4. 최종 단계 (별도 commit): namespace `A2Z.Drawing` 분리 — internal/public 접근자 정리

**namespace `A2Z` 유지** (Codex 권고) — 분리는 마지막에. internal 접근 정리가 본질 잡아먹지 않게.

---

## 8. 위험·롤백

| 위험 | 완화 |
|---|---|
| Step B1에서 `BuildMfgSceneCore` 추출 시 SDK 호출 순서 미세 변경 → 캔버스 깨짐 | Pre-0 메타데이터 diff (Note/Measure/ShapeDrawing.Count, RenderMode 등)로 잡음 |
| MfgViewPose 8필드 중 ScreenAxisRotation 관련 미보존 | 회전 적용 책임을 Form1 어댑터에 두고 Pose는 의도만 전달 (계산 X) |
| `.csproj` 수정 누락 → 새 파일 빌드 제외 → 컴파일 OK지만 런타임 누락 | 매 파일 신설 commit에 csproj diff 포함 검증 |
| Excel/Legacy 분리 commit에서 회귀 — 사용자가 평소 Excel 사용 중 | C1은 옵션 인자만 추가, 기본 동작 유지. Excel 본진 검증 우선 |
| sheetOverride 명시화 (D1) 시 호출처 누락 | grep `CollectBOMInfo(` 전수 (현재 5곳 모두 확인됨) |
| ApplySheetSelection 추출 시 핸들러 진입 외 경로 누락 | LvDrawingSheet 관련 이벤트 핸들러 전수 검증 (`SelectedIndexChanged` 외 `Click` 등) |

**롤백**: 각 Step 별도 commit. `git revert <hash>` 한 줄.

---

## 9. 함께 처리할 작은 작업
- **버튼 라벨**: "도면 리스트 출력" → "도면 일괄 출력" (Form1.Designer.cs 한 줄)
- **사용자 매뉴얼**: 라벨 일치
- **`.gitignore` 추가**: `docs/리팩토링/_baseline/`
- **STATUS.md**: Phase B 진행 추적

---

## 10. 다음 작업 (계획 승인 후)
1. **(선택)** v3 codex 4차 재검토 — 추가 빈틈 확인
2. **Pre-0 베이스라인 캡처** (사용자 사내 PC, 수동·자동 양쪽)
3. **Step D1 단독 commit** — `CollectBOMInfo` 호출처 3곳 sheet 전달 (가장 작은 첫 변경)
4. 게이트 G1까지 진행 후 사내 검증
5. 결과에 따라 다음 묶음 진입

---

## 변경 이력
| 날짜 | 변경 | 작성 |
|---|---|---|
| 2026-05-18 | v1 — Step 1 = ExecuteMfgDrawing 직접 치환 (단순 통합) | HYI + Claude |
| 2026-05-18 | v2 — Codex 1차 반영: 공통 코어 추출 + 파일 분할안 + 베이스라인 | HYI + Claude + Codex |
| 2026-05-18 | **v3 — Codex 2·3차 반영**: 순서 재배열(D1·E1 선행), MfgViewPose 8필드, DrawingContext + IUdaReader/IDimensionEngine, .csproj 패턴, R12 3게이트, sheetOverride 사실 정정 | HYI + Claude + Codex |
