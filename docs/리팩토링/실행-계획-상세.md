---
title: 도면 출력 경로 통합 — 실행 계획 상세
status: 사용자 컨펌 대기
created: 2026-05-18
parent: 도면-출력-통합.md (v3.1)
브랜치: refactor/dead-code
---

# 실행 계획 상세 — Step별 10항목

> 사용자 요청: "작업 들어가기 전에 하나부터 열까지 상세하게 설명"
> Codex 5차 권고 10항목: 목적 / 파일·라인 / before-after / 영향 범위 / 검증 명령 / 검증 결과 / 롤백 / 커밋 메시지 / 컨펌 지점 / 다음 commit 예고

## 전체 작업 큐 (12 commit)

| # | Commit | 분류 | 영향 |
|---|---|---|---|
| 0 | hygiene-gitignore | hygiene | `.gitignore` |
| 1 | Pre-0 baseline 수집 (코드 변경 0) | 검증 인프라 | 로컬 baseline 파일들 |
| 2 | D1 — CollectBOMInfo sheetOverride 명시화 | refactor | Form1.DrawingSheets.cs (3곳) |
| 3 | E1 — Osnap 캐시 명시 전달 | refactor | Form1.DrawingSheets.cs (3곳) + Form1.cs (세대 카운터) |
| 4 | A — ApplySheetSelection 추출 | refactor | Form1.DrawingSheets.cs + Form1.Stru.cs |
| 5 | **G1 게이트** — 사내 PC 실기 검증 (사용자 작업) | 검증 | (없음) |
| 6 | B1a — MfgViewPose + BuildMfgSceneCore 스켈레톤 | structural | Models/MfgViewPose.cs (신규) + Form1.MfgDrawing.cs + A2Z.csproj |
| 7 | B1b — BuildMfgSceneCore 본문 추출 | refactor | Form1.MfgDrawing.cs |
| 8 | B2 — ExecuteMfgDrawing 어댑터 축소 | refactor | Form1.MfgDrawing.cs |
| 9 | B3 — RenderMfgViewForDrawing 어댑터 축소 | refactor | Form1.MfgDrawing.cs |
| 10 | **G2 게이트** — 사내 PC 가공도 검증 + smoke (가공도→일반) | 검증 | (없음) |
| 11 | C1 — SheetTemplateMode enum + Excel/Legacy renderer 분리 | refactor | Form1.DrawingSheets.cs |
| 12 | C2 — SheetScene/SheetCanvas 추출 | structural | Drawing/SheetScene.cs (신규) + Form1.DrawingSheets.cs + A2Z.csproj |
| 13 | **G3 게이트** — 사내 PC 일반 시트 검증 (Excel + Legacy) | 검증 | (없음) |
| 14 | F — SheetKind enum | OCP 개선 | Models.cs + 분기 사용처 10곳+ |
| 15 | hygiene-rename — 버튼 라벨 + 매뉴얼 | hygiene | Form1.Designer.cs + docs/사용자-매뉴얼/ |

총 **12개 코드 commit + 3개 hygiene commit + 3개 검증 게이트** (= 작업 항목 18개, 그 중 git commit 15개).
> Codex 6차 지적(M1): "15-commit" 단일 라벨은 부정확. 실제는 commit 15 + 게이트 3 + Pre-0(비추적) 1.

---

## Commit 0 — hygiene: .gitignore 추가

| 항목 | 내용 |
|---|---|
| **목적** | Pre-0 baseline 산출물(PDF/스크린샷/메타)을 git에 안 올리도록. D1 이후 모든 commit의 순도 확보 |
| **변경 파일** | `.gitignore` |
| **before** | `bin/`, `obj/`, `*.suo` 등 표준 무시 |
| **after** | `+ docs/리팩토링/_baseline/` 추가 |
| **영향 범위** | 빌드·동작 영향 0. 추적 파일 변동 0 (해당 폴더 아직 없음) |
| **검증 명령** | `git status` — 변경 1줄 / `git check-ignore docs/리팩토링/_baseline/test.pdf` → ignored |
| **검증 결과** | `.gitignore` 1줄 추가, 추적 파일 변경 0 |
| **롤백** | `git revert HEAD` (1초) |
| **commit 메시지** | `chore(gitignore): baseline 폴더 추적 제외` |
| **컨펌 지점** | 자동 진행 (영향 0) |
| **다음** | Pre-0 baseline 수집 (사용자 사내 PC) |

---

## Commit 1 — Pre-0 baseline 수집 (코드 변경 0)

| 항목 | 내용 |
|---|---|
| **목적** | 회귀 비교 기준 확보. 향후 모든 변경의 "변하면 안 되는 것" 정의 |
| **변경 파일** | 코드 0. `docs/리팩토링/_baseline/2026-05-18/` 폴더에 산출물만 |
| **before** | 베이스라인 없음 |
| **after** | 다음 항목 폴더 안에 저장:<br>**PDF**: 수동 가공도/제작도/조립도/설치도 × 1세트, 자동 일괄 출력 1세트<br>**lvDimension CSV**: 수동/자동 각 1<br>**3D 스크린샷**: 수동 시트 선택 시 4종 × 자동 1<br>**메타데이터**: `DiagLog`, `bomList.Count`, `xraySelectedNodeIndices.Count`, `Note/Measure/ShapeDrawing.Count`, `RenderMode`, `XRay.Enable`, **`lvDrawingBOMInfo` row count/hash**, **`chainDimensionList` count/hash**, **선택 sheet 번호/base/member count**, **2D canvas size/object count**<br>**재현 정보**: 입력 파일 경로, 산출물 경로, PDF/이미지명, 날짜, 브랜치(`refactor/dead-code`), 커밋 해시 |
| **영향 범위** | 코드 0. 로컬 폴더만. `.gitignore`로 git 추적 X |
| **검증 명령** | 사내 PC에서 수동 실행 → 폴더 안 파일 개수 확인<br>`ls docs/리팩토링/_baseline/2026-05-18/` |
| **검증 결과** | PDF ≥ 5개, CSV ≥ 2개, PNG ≥ 5개, meta.txt 1개 (재현 정보 포함) |
| **롤백** | 폴더 삭제 (`rm -rf docs/리팩토링/_baseline/2026-05-18/`) |
| **commit 메시지** | (없음 — git 추적 X) |
| **컨펌 지점** | **사용자 검증 필수** — 사내 PC에서 캡처 완료 보고 |
| **다음** | D1 단독 commit (BOM sheetOverride) |

---

## Commit 2 — Step D1: CollectBOMInfo sheetOverride 명시화

| 항목 | 내용 |
|---|---|
| **목적** | 자동 경로(`ProcessSingleStruFull`)가 `lvDrawingSheet.SelectedItems[0]`에 의존해 BOM 정보가 깨질 위험 제거. Step A의 선행 조건 |
| **변경 파일** | `A2Z/Form1.DrawingSheets.cs` — 3곳<br>L652 (LvDrawingSheet_SelectedIndexChanged 내)<br>L1376 (GenerateSheetDrawing2D legacy 분기 내)<br>L1710 (GenerateSheetDrawing2D Excel 분기 내) |
| **before** | ```csharp<br>// L652<br>CollectBOMInfo(false);<br>// L1376<br>CollectBOMInfo(false);<br>// L1710<br>CollectBOMInfo(false);<br>``` |
| **after** | ```csharp<br>// L652 (핸들러 — sheet 변수는 윗줄에서 `selectedItem.Tag`)<br>CollectBOMInfo(false, sheet);<br>// L1376 (GenerateSheetDrawing2D 인자 sheet 그대로)<br>CollectBOMInfo(false, sheet);<br>// L1710 (동일)<br>CollectBOMInfo(false, sheet);<br>``` |
| **영향 범위** | 수동 경로: 변경 0 (sheet === lvDrawingSheet.SelectedItems[0] 동일). 자동 경로: `lvi.Selected=true` 트릭이 아직 살아 있어 BOM은 동일하지만, **명시 인자로 의존 끊김** = 향후 Step A 안전 |
| **검증 명령** | `msbuild A2Z.sln /p:Configuration=Debug /v:minimal /nologo` → 성공<br>로컬에서 수동 1시트 출력 → BOM 정보 정상<br>(선택) 자동 일괄 출력 → 시트별 BOM 정상 |
| **검증 결과** | 빌드 green, BOM 메타데이터(`lvDrawingBOMInfo` row count) Pre-0와 일치 |
| **롤백** | `git revert <D1_hash>` (1초) — 호출처 3줄만 되돌림 |
| **commit 메시지** | `refactor(d1): CollectBOMInfo 호출처 3곳 sheet 명시 전달`<br><br>`- L652/L1376/L1710 모두 sheet 인자로 전달`<br>`- lvDrawingSheet.SelectedItems[0] 의존 끊음 (Step A 선행 조건)`<br>`- 동작 변경 0, 명시화만`<br>`- 관련: docs/리팩토링/도면-출력-통합.md v3.1 Step D1` |
| **컨펌 지점** | 빌드 + 수동 1시트 BOM 정상 확인 후 push (R12) |
| **다음** | E1 — Osnap 캐시 명시 전달 |

---

## Commit 3 — Step E1: Osnap 캐시 명시 전달

| 항목 | 내용 |
|---|---|
| **목적** | `_lastCollectedNodeOsnapMap` 캐시가 `CompleteMainDimensionPostClash`(:428)에서만 사용되고 시트 진입·2D 출력·Excel에서는 `preBuiltMap=null`로 재수집. 모든 호출 지점에서 캐시 전달 → 시트당 1~3초 단축 (성능 핫스팟 H3) |
| **변경 파일** | `A2Z/Form1.DrawingSheets.cs` — 3곳 (:614, :1358, :1692)<br>+ `A2Z/Form1.cs` — 세대 카운터 필드 추가 |
| **before** | ```csharp<br>// :614<br>chainDimensionList = ComputeViewDimensionsForMembers(<br>    sheet.MemberIndices, null, 0.5f, null);<br>// :1358, :1692 동일 패턴<br>``` |
| **after** | ```csharp<br>// Form1.cs<br>private int _osnapMapGeneration = 0;<br>private int _lastSeenOsnapGeneration = -1;<br><br>// :614<br>var cache = (_osnapMapGeneration == _lastSeenOsnapGeneration)<br>    ? _lastCollectedNodeOsnapMap<br>    : null;<br>chainDimensionList = ComputeViewDimensionsForMembers(<br>    sheet.MemberIndices, null, 0.5f, cache);<br>_lastSeenOsnapGeneration = _osnapMapGeneration;<br>``` |
| **영향 범위** | 동작 동일 (캐시 유효 시 빠르게, 무효 시 null fallback = 기존 동작). 세대 카운터로 STRU 전환 시 캐시 무효화 안전 |
| **검증 명령** | 빌드 + 수동 시트 5장 출력 → DiagLog에 `ComputeViewDimensionsForMembers=Xms` 시간 비교 → Pre-0 대비 단축 |
| **검증 결과** | 빌드 green, 시트당 평균 1~3초 단축, 치수 결과 동일 |
| **롤백** | `git revert <E1_hash>` |
| **commit 메시지** | `refactor(e1): Osnap 캐시 모든 호출 지점에 명시 전달`<br><br>`- :614/:1358/:1692에서 _lastCollectedNodeOsnapMap 전달`<br>`- 세대 카운터(_osnapMapGeneration)로 캐시 유효성 방어`<br>`- 시트당 1~3초 단축 (T-032 완성)`<br>`- 관련: T-032, v3.1 Step E1` |
| **컨펌 지점** | 빌드 + 시트 5장 시간 비교 후 push |
| **다음** | A — ApplySheetSelection 추출 |

---

## Commit 4 — Step A: ApplySheetSelection 추출

| 항목 | 내용 |
|---|---|
| **목적** | 자동 경로의 `lvi.Selected=true + Thread.Sleep(200)` UI 트릭 제거. 핸들러 본체를 메서드로 추출해 자동·수동 양쪽이 같은 함수 호출 |
| **변경 파일** | `A2Z/Form1.DrawingSheets.cs` (L517~692 핸들러 본체 분리)<br>`A2Z/Form1.Stru.cs` (L643~648 트릭 제거 → 직접 호출) |
| **before** | ```csharp<br>// Form1.DrawingSheets.cs<br>private void LvDrawingSheet_SelectedIndexChanged(<br>    object sender, EventArgs e)<br>{<br>    // 175줄 본체 — 가시성·XRay·치수 분기 전부<br>}<br>// Form1.Stru.cs L643~648<br>lvi.Selected = true;<br>Application.DoEvents();<br>Thread.Sleep(200);<br>GenerateSheetDrawing2D(sheet);<br>``` |
| **after** | ```csharp<br>// Form1.DrawingSheets.cs<br>public void ApplySheetSelection(DrawingSheetData sheet)<br>{<br>    // 기존 핸들러 본체 175줄 그대로 이동<br>}<br>private void LvDrawingSheet_SelectedIndexChanged(<br>    object sender, EventArgs e)<br>{<br>    var sheet = (sender as ListView)?.SelectedItems[0]?.Tag as DrawingSheetData;<br>    if (sheet != null) ApplySheetSelection(sheet);<br>}<br>// Form1.Stru.cs L643~648<br>ApplySheetSelection(sheet);  // lvi.Selected/DoEvents/Sleep 모두 제거<br>GenerateSheetDrawing2D(sheet);<br>``` |
| **영향 범위** | 수동 경로: 동일 (핸들러 → 메서드 호출). 자동 경로: 시트당 200ms 단축, 이벤트 타이밍 의존 사라짐 |
| **검증 명령** | 빌드 + 수동 시트 5장 클릭 → 동일 동작<br>자동 일괄 → 시간 단축 측정 (시트 N개 × 200ms) |
| **검증 결과** | 수동 동일 + 자동 (N × 200ms) 단축 |
| **롤백** | `git revert <A_hash>` |
| **commit 메시지** | `refactor(a): ApplySheetSelection 메서드 추출`<br><br>`- 핸들러 본체(175줄)를 public 메서드로 분리`<br>`- 자동 경로 lvi.Selected/Sleep(200) 트릭 제거 → 직접 호출`<br>`- DRY 첫 적용: 수동·자동이 같은 함수 호출`<br>`- 관련: v3.1 Step A` |
| **컨펌 지점** | **G1 게이트** — 사내 PC 실기 검증 (D1+E1+A 묶음) |
| **다음** | G1 통과 후 B1a — Mfg 스켈레톤 |

---

## 게이트 G1 — 사용자 사내 PC 검증

| 항목 | 내용 |
|---|---|
| **목적** | D1+E1+A 묶음의 회귀 0 확인. BOM·Osnap·시트 선택이 모두 정상 |
| **시나리오** | 1) 모델 열기<br>2) 치수 추출 → BOM 정확<br>3) 일반 시트 5장 클릭 → 치수·BOM 정상 (시간 단축 체감)<br>4) 자동 일괄 출력 → 모든 시트 PDF 정상, 시간 단축 체감 |
| **합격 기준** | 베이스라인 PDF·CSV·메타 diff 0 또는 의도된 변경(시간 단축)만 |
| **불합격 시** | 문제 commit `git revert`, 원인 분석 후 재진입 |

---

## Commit 6 — Step B1a: MfgViewPose 스켈레톤 (compile-only)

| 항목 | 내용 |
|---|---|
| **목적** | `MfgViewPose` 클래스와 `BuildMfgSceneCore` 빈 스텁만 도입. 빌드 통과 + API 모양 고정. 동작 변경 0 |
| **변경 파일** | **신규**: `A2Z/Models/MfgViewPose.cs`<br>**수정**: `A2Z/Form1.MfgDrawing.cs` (빈 스텁 1개)<br>**수정**: `A2Z/A2Z.csproj` (Compile Include 1줄) |
| **before** | (해당 파일 없음) |
| **after** | ```csharp<br>// Models/MfgViewPose.cs (신규)<br>namespace A2Z<br>{<br>    internal sealed class MfgViewPose<br>    {<br>        public VIZCore3D.NET.Data.CameraData CameraData { get; set; }<br>        public string ViewDirection { get; set; }<br>        public string LongestAxis { get; set; }<br>        public VIZCore3D.NET.Data.CameraDirection CameraDirection { get; set; }<br>        public bool UsedMinusCamera { get; set; }<br>        public bool ApplyZ90 { get; set; }<br>        public bool ApplyR180 { get; set; }<br>        public float OrientationAngle { get; set; }<br>        public bool RequiresPostSelectionRotation<br>            => ApplyZ90 \|\| ApplyR180;<br>    }<br>}<br><br>// Form1.MfgDrawing.cs (빈 스텁)<br>private MfgViewPose BuildMfgSceneCore(int bomIndex)<br>{<br>    throw new System.NotImplementedException("B1b에서 구현");<br>}<br><br>// A2Z.csproj — Models.cs 옆/뒤 그루핑 (Codex 6차 M2, 빌드 영향 0)<br><Compile Include="Models\MfgViewPose.cs" /><br>``` |
| **영향 범위** | 동작 0 (스텁 호출 없음). 빌드 검증용 |
| **검증 명령** | `msbuild A2Z.sln /p:Configuration=Debug /v:minimal /nologo` → 성공 |
| **검증 결과** | 빌드 green, 새 파일 1개, 기존 동작 동일 |
| **롤백** | `git revert <B1a_hash>` |
| **commit 메시지** | `refactor(b1a): MfgViewPose 모델 + BuildMfgSceneCore 스텁 추가`<br><br>`- Models/MfgViewPose.cs 신규 (8필드, Codex Q1 권고)`<br>`- BuildMfgSceneCore 빈 스텁 (B1b 구현 예정)`<br>`- A2Z.csproj Compile Include 추가`<br>`- 동작 변경 0, 빌드 검증만`<br>`- 관련: v3.1 Step B1a` |
| **컨펌 지점** | 빌드 green 후 push |
| **다음** | B1b — 본문 추출 |

---

## Commit 7 — Step B1b: BuildMfgSceneCore 본문 추출

| 항목 | 내용 |
|---|---|
| **목적** | `ExecuteMfgDrawing`과 `RenderMfgViewForDrawing`의 공통 3D 장면 생성 로직을 `BuildMfgSceneCore`로 이동. 두 함수는 아직 새 코어를 호출하지 않음 (다음 commit) |
| **변경 파일** | `A2Z/Form1.MfgDrawing.cs` |
| **before** | `BuildMfgSceneCore` = 빈 스텁 |
| **after** | `BuildMfgSceneCore` 본문에 부재 격리·BBox·축 판별·Osnap 수집·치수·풍선·EA 분기(`isEA=false`) 로직. `MfgViewPose` 반환. **호출자는 아직 없음** |
| **영향 범위** | 동작 0 (호출 안 됨). 코드 추가만 |
| **검증 명령** | 빌드 + 호출처 grep으로 확인 (`BuildMfgSceneCore` 호출 0건) |
| **검증 결과** | 빌드 green |
| **롤백** | `git revert <B1b_hash>` |
| **commit 메시지** | `refactor(b1b): BuildMfgSceneCore 본문 추출 (호출처 미연결)`<br><br>`- ExecuteMfgDrawing + RenderMfgViewForDrawing 공통 로직 이동`<br>`- 호출자 0건 (B2/B3에서 연결)`<br>`- 동작 변경 0` |
| **컨펌 지점** | 빌드 green 후 push |
| **다음** | B2 — Execute 어댑터 축소 |

---

## Commit 8 — Step B2: ExecuteMfgDrawing 어댑터 축소

| 항목 | 내용 |
|---|---|
| **목적** | 수동 경로 가공도 함수를 `BuildMfgSceneCore` 호출로 축소. `_lastMfgViewPose` 필드에 결과 저장 (T-036 후처리 회전 의존). **write scope는 ExecuteMfgDrawing만** |
| **변경 파일** | `A2Z/Form1.MfgDrawing.cs` (`ExecuteMfgDrawing` 본문)<br>`A2Z/Form1.cs` (`_lastMfgViewPose` 필드 추가, 기존 `_mfgDrawingZ90Applied/R180Applied/CameraSnapshot` 제거)<br>`A2Z/Form1.DrawingSheets.cs` (L663~679 후처리 회전 → `_lastMfgViewPose` 사용) |
| **before** | `ExecuteMfgDrawing` 250줄+ 본체 직접 SDK 호출 |
| **after** | ```csharp<br>private MfgViewPose _lastMfgViewPose;<br><br>private void ExecuteMfgDrawing(int bomIndex)<br>{<br>    var pose = BuildMfgSceneCore(bomIndex);<br>    _lastMfgViewPose = pose;  // 수동 경로 전용 갱신<br>    // 3D 뷰 유지 (Note·Measure는 BuildMfgSceneCore에서 추가됨)<br>}<br>``` |
| **영향 범위** | 수동 경로만. 동작 동일 (3D 뷰·치수·풍선·카메라 회전 보존). 자동 경로 영향 없음 (`RenderMfgViewForDrawing` 아직 변경 X) |
| **검증 명령** | 빌드 + 수동 가공도 출력 (PAD/PLATE·X/Y/Z·ORIENTATION 45도·Z90 케이스) + Pre-0 PDF/메타 비교 |
| **검증 결과** | 빌드 green, 4종 케이스 PDF·메타 동일 |
| **롤백** | `git revert <B2_hash>` |
| **commit 메시지** | `refactor(b2): ExecuteMfgDrawing을 BuildMfgSceneCore 어댑터로 축소`<br><br>`- 본체 ~250줄 → 5줄 (어댑터)`<br>`- _lastMfgViewPose 필드 도입 (Execute만 write)`<br>`- T-036 전역 필드(_mfgDrawingZ90Applied/R180Applied) 제거`<br>`- 동작 동일`<br>`- 관련: v3.1 Step B2, T-036` |
| **컨펌 지점** | 빌드 + 수동 4종 케이스 검증 후 push |
| **다음** | B3 — Render 어댑터 축소 |

---

## Commit 9 — Step B3: RenderMfgViewForDrawing 어댑터 축소

| 항목 | 내용 |
|---|---|
| **목적** | 자동 경로 가공도 함수를 `BuildMfgSceneCore` 호출로 축소. **`_lastMfgViewPose` write X — 지역변수만 사용** (Codex P3 제약) |
| **변경 파일** | `A2Z/Form1.MfgDrawing.cs` (`RenderMfgViewForDrawing` 본문) |
| **before** | `RenderMfgViewForDrawing` 본체 — 3D 장면 + 2D 캡처 혼재 |
| **after** | ```csharp<br>private int RenderMfgViewForDrawing(int row, int col, int bomIndex)<br>{<br>    var pose = BuildMfgSceneCore(bomIndex);  // 지역변수만<br>    // 2D 캡처·그리드 배치 (기존 로직 유지)<br>    int objId = vizcore3d.Drawing2D.Object2D<br>        .Create2DViewObjectWithModelHiddenLineAtCanvasOrigin(...);<br>    // ... fit, MoveObjectTo<br>    return objId;<br>}<br>``` |
| **영향 범위** | **수동·자동 가공도 결과가 동일한 3D 장면 보장** (BuildMfgSceneCore 단일 호출). PDF 동일 가능성 ↑ |
| **검증 명령** | 빌드 + 자동 일괄 출력 가공도 PDF + Pre-0 비교 + 수동 가공도와 비교 |
| **검증 결과** | 자동 가공도 PDF = 수동 가공도 PDF (시각 일치) |
| **롤백** | `git revert <B3_hash>` |
| **commit 메시지** | `refactor(b3): RenderMfgViewForDrawing을 BuildMfgSceneCore 어댑터로 축소`<br><br>`- 자동·수동 가공도 결과 동일 보장`<br>`- _lastMfgViewPose write X (지역변수만, Codex P3)`<br>`- DRY 본진 적용`<br>`- 관련: v3.1 Step B3` |
| **컨펌 지점** | **G2 게이트** — 사내 PC 가공도 검증 + smoke (가공도 직후 일반/설치도 출력 → 잔존 오염 확인) |
| **다음** | G2 통과 후 C1 — SheetTemplateMode enum |

---

## 게이트 G2 — 가공도 검증 + smoke

| 항목 | 내용 |
|---|---|
| **목적** | B1a~B3 묶음 회귀 0 + 잔존 상태 오염 없음 확인 |
| **시나리오** | 1) 자동 가공도 일괄 출력 → PDF 정상<br>2) **smoke**: 직후 일반 시트 선택·출력 → RenderMode/XRay/SilhouetteEdge/Note/Measure/Shape 잔존 오염 X<br>3) **smoke**: 직후 설치도 시트 선택·출력 → 정상 |
| **합격 기준** | 가공도 PDF = Pre-0 + 자동 = 수동 + 후속 시트 정상 |
| **불합격 시** | 잔존 상태 추적 (DiagLog), 필요시 BuildMfgSceneCore에 Clear 추가 |

---

## Commit 11 — Step C1: SheetTemplateMode enum + Excel/Legacy renderer 분리

| 항목 | 내용 |
|---|---|
| **목적** | `UseExcelTemplate` 전역 bool 제거. 호출자가 명시 enum으로 선택. legacy fallback 보존 |
| **변경 파일** | `A2Z/Models.cs` (`SheetTemplateMode` enum)<br>`A2Z/Form1.DrawingSheets.cs` (`GenerateSheetDrawing2D` 인자·분기, `RenderViaExcelTemplate` / `RenderViaLegacyGrid` 헬퍼) |
| **before** | `private bool UseExcelTemplate = true;` + 분기 |
| **after** | ```csharp<br>public enum SheetTemplateMode { Excel, LegacyGrid }<br><br>private void GenerateSheetDrawing2D(<br>    DrawingSheetData sheet,<br>    SheetTemplateMode mode = SheetTemplateMode.Excel)<br>{<br>    if (mode == SheetTemplateMode.Excel)<br>        RenderViaExcelTemplate(sheet);<br>    else<br>        RenderViaLegacyGrid(sheet);<br>}<br>``` |
| **영향 범위** | 기본값 `Excel` → 기존 동작 동일. Legacy는 fallback으로 보존 |
| **검증 명령** | 빌드 + 수동/자동 시트 출력 (기본 Excel) + Legacy 강제 호출 1회 (회귀 확인) |
| **검증 결과** | 기본 Excel 동작 동일, Legacy 동작 동일 |
| **롤백** | `git revert <C1_hash>` |
| **commit 메시지** | `refactor(c1): SheetTemplateMode enum + Excel/Legacy renderer 분리`<br><br>`- UseExcelTemplate 전역 bool → 명시 enum 인자`<br>`- RenderViaExcelTemplate / RenderViaLegacyGrid 헬퍼 분리`<br>`- Legacy fallback 보존 (즉시 제거 X)`<br>`- 관련: v3.1 Step C1` |
| **컨펌 지점** | 빌드 + Excel/Legacy 양쪽 동작 확인 후 push |
| **다음** | C2 — SheetScene 추출 |

---

## Commit 12 — Step C2: SheetScene/SheetCanvas 추출

| 항목 | 내용 |
|---|---|
| **목적** | 일반 시트 3D 장면·2D 캡처 분리. `ApplySheetSelection`이 `SheetScene.Build` 호출로 단순화 |
| **변경 파일** | **신규**: `A2Z/Drawing/SheetScene.cs`, `A2Z/Drawing/SheetCanvas.cs`<br>**수정**: `A2Z/Form1.DrawingSheets.cs`, `A2Z/A2Z.csproj` |
| **after 구조** | `SheetScene.Build(sheet)` 3D 장면 / `SheetCanvas.Render*` 2D 캡처. `ApplySheetSelection`은 `SheetScene.Build` 호출만 |
| **영향 범위** | 일반 시트 (Sheet1·정상·설치도). 가공도(-3)는 별도 |
| **검증 명령** | 빌드 + 일반 시트 5장 출력 + 설치도 출력 + Pre-0 비교 |
| **검증 결과** | 일반 시트 동일, 설치도 동일 |
| **롤백** | `git revert <C2_hash>` |
| **commit 메시지** | `refactor(c2): SheetScene/SheetCanvas 추출`<br><br>`- Drawing/SheetScene.cs, Drawing/SheetCanvas.cs 신규`<br>`- A2Z.csproj Compile Include 2개 추가`<br>`- 일반 시트 DRY 완성`<br>`- 관련: v3.1 Step C2` |
| **컨펌 지점** | **G3 게이트** — 사내 PC 일반 시트 검증 |
| **다음** | G3 통과 후 F — SheetKind enum |

---

## 게이트 G3 — 일반 시트 검증

| 항목 | 내용 |
|---|---|
| **목적** | C1+C2 묶음 회귀 0 + Excel/Legacy 양쪽 동작 |
| **시나리오** | 1) 일반 시트 5장 출력 (Excel)<br>2) 일반 시트 5장 출력 (Legacy 강제)<br>3) 자동 일괄 출력 |
| **합격 기준** | 모두 Pre-0와 일치 |
| **불합격 시** | 문제 commit revert |

---

## Commit 14 — Step F: SheetKind enum

| 항목 | 내용 |
|---|---|
| **목적** | `BaseMemberIndex == -3/-2/-1` 매직 넘버 분기 10곳 산재 → enum 단일화 (OCP) |
| **변경 파일** | `A2Z/Models.cs` + 분기 사용처 10곳 (DrawingSheets.cs, MfgDrawing.cs, Stru.cs) |
| **after** | ```csharp<br>public enum SheetKind { Sheet1, Normal, Installation, Manufacturing }<br>// DrawingSheetData에 속성 추가 + BaseMemberIndex 호환<br>``` |
| **영향 범위** | 동작 동일, 분기 코드 가독성 ↑ |
| **검증 명령** | 빌드 + 4종 시트 출력 |
| **검증 결과** | 동일 |
| **롤백** | `git revert <F_hash>` |
| **commit 메시지** | `refactor(f): SheetKind enum 도입 (OCP 개선)` |
| **컨펌 지점** | 빌드 + 4종 시트 정상 후 push |
| **다음** | hygiene — 라벨·매뉴얼 |

---

## Commit 15 — hygiene: 버튼 라벨 + 매뉴얼

| 항목 | 내용 |
|---|---|
| **목적** | 사용자 표현 일치: "도면 리스트 출력" → "도면 일괄 출력". 매뉴얼 동기 |
| **변경 파일** | `A2Z/Form1.Designer.cs` (1줄) + `docs/사용자-매뉴얼/` (해당 페이지) |
| **검증 명령** | 빌드 + UI 확인 |
| **commit 메시지** | `chore(rename): 도면 리스트 출력 → 도면 일괄 출력` |
| **컨펌 지점** | 사용자 확인 |
| **다음** | (모든 작업 완료) |

---

## 작업 외 — TASKS.md / CHANGELOG.md 갱신
각 commit 후 `/commit` 워크플로우를 쓰면 자동. 수동 진행 시:
- `docs/tracking/CHANGELOG.md`에 commit 별 1줄
- `docs/tracking/TASKS.md`에 새 T-xxx (T-065 "도면 출력 경로 통합") 등록 + 단계 진척

## commit 메시지 형식 (Codex 6차 M3)

위 모든 commit 메시지 예시에 **`Co-Authored-By:` 푸터가 생략돼 있음**. 실제 commit 시 CLAUDE.md 형식 준수:

```
{type}: {요약 50자 이내}

- 변경 상세 1
- 변경 상세 2
- 관련: T-xxx, v3.1 Step X

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

- `/commit` 슬래시 커맨드는 푸터 자동 추가 → 그대로 사용 OK
- 수동 `git commit` 시 HEREDOC 끝에 명시 필수
- `Co-Authored-By` 라인 + 모델 식별자(Opus 4.7) 누락 시 CLAUDE.md 규칙 위반

## D1 사전 점검 — 비정적 참조 grep 패턴 (Codex 6차 M4 보강)

D1 commit 전에 `CollectBOMInfo` 호출이 다음 비정적 경로로 묶여있지 않은지 1회 확인:

```bash
# 1) Reflection — typeof().GetMethod, MethodInfo.Invoke 등
grep -nE 'GetMethod\("CollectBOMInfo"|MethodInfo.*CollectBOMInfo|InvokeMember' A2Z/

# 2) 메서드 그룹 변환 / Delegate
grep -nE 'new (Action|EventHandler|Func<).*CollectBOMInfo|Delegate.CreateDelegate.*CollectBOMInfo' A2Z/

# 3) GetMethods().First — 메서드명 동적 매칭
grep -n 'GetMethods.*CollectBOMInfo\|GetMethods.*First.*Name' A2Z/

# 4) 비동기 람다 캡처 (Task.Run / BeginInvoke 안)
grep -nE 'Task\.Run.*CollectBOMInfo|BeginInvoke.*CollectBOMInfo|Invoke\(.*CollectBOMInfo' A2Z/

# 5) 문자열 리터럴 전수 (Designer event, serialization 등)
grep -rn '"CollectBOMInfo"' A2Z/ docs/
```

모두 0건이면 D1 단독 commit 안전. 비정적 참조 발견 시 D1 보류하고 사용자 보고.

---

## 사용자 컨펌 체크리스트

D1 착수 직전 확인:

- [ ] v3.1 계획서 읽음
- [ ] 본 실행 계획 상세 읽음
- [ ] Pre-0 baseline 사내 PC에서 캡처 완료
- [ ] `.gitignore` baseline 폴더 제외 추가 commit (Commit 0)
- [ ] 비정적 참조 확인 (Claude 자동 — reflection/designer/callback/serialization 미사용)
- [ ] D1 commit 진행 컨펌

컨펌 완료 후 Claude가 D1부터 순서대로 진행.
