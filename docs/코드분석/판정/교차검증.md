# 교차검증

문서를 쓴 쪽이 아닌 쪽이 읽고 근거를 댄 것을 모은다.

> 🔴 **지적하는 쪽은 근거만 댄다. 판정은 그 문서의 담당자가 내린다.**
> 상대 문서를 직접 고치지 않는다 — "한 파일은 한 도구만" 규칙을 지키기 위해서다.

## 무엇을 보나

1~4절은 [`verify.py`](../verify.py)가 기계로 잡는다. **5절과 6절만** 본다.

| 볼 것 | 어떻게 |
|---|---|
| **5절 알고리즘** | 수식·임계값이 코드와 맞나. 숫자는 `grep`으로 바로 확인된다 |
| **6절 ② 떼어낼 수 있다** | 정말 그런가. **놓친 공유 상태 참조가 없나** → [`자동생성/파일 구조.md`](../자동생성/파일%20구조.md) 2절과 대조 |
| **6절 ③ 못 뗀다** | 정말 못 떼나. 주입·반환값으로 풀리는 걸 구조적 한계로 적지 않았나 |
| **2절 플로우차트** | 빠진 갈래가 있나 — 특히 **예외·취소·조기 return** |

**전수 재독은 하지 않는다.** 문서를 읽고 의심 가는 곳만 코드로 확인한다.

## 담당

| 검증하는 쪽 | 검증받는 문서 | 대상 코드 |
|---|---|---|
| **Codex** | `Form1.md` · `Form1.License.md` · `Form1.ExcelTemplate.md` · `Models.md` · `Form1.Drawing2D.md` · `Form1.Dimensions.md` · `Form1.MfgDrawing.md` · `Form1.DrawingSheets.md` | 13,565줄 |
| **Claude Code** | `Form1.Clash.md` · `Form1.GlobalViews.md` · `Form1.Stru.md` · `Form1.BOM.md` · `Form1.Attribute.md` | 5,369줄 |

`Form1.MfgDrawing.md` · `Form1.DrawingSheets.md`는 작성 완료 뒤 2026-08-23에 추가 검증했다.

---

## 지적

판정 칸은 **담당자가** 채운다. 비워두고 제출할 것.

| # | 대상 문서 | 절 | 지적 | 근거 (파일:줄) | 지적자 | 판정 |
|---|---|---|---|---|---|---|
| 1 | `Form1.License.md` | 6절 ②·③ | “공유 상태를 하나도 안 쓰고 UI에도 묶이지 않는다”는 근거가 빠졌다. 이 파일은 `Form1`의 ⚠ `vizcore3d`와 `DiagLog`를 직접 쓰고, 갱신 수명은 UI 메시지 루프에서 도는 `System.Windows.Forms.Timer`에 의존한다. 분리는 가능하지만 SDK·로거뿐 아니라 타이머/수명주기 경계도 필요하다. | `A2Z/Form1.cs:28`; `A2Z/Form1.License.cs:33,70,76,114~117` | Codex | ✅ 수정함 (2026-08-23) |
| 2 | `Form1.ExcelTemplate.md` | 6절 ②·③ | “셋 다 `static` 순수 함수”, “UI 안 씀”은 코드와 다르다. `FillRevisionTable`은 인스턴스 메서드로 입력 사전을 변경하고 `DiagLog`를 호출하며, `BuildCurrentRevisionHistory`도 인스턴스 메서드로 시스템 시각을 읽는다. `SafeSubItem`은 WinForms `ListViewItem` 형식에 직접 묶인다. 분리하려면 로거·시계 주입과 UI 어댑터 경계가 필요하다. | `A2Z/Form1.ExcelTemplate.cs:31~50,60~68,88~92` | Codex | ✅ 수정함 (2026-08-23) |
| 3 | `Models.md` | 6절 ④ | `StartPointStr`·`EndPointStr`는 미사용 문자열 사본이 아니라 현재 여러 화면 목록이 직접 소비한다. 삭제하려면 먼저 좌표 표시 형식을 Presenter/계산 속성으로 옮겨야 하며, 지금 바로 지우면 치수 목록의 시작·끝 좌표가 사라진다. | `A2Z/Models.cs:25~26`; `A2Z/Form1.Dimensions.cs:2196~2197`; `A2Z/Form1.DrawingSheets.cs:568~569` | Codex | ✅ 수정함 (2026-08-23) |
| 4 | `Models.md` | 6절 ③ | SDK 좌표·카메라 형식을 “못 떼는 것”으로 분류했지만 구조적 장벽은 아니다. 모델은 SDK 동작을 호출하지 않고 해당 형식을 속성/필드로 보관할 뿐이므로, 프로젝트 소유 `Point3`·`Vector3`·`CameraPose`와 SDK 경계 변환으로 결합을 이동할 수 있다. 현재 SDK 유지 정책상 감수할 수 있다는 판단과 분리 불가능은 구분해야 한다. | `A2Z/Models.cs:19~22,104~108,216~229`; `A2Z/Models/MfgViewPose.cs:48~62` | Codex | ✅ 수정함 (2026-08-23) |
| 5 | `Form1.Drawing2D.md` | 5절 ⑥ | PDF 묶기를 “기본값 false이므로 지금은 안 쓰인다”고 단정하면 호출 구조를 놓친다. 기본 설정은 false가 맞지만 제작/조립/설치, STRU 일괄, 가공도 경로가 모두 `BeginPdfPageAccumulation`을 호출하고 설정 하나로 활성화한다. “미사용”이 아니라 **기본 비활성인 운영 경로**다. | `A2Z/App.config:62`; `A2Z/Form1.DrawingSheets.cs:1350`; `A2Z/Form1.Stru.cs:949`; `A2Z/Form1.MfgDrawing.cs:2558` | Codex | ✅ 수정함 (2026-08-23) |
| 6 | `Form1.Drawing2D.md` | 5절 ⑦ | 같은 파일명이 있으면 `_1`~`_999`로 피한다는 규칙은 마지막 충돌을 보장하지 않는다. 루프는 `_999` 경로를 만든 뒤 `n == 1000`이면 그 경로의 존재 여부를 다시 검사하지 않고 종료한다. base와 `_1`~`_999`가 모두 있으면 기존 `_999`를 반환해 덮어쓸 수 있다. | `A2Z/Form1.Drawing2D.cs:1210~1216` | Codex | ✅ 수정함 (2026-08-23) |
| 7 | `Form1.Drawing2D.md` | 6절 ② | PDF 페이지 관리가 “자기 필드와 `vizcore3d.Drawing2D`만 쓴다”는 설명은 직접 의존을 누락했다. 설정 판정은 `DrawingSheets`의 `GetAppSetting`, 비누적 페이지 정리는 같은 파일의 `CleanupDrawingSheetExportCanvas`를 호출한다. 파일명 유틸도 `DrawingSheets`의 두 sanitize 메서드에 의존하므로, 그대로는 `PdfPageAccumulator`로 이동할 수 없다. | `A2Z/Form1.Drawing2D.cs:1024~1027,1100~1105,1190~1203`; `A2Z/Form1.DrawingSheets.cs:1571,1640,1654,3483` | Codex | ✅ 수정함 (2026-08-23) |
| 8 | `Form1.Dimensions.md` | 5절 ② | “반올림 뒤 다시 0.5mm 안의 점을 병합해 격자 경계 분리를 보완한다”는 설명이 실제 비교와 다르다. 좌표를 0.5 배수로 만든 뒤 성분별 차이가 **엄격히 `< 0.5`** 인지만 보므로, 서로 다른 인접 격자점의 차이 0.5는 합쳐지지 않는다. 결과적으로 후단 검사는 같은 반올림 좌표만 제거한다. | `A2Z/Form1.Dimensions.cs:2234~2257` | Codex | ✅ 수정함 (2026-08-23) |
| 9 | `Form1.Dimensions.md` | 5절 ⑩ | 각도 표시의 접합·각도·뷰 판정이 축약 과정에서 달라졌다. 접합은 Osnap 끝점 3mm뿐 아니라 `clashList`의 Part 표면 접촉 또는 같은 Part도 허용하고, 직각 여부는 화면 투영각이 아닌 실제 3D 내적으로 판정한다. 그 뒤 두 축 평면의 법선이 깊이축과 가장 정렬된 **한 뷰만** 선택한다. | `A2Z/Form1.Dimensions.cs:2876~2891,2915~2941,2943~2957` | Codex | ✅ 수정함 (2026-08-23) |
| 10 | `Form1.Dimensions.md` | 6절 ② | 필터링 3종을 “목록을 받아 목록을 돌려주는 순수 계산”이라고 했지만 입력 `ChainDimensionData`의 `Priority`·`IsVisible`·`DisplayLevel`을 직접 바꾸고 `Form1.DiagLog`도 호출한다. 분리 자체는 가능하나 불변 결과를 새로 만들거나 변경 효과를 명시하고 로거를 주입해야 한다. | `A2Z/Form1.Dimensions.cs:1495~1548,1568~1695` | Codex | ✅ 수정함 (2026-08-23) |
| 11 | `Form1.Attribute.md` | 3절 | 공유 필드 `selectedObjectIndex`·`selectedObjectName`은 존재하지 않는 이름. 실제는 `selectedAttributeNodeIndex` | `A2Z/Form1.cs:104`; grep 0건 | Claude | ✅ 수정함 (실제 위치 6절 ③, 2026-08-23) |
| 12 | `Form1.BOM.md` | 6절 ③ | 공유 필드 목록의 `osnapList`는 지역변수 이름. 실제 공유 필드는 `osnapPoints`·`osnapPointsWithNames` | grep: 전부 메서드 내 지역 선언 | Claude | ✅ 수정함 (2026-08-23) |
| 13 | `Form1.Clash.md` | 5-5절 | "콜백을 빠져나온 뒤 최대 2초 폴링"의 2초 상한을 코드에서 찾지 못함. Stopwatch는 로그용으로만 보임 — 근거 위치 제시 요청 | grep: `2000`·`TimeSpan` 상한 0건 | Claude | ⚪ 오해 — `maxBusyWaitCount=40` × `Task.Delay(50)` ≈ 2초 (`A2Z/Form1.Clash.cs:910~928`) |
| 14 | `Form1.Clash.md` | 6절 ④ | ✅ 확인 — `bomInfoNodeGroupMap`·`PreparedBomNodeGroupMap` 쓰기 전용 연쇄 주장이 맞음 (읽기 0건) | grep 전수; 죽은 코드.md #6 | Claude | 🔵 의도됨 — 쓰기 전용 상태이므로 삭제 대상 판단 유지 (2026-08-23) |
| 15 | `Form1.MfgDrawing.md` | 5절 ⑥ | “가로축 max/min + 세로축 max/min = 4점만 남긴다”는 실제 반환 수와 다르다. 호출부는 보이는 두 축 각각에 `FilterOsnapForDimAxis`를 호출해 결과를 `AddRange`하고, 축 사이 중복 제거를 하지 않는다. 필터 한 번이 A/B/C/D 최대 4점을 강제 포함하므로 최종 목록은 4점이 아니라 최대 8점이며 같은 극점이 중복될 수도 있다. | `A2Z/Form1.MfgDrawing.cs:2197~2214`; `A2Z/Form1.Dimensions.cs:2634~2753` | Codex | ✅ 수정함 (2026-08-23) |
| 16 | `Form1.MfgDrawing.md` | 6절 ② | UDA 조회 6종이 “`vizcore3d.Object3D.UDA`와 캐시 하나만 쓴다”는 설명은 부모 탐색 의존을 빠뜨렸다. 두 uncached 조회는 매 단계 `vizcore3d.Object3D.FromIndex`로 노드를 읽고 `ParentIndex`를 따라간다. `UdaReader`로 옮길 수는 있지만 UDA API뿐 아니라 노드 트리 조회 어댑터도 필요하다. | `A2Z/Form1.MfgDrawing.cs:2979~3025,3138~3185` | Codex | ✅ 수정함 (2026-08-23) |
| 17 | `Form1.MfgDrawing.md` | 6절 ② | ORIENTATION 4종 전체를 “문자열 → 축·각도, SDK도 화면도 안 쓰는” 순수 파서로 묶을 수 없다. `ParseOrientation`은 문자열이 아니라 `nodeIndex`를 받아 `GetUdaValue`로 SDK-backed UDA를 조회하고, `GetOrientationLabel`도 이를 호출한다. 순수 파서로 떼려면 UDA 조회와 문자열 파싱을 먼저 분리해야 한다. | `A2Z/Form1.MfgDrawing.cs:3197~3200,3261~3268,3466~3527` | Codex | ✅ 수정함 (2026-08-23) |
| 18 | `Form1.MfgDrawing.md` | 6절 ② | 벡터 헬퍼 전부가 순수 함수라는 분류에 `MfgAxisUpPositive`는 들어갈 수 없다. 이 메서드는 현재 카메라의 화면축을 `vizcore3d.View.GetCameraAxis()`로 읽어 결과를 정하므로 카메라 상태 의존 함수다. 축 벡터를 인자로 받도록 바꾼 뒤에야 기하 유틸로 분리할 수 있다. | `A2Z/Form1.MfgDrawing.cs:1007~1021` | Codex | ✅ 수정함 (2026-08-23) |
| 19 | `Form1.DrawingSheets.md` | 5절 ⑦ | “모든 시트의 치수를 미리 계산하고 이후 클릭은 치수 재계산 없이 캐시만 붙인다”는 가공도 시트를 빠뜨렸다. 사전 준비 루프는 `BaseMemberIndex == -3`인 가공도를 명시적으로 건너뛰고, 가공도 클릭 때는 `ExecuteMfgDrawing`을 다시 실행한다. 일반·설치 시트의 캐시 정책과 가공도 경로를 구분해야 한다. | `A2Z/Form1.DrawingSheets.cs:501~505,719~727` | Codex | ✅ 수정함 (2026-08-23) |
| 20 | `Form1.DrawingSheets.md` | 6절 ② | 현재 `GenerateDrawingSheets`를 그대로 “입력만 받아 목록을 반환하는 순수 그래프 계산”으로 설명할 수 없다. 반환형은 `void`이고 `drawingSheetList`·`lvDrawingSheet`를 직접 비우고 채우며, L33의 `CreateFullDrawingSheetData`는 선택 노드를 SDK로 조회하고 L167의 설치 연결 준비도 SDK 접합 데이터를 조회한다. 그래프 코어를 별도 함수로 추출하는 제안은 가능하지만 현재 444줄 전체의 결합도와는 다르다. | `A2Z/Form1.DrawingSheets.cs:20~24,33~35,117,167,239~460,464~478`; `A2Z/Form1.GlobalViews.cs:290~355` | Codex | ✅ 수정함 (2026-08-23) |
| 21 | `Form1.DrawingSheets.md` | 6절 ② | 배율 추정 두 메서드가 “영역과 bbox만 받는” 배치 계산이라는 설명은 실제 시그니처·의존과 다르다. `EstimateFitScaleForCell`은 SDK `Drawing2D.GridStructure`에서 셀·여백을 직접 읽고, `EstimateFitScaleForViewArea`는 참조 프레임이 없으면 SDK `Object3D.GetBoundBox`를 호출한다. 순수 계산으로 빼려면 영역과 bbox를 값으로 계산해 전달하는 앞단이 필요하다. | `A2Z/Form1.DrawingSheets.cs:3424~3431,3910~3939` | Codex | ✅ 수정함 (2026-08-23) |

### 판정 표기

| | |
|---|---|
| ✅ **수정함** | 문서를 고쳤다. 커밋 해시를 같이 적는다 |
| 🔵 **의도됨** | 지적이 맞지만 그대로 둔다. 이유를 적는다 |
| ⚪ **오해** | 지적이 코드를 잘못 읽은 것. 근거를 적는다 |
| 🟡 **보류** | 실기 확인이 필요하다 |

---

## 요약

검증이 끝나면 여기에 숫자만 남긴다.

| | 지적 | 수정 | 의도됨 | 오해 | 보류 |
|---|---|---|---|---|---|
| Claude Code 문서 | 17 | **10** | 0 | 0 | 0 |
| Codex 문서 | 3 (+확인 1) | **2** | **1** | **1** | 0 |

---

## 3차 검증 — 2026-08-23

대상은 Claude Code 문서 8개(`Form1` · `License` · `ExcelTemplate` · `Models` · `Drawing2D` · `Dimensions` · `MfgDrawing` · `DrawingSheets`)다. 상대 문서와 코드는 수정하지 않았다.

### ① 수정 반영분 재확인

교차검증 #1~10·#15~21의 `✅ 수정함`을 다시 코드와 대조했다. **고친 단락만 맞더라도 같은 문서의 다른 절에 지적 전 표현이 남아 있으면 불일치**로 판정했다.

| 기존 # | 결과 | 재확인 내용 | 근거 |
|---|---|---|---|
| 1 | ✅ 일치 | SDK·로거뿐 아니라 `MessageBox`와 WinForms Timer 수명주기까지 분리 조건에 반영했다. | `Form1.License.md:158~185`; `A2Z/Form1.License.cs:33,48~50,70,76,114~117` |
| 2 | ✅ 일치 | 인스턴스 메서드·입력 사전 변경·시계·로그와 `ListViewItem` 형식 결합을 모두 구분했다. | `Form1.ExcelTemplate.md:97~100,198~217`; `A2Z/Form1.ExcelTemplate.cs:31~50,60~68,88~92` |
| 3 | ✅ 일치 | 두 문자열 속성은 현재 UI 소비자 때문에 즉시 삭제할 수 없고, 표시 변환을 Presenter·계산 속성으로 옮긴 뒤에만 삭제한다고 고쳤다. | `Models.md:266~270`; `A2Z/Form1.Dimensions.cs:2191~2199`; `A2Z/Form1.DrawingSheets.cs:562~570` |
| 4 | ✅ 일치 | SDK 형식 결합을 구조적 불가가 아니라 현재 비용을 감수하지 않기로 한 선택으로 구분했다. | `Models.md:258~264`; `A2Z/Models.cs:19~25,104~108,216~229`; `A2Z/Models/MfgViewPose.cs:48~62` |
| 5 | ✅ 일치 | PDF 묶음을 미사용 코드가 아니라 설정으로 켤 수 있는 기본 비활성 운영 경로라고 고쳤다. | `Form1.Drawing2D.md:215~224`; `A2Z/Form1.Drawing2D.cs:998~1027`; `A2Z/App.config:62` |
| 6 | ✅ 일치 | `_999` 후보의 마지막 존재 검사가 없어 덮어쓸 수 있는 경계조건을 정확히 적었다. | `Form1.Drawing2D.md:236~245`; `A2Z/Form1.Drawing2D.cs:1210~1216` |
| 7 | ❌ 불일치 | 6절 표에는 `DrawingSheets` 헬퍼 3종 의존을 넣었지만 같은 절 요약은 여전히 PDF 페이지 관리를 **“완전히 독립”**이라고 단정한다. | `Form1.Drawing2D.md:267,292~300`; `A2Z/Form1.Drawing2D.cs:1024~1027,1100~1105,1190~1203` |
| 8 | ❌ 불일치 | 5절 설명은 정확해졌지만 2절 플로우차트에 여전히 `MergeCoordinates — 0.5mm 안은 같은 점`이라고 적혀 있다. 원좌표가 0.5mm 미만이어도 서로 다른 격자로 반올림되면 합쳐지지 않는다. | `Form1.Dimensions.md:62,184~190`; `A2Z/Form1.Dimensions.cs:2234~2257` |
| 9 | ✅ 일치 | Clash·같은 Part·3mm 근접의 세 접합 경로, 실제 3D 내적, 법선 기준 단일 뷰 선택을 모두 반영했다. | `Form1.Dimensions.md:312~323`; `A2Z/Form1.Dimensions.cs:2876~2891,2915~2957` |
| 10 | ❌ 불일치 | 6절 표에는 제자리 변경과 `DiagLog` 의존을 적었지만 같은 절 요약은 `DimensionFilter`를 다시 **“순수 계산”**이라고 부른다. | `Form1.Dimensions.md:344~354,417~435`; `A2Z/Form1.Dimensions.cs:1495~1548,1568~1695` |
| 15 | ❌ 불일치 | 5절은 최대 8점으로 고쳤지만 2절 플로우차트와 4절 의존 표에는 여전히 **“4점 선별”**이 남아 있다. | `Form1.MfgDrawing.md:107,200,283~291`; `A2Z/Form1.MfgDrawing.cs:2197~2214`; `A2Z/Form1.Dimensions.cs:2634~2753` |
| 16 | ✅ 일치 | UDA API·캐시 외에 `FromIndex`·`ParentIndex` 노드 트리 어댑터가 필요함을 반영했다. | `Form1.MfgDrawing.md:342~346`; `A2Z/Form1.MfgDrawing.cs:2979~3025,3138~3185` |
| 17 | ✅ 일치 | 순수 문자열 파싱 2종과 UDA 조회가 섞인 `ParseOrientation`·`GetOrientationLabel`을 분리해 기술했다. | `Form1.MfgDrawing.md:347~348`; `A2Z/Form1.MfgDrawing.cs:3197~3200,3261~3268,3466~3527` |
| 18 | ✅ 일치 | `MfgAxisUpPositive`를 순수 벡터 헬퍼에서 빼고 현재 카메라 상태 의존으로 분류했다. | `Form1.MfgDrawing.md:349~350`; `A2Z/Form1.MfgDrawing.cs:1007~1021` |
| 19 | ❌ 불일치 | 5절은 가공도 예외를 반영했지만 2절 플로우차트는 모든 시트가 사전 준비된 뒤 **“이후 클릭은 캐시만 적용”**된다고 남아 있다. | `Form1.DrawingSheets.md:71~72,280~286`; `A2Z/Form1.DrawingSheets.cs:501~505,719~727` |
| 20 | ❌ 불일치 | 6절 표는 그래프 코어만 추출한다고 고쳤지만 바로 아래와 요약에서 여전히 `SheetSplitter -444`, **“444줄이 순수 계산으로 빠진다”**고 단정한다. 중복 제거도 표가 제시한 L39~160 밖인 L187~220에 있다. | `Form1.DrawingSheets.md:318,324,358~369`; `A2Z/Form1.DrawingSheets.cs:20~24,39~220,239~460` |
| 21 | ✅ 일치 | 두 배율 추정기가 현재 SDK를 직접 읽으며 영역·BBox 값 전달 경계가 선행돼야 함을 반영했다. | `Form1.DrawingSheets.md:314~321`; `A2Z/Form1.DrawingSheets.cs:3424~3431,3910~3939` |

**재확인 결과: 일치 11건 · 불일치 6건** (`#7` · `#8` · `#10` · `#15` · `#19` · `#20`).

### ② 2·3·4절 무작위 표본 15개

재현 가능한 층화 무작위 표본이다. 8개 문서에서 2절 Mermaid 조건 갈래 47개, 3절의 명시적 읽기/쓰기 행 34개, 4절의 파일 간 호출 행 20개를 후보로 잡고, 고정 시드 `20260823`으로 범주마다 5개씩 뽑았다.

| 표본 | 범주 | 대상 주장 | 결과 | 코드 대조 근거 |
|---|---|---|---|---|
| S01 | 2절 갈래 | `MfgDrawing:62` — 다른 작업 중이면 안내 후 종료 | ✅ 일치 | 취소 가능 작업이 진행 중이거나 시트 목록이 비활성이면 `MessageBox` 뒤 `return` (`A2Z/Form1.MfgDrawing.cs:2858~2868`) |
| S02 | 2절 갈래 | `Dimensions:78` — 뷰가 X·Y·Z면 `ViewDirection`으로 필터하고 빈 값은 공통 | ✅ 일치 | `viewDirection`이 있으면 `d.ViewDirection`이 빈 치수 또는 해당 뷰를 포함한 치수만 선택 (`A2Z/Form1.Dimensions.cs:405~417`) |
| S03 | 2절 갈래 | `MfgDrawing:88` — 일반 부재는 Y가 최장축이면 X뷰, 그 외 Y뷰 | ✅ 일치 | 일반 분기의 `switch`: Y→`X_PLUS`, default→`Y_PLUS` (`A2Z/Form1.MfgDrawing.cs:1920~1927`) |
| S04 | 2절 갈래 | `DrawingSheets:85` — 제작도가 아니면 세계축 유지 | ✅ 일치 | `BaseMemberIndex == -1`일 때만 `TryBuildDrawingReferenceFrame` 결과를 대입하고 나머지는 null 세계축 경로 (`A2Z/Form1.DrawingSheets.cs:2131~2136`) |
| S05 | 2절 갈래 | `MfgDrawing:105` — EA가 아니면 EA 보정을 건너뛰고 은선 Osnap 필터로 진행 | ✅ 일치 | EA 블록은 `if (isEA && ...)`; 종료 뒤 공통 `FilterHiddenLineOsnap`으로 진행 (`A2Z/Form1.MfgDrawing.cs:2044~2049,2193~2195`) |
| S06 | 3절 상태 | `Dimensions:114` — `osnapPointsWithNames` 읽기 | ✅ 일치 | 이 파일에서는 Count·Select 등 읽기만 하며 대입·Clear·Add 없음 (`A2Z/Form1.Dimensions.cs:2112,2147,2172,2205,2220`) |
| S07 | 3절 상태 | `Dimensions:115` — `bomList` 읽기 | ✅ 일치 | Count·인덱싱·순회·`FirstOrDefault`만 사용 (`A2Z/Form1.Dimensions.cs:239,272~274,329,1922~1923`) |
| S08 | 3절 상태 | `Dimensions:113` — `xraySelectedNodeIndices` 읽기·쓰기 | ✅ 일치 | BBox 입력으로 읽고, 치수 추출 진입에서 `Clear`·`Add`로 갱신 (`A2Z/Form1.Dimensions.cs:510~514,2117,2136~2139`) |
| S09 | 3절 상태 | `Drawing2D:111` — `bomList` 읽기 | ✅ 일치 | Count·순회·조회에만 쓰며 목록 변경 없음 (`A2Z/Form1.Drawing2D.cs:45,70,425~426,824,832,927`) |
| S10 | 3절 상태 | `DrawingSheets:123` — `bomInfoNodeGroupMap`·`bodyToPartIndexMap` 읽기·쓰기 | ❌ 불일치 | `DrawingSheets.cs`에는 `bomInfoNodeGroupMap` 참조가 0건이고 `bodyToPartIndexMap`도 L49~51에서 읽기만 한다. 실제 쓰기는 `Clash.cs:639~641`과 `BOM.cs:45,93`이다. |
| S11 | 4절 호출 | `Drawing2D:156` — `MergeCoordinates`·`AddChainDimensionByAxis`는 `Dimensions.cs` 기능 | ✅ 일치 | 호출 `A2Z/Form1.Drawing2D.cs:605~616`; 선언 `A2Z/Form1.Dimensions.cs:2234,2303` |
| S12 | 4절 호출 | `MfgDrawing:202` — `FillRevisionTable`·`KeepBorder`는 `ExcelTemplate.cs` 기능 | ✅ 일치 | 호출 `A2Z/Form1.MfgDrawing.cs:173~182`; 선언 `A2Z/Form1.ExcelTemplate.cs:31,80` |
| S13 | 4절 호출 | `DrawingSheets:173` — `PrepareInstallationConnectionData`는 `GlobalViews.cs` 기능 | ✅ 일치 | 호출 `A2Z/Form1.DrawingSheets.cs:167`; 선언 `A2Z/Form1.GlobalViews.cs:290` |
| S14 | 4절 호출 | `Dimensions:159` — `CreateIsoBalloonNotes`는 `DrawingSheets.cs` 기능 | ✅ 일치 | 호출 `A2Z/Form1.Dimensions.cs:339,349`; 선언 `A2Z/Form1.DrawingSheets.cs:960` |
| S15 | 4절 호출 | `Drawing2D:154` — `GenerateSheetDrawing2D`는 `DrawingSheets.cs` 기능 | ✅ 일치 | 호출 `A2Z/Form1.Drawing2D.cs:75`; 선언 `A2Z/Form1.DrawingSheets.cs:1707` |

**표본 결과: 일치 14건 · 불일치 1건.** 새 불일치는 `Form1.DrawingSheets.md` 3절의 두 매핑 필드 읽기/쓰기 주장(S10)이다.

### 판정·조치 (Claude, 2026-08-23)

**7건(잔존 6 + S10) 전부 ✅ 수정함.** 같은 표현으로 문서 전체를 재grep해 잔존 0건 확인, verify.py 0건.

| 건 | 조치 |
|---|---|
| #7 잔존 | 정리 블록 "완전히 독립" → "자기 상태로 닫힘 (헬퍼 3종 주입 필요)" |
| #8 잔존 | 2절 플로우차트 노드 문구 교정 |
| #10 잔존 | 정리 블록 "순수 계산" → "SDK 무관 (입력 변이·로그 정리 후)" |
| #15 잔존 | 2절 노드·4절 표 "4점" → "축당 4점 × 2축 = 최대 8점" |
| #19 잔존 | 2-1 플로우차트 마지막 노드에 가공도 예외 반영 |
| #20 잔존 | "444줄 분리" → "그래프 코어 약 180줄(L39~220)" · 정리 블록 동기화 |
| S10 | 3절에서 `bomInfoNodeGroupMap` 행 삭제(참조 0건) · `bodyToPartIndexMap` 읽기로 정정 |

> 교훈: **부분 수정 패턴** — 지적을 고칠 때 표의 그 행만 고치고 요약·플로우차트에 같은 주장이 남았다.
> 이후 수정은 반드시 **옛 표현으로 문서 전체 재검색**을 짝으로 한다.
