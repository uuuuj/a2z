# 3차 검증 — 누락·표본 감사

2026-08-23. 1·2차가 "적힌 게 맞나"(문서→코드)였다면, 3차는 **"빠진 게 없나"(코드→문서)** 와 **무작위 표본 감사**다.

| 파트 | 담당 | 상태 |
|---|---|---|
| ① 누락 스캔 (기계) | Claude | ✅ 아래 |
| ② 자동생성 3개 재검증 (기계) | Claude | ✅ 아래 |
| ③ 수정 반영분 재확인 + Claude 문서 표본 15개 | Codex | ⏳ 진행 중 |
| ④ Codex 문서 표본 15개 | Claude | ✅ 아래 |

---

## ① 누락 스캔 — 50줄 이상 메서드 84개 중 손문서 미언급 12개

### 🔴 최대 발견: 죽은 사슬이 더 컸다

`RenderSheetViewForDrawing` **344줄**(DrawingSheets:3966)이 어느 손문서에도 없었다. 추적 결과 —

```
GenerateSheetDrawing2DCore (361줄, 도달 불가 — 죽은 코드 #7)
   └→ RenderSheetViewForDrawing (344줄) — 호출 4곳 전부 Core 안 (L1941~1950)
        └→ EstimateFitScaleForCell (59줄) — 호출 2곳 전부 RenderSheetView 안 (L4074·4219)
```

**옛 무템플릿 경로 사슬 = 약 764줄.** 문서 누락이 아니라 **죽은 코드 #7의 확장**이다.
→ [`죽은 코드.md`](./죽은%20코드.md) 갱신: 확정 소계 **757 → 약 1,160줄 (전체의 5.6%)**

부수 확인: 교차검증 #21이 지적한 `EstimateFitScaleForCell`의 SDK 의존은 **죽은 사슬 소속**이라 리팩토링 논의 대상에서 빠진다 (`ForViewArea`만 살아 있음).

### 나머지 11개 — 전부 호출자 실존, 문서 보강 대상 (경미)

| 메서드 | 줄 | 분류 |
|---|---|---|
| `DetectMfgAxis` (MfgDrawing:3692, 93줄) | LINE Osnap 5° 군집화로 주 길이축 판정 — **실제 알고리즘인데 소유 문서(MfgDrawing) 5절에 없음.** GlobalViews 문서 4절에만 한 줄 언급 | 🟠 보강 1순위 |
| `BuildMfgPageData` (92줄) · `TryResolveMfgHoleThroughAxis` (85줄) · `TryBuildMfgOrientationReferenceFrame` (80줄) · `GetMfgSameSideDimensionEnvelopeCanvas` (64줄) · `LogMfgAxisDetection` (54줄) | MfgDrawing 헬퍼들 — 플로우차트 노드로는 덮이나 이름 미기재 | 🟡 |
| `TryGetInstallationMainAxis` (GlobalViews:729, 66줄) | **Codex 소유 문서의 누락** → Codex 몫 | 🟡 → Codex |
| `DumpDrawingBomStruDiagnostics` (55줄) · `EnsureFabricationNeighborSearchCache` (53줄) | Clash — 진단·캐시 | 🟡 → Codex |
| `GetFabricationNeighborAssemblyNotes` (51줄) | DrawingSheets | 🟡 |
| `CreateMergedDimension` (Dimensions:1771, 50줄) | `MergeShortDimensions`의 짝 — 병합 치수 생성 | 🟡 |

> 판단: **치명 누락은 0건** (최대 건은 죽은 코드였음). 11건은 "이름까지 기재"가 안 된 수준이며, 상위 흐름 서술로는 덮여 있다. 보강은 리팩토링 방안·발표에 영향 없어 후순위.

## ② 자동생성 3개 — 독립 재산출과 전수/표본 대조

| 문서 | 방법 | 결과 |
|---|---|---|
| 버튼별 코드 위치 | Designer.cs에서 배선 37개 독립 재추출 → 표 37행 전수 대조 | ✅ **불일치 0** |
| 함수 목록 | 무작위 12개(seed 823) 선언 줄±2 대조 | ✅ **12/12** |
| 파일 구조 | 공유 상태 3행(vizcore3d 826/11 · bomList 96/8 · chainDimensionList 60/6) 재집계 | ✅ **3/3** |

## ④ Codex 문서 표본 15개 (2·3·4절 사실 주장, 문서당 3개)

| # | 문서 | 주장 | 결과 |
|---|---|---|---|
| 1 | BOM | `LargeBomTargetWarningThreshold` = 5,000 | ✅ L13 |
| 2 | BOM | `CancelableBomScanInterval` = 200 (스캔 200개마다 체크) | ✅ L15·L800 |
| 3 | BOM | `bodyToPartNameMap`/`IndexMap` 쓰기 | ✅ 대입 4곳 |
| 4 | Clash | `_silentClashPendingTestIds` 큐 + `ResetSilentClashSequence`가 정리 | ✅ L848~850 |
| 5 | Clash | `drawingSheetList` 읽기 | ✅ 5회 |
| 6 | Clash | `_silentClashActiveTestId` 대기 ID | ✅ L14 |
| 7 | Stru | `_p2aInProgress`를 BOM·Clash도 읽음 | ✅ 2·4회 |
| 8 | Stru | `_struNodeCache` 체크 목록과 1:1 | ✅ L42 |
| 9 | Stru | `xraySelectedNodeIndices` 매번 비움 | ✅ L598·843 |
| 10 | Attribute | `selectedAttributeNodeIndex` 미선택 = −1 | ✅ 초기값·리셋 |
| 11 | Attribute | `GeometryProperty.FromIndex` 조회 | ✅ L185~189 |
| 12 | Attribute | `UDA.FromIndex(int,string)` 사용 | ✅ 1회 |
| 13 | GlobalViews | `BoundsOverlapWithinClearance` = Clash.cs L763 | ✅ 선언·호출 |
| 14 | GlobalViews | `fabricationBodyToPartIndexCache` 읽기 | ✅ 1회 |
| 15 | GlobalViews | `SnapToNearestOsnap` L998 | ✅ L998 |

**15/15 일치 — 표본 오류율 0%.**

---

## 잠정 결론 (Codex ③ 대기)

- 문서→코드 방향(1·2차)에 이어 **코드→문서 방향에서도 치명 누락 0건** — 최대 미기재 건은 죽은 코드였다
- 자동생성·Codex 문서 표본 **30건 전수 일치**
- 발표에서 말할 수 있는 문장: *"20,578줄 전수 문서화 → 3중 검증(기계·상호·표본 감사) → 지적 21건 반영 → 최종 표본 30건 오류 0"*
