# 주석 검토 (4차 — Codex)

2026-08-23. 메서드 요약 주석 118개(커밋 `0289fdc`)에 대한 Codex 검토.
**지적 6건 전원 코드로 재확인 → 전부 수용·반영** (커밋은 아래 「반영」 참조).
프로토콜: Codex는 지적만, 판정·코드 반영은 Claude 단독 (충돌 방지).

| # | 파일 | 메서드 | 구분 | 지적 | 제안 문구 | 판정 |
|---|---|---|---|---|---|---|
| 1 | `Form1.Clash.cs` | `BuildDrawingBomPreparationContext` | 규칙 | 두 번째 요약 줄이 92자로 90자 제한을 넘는다. | 대상 Body 인덱스를 받아 → Part별 BOM 원자료와 BOM 번호 맵을 돌려준다.<br>대상이 null이면 모델 전체 Part를 쓰고, 고정 UDA 6개와 설정의 MA·FA 키를 읽는다.<br>STRU 미검출 또는 `Uda.DumpOnLoad=true`면 조상 체인 진단 덤프를 남긴다. | ✅ 수용 — 코드 확인 후 반영 |
| 2 | `Form1.Clash.cs` | `BuildDrawingBomSnapshot` | 사실 | “빈 MATERIAL·SIZE·T/W·MA·FA는 `-`, Q'TY는 항상 1”이라는 설명은 요약행에는 맞지 않는다. 요약행은 MATERIAL·SIZE·Q'TY가 빈 문자열이고 MA·FA가 `F`다. | 준비 컨텍스트와 Body 인덱스를 받아 → 00 요약행과 부재 행을 BOM 번호순으로 만든다.<br>요약행 T/W는 STRU GWEI를 우선하고, 없으면 부재 무게 합산으로 폴백한다.<br>부재 행의 빈 MATERIAL·SIZE·T/W·MA·FA는 `-`, Q'TY는 1로 넣는다. | ✅ 수용 — 코드 확인 후 반영 |
| 3 | `Form1.GlobalViews.cs` | `GetInstallationBodyPoints` | 사실 | `boundsFallback`은 Osnap이 없으면 Bounding Box 조회 성공 전에 이미 `true`가 된다. 따라서 Bounding Box가 null이거나 예외로 빈 목록을 돌려줘도 `true`로 남는다. | BODY 인덱스를 받아 → LINE/POINT Osnap 좌표를 중복 없이 돌려준다.<br>Osnap이 없으면 `boundsFallback=true`로 하고 Bounding Box 8개 모서리를 시도하며, 그것도 못 구하면 빈 목록을 돌려준다. | ✅ 수용 — 코드 확인 후 반영 |
| 4 | `Form1.Dimensions.cs` | `MarkNonRightAngles` | 사실 | `clashList`를 “표면접촉”으로 한정한 설명이 틀렸다. 해당 목록은 3mm Clearance 검사 결과이며, 코드는 같은 Part·`clashList` 쌍·최근 Osnap 점 3mm 이내 중 하나만 맞아도 접합으로 본다. | 부재 Body 목록과 뷰 방향을 받아 → 비직각 접합부에 파란 3D 각도 치수를 찍는다.<br>판형 부재는 빼고, 같은 Part·Clash 3mm 근접 결과·Osnap 점 3mm 이내를 접합으로 본다.<br>실제 3D 내적으로 각을 구하고 접합 평면이 가장 잘 보이는 한 뷰에만 그린다. | ✅ 수용 — 코드 확인 후 반영 |
| 5 | `Form1.MfgDrawing.cs` | `LogMfgAxisDetection` | 사실 | ORIENTATION 원문이 존재해도 `ParseOrientation`이 축을 파싱하지 못하면 기하 판정으로 폴백한다. “ORIENTATION이 있으면 그것”은 유효성 조건을 누락한다. | 부재를 받아 → LINE Osnap 주축 판정과 ORIENTATION 파싱값을 비교한 진단 로그를 남긴다.<br>ORIENTATION이 유효한 축으로 파싱되면 그 각도를, 아니면 기하 결과를 기울어짐 판정 근거로 쓴다. | ✅ 수용 — 코드 확인 후 반영 |
| 6 | `Form1.cs` | `DiagLog` | 호출처 | “모든 Form1 파일이 공용으로 부른다”는 과장이다. `Form1.Attribute.cs`는 `DiagLog`를 호출하지 않고 `Debug.WriteLine`을 직접 쓴다. | 진단 메시지를 받아 → 시각을 붙여 exe 폴더의 로그 파일과 Debug 출력에 기록한다.<br>파일 쓰기 실패는 삼켜 앱 흐름에 영향을 주지 않으며, Form1의 여러 부분 클래스가 공용으로 부른다. | ✅ 수용 — 코드 확인 후 반영 |

## 집계

- ① 전수 40건 중 지적 3건
- ② 표본 20건 중 일치 17건·불일치 3건 (오류율 15%)

## 판정 (Claude, 2026-08-23)

**6건 전원 ✅ 수용.** 각 건을 코드 본문으로 재확인했고, 전부 Codex 지적이 맞았다.

| # | 재확인 근거 |
|---|---|
| 1 | 해당 줄 실측 **106자** (Codex는 92자로 셈 — 어느 쪽이든 90자 초과). 규칙 위반 확정 |
| 2 | `Form1.Clash.cs` L551~558: 요약행은 `Material=""` `Size=""` `Quantity=""` `Ma="F"` `Fa="F"`. 부재 행만 `ToDrawingBomDisplayValue`로 빈 값이 `-`, `Quantity="1"` |
| 3 | `Form1.GlobalViews.cs` L746에서 `boundsFallback = true`가 BBox `try` **앞**에 있다. `bounds == null` 또는 예외 시 빈 목록을 돌려주면서도 플래그는 true로 남는다 |
| 4 | `Form1.Clash.cs` L1064~1065: `UseClearanceValue = true` · `ClearanceValue = 3.0f`. clashList는 **3mm 근접** 결과이지 표면접촉이 아니다. 판정도 `isClash \|\| bestD <= MarkJunctionTol` OR 조건 |
| 5 | `orientationAvailable = !IsNullOrWhiteSpace(raw) && !IsNullOrEmpty(axis)` — 원문이 있어도 축 파싱 실패면 `GEOMETRY_FALLBACK` |
| 6 | `Form1.Attribute.cs`의 `DiagLog(` 호출 **0건** (`Debug.WriteLine` 직접 사용) |

### 함께 처리한 것 (지적 #1에서 파생)

전체 `///` 주석의 90자 초과를 전수 재검사해 **이번 작업분 4건을 추가로 줄였다**
(`AddWorldAxisDistance` · `TryBuildMfgOrientationReferenceFrame` 2줄 · `CreateIsoBalloonNotes`).
기존 주석 3건(`BOM.cs:689` · `DrawingSheets.cs:1794` · `Stru.cs:798`)은 이번 범위 밖이라 두었다.

## 최종 집계

| | |
|---|---|
| 검증 층 | 초안 → 독립 검증(에이전트) → **Codex 검토(3단째 눈)** |
| 2단 검증 정정 | 118건 중 **28건** |
| 3단 Codex 지적 | 전수 40건 중 3 · 표본 20건 중 3 = **6건, 전원 수용** |
| 표본 오류율 | 20건 중 17 일치 · 3 불일치 = **15%** |
| 최종 커버리지 | 메서드 **318/318 (100%)** |

> 표본 오류율 15%는 "2단 검증을 통과하고도 6건 중 3건이 남았다"는 뜻이다.
> 주석은 문서와 달리 **코드 옆에 영구히 붙는다** — 3단을 돌린 이유이고, 앞으로 주석을 고칠 때도
> 같은 기준(본문 직접 대조)을 쓴다.
