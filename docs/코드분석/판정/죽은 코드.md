# 죽은 코드 전수 목록

파일별 13개 문서에서 나온 죽은 코드를 모으고, **각 건을 기계(grep)로 재확인**한 결과다. 2026-08-23 집계.

> ✅ **단계 0 삭제 완료.** 확정 8건 + 후속 #11·#12 전부 삭제 — 커밋 8개, 매건 빌드 통과.
> 실측 삭제 합계 **1,415줄** (0-1~0-4 460 + 0-5 733 + #6 잔여 43 + #11 65 + #12 17 + #13 97). 코드 20,578 → **19,029줄**.
> 삭제 원칙 준수: 배선·호출자 grep 재확인 → assert 경계 검증 → 삭제 → 빌드 → 커밋 (한 건씩).

## 진행 (2026-08-23 D2 승인 후 착수)

| 삭제 순서 | 상태 |
|---|---|
| 0-1 풍선 배치 272줄 | ✅ 삭제·빌드 통과 |
| 0-2 프로브 94줄 | ✅ 삭제·빌드 통과 (CS0162 경고 소멸) |
| 0-3 핸들러 8개 52줄 | ✅ 삭제·빌드 통과 |
| 0-4 GlobalViews 2함수·주석 블록·쓰기 전용 연쇄 | ✅ 삭제·빌드 통과 |
| 0-5 옛 도면 경로 사슬 | ✅ 삭제·빌드 통과 (커밋 134057d, **733줄**) — 사용자에게 경로 설명 후 승인받아 삭제 (2026-08-23). 실기 확인은 사용자가 나중에 일괄 수행 예정 |
| #6 잔여 쓰기 전용 연쇄 | ✅ 삭제·빌드 통과 (커밋 dca8fde, **43줄**) — NodeGroupMap 선언·채움 + 유일한 입력이던 PartToBodyIndices 빌드 블록 2개까지 완전 제거 |
| #13 도달 불가 함수 2건 | ✅ 삭제·빌드 통과 (**97줄**) — `PlaceImageInTemplateArea`(83줄, DrawingSheets) · `GetOrientationLabel`(14줄, MfgDrawing). **호출 그래프 도달성 분석**으로 발견 (2026-08-24) |
| #12 죽은 P/Invoke 블록 | ✅ 삭제·빌드 통과 (**17줄**) — `SendMessage`·`SetFocus` Win32 선언 + `WM_MOUSEWHEEL`·`WHEEL_DELTA` 상수 + `using System.Runtime.InteropServices`. 호출처 전부 0건, 현행 줌은 SDK `View.ZoomRatio` 사용. 주석 작업 중 발견 → 사용자 지적으로 즉시 삭제 (2026-08-24) |
| #11 모델 이동량 연쇄 | ✅ 삭제·빌드 통과 (커밋 b4f76d2, **65줄**) — 계산 블록 47 + 리셋 6 + 필드 9 + 입력 `canvasMaxOff` 선언·헬퍼 out 매개변수(가공도는 이미 `out _`)까지. 사용자 승인 2026-08-23 |

~~⚠ 남은 부스러기: `DrawingBomSnapshot.NodeGroupMap` 유지~~ → **해소 (2026-08-23, 커밋 dca8fde).**
grep 전수로 연쇄를 끝까지 추적: NodeGroupMap ← partToGroup ← PartToBodyIndices, 세 단계 모두
읽는 곳이 사슬 안에만 있어(밖 0건) 통째로 제거. 잔존 참조 0건 확인.

### 0-5 실측 기록 (2026-08-23)

문서의 "764줄"은 Core 361줄 전체를 죽은 것으로 센 값이었다. 실제로는 **Core 도입부
약 15줄(엣지 데이터 준비 + 분기)은 산 코드**라 남기고 옛 본문만 파냈다:

| 삭제분 | 줄수 |
|---|---|
| Core 옛 본문 (try~catch, `if (UseExcelTemplate)` 분기 아래) | 330 → 직행 호출 6줄로 교체 |
| `RenderSheetViewForDrawing` (doc 포함) | 350 |
| `EstimateFitScaleForCell` (doc 포함) | 54 |
| `UseExcelTemplate` 필드 + 주석 | 5 |
| **합계 (순감소)** | **733** |

부수 정정: 주석 3건 — `CreateIsoBalloonNotes` 공유처(이미 6곳이었음) · `EstimateFitScaleForViewArea`
출처 표기("옛 … 삭제") · `_lastModelShiftCanvasX/Y` 소비처 (Form1.cs).

## 검출 방법 3종 (2026-08-24 추가)

| 방법 | 무엇을 잡나 | 발견 |
|---|---|---|
| **배선 검사** (`+=` 유무) | 등록 안 된 이벤트 핸들러 | #2·#3 (핸들러 8개) |
| **grep 호출처** | 아무도 안 부르는 함수·필드 | #4·#5·#6·#11·#12 |
| 🆕 **호출 그래프 도달성** | **진입점 54개에서 출발해 못 닿는 함수** | **#13** (2건 97줄) |

> 🔑 **도달성 분석이 grep보다 강한 이유**: `BeginInvoke(new Action(Name))` 처럼
> **괄호 없이 이름만 넘기는 호출**을 grep은 놓치지만 도달성은 잡는다.
> 실제로 `StartNextSilentClashTestAfterEvent`는 grep상 "호출 0건"으로 보였으나
> `Clash.cs:1014`에서 이 형태로 불리고 있었다 — **살아 있다.** 반대 방향 오판도 이 방법이 막는다.
> 스크립트: `scratchpad/reach.py` (진입점 54 → 도달 311 / 320개)

## 확정 — 실행되지 않음이 기계로 확인된 것

| # | 위치 | 무엇 | 줄수 | 근거 |
|---|---|---|---|---|
| 1 | `Form1.Dimensions.cs` L800~1020 | **풍선 배치 블록** — `balloonEntries.Clear()` 직후 그 목록을 소비. 끝단 추적·4분면 분류·정렬·배치 루프 전부 | **약 220** | 채우는 코드가 2026-07-22 삭제됨. L893 `Clear()` → L895 `foreach` 항상 0회 |
| 2 | `Form1.Dimensions.cs` L205~236 | `btnShowAxisX/Y/Z_Click` · `btnShowISO_Click` | 32 | 코드베이스 전체에 배선(`+=`) 없음. 살아 있는 버튼은 `GlobalViews.ApplyGlobalView` |
| 3 | `Form1.DrawingSheets.cs` L1134~1153 | `btnDrawingISO/AxisX/AxisY/AxisZ_Click` | 약 20 | 배선 없음. #2와 같은 패턴 (옛 화면 호환용 껍데기) |
| 4 | `Form1.MfgDrawing.cs` L296 | `RunMfgCameraSignProbe` | **80** | 게이트 `MfgCameraSignProbeEnabled = false` 가 `const` (L1493). 주석에 "검증 후 프로브째 제거" 명시 |
| 5 | `Form1.GlobalViews.cs` L796 · L802 | `ProjectInstallationPoint` · `GetInstallationPerpendicularDistance` | 약 12 | 호출자 0건 (2026-08-23 재확인). 접합 기준을 월드축 성분으로 재설계하며 남은 것 (Codex 발견) |
| 6 | `Form1.Clash.cs` L621~641 외 | **`bomInfoNodeGroupMap` · `PreparedBomNodeGroupMap` 쓰기 전용 연쇄** — Prepared→bomInfo로 복사만 하고 아무도 안 읽음 | 약 25 + 선언 2곳 | grep 전수: Clear·대입뿐, 읽기 0건 (Codex 발견 → 2026-08-23 기계 확인) |
| 7 | `Form1.DrawingSheets.cs` L1720 | **`GenerateSheetDrawing2DCore` 사슬** — 템플릿 없는 옛 도면 경로 전체. `Core`(361) → `RenderSheetViewForDrawing`(344, L3966 — 호출 4곳 전부 Core 안) → `EstimateFitScaleForCell`(59, 호출 2곳 전부 RenderSheetView 안) | **764** | 분기 스위치 `UseExcelTemplate = true` 하드코딩 필드(L1681), false로 바꾸는 코드 없음 → 사슬 전체 도달 불가. 하위 2개는 3차 검증 누락 스캔이 발견 (2026-08-23) |
| 8 | `Form1.Drawing2D.cs` L224~230 | 주석 처리된 디버그 `MessageBox` 블록 | 7 | 주석 |

**확정 소계: 약 1,160줄** (전체 20,578줄의 **5.6%**) — 3차 검증에서 #7 사슬 +403줄 (2026-08-23)

## 미확정 — 담당자(Codex) 판정 대기

> #11은 2026-08-23 기계 확인 → 사용자 승인 → 삭제 완료 (위 진행 표). 아래 행은 기록용.

| # | 위치 | 무엇 | 상태 |
|---|---|---|---|
| 9 | `Form1.Stru.cs` L791 · L938 | `reportPdfSaved` 매개변수(기본 null) · `pdfCount`(늘 0 주장) | Codex 발견. L1015 주석은 "합산"이라 하나 실제 합산 여부 미확인 → **Codex 판정** |
| 11 | `Form1.cs` L167 · `Form1.Dimensions.cs` L577~620 | `_lastModelShiftCanvasX/Y` + 계산 블록 — "보조선 반대쪽 모델 이동" 값(T-038 v8, 2026-05-12 사용자 사양) | **기계 확인 완료 (2026-08-23)**: ① 필드 읽기 = 대입 직후 자기 DiagLog 1곳뿐 ② 계산 if 블록(44줄)은 자기완결 — 지역변수 전부 블록 안, 산출물은 필드 2개+로그뿐 ③ 유일 소비자(옛 경로 MoveObject)는 P2 때 이미 도달 불가 → **이 이동은 현행 운영에서도 원래 안 일어나고 있었음** (새 경로는 viewArea 중앙 배치로 대체). 삭제 시 동작 불변, 총 ~50줄 (블록 44 + 선언 2 + 리셋 4). 사용자 지시 시 삭제 |
| 10 | `Form1.Attribute.cs` | CSV 왕복 불일치 (`No,Key,Value` 출력 vs 2열 입력) | 죽은 코드가 아니라 결함. 교차검증 흐름에서 처리 |

## 지우면 안 되는 것 — 죽어 보이지만 자리 잡아둔 코드

| 위치 | 무엇 | 왜 남기나 |
|---|---|---|
| `Form1.ExcelTemplate.cs` L31 | `FillRevisionTable`의 5행 처리 (지금은 이력 1건뿐) | **#64 Phase 3** (이력 누적) 예정 자리 |
| `Form1.MfgDrawing.cs` L1503 | `FilterMfgDimensions` 껍데기 (한 줄 위임) | 가공도 고유 규칙 확장 지점으로 의도된 것 — 주석 명시 |
| `Form1.DrawingSheets.cs` 슬롯 164·200 | Note 채우기 주석 코드 | 입력 소스 구현 시 사용 예정 |

## 삭제 순서 제안

```
1. #1 풍선 배치 220줄     ← 가장 크고 가장 확실. ShowAllDimensions 663→440줄
2. #4 프로브 80줄          ← const false 뒤. 위험 0
3. #2·#3 죽은 핸들러 8개   ← 배선 없음 재확인 후
4. #5·#6·#8               ← 소소
5. #7 Core 사슬 764줄      ← 🟡 마지막. "템플릿 없이 뽑는" 비상 경로로 쓸 가능성을
                             사용자에게 확인 후. 지운다면 UseExcelTemplate 필드도 함께
                             (Core 361 + RenderSheetViewForDrawing 344 + EstimateFitScaleForCell 59)
```

각 건 삭제 후 **빌드 통과 확인 → 개별 커밋** (되돌리기 쉽게).
