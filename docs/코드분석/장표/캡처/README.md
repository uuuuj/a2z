# 장표에 넣을 캡처

이 폴더에 PNG를 넣고 말하면 덱에 base64로 박아 넣는다 (단일 HTML 유지).
**파일명이 정확해야 자동으로 들어간다.**

## 이미 들어간 것

| 파일명 | 들어간 장 |
|---|---|
| `폴더 구조.png` | 04 남은 것 — 왼쪽 |

## 아직 필요한 것 — 06 「제작도 — 실제 화면」

앞 장(05)의 일곱 단계를 **실제로 돌린 순서**로 다섯 칸에 들어간다.

| 파일명 | 무엇을 찍나 |
|---|---|
| `모델열기_코드.png` | `Form1.BOM.cs:182` `btnOpen_Click` — VS 편집기, 함수 전체가 보이게 |
| `STRU선택_화면.png` | 프로그램 좌측 STRU 목록에서 체크한 상태 |
| `가시성격리_화면.png` | 체크 직후 3D 뷰 — 대상만 남고 나머지가 사라진 화면 |
| `치수표시_화면.png` | 치수 추출 후 3D에 체인 치수가 그려진 화면 |
| `제작도_결과.png` | 나온 PDF 한 장 |

- 덱이 흰 바탕이라 **VS도 밝은 테마**로 찍는 쪽이 붙었을 때 자연스럽다
- 칸이 세로로 길다 (약 3:4). 가로로 넓은 화면은 필요한 부분만 잘라 찍는 편이 낫다
- 사내 서버 주소·계정·실제 도면명이 보이면 가리고 찍는다
