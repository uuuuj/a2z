# 메서드 지도 — 파일별 역할과 연결

> **발표 부록.** "이 파일에 어떤 메서드가 있고, 각각 무슨 역할이고, 서로 어떻게 이어지나".
> 질문을 받았을 때 펼치는 용도. 흐름은 [시나리오 문서](./시나리오%20—%20버튼에서%20도면까지%20값이%20흐르는%20길.md)를 본다.
> 기준 코드 2026-08-26. 메서드 318개 전부에 `/// <summary>` 주석이 있다 (3중 검증 완료).

## 요약

| 파일 | 줄 | 메서드 | 층 | 역할 |
|---|---|---|---|---|
| **Form1.cs** | 478 | 34 | 기반 | partial class Form1의 뿌리 — 공유 필드 대부분을 선언하고, 앱 시작 시 컨트롤·이벤트를 배선하며, 모든 파일이 함께 쓰는 진단 로그와 '처리 중' 오버레이·취소 장치를 제공한다. |
| **Form1.License.cs** | 145 | 8 | 중간 | VIZCore3D SDK 라이선스 서버에 붙고 30분마다 다시 붙는 일만 한다 — 로컬(127.0.0.1) 먼저, 죽어 있으면 사내 서버로 폴백. |
| **Form1.BOM.cs** | 1095 | 18 | 조립자 | 모델을 열고 → 부재 목록(BOM)과 Osnap 점을 모아 → 치수 추출 파이프라인 전체를 지휘하는 사령탑. 간섭검사 앞부분과 뒷부분을 나눠 들고 Clash 이벤트를 사이에 끼운다. |
| **Form1.Stru.cs** | 1125 | 17 | 조립자 | 모델트리에서 STRU(구조물 단위)를 찾아 목록으로 띄우고, 체크한 STRU를 하나씩 자동으로 돌려 도면 4종 PDF까지 뽑는 일괄 출력 담당. 사람이 손으로 하던 클릭 순서를 코드로 그대로 재현한다. |
| **Form1.Clash.cs** | 1431 | 30 | 조립자 | 부재 쌍마다 간섭검사를 걸어 '무엇이 무엇과 붙어 있는가'를 알아내고, 그 결과로 연결성을 판정해 도면 파이프라인의 문을 연다. 더불어 도면 우측 BOM 표(UDA 속성 기반)를 통째로 만든다. |
| **Form1.Drawing2D.cs** | 1273 | 28 | 중간 | 2D·PDF 출력의 하부 설비 + Osnap 수집 UI. 앞쪽 절반은 Osnap·간섭 목록 버튼 핸들러, 뒤쪽 절반은 도면 여러 장을 PDF 한 개로 묶는 페이지 누적 장치다. 두 덩어리는 서로 거의 무관하다. |
| **Form1.Dimensions.cs** | 2674 | 34 | 중간 | 체인치수 엔진. Osnap 좌표 → 체인치수 생성 → 겹치는 것 골라내기 → 단(段)을 나눠 3D 뷰에 치수·보조선을 그린다. 제작도·조립도·설치도·가공도 네 도면이 전부 이 파일의 ShowAllDimensions / ComputeViewDimensionsForMembers를 거친다. |
| **Form1.GlobalViews.cs** | 1206 | 28 | 조립자 | 두 가지 일이 한 파일에 있다. 앞 195줄은 탭 공통 ISO/X/Y/Z 뷰 버튼(상황에 따라 시트뷰·선택부재뷰·전체모델뷰로 갈린다), 나머지 1,000줄은 설치도 전용 — 간섭 결과로 접합 영역을 구성하고 「기준부재 끝단 → 연결부재 접합 모서리」 위치 치수를 만든다. |
| **Form1.DrawingSheets.cs** | 3564 | 56 | 조립자 | 간섭 결과를 그래프로 보고 도면을 몇 장으로 나눌지 정한 뒤, 한 장마다 엑셀 양식을 깔고 4면도(ISO·Z·X·Y)를 찍어 넣는다. 제작도·조립도·설치도 3종의 PDF 일괄 출력도 여기서 돈다. |
| **Form1.ExcelTemplate.cs** | 101 | 5 | 기반 | 엑셀 템플릿 표제부를 채울 때 쓰는 작은 헬퍼 4개 — REV 이력 표 채우기와, 괘선이 지워지지 않게 하는 값 보정. |
| **Form1.MfgDrawing.cs** | 3926 | 72 | 조립자 | 부재 하나만 화면에 남기고, 제일 긴 축이 가로가 되게 돌려 세우고, 2D로 찍은 다음, 그제서야 확정된 실측 배율로 치수와 풍선을 그린다. 미리보기(3D)와 PDF가 같은 장면 코어를 쓰고 어댑터만 다르다. 프로젝트에서 파일 수 대비 코드가 가장 무겁다. |
| **Form1.Attribute.cs** | 618 | 17 | 중간 | 부재 정보 탭. 3D에서 부재를 클릭하면 기본정보·바운딩박스·UDA·지오메트리 4개 절을 표에 채우고, UDA를 추가·편집·삭제·CSV 일괄 가져오기 할 수 있게 한다. 다른 도면 로직과 거의 독립적이다 — 밖으로 새는 것은 selectedAttributeNodeIndex 하나뿐. |
| **Models.cs** | 259 | 13 | 기반 | 13개 파일이 주고받는 데이터 모양을 한 곳에 모은 순수 데이터 클래스 10개 — 메서드는 생성자 3개뿐이고 로직이 전혀 없다. |
| | | **360** | | |

**층 구조** — 강제된 경계는 없지만 역할은 갈려 있다.

```
조립자층   남을 많이 부른다 · 버튼 뒤       DrawingSheets · MfgDrawing · Stru · BOM · Clash · GlobalViews
중간층     부르기도 불리기도 한다 · 계산    Dimensions · Drawing2D · Attribute · License
기반층     남이 많이 부른다 · 공용 상태     Form1(불림 408회) · ExcelTemplate · Models
```

> **"모듈화가 안 됐다"가 아니라 "층은 이미 있는데 컴파일러가 강제하지 않는다"** 이다.
> 리팩토링은 없는 구조를 새로 짜는 게 아니라 **이미 있는 층을 진짜 클래스로 굳히는 일**이다.

---

# Form1.cs

**478줄 · 메서드 34개 · 기반층**

> partial class Form1의 뿌리 — 공유 필드 대부분을 선언하고, 앱 시작 시 컨트롤·이벤트를 배선하며, 모든 파일이 함께 쓰는 진단 로그와 '처리 중' 오버레이·취소 장치를 제공한다.

**진입점**

- 프로그램 실행(Program.cs Application.Run) → Form1() 생성자(Form1.cs:254)
- 진행 오버레이의 [취소] 버튼 클릭 → BusyOverlayCancelButton_Click(Form1.cs:362)

## 공유 상태 선언부 (필드, 메서드 아님)

*13개 파일이 함께 읽고 쓰는 필드를 여기 한 곳에서 선언한다. 메서드 호출 없이 '값'으로만 이어지는 파일 간 결합의 실제 지점이다 — 발표에서 파일 경계를 설명할 때 여기부터 짚는다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `vizcore3d (VIZCore3DControl)` | 21 | SDK 3D 뷰어 컨트롤. 13개 파일 전부가 이 하나를 통해 SDK를 부른다 | 전 파일 | - |
| `bomList (List<BOMData>)` | 26 | 부재 목록. BOM.cs가 채우고(Form1.BOM.cs:952 Add) 도면·치수·가공도가 읽는다 | BOM 20 · DrawingSheets 32 · Clash 11 · Drawing2D 9 · Dimensions 7 · Stru 6 · MfgDrawing 5 · GlobalViews 3회 참조 | - |
| `clashList (List<ClashData>)` | 31 | 간섭검사 결과. Clash.cs가 채우고 Dimensions·DrawingSheets가 읽는다 | Clash 8 · BOM 6 · Dimensions 3 · DrawingSheets 1회 | - |
| `FabricationNeighborClashTestName · FabricationNeighborClearance (const)` | 33 | 제작도 점선용 근접 간섭검사의 검사명과 3.0f 여유값 상수 | Clash 2회 · 5회 | - |
| `fabricationNeighborClashList · fabricationNeighborPartIndices · fabricationTargetBodyIndices · fabricationTargetPartIndices` | 39 | 제작도 점선(연결 부재) 판정 결과 4종. 대상 내부 연결성 검사 결과와 분리해 둔 것 | Clash가 채움 → DrawingSheets · GlobalViews가 읽음 | - |
| `BodyBoundsData (private struct)` | 47 | Body 하나의 Min/Max XYZ 6개 float. 제작도 연결 후보 광역 필터의 bbox 단위 | Clash.cs:704·764·807·827 · GlobalViews.cs:347·685 | - |
| `fabricationBodyBoundsCache · fabricationBodyToPartIndexCache · fabricationNeighborCacheSourceBodyCount` | 57 | 위 bbox 캐시 3종. 모델을 다시 열 때 Clash.cs:656·687에서 Clear | Clash 9·6·3회 · GlobalViews 3·1회 | - |
| `osnapPoints · osnapPointsWithNames` | 65 | Osnap 좌표와 (좌표, 부재명, 축) 튜플. 축은 LINE osnap의 시작→끝 최대 성분 | Drawing2D 20·21 · BOM 9·17 · MfgDrawing 13 · Dimensions 2·6회 | - |
| `xraySelectedNodeIndices` | 76 | X-Ray 모드에서 선택된 노드. 8개 파일이 공유하는 최광역 필드 | Stru 10 · BOM 9 · Dimensions 8 · DrawingSheets 8 · GlobalViews 7 · Drawing2D 4 · Clash 2회 | - |
| `currentFilePath · selectedAttributeNodeIndex` | 81 | 현재 열린 모델 경로 · 부재정보 탭에서 선택된 노드 | Drawing2D·DrawingSheets·BOM / Attribute 17회 | - |
| `balloonOverrides · currentBalloonView · currentBalloonMemberIndices` | 91 | 풍선 위치 수동 오버라이드(BOM인덱스→XYZ) · 현재 풍선 뷰 방향 · 표시 중인 부재 목록 | Dimensions 8·1·2회 · BOM 2회 | - |
| `bodyToPartNameMap · bodyToPartIndexMap` | 101 | Body 인덱스 → 부모 Part 이름/인덱스 매핑 캐시 | BOM이 채움 → Clash 8 · Dimensions 3 · DrawingSheets 2회 | - |
| `drawingSheetList (List<DrawingSheetData>)` | 111 | 도면 시트 목록. DrawingSheets.cs가 Add하고 Stru.cs 일괄 출력이 순회한다 | DrawingSheets 18 · Stru 14 · Clash 5 · BOM 4회 | - |
| `txtMemberNameOverlay` | 116 | 3D 뷰어 위에 띄우는 부재 이름 입력 TextBox | Dimensions 13회 | - |
| `busyOverlay · busyOverlayMessage · busyOverlayCancelButton · busyOverlayBaseMessage` | 122 | '처리 중...' 오버레이 패널·문구·취소 버튼. 이 파일 안에서만 쓰인다(18회) | Form1.cs 내부 전용 | - |
| `_cancelableOperationInProgress · _cancelRequested · _lastCancellationCheckpoint · _mainDimensionInProgress` | 131 | 협력적 취소 상태 3종 + 주치수 진행 중 플래그. SDK 단일 호출은 못 끊고 다음 체크포인트에서만 멈춘다 | 취소 3종은 Form1.cs 내부 · _mainDimensionInProgress는 BOM 9 · Clash 6회 | - |
| `_lastCollectedNodeOsnapMap` | 141 | CollectAllOsnap이 마지막으로 모은 부재별 Osnap 맵. GetOsnapPoint 중복 호출 방지용 (T-032) | BOM 7 · DrawingSheets 3 · MfgDrawing 2 · Stru 1회 | - |
| `_udaValueCache` | 150 | (노드, UDA키) → 값 캐시. 가공도 미리보기의 부모 10단계 트리 walk를 부재당 5회에서 1회로 줄인 것 | MfgDrawing 6 · BOM 2 · Stru 1회 | - |
| `_lastMfgViewPose · _mfgPreviewNetRoll · _mfgActiveReferenceAxisId · _drawingActiveReferenceAxisId` | 158 | 가공도 카메라 포즈 · 직전 미리보기 화면축 회전 총량(누적 틀어짐 되돌림용) · 가공도/제작도 참조축 리뷰 ID | MfgDrawing 3·6·4회 · DrawingSheets 1·4회 | - |
| `_autoProcessOsnapSuccess` | 182 | Osnap 자동 처리 성공 여부 | BOM 6 · DrawingSheets 1회 | - |
| `chainDimensionList (List<ChainDimensionData>)` | 192 | 체인 치수 목록. Dimensions.cs가 채우고 Drawing2D·DrawingSheets·GlobalViews가 그린다 | Dimensions 19 · DrawingSheets 14 · BOM 12 · Drawing2D 6 · GlobalViews 5 · Stru 1회 | - |
| `_diagLogPath (static readonly)` | 198 | {exe 폴더}/logs/diag-YYYY-MM-DD.log 경로. Release 빌드에서도 남아 다른 기기 이슈를 추적한다 | DiagLog 내부 | - |

## 진단 로그

*13개 파일 전부가 부르는 공용 로거. 시각을 붙여 exe 폴더의 logs/diag-날짜.log와 VS 출력창에 같이 쓴다. 파일 쓰기 실패는 삼켜서 앱 흐름을 막지 않는다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `LogBuildStamp` | 215 | 실행할 때마다 exe의 마지막 수정 시각과 경로를 로그 맨 앞에 남긴다 — '고친 게 들어간 빌드인가'를 로그만 보고 판별하려고 | Form1() 생성자(Form1.cs:258) 단 한 곳 | DiagLog ×4 |
| `DiagLog` | 238 | 메시지에 [HH:mm:ss.fff]를 붙여 로그 파일에 append + Debug.WriteLine. 폴더가 없으면 만든다 | 전 파일 — MfgDrawing 90 · DrawingSheets 84 · Stru 43 · Dimensions 34 · Clash 24 · Drawing2D 17 · GlobalViews 16 · Form1 11 · BOM 9 · License 6 · ExcelTemplate 1회 | File.AppendAllText · Debug.WriteLine |

## 앱 시작 배선

*생성자 하나. 폼이 뜰 때 ListView 컬럼·이벤트 핸들러·VIZCore3D 컨트롤을 전부 여기서 엮는다. 다른 파일에 있는 초기화 메서드를 불러오는 지점이라 '어느 파일이 무엇을 맡는가'가 여기서 처음 드러난다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `Form1 (생성자)` | 254 | 빌드 스탬프 기록 → BOM/부재정보 컬럼 구성 → 7개 ListView 이벤트 등록 → STRU 검색 UI 생성 → SDK 초기화 → vizcore3d 컨트롤을 panelViewer에 붙이고 SDK 자체 대기창(EnableWaitForm)을 끈다(#116, 우리 오버레이와 중복이라) → OnInitializedVIZCore3D 핸들러 등록 | 진입점 — Program.cs Application.Run(new Form1()) | InitializeComponent · LogBuildStamp · SetupBOMColumns(Form1.BOM.cs:22) · SetupAttributeColumns(Form1.BOM.cs:124) · InitStruSearchUI(Form1.Stru.cs:187) · ModuleInitializer.Run · DiagLog |

## '처리 중' 오버레이

*3D 뷰어 한가운데에 회색 패널을 띄워 장시간 블로킹 작업임을 알린다. 취소 가능 구간이면 [취소] 버튼도 같이 그린다. 5개 파일 12곳이 부른다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ShowBusyOverlay` | 315 | 패널이 없으면 만들고(360×135, 맑은 고딕 12pt Bold, 취소 버튼 포함) 문구를 세운 뒤 panelViewer 중앙에 배치·표시하고 DoEvents로 즉시 그린다 | Stru 6 · BOM 6 · DrawingSheets 3 · MfgDrawing 1회 (예: Form1.Stru.cs:668 "STRU 처리 3/7") | UpdateBusyOverlayContents · Application.DoEvents |
| `HideBusyOverlay` | 470 | 패널을 감춘다. ShowBusyOverlay와 짝이고 반드시 finally에서 부른다 | BOM 4 · Clash 3 · Stru 1 · DrawingSheets 1 · MfgDrawing 1회 | - |
| `UpdateBusyOverlayContents` | 379 | 취소 가능 여부에 따라 패널 크기(360×135 ↔ 300×80)·문구·취소 버튼 표시/활성을 다시 맞춘다. 취소 요청됐으면 버튼을 '취소 요청됨'으로 바꿔 잠그고 안내 한 줄을 덧붙인다 | ShowBusyOverlay(348) · BusyOverlayCancelButton_Click(369) · ProcessCancelableUiCheckpoint(408) · EndCancelableOperation(436) — 전부 파일 내부 | - |

## 협력적 취소

*SDK 단일 호출은 중간에 끊을 수 없어서, 호출과 호출 사이 '안전 체크포인트'에만 멈추는 방식. Begin/End로 구간을 열고 닫고, 루프 안에서 Process/Throw/Is 중 하나로 확인한다. STRU 일괄 출력·주치수 추출·PDF 출력이 이 위에서 돈다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `BeginCancelableOperation` | 420 | 구간 시작 — 취소 요청·체크포인트 기록을 지우고 진행 중 플래그를 켠다. 오버레이는 안 건드리므로 취소 버튼은 뒤따르는 ShowBusyOverlay에서 나타난다 | Stru.cs:569(일괄 출력) · BOM.cs:376(주치수) · DrawingSheets.cs:1341(시트 PDF) · MfgDrawing.cs:2857(가공도 PDF) | - |
| `EndCancelableOperation` | 431 | 구간 종료 — 상태 3개를 지우고 오버레이에서 취소 버튼을 거둔다. HideBusyOverlay와 함께 부른다 | Stru.cs:767 · BOM.cs:500 · DrawingSheets.cs:1452 · MfgDrawing.cs:2869 | UpdateBusyOverlayContents |
| `BusyOverlayCancelButton_Click` | 362 | [취소] 클릭 → 진행 중이고 아직 요청 전일 때만 _cancelRequested를 세우고 오버레이를 다시 그린다. 실제 중단은 다음 체크포인트에서 | 진입점 — busyOverlayCancelButton.Click(Form1.cs:341에서 등록) | UpdateBusyOverlayContents · DiagLog · Application.DoEvents |
| `ProcessCancelableUiCheckpoint` | 400 | 루프 안에서 진행 문구를 갱신하고 DoEvents로 UI 큐를 비운 뒤 취소면 예외를 던진다. 문구 갱신 + 취소 확인을 한 번에 하는 형태 | DrawingSheets 8 · BOM 6 · MfgDrawing 1회 | UpdateBusyOverlayContents · Application.DoEvents · ThrowIfCancellationRequested |
| `ThrowIfCancellationRequested` | 461 | 취소 요청이 있으면 체크포인트 이름을 담아 OperationCanceledException을 던진다. 정리는 호출자 catch/finally 몫 | Stru 15 · DrawingSheets 3회 · ProcessCancelableUiCheckpoint(412) | IsCancellationRequested |
| `IsCancellationRequested` | 444 | 예외를 안 던지고 bool만 돌려준다. 루프에서 조용히 빠질 때 쓴다. 직전과 다른 체크포인트일 때만 로그를 남겨 같은 줄이 반복되는 걸 막는다 | Stru 4 · Clash 3 · BOM 1회 · ThrowIfCancellationRequested(463) | DiagLog |

## 다른 파일과의 연결

- Form1.cs:26 bomList 선언 → Form1.BOM.cs:952가 Add로 채움 → Form1.DrawingSheets.cs(32회)·Drawing2D·MfgDrawing이 읽어 도면을 그림 (호출이 아니라 필드로만 이어지는 결합)
- Form1.cs:31 clashList / :39 fabricationNeighborClashList 선언 → Form1.Clash.cs가 채움 → DrawingSheets가 제작도 점선 판정에 씀
- Form1.cs:111 drawingSheetList 선언 → Form1.DrawingSheets.cs:34·117·168·182가 시트 4종을 Add → Form1.Stru.cs:940 일괄 출력이 이 리스트를 순회
- Form1.cs:192 chainDimensionList 선언 → Form1.Dimensions.cs가 채움 → Drawing2D·DrawingSheets·GlobalViews가 그림
- Form1.cs:76 xraySelectedNodeIndices — 8개 파일이 공유하는 최광역 필드 (Stru 10 · BOM 9 · Dimensions 8 · DrawingSheets 8 · GlobalViews 7 · Drawing2D 4 · Clash 2회)
- Form1.cs:254 생성자 → Form1.BOM.cs:22 SetupBOMColumns · Form1.BOM.cs:124 SetupAttributeColumns · Form1.Stru.cs:187 InitStruSearchUI 호출 (다른 파일의 UI 초기화를 여기서 당겨옴)
- Form1.cs:306 vizcore3d.OnInitializedVIZCore3D += → Form1.BOM.cs:150 Vizcore3d_OnInitializedVIZCore3D — 여기서 Form1.License.cs:39 InitializeLicense가 불린다 (Form1.cs → BOM.cs → License.cs 순)
- Form1.cs:238 DiagLog — 13개 파일 전부가 부르는 유일한 공용 로거 (MfgDrawing 90회 · DrawingSheets 84회가 최다)
- Form1.cs:315/470 ShowBusyOverlay·HideBusyOverlay — Stru·BOM·DrawingSheets·MfgDrawing·Clash 5개 파일이 장시간 작업마다 감싸서 부름
- Form1.cs:420~465 취소 4종 → Form1.Stru.cs가 최다 사용(ThrowIf 15 · Is 4 · Begin/End 각 1). STRU 일괄 출력이 가장 긴 취소 가능 구간
- Form1.cs:47 BodyBoundsData(private struct) 선언 → Form1.Clash.cs:764·807·827 · Form1.GlobalViews.cs:347·685가 매개변수·지역변수 타입으로 씀 (Form1의 중첩 타입이라 partial class 밖에서는 못 씀)

---

# Form1.License.cs

**145줄 · 메서드 8개 · 중간층**

> VIZCore3D SDK 라이선스 서버에 붙고 30분마다 다시 붙는 일만 한다 — 로컬(127.0.0.1) 먼저, 죽어 있으면 사내 서버로 폴백.

**진입점**

- 라이선스 갱신 타이머 Tick(30분 주기) → LicenseRefreshTimer_Tick(Form1.License.cs:124)
- (외부 진입) VIZCore3D 초기화 완료 → Form1.BOM.cs:153이 InitializeLicense(Form1.License.cs:39)를 호출

## 서버 후보와 상태 (필드)

*어느 서버에 붙을지, 지금 어디에 붙어 있는지를 들고 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `LicenseServers (static readonly 배열)` | 18 | 후보 2개 — 127.0.0.1:8901(로컬 기본), 60.100.164.177:8901(사내 폴백). 앞에서부터 시도 | ConnectLicenseServer · GetLicenseServerOrder · LicenseRefreshTimer_Tick | - |
| `activeLicenseServerIndex` | 28 | 마지막으로 성공한 후보 인덱스. 미인증이면 -1. 다음 갱신 때 이 서버부터 시도한다 | ConnectLicenseServer(82·92) · GetLicenseServerOrder(101) · Tick(131) | - |
| `licenseRefreshTimer` | 33 | 30분 주기 WinForms 타이머 | StartLicenseRefreshTimer | - |

## 최초 연결

*앱이 뜬 직후 한 번. 하나라도 붙으면 타이머를 걸고 true, 전부 실패하면 MessageBox를 띄우고 false를 돌려준다 — 이 false가 앱 기능 전체를 막는 분기다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `InitializeLicense` | 39 | ConnectLicenseServer 성공 → 갱신 타이머 시작 후 true. 실패 → "License Error: 라이선스 서버에 연결하지 못했습니다" + 후보별 실패 사유를 줄바꿈으로 붙여 MessageBox 띄우고 false | Form1.BOM.cs:153 (Vizcore3d_OnInitializedVIZCore3D) — 유일한 호출처. false면 즉시 return이라 2D 툴바·모델트리·이벤트 등록이 전부 안 걸린다 | ConnectLicenseServer · StartLicenseRefreshTimer · MessageBox.Show |

## 연결 시도 코어

*실제로 서버에 붙는 유일한 곳. 최초 연결과 30분 갱신이 같은 함수를 쓴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ConnectLicenseServer` | 59 | GetLicenseServerOrder 순서대로 vizcore3d.License.LicenseServer(ip, port) 호출. SUCCESS면 activeLicenseServerIndex 갱신 후 true. 예외가 나도 다음 후보를 계속 시도하고, 실패 사유는 out failures에 모아 준다. 전부 실패하면 인덱스를 -1로 되돌리고 false | InitializeLicense(42) · LicenseRefreshTimer_Tick(129) | GetLicenseServerOrder · vizcore3d.License.LicenseServer · DiagLog |
| `GetLicenseServerOrder` | 99 | 시도 순서를 yield로 흘린다 — 직전 성공 서버가 있으면 맨 앞, 나머지는 정의 순서. 잘 붙던 서버를 매번 다시 찾지 않게 하는 장치 | ConnectLicenseServer(63) | - |

## 자동 갱신

*30분마다 다시 붙어 세션 만료를 막는다. 갱신에 실패해도 앱은 죽지 않고 다음 주기에 재시도한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `StartLicenseRefreshTimer` | 112 | Interval 30×60×1000ms 타이머를 만들어 Tick에 핸들러를 걸고 Start | InitializeLicense(44) | - |
| `LicenseRefreshTimer_Tick` | 124 | 30분마다 ConnectLicenseServer 재호출. 성공/실패 모두 DiagLog만 남기고 예외는 try/catch로 삼킨다 — 갱신 실패가 앱을 멈추지 않게 | 진입점 — licenseRefreshTimer.Tick(Form1.License.cs:116에서 등록) | ConnectLicenseServer · DiagLog |

## 다른 파일과의 연결

- Form1.BOM.cs:153 `if (!InitializeLicense()) return;` → Form1.License.cs:39 — 이 파일의 유일한 외부 진입. 라이선스가 안 붙으면 Vizcore3d_OnInitializedVIZCore3D가 그 자리에서 return해 이후 모든 SDK 배선(2D 툴바·모델트리·MarineAxis·Clash 이벤트·Edge 데이터)이 안 걸린다
- Form1.License.cs:70 vizcore3d.License.LicenseServer — Form1.cs:21에 선언된 공유 vizcore3d 컨트롤을 통해 SDK를 부른다
- Form1.License.cs:76·83·89·132·136·141 DiagLog → Form1.cs:238 (총 6회)
- 이 파일의 메서드를 부르는 다른 파일은 Form1.BOM.cs 한 곳뿐 — 나머지 4개는 파일 내부 전용 (T-017로 Form1.BOM.cs에서 떼어낸 결과)

---

# Form1.BOM.cs

**1095줄 · 메서드 18개 · 조립자층**

> 모델을 열고 → 부재 목록(BOM)과 Osnap 점을 모아 → 치수 추출 파이프라인 전체를 지휘하는 사령탑. 간섭검사 앞부분과 뒷부분을 나눠 들고 Clash 이벤트를 사이에 끼운다.

**진입점**

- [파일 열기] 버튼 → btnOpen_Click (Form1.BOM.cs:182, Form1.Designer.cs:1202 배선)
- [초기화] 버튼 → btnResetToInitial_Click (Form1.BOM.cs:264, Form1.Designer.cs:1247 배선)
- [치수 추출] 버튼 → btnMainDimension_Click (Form1.BOM.cs:354, Form1.Designer.cs:1217 배선)
- [BOM] 버튼 → btnCollectBOM_Click (Form1.BOM.cs:1081, Form1.Designer.cs:1038 배선)
- VIZCore3D 초기화 완료 SDK 이벤트 → Vizcore3d_OnInitializedVIZCore3D (Form1.BOM.cs:150, Form1.cs:306 등록)
- 폼 생성자 → SetupBOMColumns (Form1.BOM.cs:22) · SetupAttributeColumns (Form1.BOM.cs:124) — Form1.cs:261·264

## 모델 열기·초기화

*SDK 초기화·라이선스 통과 후 화면 옵션을 켜고, 파일을 열거나 같은 파일을 다시 열어 모든 누적 상태를 시작 지점으로 되돌린다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `SetupBOMColumns` | 22 | lvBOM의 컬럼을 지우고 No./부재 이름/각도/중심·최소·최대 좌표/원형/용도/홀사이즈 15개를 폭까지 붙여 다시 만든다. CollectBOMData가 넣는 SubItem 순서가 이 순서와 짝이다. | 진입점 (폼 생성자, Form1.cs:261) | lvBOM.Columns.Add |
| `SetupAttributeColumns` | 124 | 부재 정보 탭의 dgvAttributes를 No/속성명(Key)/값(Value) 3열로 만들고 헤더 색·행 높이·교차 배경색까지 세팅한다. | 진입점 (폼 생성자, Form1.cs:264) | dgvAttributes.Columns.Add |
| `Vizcore3d_OnInitializedVIZCore3D` | 150 | 라이선스를 먼저 통과시키고(실패하면 즉시 반환) 2D 툴바·모델트리·Marine Axis 축 표시기를 켠 뒤, 간섭검사 완료·3D 선택 이벤트를 등록하고 Edge 데이터 생성·읽기를 켠다. | 진입점 (SDK OnInitializedVIZCore3D, Form1.cs:306 등록) | InitializeLicense(Form1.License.cs:39) · Clash_OnClashTestFinishedEvent 등록(Form1.Clash.cs:1146) · Object3D_OnObject3DSelected 등록(Form1.Attribute.cs:19) · vizcore3d.Object3D.GenerateEdgeData |
| `btnOpen_Click` | 182 | *.vizx/*.viz 파일을 고르면 → 공유 목록(bomList·clashList·osnap·chainDimension·drawingSheet·xray)과 모든 ListView를 비우고 → 열려 있던 모델을 Close 후 Open → FitToView·초록 실루엣 엣지 → Body→Part 매핑 구축 → STRU 목록 채우기까지 한다. | 진입점 ([파일 열기] 버튼) | BuildBodyToPartNameMap(46) · PopulateStruCheckList(Form1.Stru.cs:149) · vizcore3d.Model.Close/Open |
| `btnResetToInitial_Click` | 264 | 모델이 열려 있는지 확인하고 '작업 내용이 전부 초기화된다'는 Yes/No 확인창을 띄운 뒤 실제 초기화로 넘긴다. | 진입점 ([초기화] 버튼) | ResetToInitialState(286) |
| `ResetToInitialState` | 286 | 현재 파일을 그대로 다시 열어 초기 상태로 되돌린다. btnOpen_Click의 초기화 블록 + balloonOverrides + 도면정보 BOM 목록까지 비우고, 은선 점선(DASH_LINE)을 SMOOTH로 되돌리며, 진입 시점 ViewMode를 기억했다가 2D 정리 후 복원한다(#55). | btnResetToInitial_Click(264) | BuildBodyToPartNameMap(46) · Clear2DView(Form1.Drawing2D.cs:1248) · RestoreViewMode(Form1.Drawing2D.cs:1231) |

## Body→Part 이름 매핑

*SDK가 주는 BODY 노드를 사람이 읽는 Part 이름으로 바꾸는 사전을 만든다. BOM·Osnap·치수·2D 라벨이 전부 이 사전을 통과한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `BuildBodyToPartNameMap` | 46 | Part 노드와 Body 노드를 모두 가져와, Body 인덱스보다 작거나 같은 최대 Part 인덱스를 이진 탐색으로 찾아 부모로 삼는다. bodyToPartNameMap(이름)·bodyToPartIndexMap(인덱스) 두 사전을 채우고 제작도 근접 캐시도 함께 비운다. | btnOpen_Click(182) · ResetToInitialState(334) | ResetFabricationNeighborSearchCache(Form1.Clash.cs:654) · vizcore3d.Object3D.GetPartialNode |
| `GetPartNameFromBodyIndex` | 114 | Body 인덱스를 사전에서 찾아 Part 풀네임을 돌려주고, 없으면 넘겨받은 fallback 이름을 그대로 준다. | CollectAllOsnap(718) · CollectBOMData(882) · Form1.Dimensions.cs:2161 · Form1.Drawing2D.cs:487 | (없음 — 사전 조회만) |

## 치수 추출 파이프라인 지휘

*[치수 추출] 한 번 누르면 BOM 수집 → 간섭검사 → (비동기 콜백) → Osnap → 체인치수 → 시트 생성까지 이어지는 흐름을 앞뒤 두 토막으로 나눠 들고 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnMainDimension_Click` | 354 | 파이프라인 앞토막. 재진입을 막고 도면 출력 버튼들을 잠근 뒤, xraySelectedNodeIndices를 비워 '항상 현재 visible 기준'으로 만들고(T-026), 대상이 5000개 이상이면 경고창을 띄운다. BOM 수집 후 DetectClash를 시작하고 — 시작에 실패하면(쌍 0개=단일 부재) 뒷토막을 직접 호출한다. | 진입점 ([치수 추출] 버튼) | GetBOMTargetNodes(809) · CollectBOMData(857) · DetectClash(Form1.Clash.cs:1021) · CompleteMainDimensionPostClash(545) · ShowBusyOverlay/BeginCancelableOperation(Form1.cs) · CaptureDrawingExportControlStates/SetDrawingExportControlsEnabled(Form1.DrawingSheets.cs:1554·1579) |
| `CompleteMainDimensionPostClash` | 545 | 파이프라인 뒷토막. Osnap을 모으고, 보이는 부재만 골라 3뷰×2축 체인치수를 계산해 chainDimensionList와 lvDimension에 넣는다. 3D 뷰에는 치수를 그리지 않고(T-029) Measure·ShapeDrawing을 비운 뒤, 도면 시트를 생성하고 오버레이를 먼저 내린 다음 요약 팝업을 띄운다(T-033). 일괄 출력 중이면 팝업을 건너뛴다. | btnMainDimension_Click(439, 단일 부재 경로) · Clash_OnClashTestFinishedEvent(Form1.Clash.cs:1308, 정상 경로) | CollectAllOsnap(691) · ComputeViewDimensionsForMembers(Form1.Dimensions.cs:2117) · GenerateDrawingSheets(Form1.DrawingSheets.cs:20) · CancelMainDimensionAtCheckpoint(470) · FinishMainDimensionOperation(493) |

## 취소·진행 상태 관리

*긴 작업 중간중간 취소를 확인하고, 어떤 경로로 끝나든 버튼·오버레이·부분 결과를 시작 전 상태로 되돌린다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `CancelMainDimensionAtCheckpoint` | 470 | 체크포인트 이름을 받아 취소 요청이 있으면 임시 결과를 정리·종료하고 '중단 위치'를 알려주는 창을 띄운 뒤 true를 준다. true면 호출자는 즉시 반환해야 한다. 치수 추출 중이 아니면 정리 없이 true만. | btnMainDimension_Click · CompleteMainDimensionPostClash (총 11곳) · Form1.Clash.cs:929·961·1159 | IsCancellationRequested(Form1.cs:444) · ResetSilentClashSequence(Form1.Clash.cs:852) · ClearCanceledOperationArtifacts(514) · FinishMainDimensionOperation(493) |
| `FinishMainDimensionOperation` | 493 | 진행 플래그를 내리고 오버레이 해제·취소 작업 종료·[치수 추출] 버튼과 도면 출력 버튼들의 활성 상태 복원까지 한다. 진행 중이 아니면 아무것도 안 해 중복 호출이 안전하다. | btnMainDimension_Click(9곳) · CompleteMainDimensionPostClash · CancelMainDimensionAtCheckpoint · Form1.Clash.cs:968·1287·1314 | HideBusyOverlay/EndCancelableOperation(Form1.cs) · RestoreDrawingExportControlStates(Form1.DrawingSheets.cs:1595) |
| `ClearCanceledOperationArtifacts` | 514 | 중간 취소 뒤 남은 반쪽 결과를 지운다. 2D 객체·캔버스와 3D 노트·측정·형상을 각각 예외를 삼키며 삭제하고, 시트·체인치수·Osnap·BOM 목록과 X-Ray 선택·UDA 캐시를 비운다. | CancelMainDimensionAtCheckpoint(478) · Form1.Stru.cs:725 (일괄 출력 취소) | ResetSilentClashSequence(Form1.Clash.cs:852) · vizcore3d.Drawing2D.Object2D.DeleteAll* |

## BOM·Osnap 수집

*어떤 부재를 대상으로 삼을지 정하고(X-Ray 선택 우선, 없으면 visible), 그 부재들의 바운딩박스·홀·용도와 Osnap 점을 실제로 긁어 담는다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetBOMTargetNodes` | 809 | 모든 BODY 노드를 훑어 대상을 고른다. xraySelectedNodeIndices가 있으면 그 집합(부모 Part 포함)만, 없으면 실제 Visible인 것만. 하나도 못 고르면 전체를 준다. 200개마다 취소 체크포인트를 지난다. | btnMainDimension_Click(390) · CollectAllOsnap(704) · CollectBOMData(867) | ProcessCancelableUiCheckpoint(Form1.cs:400) · vizcore3d.Object3D.GetPartialNode/FromIndex |
| `CollectBOMData` | 857 | 대상 BODY마다 Part 이름·바운딩박스 최소/최대/중심·CIRCLE Osnap 최대 반지름·UDA PURPOSE를 모아 BOMData를 만든다. Z_Max 내림차순으로 정렬한 뒤 홀 검출을 돌리고 lvBOM을 채운다. 한 건이라도 있으면 true. | btnMainDimension_Click(413) · btnCollectBOM_Click(1083) · Form1.Dimensions.cs:1804 · Form1.Stru.cs:869 (STRU 일괄 출력) | GetBOMTargetNodes(809) · GetPartNameFromBodyIndex(114) · DetectHoles(1010) · vizcore3d.Object3D.GetBoundBox/GetOsnapPoint/UDA |
| `CollectAllOsnap` | 691 | 대상 BODY마다 Osnap 점을 뽑아 LINE은 양 끝점을 축 추정과 함께, POINT는 중심을 담는다(CIRCLE은 곡면이라 치수에서 제외). osnapPoints·osnapPointsWithNames와 부재별 맵 _lastCollectedNodeOsnapMap을 동시에 채워 뒤에서 GetOsnapPoint를 다시 부르지 않게 한다(T-032). | CompleteMainDimensionPostClash(557) · Form1.Dimensions.cs:1824 | GetBOMTargetNodes(704) · GetPartNameFromBodyIndex(718) · CacheMfgAxisDetection(Form1.MfgDrawing.cs:3679) · EstimateOsnapLineAxis(Form1.Drawing2D.cs:796) |
| `DetectHoles` | 1010 | bomList의 부재마다 SDK 홀 정보 API로 홀·슬롯홀을 채우고, 직경별로 묶어 'Ø12.0x4' 형태의 홀사이즈 문자열과 '(W*L*D)*N' 형태의 슬롯사이즈 문자열을 만든다. 이름은 '검출'이지만 실제 판정은 API가 한다(2026-06-23 원기둥 휴리스틱 전면 교체). | CollectBOMData(959) | GetMfgHolesFromApi(Form1.MfgDrawing.cs:1455) · ProcessCancelableUiCheckpoint(Form1.cs:400) |
| `btnCollectBOM_Click` | 1081 | BOM만 따로 수집해 결과 개수를 팝업으로 알린다. 치수·간섭검사는 안 돈다. | 진입점 ([BOM] 버튼) | CollectBOMData(857) |

## 다른 파일과의 연결

- btnMainDimension_Click(Form1.BOM.cs:430) → DetectClash(Form1.Clash.cs:1021) → (SDK 비동기) Clash_OnClashTestFinishedEvent(Form1.Clash.cs:1146) → 다시 이 파일의 CompleteMainDimensionPostClash(Form1.BOM.cs:545) — 파일 두 개를 왕복하는 비동기 고리. 발표에서 '한 함수로 안 이어지는' 이유가 여기.
- CollectBOMData(Form1.BOM.cs:857)가 bomList를 채운다 → IsSingleConnectedComponent(Form1.Clash.cs:1329)의 연결성 판정 대상, BuildDrawingBomPreparationContext(Form1.Clash.cs:226)의 BOM 번호 원본, Form1.DrawingSheets.cs의 시트 부재 선정 재료가 전부 이 목록.
- BuildBodyToPartNameMap(Form1.BOM.cs:46)이 bodyToPartNameMap·bodyToPartIndexMap을 채운다 → GetPartNameFromBodyIndex를 Form1.Dimensions.cs:2161·Form1.Drawing2D.cs:487이 쓰고, bodyToPartIndexMap은 Form1.Clash.cs:156·229·490·1340에서 Body↔Part 환산에 쓴다.
- CollectAllOsnap(Form1.BOM.cs:691)이 _lastCollectedNodeOsnapMap(Form1.cs:137 선언)을 채운다 → CompleteMainDimensionPostClash가 ComputeViewDimensionsForMembers(Form1.Dimensions.cs:2117)에 그대로 넘기고, Form1.DrawingSheets.cs·Form1.MfgDrawing.cs:1926도 같은 맵을 재사용해 GetOsnapPoint 재호출을 피한다.
- CompleteMainDimensionPostClash(Form1.BOM.cs:644)가 GenerateDrawingSheets(Form1.DrawingSheets.cs:20)를 호출 → drawingSheetList·lvDrawingSheet가 여기서 생기고, 그 뒤 STRU 일괄 출력(Form1.Stru.cs:940)이 이 목록이 차기를 폴링한다.
- DetectHoles(Form1.BOM.cs:1032)는 홀 판정을 직접 안 하고 GetMfgHolesFromApi(Form1.MfgDrawing.cs:1455)에 위임 — BOM 표 홀사이즈와 가공도 풍선이 같은 단일 출처를 쓴다.
- btnOpen_Click(Form1.BOM.cs:248) → PopulateStruCheckList(Form1.Stru.cs:149). 모델을 열면 STRU 목록이 자동으로 채워지는 연결.
- Vizcore3d_OnInitializedVIZCore3D(Form1.BOM.cs:153·166·169) → InitializeLicense(Form1.License.cs:39) 통과 후 Clash 완료 이벤트(Form1.Clash.cs:1146)와 3D 선택 이벤트(Form1.Attribute.cs:19)를 등록.
- ClearCanceledOperationArtifacts(Form1.BOM.cs:514)는 STRU 일괄 출력 취소 경로(Form1.Stru.cs:725)에서도 그대로 쓰인다 — 취소 정리 로직의 단일 지점.
- _mainDimensionInProgress·_p2aInProgress(Form1.cs:134 / Form1.Stru.cs:53)를 이 파일이 읽어 재진입과 팝업을 막는다 — Form1.Stru.cs·Form1.Clash.cs와 공유하는 스위치.

---

# Form1.Stru.cs

**1125줄 · 메서드 17개 · 조립자층**

> 모델트리에서 STRU(구조물 단위)를 찾아 목록으로 띄우고, 체크한 STRU를 하나씩 자동으로 돌려 도면 4종 PDF까지 뽑는 일괄 출력 담당. 사람이 손으로 하던 클릭 순서를 코드로 그대로 재현한다.

**진입점**

- [도면 일괄 출력] 버튼 → btnExtractDrawingList_Click (Form1.Stru.cs:514, Form1.Designer.cs:485 배선)
- [전체 선택/해제] 버튼 → btnSelectAllStru_Click (Form1.Stru.cs:347, Form1.Designer.cs:322 배선)
- STRU 목록 체크박스 클릭 → ClbStruList_ItemCheck (Form1.Stru.cs:362, Form1.Designer.cs:300 배선)
- STRU 목록 행(이름) 클릭 → ClbStruList_SelectedIndexChanged (Form1.Stru.cs:443, Form1.Designer.cs:301 배선)
- [검색] 버튼 → BtnStruSearch_Click (Form1.Stru.cs:251, Designer가 아니라 코드로 배선 — Form1.Stru.cs:236)
- 폼 생성자 → InitStruSearchUI (Form1.Stru.cs:187, Form1.cs:276)

## STRU 식별·목록 채우기

*'STRU가 무엇인가'를 모델링 컨벤션(FRMWORK 자식의 부모)으로 판정해 목록을 만든다. 룰을 나중에 더 붙일 수 있게 union 구조로 열어뒀다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `CollectStruList` | 62 | 모든 ASSEMBLY를 모수로 잡고 룰 집합(현재 1개)의 결과를 HashSet으로 합쳐 STRU를 뽑아 이름순 정렬한다. 상위 30건은 진단 로그로 찍고, 룰이 0건이면 'NodeName이 / 로 시작 + 공백 없음' 안전망으로 대체한다. | PopulateStruCheckList(157) | RuleByFrameworkChildParent(128) · vizcore3d.Object3D.FromFilter(ASSEMBLY) · DiagLog |
| `RuleByFrameworkChildParent` | 128 | STRU 식별 룰 1 — NodeName이 'FRMWORK '(뒤 공백 포함, 대소문자 무시)로 시작하는 어셈블리를 찾아 그 부모 인덱스를 돌려준다. 부모 트래버스는 1단계만, 재귀 없음. | CollectStruList(87) | DiagLog |
| `PopulateStruCheckList` | 149 | clbStruList를 비우고 STRU를 새로 수집해 체크 해제 상태로 넣는다. 검색 자동완성 소스와 'STRU 목록 (N개)' 제목도 같이 갱신한다. Designer에 컨트롤이 없으면 로그만 남기고 반환. | btnOpen_Click(Form1.BOM.cs:248) — 모델을 열 때 | CollectStruList(62) · GetStruDisplayName(175) |
| `GetStruDisplayName` | 175 | 표시 이름을 NodeName → NodePath → '(Index N)' 순으로 고른다. 목록 표시와 검색 매칭이 같은 규칙을 쓰게 하는 한 줄짜리 기준점. | PopulateStruCheckList(161) · SearchStruByName(296) · FindStruIndexByName(335·339) | (없음) |

## STRU 이름 검색

*이름으로 STRU를 찾아 그것만 화면에 남기고 카메라를 붙인다. 검색 UI 자체를 Designer 대신 코드로 만든다(#36/#48).*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `InitStruSearchUI` | 187 | groupBoxStru 하단에 높이 34짜리 패널을 붙이고 라벨·텍스트박스·[검색] 버튼을 배치한다. 패널을 자식보다 먼저 붙여 Dock 폭을 확정하는데, 안 그러면 Right Anchor 버튼이 패널 밖으로 밀려 안 보인다. | 진입점 (폼 생성자, Form1.cs:276) | BtnStruSearch_Click 이벤트 배선(236) |
| `BtnStruSearch_Click` | 251 | 검색 입력칸 텍스트를 그대로 검색 본체에 넘긴다. | 진입점 ([검색] 버튼, 코드 배선 236) | SearchStruByName(261) |
| `SearchStruByName` | 261 | 이름으로 STRU를 찾아 그 후손 BODY만 남긴다 — 전체 BODY를 Show(false)하고 그 STRU만 Show(true). 목록에서도 그 항목을 선택하고, 같은 항목을 재검색해 선택 이벤트가 안 뜨는 경우에만 카메라 fit을 직접 호출한다. | BtnStruSearch_Click(253) | FindStruIndexByName(330) · GetStruDisplayName(175) · PerformFlyToSelectedStru(457) · vizcore3d.Object3D.GetChildObject3d/Show |
| `FindStruIndexByName` | 330 | _struNodeCache에서 완전일치(대소문자·앞뒤공백 무시)를 먼저 찾고, 없으면 부분일치 첫 매칭. 둘 다 없으면 -1. | SearchStruByName(280) | GetStruDisplayName(175) |

## 목록 상호작용 — 체크는 강조, 선택은 카메라

*체크박스 클릭과 행 이름 클릭을 서로 다른 동작으로 갈라놓는다. WinForms가 한 번의 클릭으로 두 이벤트를 다 쏘기 때문에 가드가 필요하다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnSelectAllStru_Click` | 347 | 전부 체크돼 있으면 전부 해제, 그 외에는 전부 체크하는 토글. | 진입점 ([전체 선택/해제] 버튼) | clbStruList.SetItemChecked |
| `ClbStruList_ItemCheck` | 362 | 가드(_suppressStruSelChanged)를 켜고 본체를 부른 뒤, BeginInvoke로 메시지 큐 맨 끝에서 가드를 푼다. 같은 클릭으로 함께 발생하는 행 선택 이벤트의 카메라 fit을 막기 위한 장치. | 진입점 (clbStruList.ItemCheck, Form1.Designer.cs:300) | ItemCheckCore(385) |
| `ItemCheckCore` | 385 | ItemCheck는 상태가 바뀌기 *직전*에 오므로 e.NewValue를 반영한 '미래 체크 집합'을 만들고, 그 STRU들의 후손 BODY를 전부 합쳐 RestoreColorAll → Select(true)로 강조한다. 카메라 fit은 의도적으로 안 한다(체크할 때 시점이 튀지 않게). | ClbStruList_ItemCheck(368) | vizcore3d.BeginUpdate/EndUpdate · Object3D.GetChildObject3d · Color.RestoreColorAll · Object3D.Select |
| `ClbStruList_SelectedIndexChanged` | 443 | BeginInvoke로 한 메시지 사이클 미룬 뒤 카메라 fit 본체를 부른다. 미루는 이유는 ItemCheck가 가드를 켤 시간을 주기 위해서. | 진입점 (clbStruList.SelectedIndexChanged, Form1.Designer.cs:301) | PerformFlyToSelectedStru(457) |
| `PerformFlyToSelectedStru` | 457 | 가드가 켜져 있으면 그냥 반환. 아니면 선택된 STRU의 후손 BODY를 Show(true)로 켜고 FlyToObject3d(1.2배)로 카메라를 붙인다. 다른 부재 가시성과 강조 색은 건드리지 않아 체크 강조가 살아남는다. | ClbStruList_SelectedIndexChanged(447·449) · SearchStruByName(319, 같은 항목 재검색 시) | vizcore3d.Object3D.GetChildObject3d/Show · View.FlyToObject3d |

## 일괄 도면 출력 본진 (T-064 P2)

*체크된 STRU를 순서대로 돌며 '부재 격리 → BOM 수집 → 간섭검사 → 자동 시트 생성 → 시트별 2D → PDF 1개'를 반복한다. 이 파일에서 가장 긴 두 메서드.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnExtractDrawingList_Click` | 514 | 바깥 루프. 재진입·모델 열림·체크 개수를 확인하고 저장 폴더(exe 옆 Drawings/)를 만든 뒤, 진입 전에 전체 모델 표시·2D 캔버스·3D 풍선·시트·치수·BOM·X-Ray·Osnap 캐시를 전부 비운다(안 비우면 시트 폴링이 60초 타임아웃). 엑셀 템플릿 로고를 1회 매핑하고, STRU마다 본체를 부른 뒤 그때까지 그린 페이지를 PDF 1개로 마감한다. 실패해도 다음 STRU로 넘어간다. finally에서 가시성 복원 + 500ms 대기 후 진행 플래그 해제(늦게 도착하는 콜백이 '자동 처리 완료' 팝업을 띄우는 것을 막기 위함). | 진입점 ([도면 일괄 출력] 버튼) | ProcessSingleStruFull(803) · FlushPendingMergedPdf(Form1.Drawing2D.cs:1152) · ClearCanceledOperationArtifacts(Form1.BOM.cs:514) · ResolveDrawingAssetPath(Form1.DrawingSheets.cs:3474) · vizcore3d.Drawing2D.Template.Set2DViewTemplateMark · IsCancellationRequested(Form1.cs:444) |
| `ProcessSingleStruFull` | 803 | STRU 1개 처리. ①후손 BODY 수집 ②전체 BODY 숨기고 그 STRU만 표시(가시성으로만 격리) ③xraySelectedNodeIndices를 비우고 CollectBOMData로 bomList를 STRU 부재만으로 좁힘 ④DetectClash 시작 후 최대 60초, drawingSheetList가 찰 때까지 IsBusy 폴링 ⑤STRU 폴더 만들고 PDF 누적 시작 ⑥가공도가 아닌 시트마다 ApplySheetSelection + GenerateSheetDrawing2D로 페이지를 쌓음 ⑦가공도는 묶어서 수동 함수에 위임. 실패한 페이지는 통째로 버려 반쪽 도면이 PDF에 끼지 않게 한다. | btnExtractDrawingList_Click(673) | CollectBOMData(Form1.BOM.cs:857) · DetectClash(Form1.Clash.cs:1021) · GenerateDrawingSheets(Form1.DrawingSheets.cs:20) · ApplySheetSelection(Form1.DrawingSheets.cs:651) · GenerateSheetDrawing2D(Form1.DrawingSheets.cs:1744) · GenerateMfgDrawingManual(Form1.MfgDrawing.cs:2454) · BeginPdfPageAccumulation/CleanupBetweenPdfPages/DiscardCurrentPdfPage/BuildMergedDrawingPdfPath(Form1.Drawing2D.cs) · SanitizeFileName(Form1.DrawingSheets.cs:1682) · ThrowIfCancellationRequested(Form1.cs:461) |

## 시트 종류 라벨 헬퍼

*시트가 제작도인지 조립도인지를 BaseMemberIndex 하나로 판정하고, 같은 종류가 여러 장이면 순번을 붙인다. 도면 제목란과 진행 오버레이 문구가 이걸 쓴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetSheetKindLabel` | 1093 | BaseMemberIndex로 종류를 판정 — -1=제작도, -2=설치도, -3=가공도, 0 이상=조립도(1-hop Clash 이웃 시트). | GetSheetKindLabelWithSequence(1113·1117) · Form1.DrawingSheets.cs:1764·1996·2001·2068·2719·2725·2729 | (없음 — 분기만) |
| `GetSheetKindLabelWithSequence` | 1110 | '조립도 (2/5)' 처럼 순번을 붙인다(#122). 모수는 이번 출력 대상이 아니라 도면 목록 전체 — 개별 출력과 일괄 출력에서 같은 도면이 다른 번호를 받으면 도면 간 참조가 깨지기 때문. 같은 종류가 1장뿐이면 순번을 안 붙이고, 가공도는 자체 경로에서 붙이므로 제외한다. | Form1.DrawingSheets.cs:1915 (도면 제목란 data[3]) | GetSheetKindLabel(1093) |

## 다른 파일과의 연결

- ProcessSingleStruFull(Form1.Stru.cs:878) → DetectClash(Form1.Clash.cs:1021) → (SDK 비동기) Clash_OnClashTestFinishedEvent(Form1.Clash.cs:1146) → CompleteMainDimensionPostClash(Form1.BOM.cs:545) → GenerateDrawingSheets(Form1.DrawingSheets.cs:20). 그 사이 ProcessSingleStruFull(906~935)은 drawingSheetList.Count>0을 최대 60초 폴링한다 — 자동화가 비동기 콜백을 '기다리는' 지점.
- ProcessSingleStruFull(Form1.Stru.cs:869)이 CollectBOMData(Form1.BOM.cs:857)를 직접 불러 bomList를 STRU 부재만으로 좁힌다 → IsSingleConnectedComponent(Form1.Clash.cs:1326)가 STRU 내부만 연결성 검사하게 만드는 장치. 이걸 안 하면 STRU가 다른 부재와 분리된 그룹으로 세어져 컴포넌트>1로 차단된다.
- 시트 루프(Form1.Stru.cs:993·997)가 ApplySheetSelection(Form1.DrawingSheets.cs:651) + GenerateSheetDrawing2D(Form1.DrawingSheets.cs:1744)를 호출 — 사용자가 시트 행을 클릭하고 [2D 출력]을 누르는 흐름을 이벤트 시뮬 없이 메서드 직접 호출로 재현(2026-05-19, 시트당 200ms 단축).
- 가공도는 Form1.Stru.cs:1046에서 GenerateMfgDrawingManual(Form1.MfgDrawing.cs:2454)에 묶음으로 위임 — 수동 [가공도] 버튼과 같은 함수를 쓴다.
- PDF 누적·저장은 전부 Form1.Drawing2D.cs 소유 — _pendingMergedPdfPath(981)에 경로를 먼저 박고(Form1.Stru.cs:962) BeginPdfPageAccumulation(988)으로 열어 CleanupBetweenPdfPages(1098)로 페이지를 이어붙이다가 btnExtractDrawingList_Click(693)의 FlushPendingMergedPdf(1152)가 STRU 1개 = PDF 1개로 마감한다(#119).
- _struNodeCache(Form1.Stru.cs:42)는 Form1.DrawingSheets.cs:3134에서도 읽어 어떤 Part가 어느 STRU 소속인지 판정한다 — 캐시 소유는 이 파일, 소비는 두 곳.
- _p2aInProgress(Form1.Stru.cs:53)는 Form1.BOM.cs:356·657과 Form1.Clash.cs:972·1292에서 읽힌다 — 일괄 출력 중 모달 팝업을 전부 막는 3파일 공유 스위치. 켜져 있으면 연결성 실패 안내창도, '자동 처리 완료' 요약창도 안 뜬다.
- ProcessSingleStruFull(Form1.Stru.cs:858)이 xraySelectedNodeIndices를 비운다 → GetBOMTargetNodes(Form1.BOM.cs:817)와 DetectClash(Form1.Clash.cs:1038)의 필터 분기가 'visible 기준'으로 떨어진다. 격리는 가시성으로만 한다는 설계.
- btnExtractDrawingList_Click(Form1.Stru.cs:725) 취소 경로가 ClearCanceledOperationArtifacts(Form1.BOM.cs:514)를 재사용 — 치수 추출 취소와 같은 정리 로직.
- GetSheetKindLabel/WithSequence(Form1.Stru.cs:1093·1110)는 Form1.DrawingSheets.cs가 오버레이 문구(1996~2729)와 도면 제목란(1915)에 쓴다 — 이 파일에 있지만 소비자는 거의 DrawingSheets.

---

# Form1.Clash.cs

**1431줄 · 메서드 30개 · 조립자층**

> 부재 쌍마다 간섭검사를 걸어 '무엇이 무엇과 붙어 있는가'를 알아내고, 그 결과로 연결성을 판정해 도면 파이프라인의 문을 연다. 더불어 도면 우측 BOM 표(UDA 속성 기반)를 통째로 만든다.

**진입점**

- [BOM 수집] 버튼 → btnCollectBOMInfo_Click (Form1.Clash.cs:21, Form1.Designer.cs:419 배선)
- [Clash] 버튼 → btnClashDetection_Click (Form1.Clash.cs:1130, Form1.Designer.cs:1026 배선)
- SDK 간섭검사 완료 이벤트 → Clash_OnClashTestFinishedEvent (Form1.Clash.cs:1146, Form1.BOM.cs:166 등록)

## 도면 BOM 표 조립

*도면 우측에 찍히는 BOM 표(No/ITEM/MATERIAL/SIZE/Q'TY/T/W/MA/FA)를 만든다. 시트마다 미리 계산해 두고 클릭 때는 꺼내 쓰는 캐시 구조.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnCollectBOMInfo_Click` | 21 | 알림창을 켠 상태로 BOM 표 수집을 부른다. | 진입점 ([BOM 수집] 버튼) | CollectBOMInfo(66) |
| `CollectBOMInfo` | 66 | 대상 시트(인자 → 선택된 시트 → 없으면 모델 전체) 기준으로 BOM 표를 만들어 lvDrawingBOMInfo에 채운다. 시트에 준비된 BOM이 있으면 계산을 건너뛰고 바로 적용(cacheHit). showAlert=false면 팝업 없이 진단 로그만 남긴다. | btnCollectBOMInfo_Click(23) · Form1.DrawingSheets.cs:789·1873 · Form1.MfgDrawing.cs:2557·2765 | BuildDrawingBomPreparationContext(142) · BuildDrawingBomSnapshot(476) · StorePreparedBomSnapshot(602) · ApplyBomSnapshot(625) · ApplyPreparedBomInfo(613) |
| `PrepareDrawingSheetBomCaches` | 117 | 도면 목록을 띄우기 전에 모든 시트의 BOM 행을 한 번에 준비한다. 전 시트의 Body를 합쳐 컨텍스트를 한 번만 만들고 시트마다 스냅샷을 저장한 뒤 첫 시트를 표시한다 — 시트 클릭 때 느려지지 않게 하는 선불 계산. | Form1.DrawingSheets.cs:234 (시트 목록 생성 직후) | BuildDrawingBomPreparationContext(142) · BuildDrawingBomSnapshot(476) · StorePreparedBomSnapshot(602) · ApplyPreparedBomInfo(613) |
| `BuildDrawingBomPreparationContext` | 142 | 대상 Body를 Part로 묶고 Part마다 UDA를 딱 한 번 읽어 Part별 원자료와 BOM 번호 맵을 만든다. 읽는 키는 SPREF·MATREF·GWEI·POSSTART·POSEND·STRU 6개 고정 + App.config에서 오는 MA·FA. STRUCTURE 노드를 하나도 못 찾았거나 Uda.DumpOnLoad=true면 조상 체인 덤프를 남긴다(느려서 평소엔 안 돈다). | CollectBOMInfo(80) · PrepareDrawingSheetBomCaches(121) | ReadDrawingBomPartData(246) · DumpDrawingBomStruDiagnostics(373) · GetAppSetting(Form1.DrawingSheets.cs:3167) · bodyToPartIndexMap·bomList 조회 |
| `BuildDrawingBomSnapshot` | 476 | 요약행(00 Support&Seat) 1개 + 부재 행들을 BOM 번호순으로 만든다. 요약행 T/W는 STRU 노드의 GWEI를 우선 쓰고 없을 때만 부재 무게 합산으로 폴백한다 — 자체 GWEI가 없는 부재가 조상 무게를 물려받아 합산이 부풀려지므로 합산으로 덮어쓰지 않고 경고만 남긴다(#67). 부재 행은 칸마다 자기 값만 보고 판단해 빈 칸만 '-'로 나간다. | CollectBOMInfo(89) · PrepareDrawingSheetBomCaches(125) | ToDrawingBomDisplayValue(1429) · bodyToPartIndexMap 조회 · DiagLog |
| `StorePreparedBomSnapshot` | 602 | 시트의 PreparedBomRows를 스냅샷으로 교체하고 BomPrepared 플래그를 켠다. | CollectBOMInfo(91) · PrepareDrawingSheetBomCaches(126) | (없음) |
| `ApplyPreparedBomInfo` | 613 | 시트에 준비된 행이 있으면 그대로 화면에 표시한다. 시트가 null이거나 준비 전이면 아무것도 안 한다. | CollectBOMInfo(97) · PrepareDrawingSheetBomCaches(130) · Form1.DrawingSheets.cs:787 | ApplyBomSnapshot(625) |
| `ApplyBomSnapshot` | 625 | lvDrawingBOMInfo를 비우고 8열(No/ITEM/MATERIAL/SIZE/Q'TY/T/W/MA/FA)로 다시 채운다. BeginUpdate/EndUpdate로 깜빡임을 막는다. | CollectBOMInfo(93) · ApplyPreparedBomInfo(618) | lvDrawingBOMInfo.Items.Add |

## UDA 읽기·값 정규화

*모델 속성(UDA)에서 ITEM·SIZE·재질·무게를 꺼내 사람이 읽는 문자열로 바꾼다. 속성이 부재가 아니라 조상에 붙어 있는 경우가 많아 부모 방향으로 걸어 올라간다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ReadDrawingBomPartData` | 246 | Part에서 부모 방향 최대 10단계까지 올라가며 각 UDA의 '첫 값'을 모은다. SPREF를 '/'·':' 기준으로 잘라 ITEM/SIZE로 나누고, POSSTART~POSEND 거리를 SIZE 뒤에 'x길이'로 붙인다. 요약행 T/W용 STRUCTURE 노드는 'STRU 값과 이름이 같은 조상'으로 찾는다 — STRUCTURE 자신은 STRU가 비어 있기 때문(2026-07-28 실기 확인). 못 찾으면 StruNodeIndex=-1. | BuildDrawingBomPreparationContext(209) | ReadDrawingBomUdaValue(451) · ParsePosString(1408) · FormatDrawingBomWeight(433) · vizcore3d.Object3D.FromIndex |
| `ReadDrawingBomUdaValue` | 451 | 이미 값이 있으면 그대로 두고, 없을 때만 실제 키로 UDA를 읽어 앞뒤 공백을 떼어 돌려준다. 키 매핑이 없거나 SDK 조회가 실패하면 현재 값 유지(예외 삼킴). 조상 walk-up의 '첫 값 우선' 규칙을 이 한 함수가 구현한다. | ReadDrawingBomPartData(9곳) · DumpDrawingBomStruDiagnostics(403·404) | vizcore3d.Object3D.UDA.FromIndex |
| `FormatDrawingBomWeight` | 433 | '11.4317kg' 같은 GWEI 원문에서 숫자·소수점·부호·쉼표만 남겨 소수 2자리로 정규화한다. 숫자로 못 읽으면 원문을 그대로 돌려주고 weight는 0. | ReadDrawingBomPartData(347·350) | double.TryParse (static 메서드) |
| `ParsePosString` | 1408 | T-061 — POSSTART/POSEND 문자열에서 정규식으로 숫자 3개를 순서대로 뽑는다. 'S'/'U' 토큰의 의미는 미정이라 등장 순서로만 매칭하고, 3개 미만이면 0으로 채운다. | ReadDrawingBomPartData(333·334) | Regex.Matches |
| `ToDrawingBomDisplayValue` | 1429 | 비었거나 공백뿐이면 '-', 아니면 그대로. 도면 BOM 표의 빈 칸 표시 규칙 한 줄. | BuildDrawingBomSnapshot(576·577·579·582·583) | (없음) |
| `DumpDrawingBomStruDiagnostics` | 373 | 요약행 T/W용 STRU 노드를 못 찾았을 때 원인을 눈으로 보려고 부재 최대 3개의 조상 체인을 이름·STRU·GWEI와 함께 로그로 쏟는다. 첫 부재의 조상에 대해서는 비어 있지 않은 UDA를 전부 남긴다 — 무게가 실제로 어느 노드 어느 키에 있는지 확인용(#66·#198). | BuildDrawingBomPreparationContext(223) | ReadDrawingBomUdaValue(451) · vizcore3d.Object3D.UDA.Keys/FromIndex · DiagLog |

## 제작도 연결 후보 탐색 (바운딩박스 광역 필터)

*제작도 ISO 점선에 쓸 '바깥에 붙어 있는 부재'를 찾는다. 전체 모델과 쌍 검사를 하면 폭발하므로 바운딩박스로 후보를 먼저 줄이고, 최종 판정은 Clash에 맡긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ResetFabricationNeighborSearchCache` | 654 | 바운딩박스 캐시·Body→Part 캐시·기준 Body 수를 비우고 근접 검사 결과도 함께 지운다. 모델을 새로 열어 매핑을 다시 만들 때 호출된다. | BuildBodyToPartNameMap(Form1.BOM.cs:50) | ClearFabricationNeighborResults(666) |
| `ClearFabricationNeighborResults` | 666 | 연결 Clash 목록·연결 Part 집합·대상 Body/Part 집합만 비운다. 무거운 바운딩박스 캐시는 남긴다. | ResetFabricationNeighborSearchCache(659) · DetectClash(1025) | (없음 — 4개 컬렉션 Clear) |
| `EnsureFabricationNeighborSearchCache` | 678 | 모델 전체 Body의 바운딩박스와 실제 부모 Part를 한 번만 수집해 캐시한다. Body 수가 그대로면 재사용, 달라지면 다시 만든다. 개별 Body 실패는 로그만 남기고 넘어간다. | GetFabricationNeighborCandidates(782) | ResolveActualParentPartIndex(736) · vizcore3d.Object3D.GetBoundBox/GetPartialNode |
| `ResolveActualParentPartIndex` | 736 | Body에서 부모 체인을 최대 20단계 올라가며 실제 소속 Part를 찾는다. 못 찾으면 bodyToPartIndexMap으로 폴백하고 거기에도 없으면 -1. | EnsureFabricationNeighborSearchCache(715) | vizcore3d.Object3D.FromIndex · bodyToPartIndexMap 조회 |
| `BoundsOverlapWithinClearance` | 763 | 두 바운딩박스가 X·Y·Z 세 축 모두에서 여유 거리 이내로 겹치면 true. 제작도 후보 선별(3mm)과 설치도 연결 부재 판정(0.5mm)이 같은 함수를 다른 값으로 쓴다. | GetFabricationNeighborCandidates(830·835) · Form1.GlobalViews.cs:351 (0.5mm) | (없음 — 비교식 하나) |
| `GetFabricationNeighborCandidates` | 777 | 제작 대상 전체를 감싼 큰 박스로 1차 거르고, 통과한 것만 개별 대상 박스와 다시 대조해 후보를 뽑는다. 대상 자신과 같은 Part에 속한 Body는 제외. 이건 광역 필터일 뿐이고 최종 연결 판정은 Clash 결과가 한다. | DetectClash(1088, includeOutsideNeighbors=true일 때만) | EnsureFabricationNeighborSearchCache(678) · BoundsOverlapWithinClearance(763) · DiagLog |

## 무창 간섭검사 직렬 실행

*SDK 진행창을 띄우지 않고 등록된 검사를 하나씩 순서대로 돌린다. SDK가 한 번에 하나만 처리하고 완료가 이벤트로 오기 때문에 큐 + 상태머신이 필요하다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ResetSilentClashSequence` | 852 | 대기 ID 큐·활성 플래그·현재 ID·진행 카운트를 전부 초기값으로 되돌린다. 새 검사 시작 전, 취소·예외·후속 시작 실패 시 호출된다. | StartSilentClashSequence(867·881) · StartNextSilentClashTestAfterEvent(927) · HandleSilentClashStartFailure(959·966) · DetectClash(1121) · Clash_OnClashTestFinishedEvent(1157·1312) · Form1.BOM.cs:477·516 | (없음 — 5개 필드 리셋) |
| `StartSilentClashSequence` | 865 | 테스트 ID 목록에서 음수·중복을 걸러 큐에 넣고 첫 검사를 무창으로 시작한다. 시작되면 true, 큐가 비었거나 첫 시작이 실패하면 상태를 되돌리고 false. | DetectClash(1117) | ResetSilentClashSequence(852) · StartNextSilentClashTest(889) |
| `StartNextSilentClashTest` | 889 | 큐 맨 앞 ID를 SDK에 무창(두 번째 인자 false)으로 실행시킨다. 시작되면 큐에서 빼고 true, 실패하면 활성 ID를 -1로 돌리고 false. SDK가 Busy인 동안 부르면 실패할 수 있다. | StartSilentClashSequence(878) · StartNextSilentClashTestAfterEvent(936) | vizcore3d.Clash.PerformInterferenceCheck · DiagLog |
| `StartNextSilentClashTestAfterEvent` | 914 | 완료 콜백이 반환된 뒤 SDK Busy가 풀리기를 50ms씩 최대 40회(=2초) 기다렸다가 다음 검사를 시작한다. 도중에 취소가 걸리면 상태를 정리하고 빠진다. 끝까지 못 시작하면 실패 처리로 넘긴다. | AdvanceSilentClashSequence(1014, BeginInvoke로 UI 큐에 태워서) | IsCancellationRequested(Form1.cs:444) · StartNextSilentClashTest(889) · CancelMainDimensionAtCheckpoint(Form1.BOM.cs:470) · HandleSilentClashStartFailure(955) |
| `HandleSilentClashStartFailure` | 955 | 후속 검사 시작에 실패했을 때 상태를 초기화하고 치수 추출 또는 오버레이를 정리한다. STRU 일괄 출력 중이면 최소한의 시트 생성을 시도하고(팝업 없이), 아니면 '다시 실행해주세요' 안내창을 띄운다. | StartNextSilentClashTestAfterEvent(947) | ResetSilentClashSequence(852) · FinishMainDimensionOperation(Form1.BOM.cs:493) · GenerateDrawingSheets(Form1.DrawingSheets.cs:20) |
| `AdvanceSilentClashSequence` | 990 | 완료 이벤트마다 불려 '이게 마지막인가'를 판정한다. 기다리던 ID가 아니면 무시하고 false. 큐가 남았으면 다음 검사를 UI 메시지 큐로 예약하고 false — 즉 false면 호출자는 결과 처리를 하지 않는다. 큐가 비었을 때만 true를 줘서 전체 결과 처리로 진입시킨다. | Clash_OnClashTestFinishedEvent(1165) | BeginInvoke(StartNextSilentClashTestAfterEvent) · DiagLog |

## 간섭검사 실행·결과 처리·연결성 판정

*부재 쌍마다 ClashTest를 등록해 돌리고, 완료 이벤트에서 결과를 모아 '부재들이 한 덩어리인가'를 BFS로 판정한다. 이 판정을 통과해야만 치수·시트가 생긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `DetectClash` | 1021 | 대상 부재를 정하고(X-Ray 선택 우선, 없으면 visible) 모든 2개 조합에 대해 GROUP_VS_GROUP ClashTest를 등록한다. 여유거리·범위 모두 3.0mm(T-063 — Range가 Clearance보다 작으면 검사 자체가 안 돈다), 관통 허용 1.0mm. includeOutsideNeighbors면 제작도용 '대상 대 근접후보' 검사를 하나 더 붙인다. 등록이 0건이면 false, 아니면 직렬 실행을 시작한다. | btnMainDimension_Click(Form1.BOM.cs:430) · btnClashDetection_Click(1132) · ProcessSingleStruFull(Form1.Stru.cs:878) | ClearFabricationNeighborResults(666) · GetFabricationNeighborCandidates(777) · StartSilentClashSequence(865) · vizcore3d.Clash.Clear/Add |
| `btnClashDetection_Click` | 1130 | 간섭검사만 따로 시작한다(근접후보 검사 없이). 시작되면 '완료되면 알림창이 표시됩니다' 팝업. | 진입점 ([Clash] 버튼) | DetectClash(1021) |
| `Clash_OnClashTestFinishedEvent` | 1146 | 이 파일의 심장. 마지막 검사가 아니면 다음 것을 이어 돌리고 빠진다. 마지막이면 등록된 모든 ClashTest 결과를 PART 단위로 모아 A-B/B-A 중복을 걸러 clashList와 제작도 연결용 목록으로 나눠 담고, Z값 내림차순 정렬 후 lvClash에 넣는다. 그다음 연결성을 판정해 — 통과하면 Osnap·치수·시트로 넘기고, 실패하면 안내창을 띄우고 멈춘다(일괄 출력 중엔 팝업 없이 로그만). | 진입점 (SDK Clash.OnClashTestFinishedEvent, Form1.BOM.cs:166 등록) | AdvanceSilentClashSequence(990) · IsSingleConnectedComponent(1326) · CompleteMainDimensionPostClash(Form1.BOM.cs:545) · CancelMainDimensionAtCheckpoint/FinishMainDimensionOperation(Form1.BOM.cs) · vizcore3d.Clash.GetResultItem |
| `IsSingleConnectedComponent` | 1326 | T-023 v3 — bomList의 부재들이 Clash 인접 그래프에서 한 덩어리인지 BFS로 센다. Clash는 Part 인덱스, bomList는 Body 인덱스라 먼저 Part→Body 역매핑을 만든 뒤 양방향 인접 리스트를 세운다. 성분이 2개째 발견되는 순간 즉시 false로 빠진다. 부재가 1개면 무조건 true. | Clash_OnClashTestFinishedEvent(1284) | bodyToPartIndexMap 조회 (Form1.BOM.cs가 채운 사전) |

## 다른 파일과의 연결

- Clash_OnClashTestFinishedEvent(Form1.Clash.cs:1146)는 Form1.BOM.cs:166에서 SDK에 등록된다 → 치수 추출([치수 추출] 버튼)과 STRU 일괄 출력이 모두 이 콜백을 거쳐 CompleteMainDimensionPostClash(Form1.BOM.cs:545)로 이어진다. 이 파일이 '중간에서 받아 넘기는' 구조.
- IsSingleConnectedComponent(Form1.Clash.cs:1326)는 Form1.BOM.cs가 채운 bomList·bodyToPartIndexMap과 이 파일의 clashList를 함께 읽는다 → 판정에 실패하면 CompleteMainDimensionPostClash를 아예 안 불러 시트가 생기지 않는다. STRU 일괄 출력이 CollectBOMData로 bomList를 좁히는 이유(Form1.Stru.cs:862 주석)가 정확히 이 함수.
- Clash_OnClashTestFinishedEvent(1173·1232~1237)가 fabricationNeighborClashList·fabricationNeighborPartIndices·fabricationTargetPartIndices(선언 Form1.cs:39~42)를 채운다 → Form1.DrawingSheets.cs:3032~3072(제작도 연결 부재 목록·이름표)와 Form1.GlobalViews.cs:297~405(ISO 점선·연결 부재 표시)가 읽는다. 호출이 아니라 공유 필드로만 이어지는 구간.
- EnsureFabricationNeighborSearchCache(Form1.Clash.cs:678)가 fabricationBodyBoundsCache(Form1.cs:57)를 채운다 → Form1.GlobalViews.cs:351이 그 캐시를 그대로 읽고 BoundsOverlapWithinClearance(Form1.Clash.cs:763)를 0.5mm로 다시 불러 설치도 연결 부재를 판정한다.
- ResetFabricationNeighborSearchCache(Form1.Clash.cs:654)는 BuildBodyToPartNameMap(Form1.BOM.cs:50)이 부른다 — 모델을 새로 열면 바운딩박스 캐시가 함께 초기화되는 연결.
- CollectBOMInfo(Form1.Clash.cs:66)는 Form1.DrawingSheets.cs:789·1873과 Form1.MfgDrawing.cs:2557·2765에서 showAlert=false로 불린다 → 시트 선택·시트 2D 생성·가공도 생성 때마다 도면 BOM 표가 갱신된다.
- PrepareDrawingSheetBomCaches(Form1.Clash.cs:117)는 Form1.DrawingSheets.cs:234에서 시트 목록을 만든 직후 1회 호출 → 시트별 PreparedBomRows를 선불로 채워 이후 시트 클릭이 계산 없이 표시되게 한다.
- BuildDrawingBomPreparationContext(Form1.Clash.cs:229)는 Form1.BOM.cs의 bomList 순서를 그대로 BOM 번호(i+1)로 삼는다 → BOM 표 No. 열과 lvBOM 탭의 No. 열이 같은 번호를 쓴다(Form1.DrawingSheets.cs:817 주석이 이 규칙에 의존).
- MA·FA 속성 키는 코드가 아니라 App.config에서 온다 — GetAppSetting(Form1.DrawingSheets.cs:3167), 기본값 ':SHI_MA' / ':SHI_FA'(Form1.Clash.cs:27·30). 모델마다 키가 달라 뺀 것.
- HandleSilentClashStartFailure(Form1.Clash.cs:975)와 Clash_OnClashTestFinishedEvent(1292)가 _p2aInProgress(Form1.Stru.cs:53)를 읽어 일괄 출력 중에는 모달을 띄우지 않고 GenerateDrawingSheets(Form1.DrawingSheets.cs:20)로 최소 시트라도 만든다.

---

# Form1.Drawing2D.cs

**1273줄 · 메서드 28개 · 중간층**

> 2D·PDF 출력의 하부 설비 + Osnap 수집 UI. 앞쪽 절반은 Osnap·간섭 목록 버튼 핸들러, 뒤쪽 절반은 도면 여러 장을 PDF 한 개로 묶는 페이지 누적 장치다. 두 덩어리는 서로 거의 무관하다.

**진입점**

- btnGenerate2D_Click (L37) — 「2D 생성」 버튼
- btnExportPDF_Click (L82) — 「PDF 출력」 버튼
- btnCollectOsnap_Click (L183) — 「Osnap 수집」 버튼
- btnClashShowSelected_Click (L346) · btnClashShowAll_Click (L641) — 간섭 표시 버튼
- btnOsnapAdd/Delete/ShowSelected/ClearBalloon_Click (L684·746·850·956)
- LvBOM_DoubleClick (L127) · LvClash_DoubleClick (L155) · LvOsnap_SelectedIndexChanged (L818) — Form1.cs L267~272 배선
- GeometryUtility_OnOsnapPickingItem (L706) — SDK Osnap 픽킹 이벤트
- PDF 누적 8종 — DrawingSheets·MfgDrawing·Stru가 부르는 하부 설비

## 2D·PDF 버튼 (구 경로)

*치수 탭의 「2D 생성」·「PDF 출력」 두 버튼. 도면정보 탭의 신버전 출력과 같은 엔진을 쓰되 전체 BOM을 대상으로 임시 시트를 하나 만들어 넘긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnGenerate2D_Click` | 37 | BOM 전체 부재로 임시 DrawingSheetData(SheetNumber=1, BaseMemberIndex=-1)를 만들어 도면 생성 엔진에 넘긴다. 이름은 선택 부재 → 없으면 모델 파일명. | Designer.cs L1014 | GenerateSheetDrawing2D (DrawingSheets.cs L1744) · selectedAttributeNodeIndex(Attribute.cs가 씀) · bomList · currentFilePath |
| `btnExportPDF_Click` | 82 | 지금 2D 뷰에 그려진 도면을 SDK 내장 API로 벡터 PDF로 저장한다. 노란 선택 테두리를 먼저 지우고, 저장 후 3D 뷰를 부드러운 음영으로 되돌려 은선·X-Ray 잔상을 없앤다. | Designer.cs L1002 | SDK Drawing2D.Object2D.Export2PDFBy2DView · View.SetRenderMode |
| `GetSolutionPath` | 16 | 실행 폴더에서 위로 올라가며 .sln이 있는 폴더를 찾는다. 없으면 실행 폴더 그대로. | DrawingSheets.cs L3465·L3466 (템플릿 파일 경로 조립) | - |

## Osnap 수집·편집

*부재 모서리·꼭짓점 좌표(Osnap)를 모아 lvOsnap에 채우는 경로. LINE은 시작·끝 두 점, POINT는 중심만 쓰고 CIRCLE·SURFACE는 버린다 — 이 규칙이 세 곳에 똑같이 복제돼 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnCollectOsnap_Click` | 183 | 「Osnap 수집」 버튼. X-Ray 선택이 있으면 그 부재만, 없으면 화면에 보이는 Body만 골라 Osnap을 모으고 유형별 개수를 팝업으로 알린 뒤, 곧바로 치수 추출까지 이어서 실행한다. | Designer.cs L990 | SDK GetPartialNode·GetOsnapPoint → EstimateOsnapLineAxis → ExtractDimensionForSelectedNodes |
| `CollectOsnapForSelectedNodes` | 464 | 노드 인덱스 목록만 받아 Osnap을 모으는 조용한 버전(팝업 없음). 부재 이름은 Body가 아니라 Part 풀네임을 쓴다. | btnClashShowSelected_Click (L450) | GetPartNameFromBodyIndex(BOM.cs) · SDK GetOsnapPoint · EstimateOsnapLineAxis |
| `ExtractDimensionForSelectedNodes` | 578 | 모아둔 Osnap으로 X/Y/Z 세 축 체인치수를 만들어 chainDimensionList·lvDimension을 채우고 3D에 표시한다. btnExtractDimension_Click의 축약판이다. | btnCollectOsnap_Click (L334) · btnClashShowSelected_Click (L453) | MergeCoordinates → AddChainDimensionByAxis ×3 → ShowAllDimensions() (전부 Dimensions.cs) |
| `EstimateOsnapLineAxis` | 796 | Osnap LINE의 시작→끝 벡터에서 성분이 가장 큰 축을 그 선의 축으로 본다. lvOsnap 「축」 칸 값. | btnCollectOsnap_Click · CollectOsnapForSelectedNodes · BOM.cs L736 | - |
| `btnOsnapAdd_Click` | 684 | 「Osnap 추가」 버튼. SDK 픽킹 이벤트를 (중복 방지로 뺐다 다시) 걸고 Osnap 모드를 켠 뒤, 뷰어를 클릭해 좌표를 찍으라는 안내를 띄운다. | Designer.cs L779 | SDK GeometryUtility.ShowOsnap · OnOsnapPickingItem 구독 |
| `GeometryUtility_OnOsnapPickingItem` | 706 | 뷰어에서 클릭한 좌표를 목록에 한 줄 추가한다. 선택된 개체가 있으면 그 이름을, 없으면 "수동 추가"로 넣고 축은 비운다. | SDK 이벤트 (btnOsnapAdd_Click이 구독) | osnapPoints·osnapPointsWithNames 쓰기 · lvOsnap |
| `btnOsnapDelete_Click` | 746 | 선택한 Osnap 행을 인덱스 역순으로 지워 두 배열과 ListView를 동시에 맞추고 번호를 다시 매긴다. | Designer.cs L790 | - |
| `btnOsnapShowSelected_Click` | 850 | 선택한 Osnap 좌표에 빨간 구 마커와 좌표·홀사이즈 풍선을 찍고, 그 좌표를 품은 BOM 부재로 카메라를 날린다. 품은 부재가 없으면 회전 중심만 옮긴다. | Designer.cs L801 | SDK ShapeDrawing.AddSphere · Review.Note.AddNoteSurface · View.FlyToObject3d · bomList 조회 |
| `btnOsnapClearBalloon_Click` | 956 | 「풍선 지우기」 — Note와 ShapeDrawing 구 마커를 둘 다 없앤다. | Designer.cs L812 | SDK Review.Note.Clear · ShapeDrawing.Clear |
| `LvOsnap_SelectedIndexChanged` | 818 | Osnap 행을 고르면 그 부재이름과 같은 BOM 부재를 3D에서 강조하고 카메라를 맞춘다. 간섭 목록이 코드로 행을 선택하는 흐름에서는 억제 플래그로 건너뛴다. | Form1.cs L272 | bomList 조회 · SDK Select·FlyToObject3d · _suppressOsnapSelChanged(L811, Dimensions.cs가 켠다) |

## 간섭(Clash) 표시 버튼

*간섭 목록에서 고른 항목을 X-Ray로 띄우고, 그 부재에 대해 Osnap 수집과 치수 추출까지 자동으로 이어 붙인다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnClashShowSelected_Click` | 346 | 선택한 간섭쌍 부재만 X-Ray로 남기고 노란 삼각 심볼을 충돌 지점에 찍은 뒤, 그 부재로 카메라를 날리고 Osnap 수집 → 치수 추출을 연달아 실행한다. xraySelectedNodeIndices를 여기서 갈아 끼운다. | Designer.cs L599 | CollectOsnapForSelectedNodes → ExtractDimensionForSelectedNodes · SDK XRay.Select · Clash.ShowResultSymbol · bomList |
| `btnClashShowAll_Click` | 641 | X-Ray를 끄고 숨긴 부재를 되살려 전체 모델을 보여준 뒤 치수를 다시 표시한다. xraySelectedNodeIndices를 비운다. | Designer.cs L610 | RestoreAllPartsVisibility(MfgDrawing.cs L23) · ShowAllDimensions()(Dimensions.cs) |
| `LvClash_DoubleClick` | 155 | 간섭 행 더블클릭 → 색을 전부 복원하고 그 두 부재만 선택해 카메라를 맞춘다. | Form1.cs L268 | SDK Object3D.Color.RestoreColorAll · Select · FlyToObject3d |
| `LvBOM_DoubleClick` | 127 | BOM 행 더블클릭 → 그 부재 하나만 선택하고 카메라를 맞춘다. | Form1.cs L267 | SDK Object3D.Select · View.FlyToObject3d |

## 다중 페이지 PDF 누적 (#119)

*도면 한 장 그릴 때마다 캔버스를 비우면 PDF도 한 장씩 떨어진다. 누적 중에는 캔버스를 덧붙여 쌓아두고 마지막에 한 번만 저장한다. 기본은 꺼져 있다(App.config Pdf.MergePages=false) — 조립도처럼 장수가 많으면 SDK 네이티브가 죽기 때문. 상태는 필드 4개(_pdfPageAccumulating·_pdfPageCount·_activeDrawingCanvasIdx·_pendingMergedPdfPath)에 들어 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `BeginPdfPageAccumulation` | 988 | 누적을 시작한다. 이미 누적 중이면 바깥 누적이 주인이므로 false를 돌려 중첩을 안 만들고, 설정이 꺼져 있어도 false. 시작할 때만 이전 도면 잔재를 한 번 지운다. | DrawingSheets.cs L1355 · MfgDrawing.cs L2526 · Stru.cs L964 | IsPdfPageMergeEnabled · Clear2DView |
| `IsPdfPageMergeEnabled` | 1022 | App.config의 Pdf.MergePages 값을 읽는다. 기본 false. | BeginPdfPageAccumulation (L998) | GetAppSetting (DrawingSheets.cs L3167) |
| `PrepareDrawingCanvas` | 1054 | 도면 한 장 그리기 직전 캔버스를 준비한다. 누적이 아니면 비우고 1번 캔버스를 쓰고, 누적이면 새 캔버스를 덧붙여 그 번호를 활성으로 잡는다. | DrawingSheets.cs L1814 · MfgDrawing.cs L283 | Clear2DView · SDK Drawing2D.View.AddCanvasBy2DView·SetSelectCanvas |
| `SaveCurrentDrawingToPdf` | 1031 | 묶지 않는 모드에서 지금 뷰의 도면 한 장을 그대로 PDF로 저장한다. 선택 테두리를 먼저 지운다. | DrawingSheets.cs L1389 · MfgDrawing.cs L2693 | SDK Drawing2D.Render · Export2PDFBy2DView |
| `EndPdfPageAccumulation` | 1115 | 쌓아둔 캔버스 전체를 PDF 한 개로 저장하고 누적을 끝낸다. 페이지가 0이면 저장을 건너뛰고, 실패는 다시 던지되 finally에서 상태는 반드시 닫는다. | DrawingSheets.cs L1434 · MfgDrawing.cs L2742 · FlushPendingMergedPdf | SDK Export2PDFBy2DView |
| `FlushPendingMergedPdf` | 1152 | 일괄 출력이 예약해둔 묶음 PDF 경로를 꺼내 저장하고 누적을 닫는다. 취소·실패로 중간에 끊겼어도 그때까지 그린 페이지는 살린다. | Stru.cs L693 (도면 일괄 출력 마무리) | EndPdfPageAccumulation · _pendingMergedPdfPath(Stru.cs L962가 채움) |
| `DiscardCurrentPdfPage` | 1079 | 도면 생성이 실패했을 때 방금 만든 반쪽 캔버스를 지워 PDF 페이지로 남지 않게 한다. | DrawingSheets.cs L1411 · MfgDrawing.cs L2717 · Stru.cs L1022 | SDK Drawing2D.View.RemoveCanvasBy2DView |
| `CleanupBetweenPdfPages` | 1098 | 장과 장 사이 메모리 정리. 누적 중에는 쌓은 캔버스를 지우면 안 되므로 GC만 돌린다. UI 데드락 때문에 WaitForPendingFinalizers는 뺐다(issue #116). | DrawingSheets.cs L1412 · Stru.cs L1011·L1077 | CleanupDrawingSheetExportCanvas (DrawingSheets.cs L1609) |
| `BuildMergedDrawingPdfPath` | 1188 | 묶음 PDF 파일명을 {폴더}\{STRU}_{종류}.pdf 로 조립한다. STRU 이름을 못 구하면 모델명으로 대체하고, 같은 이름이 있으면 뒤에 번호를 붙이며, 경로가 240자를 넘으면 MAX_PATH 경고를 로그에 남긴다. | DrawingSheets.cs L1354·L1388 · MfgDrawing.cs L2525·L2692 · Stru.cs L962 | SanitizeFileName · SanitizeStruForFileName (DrawingSheets.cs) |

## 2D 뷰 초기화

*이전 도면 잔상을 지우고 뷰 모드를 되돌리는 두 개. 출력 경로 전체가 이 둘에 의존한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `Clear2DView` | 1248 | 2D 뷰를 Both로 열어둔 채 2D 객체·비객체·캔버스를 전부 지우고 다시 렌더한다. 예전엔 ViewMode를 두 번 왕복 토글했는데 화면이 장마다 깜빡여서(2026-07-22) 삭제 API만 쓰도록 바꿨다. | BOM.cs L336 · DrawingSheets.cs L1619 · BeginPdfPageAccumulation · PrepareDrawingCanvas | SDK Drawing2D.Object2D.DeleteAll* · View.RemoveCanvasBy2DView · Render |
| `RestoreViewMode` | 1231 | 진입 시점에 잡아둔 ViewMode로 되돌린다. Clear2DView와 Model.Open이 2D 뷰를 강제로 열기 때문에, 화면 구성을 바꾸면 안 되는 경로가 이걸로 원복한다. 이미 같은 모드면 아무것도 안 해 깜빡임이 없다. | BOM.cs L338 (모델 로드 후) | - |

## 다른 파일과의 연결

- PDF 누적 8종은 전부 이 파일에 있고 호출자는 전부 밖에 있다 — DrawingSheets(제작도·조립도·설치도), MfgDrawing(가공도), Stru(도면 일괄 출력). 이 파일이 출력 3인방의 공용 하부 설비다.
- _pendingMergedPdfPath (L981) — Stru.cs L962가 값을 넣고 Stru.cs L693이 FlushPendingMergedPdf로 꺼내 쓴다. 취소가 STRU 처리 중간에서 예외로 튀어나오기 때문에 저장을 호출자 쪽으로 미룬 구조.
- _activeDrawingCanvasIdx (L975) — DrawingSheets.cs L2003이 읽어 SetSelectCanvas에 넘긴다. 묶음 출력 중에는 1번이 아닐 수 있다.
- _suppressOsnapSelChanged (L811) — 선언은 여기, 켜고 끄는 곳은 Dimensions.cs L1640·1642. 간섭 행 선택이 Osnap 행 선택 이벤트를 다시 트리거하는 것을 막는다.
- osnapPoints / osnapPointsWithNames (Form1.cs L71) — 이 파일이 세 경로(버튼 수집·자동 수집·수동 픽킹)로 채우고, Dimensions.cs와 MfgDrawing.cs가 읽는다.
- xraySelectedNodeIndices — btnClashShowSelected_Click이 채우고 btnClashShowAll_Click이 비운다. GlobalViews.ApplyGlobalView가 이 값의 유무로 「선택 부재 뷰」와 「전체 모델 뷰」를 가른다.
- chainDimensionList — ExtractDimensionForSelectedNodes가 채우고 ShowAllDimensions(Dimensions.cs)가 읽는다.
- selectedAttributeNodeIndex (Form1.cs L86) — Attribute.cs가 3D 선택 때 채우고, btnGenerate2D_Click L55가 시트 이름을 정할 때 읽는다.
- 밖으로 부르는 것 — GenerateSheetDrawing2D·SanitizeFileName·SanitizeStruForFileName·GetAppSetting·CleanupDrawingSheetExportCanvas (DrawingSheets.cs) / RestoreAllPartsVisibility (MfgDrawing.cs) / GetPartNameFromBodyIndex (BOM.cs) / MergeCoordinates·AddChainDimensionByAxis·ShowAllDimensions (Dimensions.cs).

---

# Form1.Dimensions.cs

**2674줄 · 메서드 34개 · 중간층**

> 체인치수 엔진. Osnap 좌표 → 체인치수 생성 → 겹치는 것 골라내기 → 단(段)을 나눠 3D 뷰에 치수·보조선을 그린다. 제작도·조립도·설치도·가공도 네 도면이 전부 이 파일의 ShowAllDimensions / ComputeViewDimensionsForMembers를 거친다.

**진입점**

- btnExtractDimension_Click (L1786) — 「치수」 버튼
- btnDimensionShowSelected_Click (L17) — 「선택 보기」 버튼
- btnDimensionDelete_Click (L133) — 「선택 삭제」 버튼
- btnBalloonAdjust_Click (L205) — 「풍선 위치 조정」 버튼
- LvDimension_SelectedIndexChanged (L1549) — 치수 목록 행 선택 (Form1.cs L273 배선)
- LvClash_SelectedIndexChanged (L1579) — 간섭 목록 행 선택 (Form1.cs L269 배선)
- ShowAllDimensions (L360) — 다른 파일이 부르는 최대 진입점 (Drawing2D 2건 · DrawingSheets 3건 · GlobalViews 2건)
- ComputeViewDimensionsForMembers (L2117) — BOM 1건 · DrawingSheets 3건
- MarkNonRightAngles (L2471) — DrawingSheets 1건

## 버튼 핸들러 (화면 진입)

*사용자가 치수 탭 버튼을 눌렀을 때 실행되는 4개. 이 파일에서 가장 바깥층이다. Designer.cs가 Click 이벤트로 배선한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnExtractDimension_Click` | 1786 | 「치수」 버튼. X-Ray 선택을 비우고 BOM·Osnap을 현재 보이는 부재 기준으로 재수집한 뒤, X/Y/Z 세 축 체인치수를 만들어 chainDimensionList와 lvDimension을 채우고 3D에 표시한다. | Designer.cs L978 (btnExtractDimension.Click) | CollectBOMData·CollectAllOsnap (BOM.cs) → MergeCoordinates → AddChainDimensionByAxis ×3 → ShowMemberNameOverlay → ShowAllDimensions() |
| `btnDimensionShowSelected_Click` | 17 | 치수 목록에서 고른 행만 3D에 파란 축치수로 찍는다. 같은 축이 여러 개면 50mm씩 오프셋을 벌려 겹치지 않게 하고, 첫 치수 중심을 회전 중심으로 잡는다. | Designer.cs L679 | SDK Review.Measure.SetStyle / AddCustomAxisDistance · View.SetPivotPosition |
| `btnDimensionDelete_Click` | 133 | 선택한 치수 행을 역순으로 지우고 chainDimensionList와 번호를 재정렬한 뒤, 남은 치수 전부를 3D에 다시 그린다. | Designer.cs L690 | SDK Review.Measure.Clear / AddCustomAxisDistance |
| `btnBalloonAdjust_Click` | 205 | 풍선 위치를 손으로 미는 다이얼로그를 띄운다. 부재 콤보 + X/Y/Z 오프셋 입력이며, 「적용」을 누르면 balloonOverrides에 누적 저장하고 ISO 풍선을 다시 그린다. | Designer.cs L948 | CreateIsoBalloonNotes (DrawingSheets.cs L976) · balloonOverrides 쓰기 |
| `ShowMemberNameOverlay` | 1764 | 3D 뷰어 패널 위에 부재 이름을 고칠 수 있는 TextBox를 겹쳐 띄운다. 없으면 처음 한 번 만들어 붙이고, 이후엔 값만 갈아 끼운다. | btnExtractDimension_Click (L1840) | panelViewer.Controls.Add · txtMemberNameOverlay (Form1.cs L116) |

## 목록 3종 연동 선택

*간섭 행 하나를 고르면 관련 Osnap 행과 치수 행이 자동으로 같이 선택되고 3D도 그 부재로 날아간다. 서로를 다시 트리거하지 않도록 억제 플래그 두 개로 막는다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `LvClash_SelectedIndexChanged` | 1579 | 간섭 행 선택 → 두 부재 이름·결합 BBox를 모아 3D 강조 + 카메라 fit 하고, 관련 Osnap 행·치수 행을 자동 선택한다. 자동 선택 동안 두 억제 플래그를 켠다. | Form1.cs L269 (lvClash.SelectedIndexChanged) | SelectRelatedOsnapItems · SelectRelatedDimensionItems · bomList 조회 · _suppressOsnapSelChanged(Drawing2D.cs L811) · _suppressDimSelChanged |
| `LvDimension_SelectedIndexChanged` | 1549 | 치수 행 선택 → 그 치수의 MemberIndices 부재들을 3D에서 빨갛게 선택하고 카메라를 맞춘다. 억제 플래그가 켜져 있거나 부재 인덱스가 비면 아무것도 안 한다. | Form1.cs L273 (lvDimension.SelectedIndexChanged) | SDK Object3D.Color.RestoreColorAll · Select · View.FlyToObject3d |
| `SelectRelatedOsnapItems` | 1658 | 부재 이름이 같거나 좌표가 간섭 BBox(공차 1mm) 안에 드는 lvOsnap 행을 전부 선택하고 첫 행으로 스크롤한다. | LvClash_SelectedIndexChanged (L1641) | osnapPoints (Form1.cs 공유 필드) 읽기 · lvOsnap |
| `SelectRelatedDimensionItems` | 1710 | 치수의 시작점이나 끝점이 간섭 BBox(공차 1mm) 안에 들면 그 lvDimension 행을 선택한다. | LvClash_SelectedIndexChanged (L1646) | lvDimension 행 Tag의 ChainDimensionData 읽기 |

## 치수 계산 엔진 (Osnap → 체인치수)

*부재 Osnap 점을 받아 어느 뷰의 어느 축에 몇 mm 치수를 만들지 결정한다. SDK·화면을 건드리지 않는 순수 계산이라 제작도·가공도가 그대로 재사용한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ComputeViewDimensionsForMembers` | 2117 | 부재 목록을 받아 3뷰 × 보이는 2축 = 6조합의 체인치수를 한 번에 만든다. Osnap 맵은 미리 수집된 캐시를 먼저 쓰고 빠진 부재만 SDK로 보충한다. 같은 치수가 여러 뷰에 나오면 ViewDirection을 "X,Y"처럼 콤마로 합치고, 좌표→부재 역색인으로 MemberIndices를 채운다. | BOM.cs L585 · DrawingSheets.cs L538·L606·L760 | FilterOsnapForDimAxis → MergeCoordinates → AddChainDimensionByAxis · GetPartNameFromBodyIndex(BOM.cs) · DrawingReferenceWorldToLocal(DrawingSheets.cs) |
| `FilterOsnapForDimAxis` | 2318 | 한 뷰·한 치수축에서 쓸 Osnap 점만 골라낸다. 부재(Part)당 주축 최대 1점 → 같은 치수축 값 중복 제거 → 화면 네 귀퉁이 필수점 A/B/C/D 강제 포함 순서다. | ComputeViewDimensionsForMembers (L2257) · MfgDrawing.cs L1131·L2181 | VertexAxisGetter · RoundToTolerance |
| `AddChainDimensionByAxis` | 1987 | 한 축의 점들을 오름차순으로 늘어놓고 인접 점 사이 순차 치수를 만든다. 같은 축값이 겹치면 필터축(뷰의 나머지 축) 최소인 점만 남기고, 점이 3개 이상이면 맨 처음~맨 끝 전체 치수(IsTotal)를 하나 더 붙인다. | btnExtractDimension_Click · ComputeViewDimensionsForMembers · Drawing2D.cs L600·604·608 · MfgDrawing.cs L1135·L1216·L2210 | GetRemainingAxis · GetAxisValue · RoundToTolerance · GetViewNameByAxis |
| `MergeCoordinates` | 1913 | Osnap 좌표를 공차(0.5mm) 단위로 반올림해 사실상 같은 점을 하나로 합친다. | btnExtractDimension_Click · ComputeViewDimensionsForMembers · Drawing2D.cs L597 · MfgDrawing.cs L1134·L2199 | RoundToTolerance |
| `GetRemainingAxis` | 1972 | 뷰 방향과 치수축을 빼고 남는 축 하나를 돌려준다. 이게 치수선을 밀어낼 오프셋축이자 Osnap 필터축이 된다. | ShowAllDimensions(4곳) · AddChainDimensionByAxis · MfgDrawing.cs 9곳 | - |
| `GetAxisValue` | 1498 | Vector3D에서 X/Y/Z 중 지정한 축 좌표 하나를 꺼낸다. 모르는 축이면 0. | ShowAllDimensions · DrawDimension · ApplySmartFiltering · AddChainDimensionByAxis · MfgDrawing.cs 6곳 | - |
| `VertexAxisGetter` | 2443 | GetAxisValue의 Vertex3D 버전을 람다로 돌려준다. FilterOsnapForDimAxis가 주축·보조축·치수축 세 개를 미리 잡아두고 쓴다. | FilterOsnapForDimAxis (L2332~2334) | - |
| `RoundToTolerance` | 1942 | 값을 공차 배수로 반올림한다. 좌표 병합·중복 제거 키의 기준. | MergeCoordinates · AddChainDimensionByAxis · ComputeViewDimensionsForMembers · FilterOsnapForDimAxis | - |
| `GetViewNameByAxis` | 1950 | 축 문자를 도면 용어로 바꾼다. X=측면도 / Y=정면도 / Z=평면도. lvDimension 「뷰」 칸에 그대로 들어간다. | AddChainDimensionByAxis (L2057·L2082) | - |

## 스마트 필터링 (몇 개를 보일까)

*만든 치수를 다 넣으면 겹쳐서 못 읽는다. 우선순위를 매기고 → 짧은 것끼리 묶고 → 텍스트 폭 기준으로 겹치는 것을 2단으로 올리거나 숨긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ApplySmartFiltering` | 1241 | 축별로 전체치수·필수치수는 무조건 넣고, 나머지는 우선순위 높은 순으로 텍스트 폭이 안 겹칠 때만 0단에 배치한다. 겹치면 우선순위 5 이상만 1단으로 내리고 그것도 겹치면 IsVisible=false로 숨긴다. 축당 최대 개수 상한이 있다. | ShowAllDimensions (L423, 축당 8개·최소간격 25mm) · MfgDrawing.cs L1445 (가공도 전용 상수) | AssignDimensionPriorities → MergeShortDimensions → GetAxisValue |
| `AssignDimensionPriorities` | 1168 | 축별 거리 분포를 정규화해 우선순위 1~10을 매긴다. 필수치수(설치 위치)와 전체치수는 무조건 10, 나머지는 상대 크기 상위 30%=8 / 중간=5 / 하위=3 / 최하위=1. | ApplySmartFiltering (L1250) | - |
| `MergeShortDimensions` | 1377 | 위치 순으로 훑으면서 최소길이 미만 치수가 연달아 나오면 한 덩어리로 모아 누적 치수로 바꾼다. 혼자 있는 짧은 치수는 우선순위만 2 깎아 그대로 둔다. | ApplySmartFiltering (L1276) | CreateMergedDimension |
| `CreateMergedDimension` | 1444 | 짧은 치수 묶음의 맨 앞점~맨 뒷점을 잇는 새 체인치수 하나를 만든다. IsMerged=true, 우선순위 6. | MergeShortDimensions (L1409·L1428) | - |
| `ComputePositiveOffsetByOsnapExtreme` | 1516 | 치수선을 모델의 어느 쪽으로 밀지 정한다. Osnap 좌표의 최대·최소 중 모델 중앙에서 더 먼 쪽이 이기고, 그쪽이 최대면 + 방향(true). | ShowAllDimensions (L563) · MfgDrawing.cs L1176·L2256 | - |

## 3D 표시 (어디에 그릴까)

*필터를 통과한 치수를 실제로 SDK 측정 객체와 보조선으로 만든다. 이 파일의 몸통이자 다른 4개 파일이 부르는 지점.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ShowAllDimensions` | 360 | chainDimensionList를 viewDirection으로 걸러 → 스마트 필터링 → baseline(부재 BBox ∪ 치수 끝점) 계산 → 1단/2단/전체 세 단으로 나눠 DrawDimension을 돌리고 → 모아둔 보조선을 한 번에 ShapeDrawing으로 긋는다. 2D 출력이면 검은 0.15mm 선 + ShapeDrawing ID를 반환한다. 예외가 나도 BeginUpdate 짝을 복구해 화면 멈춤을 막는다. | btnExtractDimension_Click · Drawing2D.cs L630·L673 · DrawingSheets.cs L962·L2283·L2439 · GlobalViews.cs L133·L191 | ApplySmartFiltering · ComputeCanvasAbsoluteOffsets · GetRemainingAxis · GetAxisValue · ComputePositiveOffsetByOsnapExtreme · DrawDimension · GetDrawingReferenceAxisVertex |
| `DrawDimension` | 708 | 치수 한 개를 그린다. 뷰×축 조합으로 오프셋 방향과 baseline을 정하고, 참조축 프레임이 있으면 로컬↔월드를 오가며 UserAxis 치수를, 없으면 월드축 치수를 SDK에 추가한다. 2단 승격 치수는 텍스트를 종이 절대 2.5mm 밀고, 마지막에 양 끝 보조선 두 줄을 extensionLines에 쌓는다. | ShowAllDimensions (L600·L617·L635) · MfgDrawing.cs L886 | TryNormalizeDimensionAxis · GetBoundingBoxProjectionRange · MovePointToAxisProjection · SetDrawingReferenceLocalAxisValue · AddWorldAxisDistance · OffsetTowardLineEnd · DrawingReferenceLocalToWorld(DrawingSheets.cs) |
| `ComputeCanvasAbsoluteOffsets` | 338 | 종이 위 절대 mm(제작도 7.5/7.5 · 가공도 9/9)를 축척으로 나눠 모델좌표 오프셋으로 역산한다. 보조선 길이 정책의 단일 출처. | ShowAllDimensions (L525) · MfgDrawing.cs L843 | - |
| `AddWorldAxisDistance` | 1035 | 축 문자를 SDK Axis 열거형으로 바꿔 AddCustomAxisDistance를 부르고 측정 ID를 돌려준다. 월드축 기본 경로와 UserAxis 실패 폴백 양쪽에서 쓰인다. | DrawDimension (L894) | SDK Review.Measure.AddCustomAxisDistance |
| `OffsetTowardLineEnd` | 1141 | 보조선을 모델 표면에서 gap만큼 띄워 시작시킨다. gap이 선 길이보다 크면 끝점을 그대로 돌려 선이 뒤집히는 것을 막는다. | DrawDimension (L978·L986) | - |

## 참조축 좌표 헬퍼 (기울어진 부재용)

*부재가 월드축에 안 맞게 기울어 있으면 DrawingReferenceFrame의 로컬축으로 치수를 잰다. 그 좌표 변환·정규화를 담당하는 작은 5개.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetDrawingReferenceAxisVertex` | 997 | 참조축 프레임에서 X/Y/Z 축 방향벡터 하나를 꺼낸다. 프레임이 없으면 null → DrawDimension이 월드축 경로로 간다. | ShowAllDimensions (L605·606·622·623·639·640) | - |
| `TryNormalizeDimensionAxis` | 1060 | 축 벡터를 단위벡터로 만든다. null이거나 길이가 0에 가까우면 false. 측정축·오프셋축 둘 다 성공해야 UserAxis 경로를 탄다. | DrawDimension (L811·812) | - |
| `MovePointToAxisProjection` | 1077 | 점을 지정한 축 방향으로 밀어 그 축 투영값이 목표와 같아지게 한다. 프레임 없는 UserAxis 경로에서 치수선을 기준선까지 옮길 때 쓴다. | DrawDimension (L842·843) | - |
| `GetBoundingBoxProjectionRange` | 1094 | 경계상자 8꼭짓점을 축에 투영해 최소·최대를 구한다. 기울어진 축 기준 치수 기준선을 모델 바깥에 놓기 위한 계산. | DrawDimension (L836) | - |
| `SetDrawingReferenceLocalAxisValue` | 1018 | 점의 한 축 좌표만 제자리에서 갈아 끼운다. 입력 객체를 직접 고치는 점에 주의. | DrawDimension (L827·828·950·951) | - |

## 비직각 접합 각도 표시

*두 부재가 90° 배수가 아닌 각으로 만나면 그 사잇각을 도면에 찍는다. 파일 맨 끝에 상수 2개와 함께 홀로 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `MarkNonRightAngles` | 2471 | 부재별로 Osnap LINE 세그먼트를 방향(공차 cos 5°)별 길이 합으로 묶어 길이축을 잡고, 접합한 쌍의 3D 사잇각이 90° 배수(공차 1°)가 아니면 파란 각도 치수를 찍는다. 판형(PAD/PLATE)은 제외하고, 접합 평면 법선이 깊이축에 가장 가까운 뷰 한 곳에만 그린다. | DrawingSheets.cs L2288 (ShowAllDimensions 직후) | IsPadOrPlateFromSpref(MfgDrawing.cs) · bodyToPartIndexMap · clashList · SDK GetOsnapPoint · Review.Measure.AddCustom3PointAngle |

## 다른 파일과의 연결

- chainDimensionList (Form1.cs L192) — 이 파일의 btnExtractDimension_Click이 채우고, GlobalViews.ExtractInstallationDimensions·Drawing2D.ExtractDimensionForSelectedNodes·DrawingSheets(시트 선택/2D 출력)도 각자 채운다. ShowAllDimensions는 오직 이 리스트만 읽어 그린다 — 계산과 표시가 이 필드 하나로 이어진다.
- xraySelectedNodeIndices (Form1.cs L76) — ShowAllDimensions가 baseline BBox의 기준으로 읽는다. GlobalViews·Drawing2D·DrawingSheets·Stru가 쓴다. btnExtractDimension_Click은 여기를 비운 뒤 현재 보이는 Body로 다시 채운다.
- osnapPointsWithNames / osnapPoints (Form1.cs L71) — BOM.CollectAllOsnap과 Drawing2D의 Osnap 수집이 채우고, btnExtractDimension_Click·SelectRelatedOsnapItems가 읽는다.
- _suppressOsnapSelChanged — 선언은 Drawing2D.cs L811, 켜고 끄는 곳은 이 파일 LvClash_SelectedIndexChanged L1640. 두 파일이 플래그 하나로 이벤트 연쇄를 막는다.
- ShowAllDimensions ← Drawing2D(2) · DrawingSheets(3) · GlobalViews(2) · 이 파일(1). 총 8개 호출처가 있는 이 프로젝트 최대 허브.
- ComputeViewDimensionsForMembers ← BOM.cs L585 · DrawingSheets.cs L538·L606·L760. 시트 하나 고를 때마다 여기로 들어온다.
- MfgDrawing.cs가 재사용하는 것 — DrawDimension·AddChainDimensionByAxis·MergeCoordinates·FilterOsnapForDimAxis·ApplySmartFiltering·ComputeCanvasAbsoluteOffsets·ComputePositiveOffsetByOsnapExtreme·GetRemainingAxis·GetAxisValue. 가공도는 값(상수)만 다르고 식은 이 파일 것을 그대로 탄다.
- 밖으로 부르는 것 — CollectBOMData·CollectAllOsnap·GetPartNameFromBodyIndex (BOM.cs) / CreateIsoBalloonNotes·DrawingReferenceWorldToLocal·DrawingReferenceLocalToWorld (DrawingSheets.cs) / IsPadOrPlateFromSpref (MfgDrawing.cs) / DiagLog (Form1.cs L238).
- balloonOverrides·currentBalloonView·currentBalloonMemberIndices (Form1.cs L91·96·187) — btnBalloonAdjust_Click이 쓰고 DrawingSheets.CreateIsoBalloonNotes가 읽는다.
- (문서 주의) docs/코드분석/파일별/Form1.Dimensions.md는 줄수 2,985 · ShowAllDimensions 663줄 · 죽은 버튼 핸들러 4개(btnShowAxisX/Y/Z·btnShowISO)를 적고 있으나 현재 코드에는 전부 없다. 실제는 2,674줄 / ShowAllDimensions 344줄이며 죽은 풍선 블록과 죽은 핸들러는 2026-08-23에 삭제됐다(L662~666 주석).

---

# Form1.GlobalViews.cs

**1206줄 · 메서드 28개 · 조립자층**

> 두 가지 일이 한 파일에 있다. 앞 195줄은 탭 공통 ISO/X/Y/Z 뷰 버튼(상황에 따라 시트뷰·선택부재뷰·전체모델뷰로 갈린다), 나머지 1,000줄은 설치도 전용 — 간섭 결과로 접합 영역을 구성하고 「기준부재 끝단 → 연결부재 접합 모서리」 위치 치수를 만든다.

**진입점**

- btnGlobalISO_Click / btnGlobalAxisX_Click / btnGlobalAxisY_Click / btnGlobalAxisZ_Click (L17·25·33·41) — Designer.cs L1261~1306 배선
- PrepareInstallationConnectionData (L290) ← DrawingSheets.cs L167
- ExtractInstallationDimensions (L246) ← DrawingSheets.cs L748·L905
- ComputeInstallationDimensions (L1067) ← DrawingSheets.cs L534·L614
- BuildInstallationPlacementAnchor (L888) ← DrawingSheets.cs L2590 (ISO 연결 이름 위치)

## 글로벌 뷰 버튼 (탭 공통)

*화면 어느 탭에 있든 같은 ISO/X/Y/Z 버튼을 쓴다. 지금 상태를 보고 셋 중 하나로 갈라진다 — 시트가 선택돼 있으면 시트 뷰, X-Ray 선택이 있으면 그 부재 뷰, 아니면 전체 모델 뷰.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnGlobalISO_Click` | 17 | ApplyGlobalView("ISO")만 부른다. | Designer.cs L1261 | ApplyGlobalView |
| `btnGlobalAxisX_Click` | 25 | ApplyGlobalView("X")만 부른다. | Designer.cs L1276 | ApplyGlobalView |
| `btnGlobalAxisY_Click` | 33 | ApplyGlobalView("Y")만 부른다. | Designer.cs L1291 | ApplyGlobalView |
| `btnGlobalAxisZ_Click` | 41 | ApplyGlobalView("Z")만 부른다. | Designer.cs L1306 | ApplyGlobalView |
| `ApplyGlobalView` | 49 | 세 갈래 분기 그 자체. ① 도면정보 탭에서 시트가 선택돼 있으면 시트 뷰 ② xraySelectedNodeIndices가 있으면 선택 부재 뷰 ③ 아니면 전체 모델 뷰. 예외는 팝업으로 알린다. | 글로벌 버튼 4개 | ApplyDrawingSheetView (DrawingSheets.cs L853) · ApplySelectedNodesView · ApplyFullModelView |
| `ApplySelectedNodesView` | 79 | X-Ray를 켠 채 선택 부재만 불투명하게 남기고 초록 실루엣을 두른 뒤, 축 방향으로 카메라를 옮기고 그 부재에 화면을 맞춘다. ISO면 부재번호 풍선을, X/Y/Z면 치수를 표시한다. | ApplyGlobalView (L63) | CreateIsoBalloonNotes (DrawingSheets.cs) 또는 ShowAllDimensions (Dimensions.cs) · SDK XRay·FlyToObject3d |
| `ApplyFullModelView` | 140 | X-Ray를 끄고 숨긴 부재를 되살려 전체 모델을 축 방향으로 보여준다. xraySelectedNodeIndices를 비우고, ISO면 bomList 전체 인덱스로 풍선을, X/Y/Z면 치수를 표시한다. | ApplyGlobalView (L68) | RestoreAllPartsVisibility (MfgDrawing.cs) · CreateIsoBalloonNotes 또는 ShowAllDimensions · SDK FitToView |

## 설치도 진입점

*설치도 시트 하나에 대해 ① 접합 데이터를 준비하고 ② 위치 치수를 뽑아 화면 목록까지 채우는 두 개. DrawingSheets가 부른다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `PrepareInstallationConnectionData` | 290 | 제작도 근접 간섭 결과에서 「시트 부재 ↔ 외부 부재」 쌍만 골라, BODY 조합마다 실제 접합 영역을 만들어 sheet.InstallationConnections에 쌓는다. 접합선도 Mesh도 없으면 간섭 HotPoint 한 점으로 대신한다(IsProximityFallback). 끝에 A/B/C 라벨을 붙이고 점선으로 그릴 상대 STRU 범위를 sheet.InstallationContextIndices에 넣는다. | DrawingSheets.cs L167 | GetBodyIndicesForPart · GetBodyContactAreas · FindClosestBodyToPoint · AssignInstallationConnectionLabels · ExpandInstallationContextToStru · FindParentStru·FindNearestParentAssembly·BoundsOverlapWithinClearance (DrawingSheets.cs·Clash.cs) |
| `ExtractInstallationDimensions` | 246 | 설치도 치수를 계산해 chainDimensionList와 lvDimension을 채우고 번호를 매긴다. 마지막에 xraySelectedNodeIndices를 시트 부재로 고정한다 — 연결부재까지 넣으면 상대 서포트 BBox가 baseline을 밀어 보조선이 뷰마다 길어지기 때문(#63). | DrawingSheets.cs L748·L905 | ComputeInstallationDimensions · SDK Measure·Note·ShapeDrawing.Clear |

## 설치 위치 치수 계산

*접합 영역에서 「어느 축으로 몇 mm 떨어져 붙었나」를 뽑는 핵심. 접합이 그 축으로 부재를 절반 이상 덮으면 가로지르는(관통) 접합으로 보고 그 축은 생략한다 — 크기가 아니라 접합 형태로 판정한다(2026-07-23 재설계).*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ComputeInstallationDimensions` | 1067 | BODY 쌍별로 위치 기준점을 잡고, 채택된 축 성분마다 순수 축정렬 치수(다른 두 좌표는 끝단과 공유)를 만든다. 성분 축이 깊이 방향이 되는 뷰에는 배정하지 않는다 — 두 끝점이 화면에서 겹쳐 치수가 뭉개지기 때문. 마지막에 뷰·축·좌표 기준 중복 제거. | ExtractInstallationDimensions · DrawingSheets.cs L534·L614 | BuildInstallationPlacementAnchor · AddInstallationDimension · GetVectorAxisValue |
| `BuildInstallationPlacementAnchor` | 888 | 대상 BODY의 길이축·접합점 중심·축별 끝단·접합 모서리를 담은 기준점을 만든다. X/Y/Z 각 축에 대해 접합 extent ÷ 부재 extent(=덮음률)가 0.5 이상이면 가로지름으로 보고 빼고, 미만이면 그 축 성분을 채택한다. 접합점이 하나도 없으면 null(치수 생략). | ComputeInstallationDimensions (L1095) · DrawingSheets.cs L2590 (ISO 연결 이름 위치) | GetInstallationBodyPoints · TryGetInstallationMainAxis · SelectInstallationTargetEndForAxis · GetVectorAxisValue |
| `TryGetInstallationMainAxis` | 772 | 실제 접촉 BODY의 길이축을 구한다. 가공도에서 검증된 LINE 방향 5도 군집·길이 합 최대 판정을 재사용하고, LINE Osnap이 없을 때만 BBox 최장축으로 폴백한다. | BuildInstallationPlacementAnchor (L910) | GetMfgAxisDetection (MfgDrawing.cs L3691) · SDK GetBoundBox |
| `SelectInstallationTargetEndForAxis` | 862 | 지정한 축에서 연결 모서리에 가까운 쪽 끝단면을 잡고, 그 끝단면 후보(동률 공차 0.5mm) 중 축 직교 평면 거리가 가장 가까운 점을 끝단으로 고른다. | BuildInstallationPlacementAnchor (L946·L986) | GetVectorAxisValue · PerpendicularDistanceInPlane · Distance3D |
| `AddInstallationDimension` | 1161 | 위치 치수 한 건을 ChainDimensionData로 만들어 결과 목록에 넣는다. 축 성분 차이가 3mm 이하면 넣지 않는다 — 끝단 근접 연결·어셈블리 틈 잔여가 겹친 선으로 나오는 것을 막는다. | ComputeInstallationDimensions (L1133) | GetVectorAxisValue |
| `PerpendicularDistanceInPlane` | 842 | 지정 축 성분을 뺀 나머지 두 성분만으로 두 점 거리를 잰다. 끝단 후보를 연결 모서리에 가까운 순으로 줄 세울 때 쓴다. | SelectInstallationTargetEndForAxis (L878) | - |
| `GetVectorAxisValue` | 1195 | Vector3D에서 지정 축 좌표를 꺼낸다. Dimensions.cs의 GetAxisValue와 같은 일을 하는 이 파일 전용 사본 — 설치도 경로 전체가 이걸 쓴다. | 이 파일 안에서만 11곳 (SelectInstallationTargetEndForAxis·BuildInstallationPlacementAnchor·ComputeInstallationDimensions·AddInstallationDimension) | - |

## 접합 영역 구성

*두 BODY가 실제로 맞닿는 곳을 SDK 접합선(없으면 접합 Mesh)으로 구하고, 이어진 것끼리 한 영역으로 묶은 뒤 각 점을 실제 모서리 Osnap에 스냅한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetBodyContactAreas` | 542 | 두 BODY의 SDK 접합선을 1mm 이내로 이어진 영역으로 묶어 돌려준다. 접합선이 없으면 접합 Mesh를, 둘 다 없으면 빈 목록을. 영역의 각 점은 양쪽 BODY의 LINE/POINT Osnap에 3mm 이내면 스냅하고 같은 좌표는 하나로 합친다. | PrepareInstallationConnectionData (L355) | MergeConnectedContactSegments · GetLinePointOsnaps · SnapToNearestOsnap · SDK GetObjectCollisionLine·GetJunctionMesh |
| `MergeConnectedContactSegments` | 607 | 접합 선분 중 어느 점이든 1mm 이내로 맞닿는 것끼리 한 영역으로 합친다. 새 선분이 여러 영역에 동시에 닿으면 그 영역들까지 하나로 병합한다. SDK를 안 건드리는 순수 계산. | GetBodyContactAreas (L568) | Distance3D |
| `SnapToNearestOsnap` | 1028 | 점을 가장 가까운 Osnap 좌표로 끌어당긴다. 허용거리를 넘으면 원래 점 그대로. 접합선·Mesh 점을 실제 모서리 좌표에 맞추는 용도. | GetBodyContactAreas (L593) | Distance3D |
| `GetLinePointOsnaps` | 701 | BODY들의 Osnap 중 LINE의 시작·끝점과 POINT 중심만 모은다. CIRCLE 등은 뺀다 — 원형은 설치 위치 기준에서 제외한다는 규칙. 중복은 안 합치므로 호출측이 정리한다. | GetBodyContactAreas (L589) · GetInstallationBodyPoints (L741) | SDK Object3D.GetOsnapPoint |
| `GetInstallationBodyPoints` | 737 | 끝단 후보가 될 Osnap 좌표를 중복 없이 돌려준다. Osnap이 하나도 없으면 boundsFallback을 켜고 경계상자 8모서리로 대신한다. | BuildInstallationPlacementAnchor (L900·L902) | GetLinePointOsnaps · SDK GetBoundBox |
| `Distance3D` | 1053 | 두 점의 유클리드 직선거리. 선분 병합·Osnap 스냅·끝단 정렬이 공용으로 쓴다. | MergeConnectedContactSegments · SnapToNearestOsnap · SelectInstallationTargetEndForAxis | - |

## 노드 탐색·라벨 헬퍼

*Part↔BODY를 오가고, 표시 범위를 STRU 전체로 넓히고, 연결부재에 A/B/C 라벨을 붙이는 작은 것들.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ExpandInstallationContextToStru` | 433 | #63. 접합이 확인된 외부 Part 하나가 아니라 그 Part가 속한 STRU 전체의 하위 BODY를 점선 표시 대상으로 넓힌다. 시트 부재는 실선이므로 빼고, STRU 조상을 못 찾은 Part는 그 Part만 남긴다. 접합 판정·치수·라벨은 종전대로 실제 접합 Part 기준이다. | PrepareInstallationConnectionData (L410) | FindParentStru (DrawingSheets.cs L3132) · GetDescendantBodyIndices |
| `GetBodyIndicesForPart` | 489 | Part 인덱스 → 그 Part에 속한 BODY 인덱스 목록. 제작도 근접 검사의 BODY→Part 캐시를 먼저 뒤지고, 없으면 SDK로 하위 자식 중 BODY만 모은다. SDK 예외는 조용히 삼킨다. | PrepareInstallationConnectionData (L320·L321) | fabricationBodyToPartIndexCache (Clash.cs가 채움) · SDK GetChildObject3d |
| `GetDescendantBodyIndices` | 515 | 노드 인덱스 → 자기 자신(BODY일 때)과 모든 하위 BODY 인덱스를 중복 없이. SDK 예외는 조용히 삼킨다. | ExpandInstallationContextToStru (L460) | SDK FromIndex · GetChildObject3d |
| `FindClosestBodyToPoint` | 679 | 한 점에서 경계상자까지 바깥 거리가 가장 짧은 BODY를 고른다. 접합선도 Mesh도 없을 때 간섭 HotPoint의 대표 BODY를 정하는 폴백. | PrepareInstallationConnectionData (L378·L379) | fabricationBodyBoundsCache (Clash.cs가 채움) |
| `AssignInstallationConnectionLabels` | 636 | 연결을 상대 Assembly·Part 단위로 묶어 이름순 정렬한 뒤 A, B, … 라벨을 써 넣는다. 같은 상대 Part의 접합영역 여러 개는 같은 라벨을 받는다. | PrepareInstallationConnectionData (L397) | ToAlphabeticLabel |
| `ToAlphabeticLabel` | 661 | 0부터의 순번을 엑셀 열 이름 방식(A…Z, AA, AB…)으로 바꾼다. | AssignInstallationConnectionLabels (L651) | - |

## 다른 파일과의 연결

- sheet.InstallationConnections (Models.cs L197) — PrepareInstallationConnectionData가 채우고, ComputeInstallationDimensions(위치 치수)와 DrawingSheets.cs L2558(ISO 연결 이름 풍선)이 읽는다. 설치도 전체가 이 리스트 하나로 이어진다.
- sheet.InstallationContextIndices (Models.cs L196) — ExpandInstallationContextToStru 결과가 여기 들어가고, DrawingSheets.cs L2128·L3004가 「점선으로 그릴 배경 부재」로 읽는다.
- fabrication* 캐시 6종 (fabricationNeighborClashList·TargetBodyIndices·TargetPartIndices·BodyBoundsCache·BodyToPartIndexCache·NeighborPartIndices, Form1.cs 선언) — 전부 Clash.cs가 제작도 근접 검사에서 채우고 이 파일이 읽기만 한다. 검사 대상과 시트 대상이 다르면 PrepareInstallationConnectionData가 L301에서 조용히 빠져나간다.
- chainDimensionList — ExtractInstallationDimensions가 비우고 다시 채운다. 그 뒤 ShowAllDimensions(Dimensions.cs)가 이걸 읽어 그린다.
- xraySelectedNodeIndices — ApplyGlobalView가 읽어 뷰 갈래를 정하고, ApplyFullModelView가 비우며, ExtractInstallationDimensions L279가 시트 부재로 통째 교체한다(#63 baseline 고정).
- 밖으로 부르는 것 — ApplyDrawingSheetView·CreateIsoBalloonNotes·FindParentStru·FindNearestParentAssembly (DrawingSheets.cs) / RestoreAllPartsVisibility·GetMfgAxisDetection (MfgDrawing.cs) / BoundsOverlapWithinClearance (Clash.cs) / ShowAllDimensions (Dimensions.cs) / DiagLog (Form1.cs).
- GetVectorAxisValue(L1195)는 Dimensions.cs의 GetAxisValue(L1498)와 동작이 같은 사본이다. 설치도 경로는 이 파일 사본만 쓰고 밖에서 부르는 곳은 없다.
- 내부 데이터 클래스 2개 — InstallationAxisComponent(L211)·InstallationPlacementAnchor(L225). 후자는 DrawingSheets.cs L2582가 타입으로 받아 쓴다(ISO 연결 이름 위치).

---

# Form1.DrawingSheets.cs

**3564줄 · 메서드 56개 · 조립자층**

> 간섭 결과를 그래프로 보고 도면을 몇 장으로 나눌지 정한 뒤, 한 장마다 엑셀 양식을 깔고 4면도(ISO·Z·X·Y)를 찍어 넣는다. 제작도·조립도·설치도 3종의 PDF 일괄 출력도 여기서 돈다.

**진입점**

- btnGenerateSheet2D_Click (L1153) — 「2D 출력」 버튼. Form1.Designer.cs L505 배선
- btnExportSheet2DPDF_Click (L1213) — 「PDF 출력」 버튼. Designer L495
- btnExportFabricationSheets_Click (L1261) — 「제작도」 버튼. Designer L475
- btnExportAssemblySheets_Click (L1269) — 「조립도」 버튼. Designer L465
- btnExportInstallationSheets_Click (L1277) — 「설치도」 버튼. Designer L455
- LvDrawingSheet_SelectedIndexChanged (L627) — 도면 시트 목록 행 선택. Form1.cs L270 배선
- LvDrawingBOMInfo_SelectedIndexChanged (L809) — BOM 표 행 선택. Form1.cs L271 배선
- GenerateDrawingSheets (L20) — 다른 파일이 부르는 진입점. Form1.BOM.cs L644(치수 추출 사슬의 끝) · Form1.Clash.cs L975 · Form1.Stru.cs L887(자동 일괄 폴백)
- GenerateSheetDrawing2D (L1744) — Form1.Drawing2D.cs L75 · Form1.Stru.cs L997(자동 일괄)
- ApplySheetSelection (L651, 이 파일 유일한 public) — Form1.Stru.cs L993 자동 일괄이 UI 트릭 없이 직접 부른다
- ApplyDrawingSheetView (L853) — Form1.GlobalViews.cs L56 (글로벌 X/Y/Z/ISO 뷰 버튼)

## ① 시트 분할 — 도면을 몇 장으로 나눌 것인가

*Clash 결과를 Part→Body로 펼쳐 인접 리스트를 만들고, 부재마다 1-hop 이웃 한 장(조립도) + BFS 전체 한 장(설치도) + 부재당 한 장(가공도)을 만든 뒤, 포함부재가 같은 시트를 지우고 1..N으로 재채번한다. drawingSheetList의 유일한 생산자.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GenerateDrawingSheets` | 20 | 치수 추출이 끝난 시점에 불려서 drawingSheetList와 lvDrawingSheet를 통째로 다시 채운다. BaseMemberIndex 음수 센티넬(-1 제작도 / -2 설치도 / -3 가공도)로 종류를 구분하고, 마지막에 치수·BOM 캐시를 미리 계산해 이후 시트 클릭이 빨라지게 만든다. | BOM.cs L644 · Clash.cs L975 · Stru.cs L887 | CreateFullDrawingSheetData · PrepareInstallationConnectionData(GlobalViews) · ShowBusyOverlay(Form1) · PrepareDrawingSheetDimensionCaches · PrepareDrawingSheetBomCaches(Clash) · ApplyPreparedDimensionsToUi |
| `CreateFullDrawingSheetData` | 469 | BOM 전체를 포함부재로 담은 Sheet 1(제작도, BaseMemberIndex = -1)을 만들어 준다. 이름은 선택 노드명 → 파일명 → "전체" 순으로 정한다. 2D 출력에서 제작도 행이 없을 때 임시 시트로도 쓴다. | GenerateDrawingSheets(L33) · btnGenerateSheet2D_Click(L1195) |  |

## ② 시트 선택 반영 — 3D 뷰·치수·BOM·풍선

*목록에서 행을 고르면 부재만 남겨 보이고, 미리 계산해둔 체인치수를 UI에 붙이고, BOM 표를 채운다. 가공도(-3)만 예외로 캐시를 안 쓰고 매번 MfgDrawing의 ExecuteMfgDrawing을 다시 돌린다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `PrepareDrawingSheetDimensionCaches` | 506 | 목록을 보여주기 전에 가공도를 뺀 모든 시트의 체인치수를 미리 계산해 sheet.PreparedDimensions에 넣는다. 엔진+부재집합이 같은 시트끼리는 한 번만 계산해 공유한다. | GenerateDrawingSheets(L233) | ComputeInstallationDimensions(GlobalViews) · ComputeViewDimensionsForMembers(Dimensions) |
| `ApplyPreparedDimensionsToUi` | 566 | 준비된 치수를 chainDimensionList에 옮기고 lvDimension을 다시 그린다. SDK 재조회·재계산이 없어 일반·설치 시트 클릭이 빠른 이유가 이것. | GenerateDrawingSheets(L237) · ApplySheetSelection(L746·754·764) |  |
| `GetDrawingSheetDimensionsFor2D` | 600 | 2D 출력 시점의 치수 원본을 고른다. 설치도(-2)는 사전 준비한 끝단→모서리 전용 치수를 그대로 쓰고, 나머지는 공용 Osnap 엔진을 참조 프레임과 함께 다시 돌린다. | GenerateSheetDrawing2D_WithExcelTemplate(L1854 · L2102 참조축 실패 폴백) | ComputeViewDimensionsForMembers(Dimensions) · ComputeInstallationDimensions(GlobalViews) |
| `LvDrawingSheet_SelectedIndexChanged` | 627 | 목록 행 선택 이벤트를 받아 억제 플래그(_suppressSheetSelectionHandler)를 보고 ApplySheetSelection에 넘기기만 한다. | Form1.cs L270 이벤트 배선 | ApplySheetSelection |
| `ApplySheetSelection` | 651 | 시트 하나를 3D 뷰에 적용하는 단일 진입점. X-Ray를 끄고 전체를 숨긴 뒤 시트 부재만 Show, 초록 실루엣 엣지, 기준부재 빨간 선택, 카메라 fit까지 한 번에 한다. 그다음 시트 종류별로 치수 분기(-3 가공도→ExecuteMfgDrawing / -2 설치도 / 그 외 Osnap)하고 BOM 표를 채운다. 수동 클릭과 자동 일괄이 공통으로 지나간다. | LvDrawingSheet_SelectedIndexChanged(L642) · ExportSheetsByKind(L1369) · Stru.cs L993 | ResetMfgPreviewViewState(MfgDrawing) · GetDrawingSheetDisplayIndices · ExecuteMfgDrawing(MfgDrawing) · ApplyPreparedDimensionsToUi · ExtractInstallationDimensions(GlobalViews) · ComputeViewDimensionsForMembers(Dimensions) · ApplyPreparedBomInfo/CollectBOMInfo(Clash) |
| `LvDrawingBOMInfo_SelectedIndexChanged` | 809 | BOM 표에서 행을 고르면 그 부재로 카메라만 날아간다. 시트의 가시성은 안 건드린다. | Form1.cs L271 이벤트 배선 |  |
| `Clear3DDimensionAnnotations` | 844 | 2D로 복사하려고 3D에 임시로 만든 Measure와 ShapeDrawing(보조선)을 지운다. | ApplyDrawingSheetView(L870) · GenerateSheetDrawing2D의 finally(L1753) |  |
| `ApplyDrawingSheetView` | 853 | 글로벌 뷰 버튼이 눌리면 선택 시트의 포함부재를 X-Ray로 강조하고 방향별로 카메라를 옮긴다. 방향 문자열이 "ISO"면 풍선을 만들고, X/Y/Z면 ShowAllDimensions로 그 뷰 치수를 3D에 그린다. Y축만 Y_MINUS 규약. | GlobalViews.cs L56 | GetDrawingSheetDisplayIndices · Clear3DDimensionAnnotations · ExtractInstallationDimensions(GlobalViews) · CreateIsoBalloonNotes · ShowAllDimensions(Dimensions) |
| `CreateIsoBalloonNotes` | 976 | ISO 뷰에서 부재마다 번호 풍선을 붙인다. 3D 고정 오프셋으로 라벨 자리를 잡고 2D AABB 겹침 검사로 밀어낸다. 화면 풍선과 도면 풍선(forDrawing2D=true)이 같은 함수를 쓴다. 노드→노트ID 사전을 돌려준다. | ApplyDrawingSheetView(L917) · GenerateSheetDrawing2D_WithExcelTemplate(L2220) · Dimensions.cs L307·L317 · GlobalViews.cs L129·L187 |  |
| `GetDrawingSheetDisplayIndices` | 2998 | 시트가 실제로 화면에 띄울 노드 목록을 준다. 설치도(-2)만 선택 STRU에 직접 연결된 외부 Part를 덧붙이고, 나머지는 MemberIndices 그대로. | ApplySheetSelection(L691) · ApplyDrawingSheetView(L865) · GenerateSheetDrawing2D_WithExcelTemplate(L2071) |  |

## ③ 한 장 그리기 — 엑셀 템플릿 + 4면도

*이 프로젝트 최대 메서드가 있는 곳. 캔버스를 A4 가로로 깔고 → 시트 부재만 격리 → 참조축 → 치수 → BOM → 엑셀 양식 치환 → {View_n} 영역마다 실선/점선 두 겹 캡처 → 보조선·풍선·치수를 2D로 옮긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GenerateSheetDrawing2D` | 1744 | 2D 도면 생성의 바깥 껍데기. Core를 부르고 finally에서 임시 3D 치수·보조선을 반드시 지운다. | btnGenerateSheet2D_Click(L1206) · ExportSheetsByKind(L1374) · Drawing2D.cs L75 · Stru.cs L997 | GenerateSheetDrawing2DCore · Clear3DDimensionAnnotations |
| `GenerateSheetDrawing2DCore` | 1762 | 히든라인 투영용 엣지 데이터(GenerateEdgeData)를 먼저 갱신한 뒤 엑셀 템플릿 경로로 넘긴다. 앞뒤에 취소 체크포인트를 둔다. ⚠ 옛 무템플릿 직접 그리기 경로는 2026-08-23에 삭제돼 지금은 분기가 없다(문서의 '361줄 죽은 경로' 기술은 낡음). | GenerateSheetDrawing2D(L1748) | GetSheetKindLabel(Stru) · ProcessCancelableUiCheckpoint(Form1) · vizcore3d.Object3D.GenerateEdgeData · GenerateSheetDrawing2D_WithExcelTemplate |
| `GenerateSheetDrawing2D_WithExcelTemplate` | 1799 | 🔴 프로젝트 최대 메서드. 주석으로 0~7단계가 그어져 있다 — 3D 어노테이션 초기화 → PrepareDrawingCanvas(297,210) → 시트 부재 X-Ray 격리 → 제작도만 로컬 참조축 → 치수 → CollectBOMInfo → 엑셀 슬롯 사전 구성 → ImportExcelWithData → GetViewAreasFromExcel → View 1~4(ISO/Z/X/Y) 루프에서 실선·점선 두 겹 캡처 + CropFit + Rescale + MoveObjectTo → 보조선/풍선/치수를 2D로 이관 → RemoveEmptyTemplateBorders. 프로젝트명 "CEDAR FLNG"·선박번호 "SN2688"이 PoC 하드코딩으로 박혀 있다. | GenerateSheetDrawing2DCore(L1781) | PrepareDrawingCanvas(Drawing2D) · TryBuildDrawingReferenceFrame · GetDrawingSheetDimensionsFor2D · CollectBOMInfo(Clash) · GetTagNoValue · GetOrCacheDrawingPaintCode · GetDpNoValue · FillRevisionTable/BuildCurrentRevisionHistory(ExcelTemplate) · ResolveDrawingTemplatePath · AddImageSlotIfExists · ActivateDrawingReferenceAxis · ReleaseActiveDrawingReferenceAxis · GetDrawingSheetDisplayIndices · GetClashNeighborPartsOutsideSheet · GetFabricationNeighborAssemblyNotes · GetInstallationNoteLabelPoint · CreateIsoBalloonNotes · EstimateFitScaleForViewArea · ShowAllDimensions/MarkNonRightAngles(Dimensions) · ApplyOrientationRotation(MfgDrawing) · ProcessCancelableUiCheckpoint(Form1) |
| `EstimateFitScaleForViewArea` | 3511 | viewArea 폭·높이와 부재 bbox로 fit 배율을 추정한다. 사용자 사양대로 Z뷰는 0.65, X·Y는 0.70을 곱한다. 참조 프레임이 있으면 그 로컬 범위를, 없으면 SDK GetBoundBox를 쓴다. ShowAllDimensions 보조선 위치 계산의 기준값이다. | GenerateSheetDrawing2D_WithExcelTemplate(L2280 추정 · L2431 설치도 실측 폴백) |  |

## ④ 참조축과 두 겹 표현 재료

*제작도만 쓰는 로컬 참조축(가장 긴 수평 모서리 기준)의 생성·활성화·해제·좌표 변환, 그리고 점선으로 그릴 '시트 밖 이웃' 산출과 그 이름 라벨 계산.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `TryBuildDrawingReferenceFrame` | 2762 | 선택 부재 안에서 가장 긴 LINE 모서리를 찾아 그 XY 투영을 로컬 X축으로, 수평 직교축을 로컬 Y축으로 삼은 프레임을 만든다(Z-up 도면 규약). 각도 허용 1.0°, 최소 선분 길이 1.0. | GenerateSheetDrawing2D_WithExcelTemplate(L1852, 제작도(-1)일 때만) | DrawingReferenceWorldToLocal |
| `ActivateDrawingReferenceAxis` | 2905 | 프레임을 SDK 참조축으로 만들어 활성화하고 카메라를 그 축 기준 방향으로 옮긴다. 실패하면 참조축을 풀고 세계축으로 폴백하며 false를 준다 — 호출자는 그때 치수까지 세계축으로 되돌린다. | GenerateSheetDrawing2D_WithExcelTemplate(L2094) | ReleaseActiveDrawingReferenceAxis |
| `ReleaseActiveDrawingReferenceAxis` | 2943 | 활성 참조축을 SDK에서 리셋·삭제하고 _drawingActiveReferenceAxisId를 -1로 돌린다. 뷰 시작·완료, 교체, 활성화 실패, 도면 종료마다 불린다. | GenerateSheetDrawing2D_WithExcelTemplate(L2074·2716·2749) · ActivateDrawingReferenceAxis(L2912·2930) |  |
| `DrawingReferenceWorldToLocal` | 2965 | 세계 좌표점을 프레임 원점 기준으로 세 축에 투영해 로컬 좌표로 바꾼다. | TryBuildDrawingReferenceFrame(L2882) · Dimensions.cs L2207(Osnap 점 변환) |  |
| `DrawingReferenceLocalToWorld` | 2982 | 로컬 좌표를 원점+세 축으로 되돌려 세계 좌표를 준다. 치수·보조선 끝점을 3D에 그릴 때 쓴다. | Dimensions.cs L731·736·829·830·952·953 (이 파일 안에서는 호출 없음 — 전량 Dimensions가 소비) |  |
| `GetClashNeighborPartsOutsideSheet` | 3028 | 제작도 ISO 점선 대상 — Clash.cs가 채운 제작도 전용 간섭 결과에서 시트 밖 Part 인덱스를 정렬해 돌려준다. 현재 시트가 검사 당시 대상과 다르면 낡은 결과를 안 쓴다. | GenerateSheetDrawing2D_WithExcelTemplate(L2175) |  |
| `GetFabricationNeighborAssemblyNotes` | 3050 | 제작도 연결 이웃을 가장 가까운 상위 Assembly(우선 STRU)로 묶어 이름 하나 + 대표 HotPoint를 준다. 같은 Assembly의 여러 Part가 닿아도 도면에 이름은 한 번만 나온다. | GenerateSheetDrawing2D_WithExcelTemplate(L2490) | FindParentStru · FindNearestParentAssembly |
| `GetInstallationNoteLabelPoint` | 3012 | 설치도 연결부재 이름 풍선의 라벨 위치를 계산한다. X뷰는 Y 방향, 그 외(Y·Z·ISO)는 X 방향으로 오프셋만큼 민다. 지금 호출자는 설치도 ISO 뷰뿐. | GenerateSheetDrawing2D_WithExcelTemplate(L2605) |  |
| `FindNearestParentAssembly` | 3105 | Part에서 부모로 올라가며 가장 가까운 Assembly 노드를 찾는다. FindParentStru 실패 시 폴백. | GetFabricationNeighborAssemblyNotes(L3078) · GlobalViews.cs L331 |  |
| `FindParentStru` | 3132 | 부재에서 부모로 올라가며 STRU 노드를 찾는다. STRU 판정은 모델 로드 때 CollectStruList가 채운 _struNodeCache 집합으로 한다. 없으면 null. | GetFabricationNeighborAssemblyNotes(L3078) · GlobalViews.cs L331·L450 |  |

## ⑤ 버튼과 종류별 일괄 PDF 출력

*살아 있는 버튼 5개와 그 뒤의 일괄 출력 파이프라인. 제작/조립/설치 버튼 셋은 4줄짜리로 ExportSheetsByKind에 종류만 바꿔 넘긴다. 컨트롤 잠금·복원, 대상 선정, 결과 표시가 여기 모여 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnGenerateSheet2D_Click` | 1153 | 「2D 출력」 — 선택된 시트(없으면 제작도 행, 그것도 없으면 임시 전체 시트)를 하나 골라 2D 도면 한 장을 캔버스에 그린다. | Designer L505 | CreateFullDrawingSheetData · GenerateSheetDrawing2D |
| `btnExportSheet2DPDF_Click` | 1213 | 「PDF 출력」 — 이미 캔버스에 그려진 2D 도면을 SDK Export2PDFBy2DView로 파일 하나에 저장한다. 저장 전에 2D 선택을 모두 해제한다. | Designer L495 |  |
| `btnExportFabricationSheets_Click` | 1261 | 「제작도」 — ExportSheetsByKind(Fabrication) 호출만. | Designer L475 |  |
| `btnExportAssemblySheets_Click` | 1269 | 「조립도」 — ExportSheetsByKind(Assembly) 호출만. | Designer L465 |  |
| `btnExportInstallationSheets_Click` | 1277 | 「설치도」 — ExportSheetsByKind(Installation) 호출만. | Designer L455 |  |
| `ExportSheetsByKind` | 1286 | 요청한 종류의 시트만 골라 한 장씩 시트 선택 → 2D 변환 → PDF 페이지 누적을 반복한다. 시작 전 컨트롤을 잠그고 취소 구간을 열며, finally에서 반드시 복원한다. 마지막 성공 시트는 재출력을 위해 캔버스에 남긴다. | 제작도/조립도/설치도 버튼 3개 | GetDrawingSheetExportKindLabel · GetDrawingSheetExportTargets · CaptureDrawingExportControlStates · SetDrawingExportControlsEnabled · BeginCancelableOperation(Form1) · ShowBusyOverlay(Form1) · GetDefaultDrawingSaveDir(MfgDrawing) · ResolveDrawingStruName · BuildMergedDrawingPdfPath/BeginPdfPageAccumulation/SaveCurrentDrawingToPdf/EndPdfPageAccumulation/DiscardCurrentPdfPage/CleanupBetweenPdfPages(Drawing2D) · SelectDrawingSheetItemForExport · ApplySheetSelection · GenerateSheetDrawing2D · RestoreDrawingExportControlStates · ShowDrawingSheetExportResult |
| `GetDrawingSheetExportTargets` | 1472 | ListView를 훑어 그 종류에 맞고 부재가 있는 행만 (행, 시트) 쌍 목록으로 준다. | ExportSheetsByKind(L1319) | MatchesDrawingSheetExportKind |
| `MatchesDrawingSheetExportKind` | 1493 | BaseMemberIndex 규약(-1 제작 / 0 이상 조립 / -2 설치)으로 시트가 그 종류인지 판정한다. 가공도(-3)는 어디에도 안 속해 항상 false. | GetDrawingSheetExportTargets(L1481) |  |
| `GetDrawingSheetExportKindLabel` | 1512 | 종류 → 한글 라벨(제작도/조립도/설치도), 그 외는 "도면". 메시지와 PDF 파일명에 쓴다. | ExportSheetsByKind(L1288) |  |
| `SelectDrawingSheetItemForExport` | 1532 | 출력 중인 행 하나만 선택·포커스·화면 노출한다. 억제 플래그를 켰다 꺼서 선택 변경 핸들러가 안 돌게 한다 — 3D 적용은 호출자가 ApplySheetSelection으로 따로 한다. | ExportSheetsByKind(L1368) |  |
| `CaptureDrawingExportControlStates` | 1554 | 도면 출력 버튼 7개 + 시트 ListView의 현재 Enabled를 사전에 찍어둔다. | ExportSheetsByKind(L1327) · BOM.cs L374 · MfgDrawing.cs L2852 |  |
| `SetDrawingExportControlsEnabled` | 1579 | 그 컨트롤들을 한꺼번에 켜거나 끈다. 긴 작업 중 중복 클릭 차단용으로 false로 부른다. | ExportSheetsByKind(L1340) · BOM.cs L375 · MfgDrawing.cs L2856 |  |
| `RestoreDrawingExportControlStates` | 1595 | 찍어둔 사전대로 각 컨트롤을 되돌린다. null이면 아무것도 안 한다. 보통 finally에서 불린다. | ExportSheetsByKind(L1455) · BOM.cs L504 · MfgDrawing.cs L2870 |  |
| `CleanupDrawingSheetExportCanvas` | 1609 | 2D 뷰를 비우고 GC를 한 번 돌려 직전 장의 2D 객체를 회수한다. 파이널라이저 대기는 UI 데드락 위험이라 일부러 안 쓴다. | Drawing2D.cs L1102 (CleanupBetweenPdfPages) |  |
| `ShowDrawingSheetExportResult` | 1635 | 일괄 출력 결과를 MessageBox로 띄운다. PDF가 여러 개면 폴더, 하나면 파일 경로를 보여주고 취소·실패가 있으면 경고 아이콘. | ExportSheetsByKind(L1464) |  |

## ⑥ UDA·표제부 값 조회

*표제부에 찍는 TAG No.·DP No.·PAINT CODE와 파일명용 STRU 이름을 부재에서 부모로 최대 10단계 walk-up해서 찾는다. MfgDrawing의 UDA 조회와 짝인 코드 뭉치.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetAppSetting` | 3167 | App.config appSettings에서 값을 읽는다. 없거나 실패하면 기본값. 실행파일 옆 A2Z.exe.config를 고치고 재시작하면 반영된다. | GetTagNoValue(L3192) · GetDpNoValue(L3206) · Clash.cs L172·173·216 · Drawing2D.cs L1024 |  |
| `GetTagNoValue` | 3190 | 표제부 TAG No. 값. 어느 속성을 읽을지는 App.config `Uda.TagNo`가 정하고 기본값은 비어 있다 — 생산설계 모델 이름의 속성명을 아직 못 받아서, 틀린 값보다 빈칸이 낫다는 판단(2026-07-29). | GenerateSheetDrawing2D_WithExcelTemplate(L1922) · MfgDrawing.cs L2522 | GetAppSetting · GetNamedUdaValue · ResolveTagBaseNodeIndex |
| `GetDpNoValue` | 3204 | 표제부 DP No. 값. 기본 속성 이름은 `:SHI_DSN_DP`(2026-08-05 담당자 확인), App.config `Uda.DpNo`로 덮어쓸 수 있다. | GenerateSheetDrawing2D_WithExcelTemplate(L1927) · MfgDrawing.cs L2523 | GetAppSetting · GetNamedUdaValue · ResolveTagBaseNodeIndex |
| `ResolveTagBaseNodeIndex` | 3218 | walk-up 출발 노드를 고른다. BaseMemberIndex가 음수 센티넬이면 walk-up이 시작도 못 하므로 시트가 실제로 담은 첫 부재 노드로 바꾼다(#120 — 조립도만 값이 채워지던 버그의 수정). | ResolveDrawingStruName(L1714) · GenerateSheetDrawing2D_WithExcelTemplate(L2557) · GetTagNoValue · GetDpNoValue |  |
| `GetStruUdaValue` | 3231 | 구조물 식별자 `STRU` 속성 값. PDF 파일명·폴더명·연결부재 이름이 쓴다. 표제부 TAG No.와는 별개다. | ResolveDrawingStruName(L1714) · GenerateSheetDrawing2D_WithExcelTemplate(L2557) | GetNamedUdaValue |
| `GetNamedUdaValue` | 3241 | 지정한 이름의 속성을 부재에서 위로 최대 10단계 올라가며 찾아 비어 있지 않은 첫 값을 준다. 이름 비교는 대소문자 무시. 못 찾으면 빈 문자열. | GetTagNoValue · GetDpNoValue · GetStruUdaValue |  |
| `GetStruPntUdaValues` | 3298 | 키 이름에 PNT가 든 STRU UDA를 walk-up으로 찾아 최대 2개 돌려준다(도면 PAINT CODE 칸이 2개라서). 값이 처음 잡히는 깊이의 값들만 모으고 더 위로 안 올라간다 — 조상마다 다른 도장 사양이 섞이면 판단 근거가 없어서. 순서는 키 이름 오름차순(UDA.Keys 순서가 보장 안 되므로). | GetOrCacheDrawingPaintCode(L3411) |  |
| `GetOrCacheDrawingPaintCode` | 3386 | 같은 도면 목록의 제작·조립·설치·가공도가 PAINT CODE를 공유하도록 출력 시점에 한 번만 조회해 모든 DrawingSheetData에 캐시한다. null=미조회 / ""=조회했지만 값 없음으로 구분해 빈 모델도 재조회하지 않는다. UDA.Keys가 무거워 BeginUpdate 밖에서만 부른다. | GenerateSheetDrawing2D_WithExcelTemplate(L1925) · MfgDrawing.cs L2593 | GetStruPntUdaValues |

## ⑦ 리소스 경로·파일명 헬퍼

*엑셀 템플릿·이미지가 배포 패키지(.sln 없음)에서도 찾아지도록 실행 폴더 우선 → 솔루션 폴더 폴백으로 경로를 푼다. PDF 파일명에 쓸 이름 정규화도 여기.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ResolveDrawingResourcePath` | 3445 | 리소스 경로 해결의 공통 기반 — 실행 폴더(하위폴더 포함) 우선, 솔루션 폴더는 개발 편의용 폴백. 옛 GetSolutionPath 방식이 배포 환경에서 템플릿·이미지를 다 놓치던 것을 고친 것(#71). | ResolveDrawingAssetPath(L3476) · ResolveDrawingTemplatePath(L3502) |  |
| `ResolveDrawingAssetPath` | 3474 | 도면 이미지 경로 — 실행 폴더 루트 → 실행 폴더 assets\ → 솔루션 assets\ 순. | AddImageSlotIfExists(L3490) · Stru.cs L633 | ResolveDrawingResourcePath |
| `ResolveDrawingTemplatePath` | 3500 | 엑셀 템플릿 경로 — 실행 폴더 templates\ → 실행 폴더 루트 → 솔루션 루트 순. | GenerateSheetDrawing2D_WithExcelTemplate(L1876, 제작도_도면.xlsx) · MfgDrawing.cs L2465(가공도_도면.xlsx) | ResolveDrawingResourcePath |
| `AddImageSlotIfExists` | 3488 | {Image_N} 슬롯에 이미지를 넣되 파일이 실제로 있을 때만 넣는다. 회사 로고는 저작권 문제로 레포에서 빠져(2026-08-21) 없는 PC가 있는데, 경로만 그대로 넘기면 없는 경로가 도면에 글자로 찍힐 수 있어서다. 슬롯을 통째로 빼면 그 칸은 빈 채로 남는다. | GenerateSheetDrawing2D_WithExcelTemplate(L1990~1993, 4개) · MfgDrawing.cs L2584~2586(3개) | ResolveDrawingAssetPath |
| `SanitizeFileName` | 1682 | 파일명에 못 쓰는 문자를 제거한다. | SanitizeStruForFileName(L1700) · Drawing2D.cs L1190·1199 · Stru.cs L946 |  |
| `SanitizeStruForFileName` | 1696 | STRU 이름을 파일명 조각으로 정규화한다. STRU 이름이 `/M02-A-ES-...` 꼴이라 그냥 넣으면 선행 `/`가 `_`로 남아서, 경로 구분자를 먼저 걷어낸다(#49). | Drawing2D.cs L1194 | SanitizeFileName |
| `ResolveDrawingStruName` | 1708 | PDF 파일명에 쓸 STRU 이름을 시트 목록에서 한 번 구한다. 표제부와 어긋나지 않게 GetStruUdaValue와 같은 값을 쓰고, 값을 가진 첫 시트를 채택한다. 하나도 없으면 빈 문자열(호출자가 대체 이름 사용). | ExportSheetsByKind(L1353) · MfgDrawing.cs L2520 | GetStruUdaValue · ResolveTagBaseNodeIndex |

## 다른 파일과의 연결

- Form1.Dimensions.cs ← 치수 계산·그리기를 전량 위임한다: ComputeViewDimensionsForMembers(캐시 준비·2D 치수) · ShowAllDimensions(뷰별 3D 치수 → shapeDrawingIds) · MarkNonRightAngles · FilterOsnapForDimAxis(현재 선언은 Dimensions.cs L2318 — 옛 문서가 이 파일에 있다고 한 것은 낡음)
- Form1.Dimensions.cs → 역방향: DrawingReferenceLocalToWorld(L2982)와 DrawingReferenceWorldToLocal(L2965)을 Dimensions.cs가 6곳에서 부른다. 치수·보조선 끝점을 로컬↔세계로 오가는 유일한 통로다
- drawingReferenceFrame 은 공유 필드가 아니라 GenerateSheetDrawing2D_WithExcelTemplate 의 지역변수(L1801). 인자로 GetDrawingSheetDimensionsFor2D → ComputeViewDimensionsForMembers → ShowAllDimensions → EstimateFitScaleForViewArea 까지 흘러간다. 참조축 활성화가 실패하면 이 변수를 null로 되돌리고 치수를 다시 계산한다(L2099) — 카메라와 치수가 다른 축을 쓰면 도면이 어긋나므로 하나가 실패하면 둘 다 되돌린다
- drawingSheetList(Form1.cs 선언): GenerateDrawingSheets 가 유일한 생산자이자 정본. Stru.cs·MfgDrawing.cs·Clash.cs 가 읽는다
- chainDimensionList(Form1.cs 선언): ApplyPreparedDimensionsToUi 가 채운다 → Dimensions·GlobalViews 가 읽는다
- xraySelectedNodeIndices(Form1.cs 선언): ApplySheetSelection 이 쓴다 → GlobalViews.cs 의 전역 뷰 버튼이 읽는다
- bomList · clashList · bodyToPartIndexMap: BOM.cs·Clash.cs 가 채운 것을 GenerateDrawingSheets 가 읽어 인접 리스트를 만든다(이 파일은 쓰지 않는다)
- fabricationNeighborClashList · fabricationNeighborPartIndices · fabricationTargetBodyIndices · fabricationTargetPartIndices: Clash.cs 가 채운다 → GetClashNeighborPartsOutsideSheet(L3028)·GetFabricationNeighborAssemblyNotes(L3050)가 읽어 제작도 ISO 점선 이웃과 이름 라벨을 만든다
- _lastCollectedNodeOsnapMap: BOM.cs 의 CollectAllOsnap(L691)이 채운다 → PrepareDrawingSheetDimensionCaches·GetDrawingSheetDimensionsFor2D·ApplySheetSelection 이 읽어 GetOsnapPoint 재조회를 없앤다
- _struNodeCache: 모델 로드 시 CollectStruList 가 채운다 → FindParentStru 가 읽는다
- Form1.MfgDrawing.cs ← 가공도(-3) 시트를 고르면 ApplySheetSelection(L741)이 ExecuteMfgDrawing 을 부른다. 비가공도 시트를 고르면 ResetMfgPreviewViewState(L675)로 가공도 미리보기의 누적 회전·참조축을 먼저 원복한다. IsAngleFromSpref(L308)·ApplyOrientationRotation(L2111)도 여기서 쓴다
- Form1.MfgDrawing.cs → 가공도가 이 파일 것을 재사용: GetTagNoValue·GetDpNoValue·GetOrCacheDrawingPaintCode·ResolveDrawingTemplatePath·AddImageSlotIfExists·ResolveDrawingStruName·Capture/Set/RestoreDrawingExportControlStates. 반대로 이 파일이 가공도 것을 쓴다: GetDefaultDrawingSaveDir(L1345)
- Form1.Clash.cs → CollectBOMInfo · PrepareDrawingSheetBomCaches · ApplyPreparedBomInfo (도면 BOM 표 8컬럼)
- Form1.GlobalViews.cs → PrepareInstallationConnectionData(설치도 접합, L167) · ExtractInstallationDimensions · ComputeInstallationDimensions. 반대로 GlobalViews 가 ApplyDrawingSheetView·CreateIsoBalloonNotes·FindParentStru·FindNearestParentAssembly 를 부른다
- Form1.Drawing2D.cs → PrepareDrawingCanvas(297,210) · BeginPdfPageAccumulation · SaveCurrentDrawingToPdf · EndPdfPageAccumulation · DiscardCurrentPdfPage · CleanupBetweenPdfPages · BuildMergedDrawingPdfPath. 반대로 Drawing2D 가 GenerateSheetDrawing2D·CleanupDrawingSheetExportCanvas·SanitizeFileName·SanitizeStruForFileName 을 부른다
- Form1.ExcelTemplate.cs → FillRevisionTable · BuildCurrentRevisionHistory · KeepBorder · SafeSubItem (표제부 개정 이력·괘선 유지)
- Form1.Stru.cs → GetSheetKindLabel. 반대로 자동 일괄 출력(ProcessSingleStruFull)이 GenerateDrawingSheets·ApplySheetSelection·GenerateSheetDrawing2D·ResolveDrawingAssetPath 를 그대로 부른다 — 수동과 자동이 같은 함수를 쓴다
- Form1.BOM.cs → GenerateDrawingSheets 를 치수 추출 사슬의 마지막(L644)에서 부른다. 반대로 BOM.cs 가 Capture/Set/RestoreDrawingExportControlStates 로 치수 추출 중 버튼을 잠근다
- Form1.cs → ShowBusyOverlay · HideBusyOverlay · ProcessCancelableUiCheckpoint · Begin/EndCancelableOperation · DiagLog

---

# Form1.ExcelTemplate.cs

**101줄 · 메서드 5개 · 기반층**

> 엑셀 템플릿 표제부를 채울 때 쓰는 작은 헬퍼 4개 — REV 이력 표 채우기와, 괘선이 지워지지 않게 하는 값 보정.

## REV 이력 표

*표제부 우하단 REV 이력 표(5행 × 6칸)를 엑셀 Input 슬롯 번호로 채운다. 제작도·조립도·설치도·가공도 템플릿이 같은 구조라 4종이 이 두 함수를 공유한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `RevRowSlotBase (static readonly int[])` | 17 | 기재행별 슬롯 시작 번호 {194, 188, 182, 176, 170}. 엑셀 44행(첫 기재행)이 194이고 리비전이 올라갈수록 위(번호 작은 쪽)로 쌓인다. 한 행 = 6칸 연속(+0 REV. / +1 DATE / +2 DESCRIPTION / +3 DRAWN / +4 CHECKED / +5 APPROVED) | FillRevisionTable(35·37·44) | - |
| `FillRevisionTable` | 31 | history(오름차순, 옛→새)를 받아 최대 5건까지 data 딕셔너리에 6칸씩 넣는다. 5건 초과분은 넣지 않고 DiagLog로 경고만. 기재행으로 쓰는 행은 6칸 전부 키를 넣어야 괘선이 산다 — 미사용 행(슬롯 170~193)은 키를 아예 안 넣어 괘선이 지워지게 둔다 | Form1.DrawingSheets.cs:1944 (제작도·조립도·설치도) · Form1.MfgDrawing.cs:187 (가공도) | KeepBorder ×6 · DiagLog |
| `BuildCurrentRevisionHistory` | 60 | 이번 출력의 REV 1건을 만든다 — Rev="0" 고정, Date=오늘(InvariantCulture로 고정해 PC 지역 달력에 안 흔들리게), Description·Drawn·Checked·Approved는 아직 입력 수단이 없어 빈 문자열(#64 Phase 2 TODO) | Form1.DrawingSheets.cs:1944 · Form1.MfgDrawing.cs:187 — 항상 FillRevisionTable의 인자로 그 자리에서 호출 | - |

## 값 보정 헬퍼

*엑셀 치환 파이프라인의 두 가지 함정을 막는 두 줄짜리 함수. 짧지만 도면 4종 전부가 부른다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `KeepBorder` | 80 | 빈 값을 공백 1칸(" ")으로 바꾼다. ImportExcelWithData는 값이 있어야 {Input}을 치환하고 RemoveEmptyTemplateBorders는 {Input}이 만든 TextBox가 있어야 괘선을 지우므로, ""를 넣으면 괘선이 사라진다 — " "여야 그 칸 괘선이 산다 | FillRevisionTable(45~50, 6회) · Form1.DrawingSheets.cs:1936·1938·1939·1940(PAINT CODE·DP NO·STRU 태그·PAINT CODE 2) · Form1.MfgDrawing.cs:178·180·181·182 | - |
| `SafeSubItem` | 88 | ListViewItem.SubItems[idx]를 범위 밖이면 예외 대신 빈 문자열로 돌려준다. 도면정보 BOM 표를 엑셀로 옮길 때 열이 모자란 행에서 죽지 않게 하는 것 | Form1.DrawingSheets.cs:1969~1975 (ITEM·MATERIAL·SIZE·Q'TY·T/W·MA·FA 7칸) · Form1.MfgDrawing.cs:132~138 (같은 7칸) | - |

## 다른 파일과의 연결

- Form1.DrawingSheets.cs:1944 `FillRevisionTable(data, BuildCurrentRevisionHistory())` → 이 파일 :31 + :60 — 제작도·조립도·설치도 표제부
- Form1.MfgDrawing.cs:187 같은 한 줄 → 가공도 표제부. 도면 4종이 REV 표 코드를 100% 공유한다
- Form1.DrawingSheets.cs:1936·1938·1939·1940 / Form1.MfgDrawing.cs:178·180·181·182 KeepBorder → 이 파일 :80 — data[166] PAINT CODE · data[168] DP NO · data[169] STRU 태그 · data[246] PAINT CODE 2 (슬롯 번호가 두 파일에 동일하게 박혀 있다)
- Form1.DrawingSheets.cs:1969~1975 / Form1.MfgDrawing.cs:132~138 SafeSubItem → 이 파일 :88 — lvDrawingBOMInfo ListView의 7개 SubItem을 엑셀 BOM 표로 옮기는 지점
- Models.cs:250 RevisionEntry → 이 파일이 유일한 사용처(5회). 다른 11개 파일은 이 타입을 안 쓴다
- Form1.ExcelTemplate.cs:37 DiagLog → Form1.cs:238 (이 파일의 유일한 로그)
- 이 파일에는 진입점(이벤트 핸들러)이 없다 — 전부 다른 파일이 부르는 순수 헬퍼다

---

# Form1.MfgDrawing.cs

**3926줄 · 메서드 72개 · 조립자층**

> 부재 하나만 화면에 남기고, 제일 긴 축이 가로가 되게 돌려 세우고, 2D로 찍은 다음, 그제서야 확정된 실측 배율로 치수와 풍선을 그린다. 미리보기(3D)와 PDF가 같은 장면 코어를 쓰고 어댑터만 다르다. 프로젝트에서 파일 수 대비 코드가 가장 무겁다.

**진입점**

- btnMfgDrawingSheet_Click (L2823) — 「가공도」 버튼. Form1.Designer.cs L515 배선. 이 파일의 유일한 버튼
- ExecuteMfgDrawing (L2326) — 도면 시트 목록에서 가공도 행을 고르면 ApplySheetSelection(DrawingSheets.cs L741)이 부른다. 3D 미리보기 어댑터
- GenerateMfgDrawingManual (L2454) — 자동 일괄 출력. Form1.Stru.cs L1046 (ProcessSingleStruFull §8) · btnMfgDrawingSheet_Click(L2859). PDF 어댑터
- RestoreAllPartsVisibility (L23) — Form1.Drawing2D.cs L648 · Form1.GlobalViews.cs L153 (가공도 격리 해제)
- CacheMfgAxisDetection (L3679) — Form1.BOM.cs L719 (CollectAllOsnap 이 Osnap 원본으로 주축 판정을 같이 캐시)
- GetMfgAxisDetection (L3691) — Form1.GlobalViews.cs L789 (설치도 길이축 판정)
- GetMfgHolesFromApi (L1455) — Form1.BOM.cs L1032
- IsPadOrPlateFromSpref (L3004) — Form1.Dimensions.cs L2498 (각도축 판정에서 판형 제외)
- IsAngleFromSpref (L3017) — Form1.DrawingSheets.cs L308
- ApplyOrientationRotation (L3220) — Form1.DrawingSheets.cs L2111 (제작도 뷰 회전)
- ResetMfgPreviewViewState (L3343) — Form1.DrawingSheets.cs L675 (비가공도 시트 선택 시 누적 회전 원복)

## ① 진입점과 PDF 페이지 조립

*버튼 → 결과 객체 → MessageBox 흐름과, 페이지 분할·BOM 스냅샷·엑셀 슬롯 구성·취소 체크포인트. GenerateMfgDrawingManual 안에는 MessageBox가 하나도 없고 결과 객체(MfgDrawingResult)로만 정보를 넘긴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnMfgDrawingSheet_Click` | 2823 | 「가공도」 클릭 → 시트 목록에서 제목이 "가공도"로 시작하는 행만 모아 → 컨트롤 잠그고 취소 구간 열고 → GenerateMfgDrawingManual 실행 → 결과 객체를 읽어 취소/성공/경고를 단일 MessageBox로 보여준다. | Designer L515 | GetDefaultDrawingSaveDir · CaptureDrawingExportControlStates/SetDrawingExportControlsEnabled/RestoreDrawingExportControlStates(DrawingSheets) · BeginCancelableOperation/EndCancelableOperation/ShowBusyOverlay/HideBusyOverlay(Form1) · GenerateMfgDrawingManual |
| `GenerateMfgDrawingManual` | 2454 | PDF 어댑터 본체. 템플릿 확인 → 뷰 상태 초기화 → BOM 표 1회 채우기(합성 시트로 CollectBOMInfo) → 스냅샷 → 페이지 5부재씩 분할 → 페이지마다 캔버스 리셋·엑셀 치환·Render·행 렌더 → 페이지를 PDF 하나에 누적 저장. 수동·자동 양쪽이 이 함수 하나를 쓴다. | btnMfgDrawingSheet_Click(L2859) · Stru.cs L1046 | ResolveDrawingTemplatePath/ResolveDrawingStruName/GetTagNoValue/GetDpNoValue/GetOrCacheDrawingPaintCode/AddImageSlotIfExists(DrawingSheets) · ResetMfgPreviewViewState · ClearMfgViewAnnotations · CollectBOMInfo(Clash) · SnapshotBomRows · SplitMfgIntoPages · BuildMfgPageData · ResetCanvasForMfgPage · EnsureViewAreasCache · RenderMfgRowToViewArea · CheckMfgCancellation · BeginPdfPageAccumulation/SaveCurrentDrawingToPdf/EndPdfPageAccumulation/DiscardCurrentPdfPage/BuildMergedDrawingPdfPath(Drawing2D) · ReleaseActiveMfgReferenceAxis · RestoreAllPartsVisibility |
| `ExecuteMfgDrawing` | 2326 | 3D 미리보기 어댑터. 누적 회전을 먼저 원복하고 코어를 부른 뒤, 미리보기 전용 후처리만 한다 — X-Ray 끄기·선택 해제·RenderMode SMOOTH·초록 실루엣·EndUpdate 뒤 FitToView·pose.ApplyZ90/ApplyR180 적용·_lastMfgViewPose 저장. 사용자 사양(2026-07-22)대로 미리보기에서는 형상 풍선을 지운다(PDF 경로는 유지). | DrawingSheets.cs L741 (ApplySheetSelection, 가공도 시트 행 선택) | ResetMfgPreviewViewState · BuildMfgSceneCore |
| `CheckMfgCancellation` | 76 | SDK 호출 사이 경계마다 불려서 진행창 문구를 갱신하고, 사용자가 취소를 눌렀으면 OperationCanceledException을 던진다. 취소 판정 함수가 null이면 아무것도 안 한다. 이 파일에서 19곳이 부른다. | RenderMfgRowToViewArea(8곳) · GenerateMfgDrawingManual(11곳) |  |
| `SplitMfgIntoPages` | 96 | 가공도 시트 목록을 페이지당 5부재(View_1~View_5)씩 MfgPage 목록으로 자른다. | GenerateMfgDrawingManual(L2588) |  |
| `SnapshotBomRows` | 121 | lvDrawingBOMInfo의 BOM 행을 8컬럼 문자열 배열 목록으로 메모리에 1회 복사한다. 페이지 루프가 live ListView에 의존하지 않게 해서 UI race를 끊는 게 목적. Row 0(요약행)도 포함한다(#67). | GenerateMfgDrawingManual(L2563) |  |
| `BuildMfgPageData` | 149 | 페이지 1개분 ImportExcelWithData용 {Input_N} 슬롯 사전을 만든다. BOM 표·도면정보는 제작도 방식 그대로 쓰고, BOM 데이터는 live가 아니라 스냅샷에서 꺼낸다. 템플릿 한도가 25행이라 넘으면 잘라내고 로그를 남긴다. | GenerateMfgDrawingManual(L2617) | FillRevisionTable/KeepBorder/SafeSubItem(ExcelTemplate) |
| `EnsureViewAreasCache` | 247 | 엑셀 템플릿의 View_1~View_5 영역을 1회만 파싱해 캐시한다. 로컬 사전 검증을 통과한 뒤에만 캐시에 대입해 잘못된 캐시가 남지 않게 한다. View 태그가 중복이면 첫 번째 위치를 쓰고 경고를 남긴다. | GenerateMfgDrawingManual(L2635) |  |
| `ResetCanvasForMfgPage` | 280 | 페이지 진입 시 2D 캔버스를 비운다. | GenerateMfgDrawingManual(L2616) |  |
| `GetDefaultDrawingSaveDir` | 289 | PDF 저장 폴더를 준다 — Application.StartupPath\Drawings. 자동·수동 공통이며 제작도 일괄 출력도 이걸 쓴다. | btnMfgDrawingSheet_Click(L2850) · DrawingSheets.cs L1345(ExportSheetsByKind) |  |
| `RestoreAllPartsVisibility` | 23 | bomList의 모든 부재를 다시 Show 한다 — 가공도 격리 해제. 출력이 끝났을 때 직전에 보던 시트 격리가 없으면 이걸로 되돌린다. | GenerateMfgDrawingManual(L2792) · Drawing2D.cs L648 · GlobalViews.cs L153 |  |

## ② 장면 코어 — 격리·축·카메라·치수 목록

*🔑 이 파일의 심장. 부재 하나만 남기고 어느 방향에서 볼지 정하고 Osnap을 모아 치수 '목록'만 쌓는다. 아직 아무것도 그리지 않는다 — 배율이 확정되기 전이라서. EA 앵글의 열린 방향·접힘 코너 판정도 여기 들어 있다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `BuildMfgSceneCore` | 1841 | 🔴 이 파일 최대 메서드. 주석으로 1~8단계가 그어져 있다 — 부재 격리(전체 숨김→이 부재만 Show) → BBox 최장축 판별 → 판형(PAD/PLATE)이면 최단축에서, 아니면 최장축이 가로로 보이는 방향으로 카메라 → ORIENTATION UDA 로컬 참조축(실패 시 화면 회전 폴백) → Osnap 수집(캐시 hit면 GetOsnapPoint 생략) → EA 앵글 열린방향·접힘코너·상하스왑 판정 → 은선 필터 → 축당 극점 선별 → 체인치수 계산 → PendingDims·PendingNotes에 '그릴 목록'만 쌓고 MfgViewPose를 돌려준다. | RenderMfgRowToViewArea(L1278, PDF) · ExecuteMfgDrawing(L2371, 미리보기) | ClearMfgViewAnnotations · IsPadOrPlateFromSpref · ParseOrientation · TryBuildMfgOrientationReferenceFrame · ActivateMfgReferenceAxis · ApplyOrientationRotation · CacheMfgAxisDetection · LogMfgAxisDetection · IsAngleFromSpref · MfgAxisUpPositive · FilterHiddenLineOsnap · FilterOsnapForDimAxis(Dimensions) · MergeCoordinates/AddChainDimensionByAxis/GetRemainingAxis/GetAxisValue/ComputePositiveOffsetByOsnapExtreme(Dimensions) · FilterMfgDimensions · MfgHeightToRight · SnapDimPointTowardDimLine · GetMfgHolesFromApi · BuildMfgPendingNotes |
| `BuildEaSecondaryScene` | 1010 | EA 앵글 부재의 2차 뷰를 만든다. 1차 참조축·측정·풍선을 지우고 2차 전용 참조축을 새로 세운 뒤, 길이·폭 체인치수를 새 pose의 PendingDims에 모은다. 코너가 슬롯 가운데를 향하도록 MirrorVertical을 정하고, 길이 치수는 위 슬롯 뷰 몫이라 스왑이면 건너뛴다. | RenderMfgRowToViewArea(L1335) | ClearMfgViewAnnotations · ActivateMfgReferenceAxis · MfgAxisUpPositive · MfgHeightToRight · SnapDimPointTowardDimLine · FilterHiddenLineOsnap · GetRemainingAxis(Dimensions) |
| `FilterMfgDimensions` | 1443 | 가공도 치수 선별 — 지금은 제작도 알고리즘(ApplySmartFiltering)을 가공도 전용 파라미터(축당 8개 / 텍스트 간격 25.0)로 부르는 한 줄짜리다. 향후 가공도 고유 규칙을 넣을 확장 지점으로 남긴 껍데기. ⚠ 상수값이 제작도와 실제로 같다. | BuildMfgSceneCore(L2214) | ApplySmartFiltering(Dimensions) |
| `FilterHiddenLineOsnap` | 3037 | 뷰 방향 기준으로 뒷면에 있는 Osnap 점을 지운다. 좌표와 BBox만 받아 도는 순수 기하 계산. | BuildEaSecondaryScene(L1116) · BuildMfgSceneCore(L2162) |  |
| `SnapDimPointTowardDimLine` | 974 | 보조선 시작점을 고친다 — 치수 끝점의 오프셋축 좌표를 같은 치수축 레벨(±tol)의 Osnap 점 중 치수선 쪽 극값으로 스냅한다. 직사각 부재는 그 극값이 외곽 모서리라 변화가 없고, 대각(beveled) 부재는 실제 대각 꼭짓점이 된다 — 보조선이 모델을 가로지르는 문제와 빈 공간에 뜨는 문제를 한 번에 없앤다. 측정값은 안 바뀐다. | BuildEaSecondaryScene(L1197·1198·1227·1228) · BuildMfgSceneCore(L2290·2291) |  |
| `MfgHeightToRight` | 911 | 세로(폭) 치수를 화면 오른쪽에 둘지 부재마다 실제 카메라 투영으로 판정한다. 고정 표를 폐기하고 일반화한 것(2026-06-30). 길이축이 카메라 우축/상축 중 어디에 더 정렬되는지로 회전 여부를 보고, 회전하면 화면-오른쪽은 상축이 된다고 본다. | DrawMfgDimsAtScale(L870) · BuildEaSecondaryScene(L1220) · BuildMfgSceneCore(L2270) |  |
| `MfgAxisUpPositive` | 947 | 3D 축(X/Y/Z)의 +방향이 캡처 화면의 '위'인지 판정한다. 카메라 세팅 후·가로화 회전 전에 부른다. ⚠ vizcore3d.View.GetCameraAxis()로 현재 카메라 상태를 읽으므로 순수 계산이 아니다 — 축 벡터를 인자로 받게 바꾸기 전에는 기하 유틸로 분리 못 한다. | DrawMfgDimsAtScale(L871) · BuildEaSecondaryScene(L1063) · BuildMfgSceneCore(L2153, 코너축 판정) |  |
| `GetMfgEaSecondaryViewDirection` | 1670 | 1차 pose를 받아 EA 2차 뷰 방향을 준다 — 최장축이 Z면 "X", 그 외는 "Z". | AssignMfgFeatureToSecondary(L1696) |  |
| `GetMfgViewDepthAxis` | 1643 | pose와 뷰 방향을 받아 그 뷰의 시선(깊이) 축 단위벡터를 준다. 참조축을 쓰는 pose면 로컬축, 아니면 월드축. 정규화 실패면 null. | AssignMfgFeatureToSecondary(L1695·1697) | TryNormalizeMfgVector |

## ③ 캡처와 실측 배율, 그리고 지연 그리기

*🔑 이 파일을 관통하는 설계 — 추정 배율로 그리면 보조선 길이가 부재·뷰마다 어긋나므로, 한 번 찍어 GetObjectSize로 실측 배율을 확정한 뒤에야 치수·풍선을 그린다. 주석이 많아도 모델 영역은 35%(MfgMinModelAreaHeightRatio) 보장한다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `RenderMfgRowToViewArea` | 1250 | 페이지의 한 행(BOM 1개)을 렌더링하는 PDF 어댑터. 코어로 장면을 만들고 → 가로화 → 캡처 → (EA면 같은 ViewArea를 위/아래 두 뷰로 쪼개 2차 장면까지) → finally에서 3D 측정·보조선·풍선을 정리한다. 회전은 캡처 후 -각도로 반드시 원복해 누적을 막는다. | GenerateMfgDrawingManual(L2659) | ClearMfgViewAnnotations · IsAngleFromSpref · BuildMfgSceneCore · GetMfgEaSharedAnnotationBudgetCanvas · ProbeAndRollLandscape · CaptureMfgSceneToViewArea · BuildEaSecondaryScene · CheckMfgCancellation |
| `CaptureMfgSceneToViewArea` | 301 | 현재 3D 장면을 DASH_LINE 렌더모드로 은선 2D 캡처하고 → GetObjectSize로 실측 → 주석 몫을 먼저 예약하되 모델 높이 35%는 보장 → RescaleObject + MoveObjectTo로 viewArea에 앉힌다. 그다음에야 치수·풍선·보조선을 2D로 옮긴다. MirrorVertical이면 배치 전 상하 미러. 실패해도 objId는 남긴다(호출자가 지운다). | RenderMfgRowToViewArea(L1307 1차 · L1346 2차) | PromoteMfgSmallPendingDimensions · GetMfgAnnotationBudgetCanvas · DrawMfgDimsAtScale · AddMfgPendingNotesAtScale |
| `ProbeAndRollLandscape` | 485 | 🔑 추측 대신 찍어서 잰다. 임시로 한 번 캡처해 objW/objH를 재고 즉시 지운 뒤, 높이가 폭보다 크면 화면축을 90° 돌려 가로로 만든다. up-vector 방식은 2D 캡처에 반영이 안 돼 폐기했고(2026-06-29) 화면축 회전만 쓴다. 적용 각도(0 또는 90)를 돌려주며 호출자가 캡처 후 -각도로 원복해야 한다. | RenderMfgRowToViewArea(L1300 1차 · L1339 2차) |  |
| `DrawMfgDimsAtScale` | 831 | 확정된 실측 newScale로 치수와 보조선을 실제로 그린다. PendingDims(offset 미적용)를 돌면서 종이 절대 오프셋을 계산해 DrawDimension을 부르고, 보조선은 파란 ShapeDrawing 라인으로 그려 ID를 pose.ShapeDrawingIds에 모은다. EA 1차/2차가 각자 캡처 직전에 부르므로 직전 뷰의 3D 측정·보조선을 먼저 비운다. | CaptureMfgSceneToViewArea(L400) | ComputeCanvasAbsoluteOffsets/DrawDimension/GetRemainingAxis(Dimensions) · GetMfgSameSideDimensionEnvelopeCanvas · MfgHeightToRight · MfgAxisUpPositive · GetMfgOrientationAxisVector |
| `AddMfgPendingNotesAtScale` | 533 | 홀·슬롯홀·EarthBoss 풍선을 확정 배율로 도면 외곽에 생성한다. 카메라 축으로 화면 가로/세로/깊이를 잡고, 치수선 최대 오프셋과 풍선 gap(6mm)·행 간격(8mm)을 모두 종이 절대값으로 공유해 부재·뷰별 편차를 없앤다. | CaptureMfgSceneToViewArea(L408) | TryNormalizeMfgVector |
| `PromoteMfgSmallPendingDimensions` | 658 | 짧은 체인 치수를 2단으로 올리는 규칙을 캡처 배율 계산 '전에' 확정한다. 치수 그리기와 주석 영역 예약이 같은 레벨 구성을 봐야 종이 여백이 맞는다. | CaptureMfgSceneToViewArea(L367) |  |
| `GetMfgSameSideDimensionEnvelopeCanvas` | 689 | 풍선을 놓는 화면 위/아래 쪽에 실제로 있는 치수선의 종이 오프셋만 돌려준다. 화면 오른쪽 치수까지 위/아래 여백에 넣던 과대 계산을 없앤 것. | GetMfgAnnotationBudgetCanvas(L772) · DrawMfgDimsAtScale(L850) | TryNormalizeMfgVector |
| `GetMfgAnnotationBudgetCanvas` | 758 | 이 뷰의 모델 슬롯에서 미리 확보할 주석 높이(종이 mm)를 계산한다. 같은 쪽 치수선·치수 문자·풍선 시작 간격·풍선 행 높이를 다 포함한다. | CaptureMfgSceneToViewArea(L368) | GetMfgSameSideDimensionEnvelopeCanvas · CalculateMfgAnnotationBudgetCanvas |
| `GetMfgEaSharedAnnotationBudgetCanvas` | 781 | EA 부재의 1차·2차 뷰 풍선 수 중 큰 쪽을 기준으로 두 뷰가 공통 예약할 주석 높이를 준다. 2차 뷰 치수는 아직 없으므로 치수 영역은 최대 3단 오프셋으로 고정해 계산한다. 풍선이 없으면 0. | RenderMfgRowToViewArea(L1284, 1차 캡처 전) | CalculateMfgAnnotationBudgetCanvas |
| `CalculateMfgAnnotationBudgetCanvas` | 809 | 풍선 개수와 같은 쪽 치수 영역 높이를 받아 치수 문자 여유 + 풍선 간격 + 글자 높이 + 행 간격×(개수−1)을 더한 종이 mm를 준다. 풍선이 0개면 0. | GetMfgAnnotationBudgetCanvas(L773) · GetMfgEaSharedAnnotationBudgetCanvas(L797) |  |

## ④ 홀·슬롯홀 형상 풍선

*SDK GetNodeHoleInfo로 홀·슬롯홀을 뽑아 관통축을 추정하고, EA 부재면 1차/2차 뷰 중 어디에 놓을지 배정한 뒤 지름·규격이 같은 것끼리 묶어 풍선 하나로 만든다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetMfgHolesFromApi` | 1455 | GetNodeHoleInfo로 홀(CIRCLE)과 슬롯홀(SLOT_HOLE)을 꺼내 목록 둘로 돌려준다. ⚠ SlotLength·Size의 의미를 SDK가 직접 안 줘서 잠정 매핑 + 진단 로그로 실측 중이라고 주석에 적혀 있다 — 슬롯홀 치수가 도면에 잘못 나갈 여지 (미확인). | BuildMfgSceneCore(L2301) · BOM.cs L1032 | TryResolveMfgHoleThroughAxis · FormatMfgVector |
| `TryResolveMfgHoleThroughAxis` | 1543 | 홀의 관통 방향을 정한다. 두께 중심점(ThicknessCenterFrom→To) 차이를 우선 쓰고, 원형 홀은 그게 없으면 CircleCenter 중 가장 먼 두 점의 차이를 쓴다. SDK XML이 AxisZ를 관통축으로 보장하지 않아 AxisX×AxisY와 평행할 때만 AxisZ로 폴백한다. 슬롯의 CircleCenter는 장축일 수 있어 관통축 추정에 안 쓴다. | GetMfgHolesFromApi(L1475) | SubtractMfgVector · TryNormalizeMfgVector · CrossMfgVector · DotMfgVector · FormatMfgVector |
| `AssignMfgFeatureToSecondary` | 1679 | 홀·슬롯홀의 관통축이 1차·2차 뷰 깊이축 중 어디에 더 나란한지로 풍선을 2차 뷰에 둘지 정한다. EA가 아니거나 축이 없으면 항상 1차. 판정 근거를 진단 로그에 남긴다. | BuildMfgPendingNotes(L1787 Hole · L1799 SlotHole) | GetMfgViewDepthAxis · GetMfgEaSecondaryViewDirection · DotMfgVector |
| `AddMfgGroupedHoleNotes` | 1719 | 홀들을 지름(소수 1자리)별로 묶어 "Ø지름" 풍선을 만든다. 2개 이상이면 "* N개"를 붙인다. 색은 녹색, 지시선은 그룹 첫 홀 중심. | BuildMfgPendingNotes(L1806 1차 · L1808 2차) |  |
| `AddMfgGroupedSlotNotes` | 1743 | 슬롯홀을 반지름·길이·깊이가 같은 것끼리 묶어 "R반지름/(폭*길이*깊이)" 풍선을 만든다. 폭은 반지름×2. 색은 보라. | BuildMfgPendingNotes(L1807 1차 · L1809 2차) |  |
| `BuildMfgPendingNotes` | 1771 | 형상별로 1·2차 뷰를 배정한 뒤 묶음 풍선을 pose.PendingNotes / SecondaryPendingNotes에 채운다. 기존 목록은 비우고 시작. 용도가 EBOS인 부재는 1차 뷰에만 "EarthBoss" 풍선을 부재 중심에 한 번 붙인다. | BuildMfgSceneCore(L2302) | AssignMfgFeatureToSecondary · AddMfgGroupedHoleNotes · AddMfgGroupedSlotNotes |
| `FormatMfgVector` | 1632 | 벡터를 소수 3자리 "(x,y,z)" 문자열로 만든다. 진단 로그 전용. null이면 "-". | GetMfgHolesFromApi(L1484~1488) · TryResolveMfgHoleThroughAxis(L1593~1596) |  |

## ⑤ UDA 조회와 부재 종류 판정

*SPREF·ORIENTATION을 부모로 최대 10단계 walk-up해서 찾고 _udaValueCache에 캐시한다. 판형(PAD/PLATE)이냐 앵글(EA)이냐가 카메라 규칙을 정반대로 가르므로 이 판정이 장면 코어의 첫 갈림길이다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `GetSprefValue` | 2944 | SPREF 값을 캐시에서 꺼내고, 없으면 조회해 캐시에 넣는다. | IsPadOrPlateFromSpref(L3006) · IsAngleFromSpref(L3019) | GetSprefValueUncached |
| `GetSprefValueUncached` | 2958 | SDK를 직접 조회해 SPREF를 찾는다. 현재 노드에 없으면 부모로 최대 10단계 올라가고, 없으면 "". SDK 예외는 모두 삼킨다. | GetSprefValue(L2948) |  |
| `IsPadOrPlateFromSpref` | 3004 | SPREF 문자열에 "PAD" 또는 "PLATE"가 있는지 본다. true면 장면 코어가 최단축에서 보게 해 평판을 정면으로 세운다. | BuildMfgSceneCore(L1869) · Dimensions.cs L2498(각도축 판정에서 판형 제외) | GetSprefValue |
| `IsAngleFromSpref` | 3017 | SPREF 왼쪽 2자리가 "EA"인지 본다(형식 `/EA100x75x10:SIZE` → `/` 제거 후 `:` 앞 두 글자). true면 열린 방향·접힘 코너 판정과 2차 뷰가 붙는다. | RenderMfgRowToViewArea(L1270) · BuildMfgSceneCore(L2012) · DrawingSheets.cs L308 | GetSprefValue |
| `GetUdaValue` | 3108 | 임의 UDA 키 값을 캐시에서 꺼내고, 없으면 조회해 캐시에 넣는다. | ParseOrientation(L3173) · TryBuildMfgOrientationReferenceFrame(L3375) · LogMfgAxisDetection(L3814) | GetUdaValueUncached |
| `GetUdaValueUncached` | 3121 | SDK를 직접 조회한다. 키 비교는 공백 제거·대문자 기준. 현재 노드에 없으면 부모로 최대 10단계, 없으면 "". | GetUdaValue(L3112) |  |
| `ParseOrientation` | 3171 | ORIENTATION UDA를 읽어 (축, 각도)로 푼다. "is N and~"면 회전 없음, "is E 45~"면 45도. N=X방향, E=Y방향. ⚠ 순수 파서가 아니다 — nodeIndex를 받아 GetUdaValue로 SDK를 먼저 조회한다. | BuildMfgSceneCore(L1904) · ApplyOrientationRotation(L3222) · LogMfgAxisDetection(L3815) | GetUdaValue |
| `ApplyOrientationRotation` | 3220 | ORIENTATION 각도만큼 카메라를 화면축으로 돌린다. 로컬 참조축을 못 세웠을 때의 폴백 경로이며, 제작도(DrawingSheets L2111)도 이걸 쓴다. | BuildMfgSceneCore(L1915·L2067) · ActivateMfgReferenceAxis(L3333, 실패 폴백) · DrawingSheets.cs L2111 | ParseOrientation |

## ⑥ 참조축 — ORIENTATION 로컬 좌표계

*ORIENTATION UDA 문자열로 직교 로컬축을 세워 SDK 참조축으로 활성화하고, 시선 자체를 그 축에 정렬한다. 실패하면 월드축 + 화면 회전으로 폴백. 참조축이 Measure.Clear에 함께 지워지므로 축 생성 전에 반드시 어노테이션을 비운다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ClearMfgViewAnnotations` | 3257 | 참조축을 먼저 해제한 뒤 3D 측정(치수)과 ShapeDrawing(보조선)을 모두 지운다. 참조축이 Measure.Clear에 딸려 지워지기 때문에 순서가 이렇다. | BuildMfgSceneCore(L1851) · BuildEaSecondaryScene(L1032) · RenderMfgRowToViewArea(L1255·L1411) · GenerateMfgDrawingManual(L2512) | ReleaseActiveMfgReferenceAxis |
| `ReleaseActiveMfgReferenceAxis` | 3268 | 활성 참조축 ID를 먼저 -1로 비운 뒤 SDK 참조축을 리셋하고 리뷰 항목을 지운다. 없으면 아무것도 안 하고, 실패는 로그만 남기고 계속 간다. | ClearMfgViewAnnotations(L3259) · ActivateMfgReferenceAxis(L3305·L3330) · ResetMfgPreviewViewState(L3359) · GenerateMfgDrawingManual finally(L2758) |  |
| `ActivateMfgReferenceAxis` | 3300 | pose의 로컬축(X·Y·원점)으로 SDK 참조축을 만들어 활성화하고 그 축 기준으로 카메라를 옮긴다. 활성화 이후 CameraDirection은 참조축 기준으로 해석된다. 실패하면 참조축 사용을 끄고 월드 카메라 + ORIENTATION 화면 회전으로 폴백. | BuildMfgSceneCore(L1910, "primary") · BuildEaSecondaryScene(L1054, "EA-secondary") | ReleaseActiveMfgReferenceAxis · ApplyOrientationRotation |
| `ResetMfgPreviewViewState` | 3343 | 🔑 직전 미리보기가 건 화면축 누적 회전을 음수로 되돌리고 참조축을 해제한다. RotateCameraByScreenAxis가 상대(누적) 회전이라 Z최장축(90°)·EA(180°) 부재를 연속 클릭하면 카메라가 점점 틀어지던 버그의 수정(2026-07-22). MoveCamera 앞에서 되돌리므로 MoveCamera가 자체 리셋해도 안전. _mfgPreviewNetRoll을 0으로 만든다. | ExecuteMfgDrawing(L2366 진입 · L2397 오류) · GenerateMfgDrawingManual(L2510) · DrawingSheets.cs L675(비가공도 시트 선택) | ReleaseActiveMfgReferenceAxis |
| `TryBuildMfgOrientationReferenceFrame` | 3369 | ORIENTATION UDA 원문에서 X·Y·Z 방향을 읽어 직교 로컬축과 부재 중심 원점을 pose에 세운다. 두 축이면 직교화하고, X나 Y 하나뿐이면 월드 up과 외적으로 보완한다. 기울기 1° 이하·UDA 없음·Z만 읽힘이면 false. 여기서는 pose만 채우고 SDK 참조축은 안 만든다. | BuildMfgSceneCore(L1907) | GetUdaValue · TryParseMfgOrientationDirection · TryNormalizeMfgVector · CrossMfgVector · SubtractMfgVector · ScaleMfgVector · DotMfgVector |
| `TryParseMfgOrientationDirection` | 3454 | "X IS N 45 E" 꼴을 정규식으로 읽어 월드 방향 단위벡터를 만든다. 각도가 있으면 1차 방위에 cos, 2차 방위에 sin을 곱해 합성한 뒤 정규화한다. 순수 문자열 파싱. | TryBuildMfgOrientationReferenceFrame(L3379~3381, X·Y·Z 3회) | TryGetMfgCardinalDirection · TryNormalizeMfgVector |
| `TryGetMfgCardinalDirection` | 3504 | 방위 문자(N/E/S/W/U/D)를 월드 단위벡터로 바꾼다. N=+X, E=+Y, U=+Z. 그 외 문자면 false. | TryParseMfgOrientationDirection(L3472·L3490) |  |
| `GetMfgOrientationAxisVector` | 3602 | pose가 참조축을 쓰는 중이면 지정 로컬축을 Vertex3D로 준다. 축 이름이 틀리거나 참조축 미사용이면 null. 치수 그리기에서 측정축·오프셋축을 로컬 기준으로 넘길 때 쓴다. | DrawMfgDimsAtScale(L882·L883) |  |

## ⑦ 주축 판정 — LINE Osnap 군집화

*부재의 LINE Osnap 방향을 5° 허용으로 군집화하고 길이 합이 가장 큰 방향을 주축으로 본다. ORIENTATION UDA 값과 비교한 진단 로그를 남기고, 설치도 길이축 판정도 이 결과를 같이 쓴다. 뒤쪽 static 6개와 중첩 타입 멤버는 이 계산 전용 유틸이다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `CacheMfgAxisDetection` | 3679 | 전체 Osnap 수집 때 이미 받은 원본으로 주축 판정을 같이 캐시한다. 반환값은 입력 목록을 그대로 돌려줘 호출자가 기존 흐름을 이어가게 한다. | BuildMfgSceneCore(L1942) · BOM.cs L719(CollectAllOsnap) | DetectMfgAxis |
| `GetMfgAxisDetection` | 3691 | 캐시된 주축 판정을 준다. 없으면 GetOsnapPoint를 돌려 판정하고 캐시에 넣는다(cacheHit로 알려준다 — miss면 느리다). | LogMfgAxisDetection(L3813) · GlobalViews.cs L789(설치도 길이축) | DetectMfgAxis |
| `DetectMfgAxis` | 3711 | LINE Osnap 방향을 5° 허용으로 군집화하고 군집별 선 길이 합이 최대인 방향을 주축으로 정한다. 단일 최장선 방향도 따로 보존해 두 기준을 실기 로그에서 비교할 수 있게 한다. | CacheMfgAxisDetection(L3683) · GetMfgAxisDetection(L3702) | NormalizeMfgAxisVector · CanonicalizeMfgAxisVector · LengthMfgAxisVector · DotMfgAxisVector · ClampMfgAxisDot · RadiansToDegrees · MfgAxisDirectionBin.Add |
| `LogMfgAxisDetection` | 3810 | 기하 주축 판정과 ORIENTATION UDA 파싱값을 비교한 "참조축판정" 진단 로그 한 줄을 남긴다. 카메라·좌표계는 안 바꾼다. ORIENTATION이 유효하게 파싱되면 그 각도를, 아니면 기하 판정을 기울어짐 근거로 쓴다. | BuildMfgSceneCore(L1989) | GetMfgAxisDetection · GetUdaValue · ParseOrientation |
| `NormalizeMfgAxisVector` | 3868 | 축 벡터를 길이 1로 정규화한다. 길이 0이면 영벡터. static 순수 함수. | DetectMfgAxis |  |
| `CanonicalizeMfgAxisVector` | 3879 | 절댓값이 가장 큰 성분이 양수가 되게 부호를 맞춘다 — 같은 선의 양쪽 방향을 하나로 취급하기 위한 정규 표현. static 순수 함수. | DetectMfgAxis |  |
| `LengthMfgAxisVector` | 3895 | 축 벡터 길이. static 순수 함수. | DetectMfgAxis · NormalizeMfgAxisVector |  |
| `DotMfgAxisVector` | 3903 | 두 축 벡터의 내적(double). static 순수 함수. | DetectMfgAxis · MfgAxisDirectionBin.Add |  |
| `ClampMfgAxisDot` | 3911 | 내적값을 −1~1로 자른다. Acos 앞에서 부동소수 오차로 NaN이 나는 걸 막는다. static 순수 함수. | DetectMfgAxis |  |
| `RadiansToDegrees` | 3919 | 라디안 → 도. static 순수 함수. | DetectMfgAxis · MfgAxisDetectionResult.IsTilted |  |
| `MfgAxisVector(생성자)` | 3245 | 중첩 struct MfgAxisVector(X·Y·Z double)의 생성자. 주축 계산 전용 값 타입. | DetectMfgAxis 및 축 유틸 전반 |  |
| `MfgAxisDirectionBin.Direction (프로퍼티)` | 3631 | 중첩 class — 길이 가중 누적 방향을 정규화해 그 군집의 대표 방향을 준다. | DetectMfgAxis · MfgAxisDirectionBin.Add |  |
| `MfgAxisDirectionBin.Add` | 3641 | 방향 단위벡터와 선 길이를 받아 길이 가중 누적에 더하고 선 개수·총 길이를 갱신한다. 기존 누적 방향과 반대면 부호를 뒤집어 같은 쪽으로 모은다. | DetectMfgAxis |  |
| `MfgAxisDetectionResult.IsTilted (프로퍼티)` | 3669 | 중첩 class — 판정된 주축이 가장 가까운 월드축에서 허용 오차(1.0°)를 넘게 벗어났는지 알려준다. | LogMfgAxisDetection · GlobalViews.cs |  |

## ⑧ 벡터 헬퍼

*VIZCore3D Vector3D용 순수 기하 함수 5개. SDK 상태를 안 읽으므로 그대로 기하 유틸로 뺄 수 있는 유일한 덩어리다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `DotMfgVector` | 3523 | 두 벡터의 내적(float). | TryResolveMfgHoleThroughAxis · AssignMfgFeatureToSecondary · TryBuildMfgOrientationReferenceFrame (총 9곳) |  |
| `CrossMfgVector` | 3533 | 두 벡터의 외적. | TryResolveMfgHoleThroughAxis · TryBuildMfgOrientationReferenceFrame (총 11곳) |  |
| `ScaleMfgVector` | 3546 | 벡터 각 성분에 배율을 곱한다. 직교화(Gram-Schmidt)에서 쓴다. | TryBuildMfgOrientationReferenceFrame(L3390·3398·3406) |  |
| `SubtractMfgVector` | 3559 | 앞 벡터에서 뒤 벡터를 뺀다. | TryResolveMfgHoleThroughAxis(L1558·1573) · TryBuildMfgOrientationReferenceFrame(L3390·3398·3406) |  |
| `TryNormalizeMfgVector` | 3572 | 길이 1로 정규화해 out으로 준다. null·NaN·무한대·길이가 거의 0이면 false. 이 파일에서 30곳 가까이 부르는 최다 호출 헬퍼. | AddMfgPendingNotesAtScale · GetMfgSameSideDimensionEnvelopeCanvas · GetMfgViewDepthAxis · TryResolveMfgHoleThroughAxis · TryBuildMfgOrientationReferenceFrame · TryParseMfgOrientationDirection |  |

## 다른 파일과의 연결

- Form1.DrawingSheets.cs ← 가공도(-3) 시트를 목록에서 고르면 ApplySheetSelection(DrawingSheets L741)이 ExecuteMfgDrawing 을 부른다. 일반·설치 시트는 치수 캐시를 쓰는데 가공도만 캐시 대상에서 빠져(PrepareDrawingSheetDimensionCaches L513) 클릭할 때마다 코어가 다시 돈다 — 가공도 클릭만 느린 이유
- Form1.DrawingSheets.cs → 표제부·경로·버튼 잠금을 제작도 것과 그대로 공유한다: GetTagNoValue(L2522) · GetDpNoValue(L2523) · ResolveDrawingStruName(L2520) · GetOrCacheDrawingPaintCode(L2593) · ResolveDrawingTemplatePath(L2465, 가공도_도면.xlsx) · AddImageSlotIfExists(L2584~2586) · CaptureDrawingExportControlStates(L2852)/SetDrawingExportControlsEnabled(L2856)/RestoreDrawingExportControlStates(L2870)
- Form1.DrawingSheets.cs → 반대 방향: 제작도가 이 파일의 ApplyOrientationRotation(L2111) · IsAngleFromSpref(L308) · GetDefaultDrawingSaveDir(L1345) · ResetMfgPreviewViewState(L675)를 쓴다
- Form1.Dimensions.cs → 치수 계산·그리기를 전량 위임한다: MergeCoordinates · AddChainDimensionByAxis · ApplySmartFiltering(FilterMfgDimensions 속) · FilterOsnapForDimAxis(축당 극점 최대 4점 × 2축) · ComputeCanvasAbsoluteOffsets · DrawDimension · GetRemainingAxis · GetAxisValue · ComputePositiveOffsetByOsnapExtreme. 반대로 Dimensions 가 IsPadOrPlateFromSpref(L2498)를 부른다
- Form1.BOM.cs → CollectAllOsnap(L691)이 CacheMfgAxisDetection(L719)을 부르며 Osnap 원본으로 주축 판정까지 같이 캐시한다. BOM.cs L1032 는 GetMfgHolesFromApi 도 부른다
- _lastCollectedNodeOsnapMap(BOM.cs 가 채움): BuildMfgSceneCore 가 읽어 캐시 hit면 GetOsnapPoint 를 건너뛴다 — 미리보기 클릭당 약 0.8초짜리 병목을 없앤 지점. 점은 복사해서 캐시 원본 오염을 막고, miss면 GetOsnapPoint 직행(안전 폴백)
- _udaValueCache(Form1.cs 선언): 이 파일이 유일한 쓰기 주체. GetSprefValue·GetUdaValue 가 SPREF·ORIENTATION 결과를 캐시한다
- _lastMfgViewPose · _mfgPreviewNetRoll · _mfgActiveReferenceAxisId(Form1.cs 선언): 미리보기 카메라 누적 회전 상쇄와 참조축 수명. ExecuteMfgDrawing 이 pose 를 _lastMfgViewPose 에 저장하고, _mfgPreviewNetRoll 은 DrawingSheets 의 ApplySheetSelection 이 ResetMfgPreviewViewState 를 통해 간접 리셋한다
- bomList(BOM.cs 가 채움): 부재 BBox·이름을 읽기만 한다. drawingSheetList 는 이 파일이 직접 안 읽고 호출자(btn/Stru)가 걸러 넘긴다
- Form1.GlobalViews.cs ← GetMfgAxisDetection(L789, 설치도 길이축 판정) · RestoreAllPartsVisibility(L153)
- Form1.Drawing2D.cs → BeginPdfPageAccumulation · SaveCurrentDrawingToPdf · EndPdfPageAccumulation · DiscardCurrentPdfPage · BuildMergedDrawingPdfPath 로 페이지를 PDF 하나에 누적한다(#119). 반대로 Drawing2D L648 이 RestoreAllPartsVisibility 를 부른다
- Form1.Clash.cs → CollectBOMInfo(L2557)를 합성 시트로 한 번 불러 BOM 표를 채운다 — 표 내용은 가공도 전체 부재로 모든 페이지에 같다
- Form1.ExcelTemplate.cs → FillRevisionTable · BuildCurrentRevisionHistory · KeepBorder · SafeSubItem (BuildMfgPageData 안, 표제부·괘선)
- Form1.Stru.cs ← GenerateMfgDrawingManual(L1046, ProcessSingleStruFull §8 자동 일괄). 수동 버튼과 완전히 같은 함수를 쓰고 결과 객체만 DiagLog 로 흘린다
- Models.cs → MfgViewPose 가 코어→어댑터 전달 통로다. PendingDims·PendingNotes·SecondaryPendingNotes·ShapeDrawingIds·ApplyZ90·ApplyR180·MirrorVertical·CornerAxis·SharedAnnotationBudgetCanvas·로컬 참조축 필드가 여기 실린다. MfgPage·MfgDrawingResult 는 아직 이 파일 안 중첩 클래스로 남아 있다(L44·L54)
- Form1.cs → DiagLog · ShowBusyOverlay · HideBusyOverlay · BeginCancelableOperation · EndCancelableOperation, 그리고 CheckMfgCancellation 이 읽는 _cancelRequested · _cancelableOperationInProgress

---

# Form1.Attribute.cs

**618줄 · 메서드 17개 · 중간층**

> 부재 정보 탭. 3D에서 부재를 클릭하면 기본정보·바운딩박스·UDA·지오메트리 4개 절을 표에 채우고, UDA를 추가·편집·삭제·CSV 일괄 가져오기 할 수 있게 한다. 다른 도면 로직과 거의 독립적이다 — 밖으로 새는 것은 selectedAttributeNodeIndex 하나뿐.

**진입점**

- Object3D_OnObject3DSelected (L19) — SDK 3D 선택 이벤트 (BOM.cs L169에서 구독)
- btnClearSelection_Click (L248) · btnExportAttributeCSV_Click (L257) — Designer.cs L1142·L1132
- btnUdaAdd/Edit/Delete/ImportCSV_Click (L364·390·443·485) — Designer.cs L1122·1112·1102·1092

## 속성 표 채우기

*3D에서 부재를 클릭하면 dgvAttributes를 4개 절로 다시 그린다. 각 Add* 메서드는 자기 절 헤더를 붙이고 행을 밀어 넣는다. 어느 하나가 실패해도 나머지 절은 계속 채워지도록 각자 try로 감쌌다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `Object3D_OnObject3DSelected` | 19 | 3D 선택 이벤트 진입점. 선택된 첫 노드의 인덱스를 selectedAttributeNodeIndex에 기억하고 라벨에 [인덱스] 이름을 찍은 뒤 표를 갱신한다. 선택이 비면 표를 초기화한다. | BOM.cs L169 (vizcore3d.Object3D.OnObject3DSelected 구독) | UpdateAttributeTable · ClearAttributeTable · SDK Object3D.FromFilter(SELECTED_TOP) |
| `UpdateAttributeTable` | 44 | 표를 비우고 기본정보 → 바운딩박스 → UDA → 지오메트리 순서로 네 번 채운다. 실패는 Debug 출력만 남기고 조용히 넘어간다. | Object3D_OnObject3DSelected · btnUdaAdd/Edit/Delete/ImportCSV_Click (편집 후 새로고침) | AddBasicNodeInfo · AddBoundingBoxInfo · AddUDAInfo · AddGeometryPropertyInfo |
| `AddBasicNodeInfo` | 71 | 「기본 정보」 절 — Node Index / Node Name / Node Type을 넣고, 이름에 /가 있으면 마지막 / 앞을 Parent Path로 하나 더 넣는다. | UpdateAttributeTable (L51) | AddSectionHeader · SDK Object3D.FromIndex |
| `AddBoundingBoxInfo` | 101 | 「바운딩 박스」 절 — Min/Max XYZ 6줄, Size XYZ 3줄, 중심점 1줄. 전부 소수 둘째 자리. | UpdateAttributeTable (L54) | AddSectionHeader · SDK Object3D.GetBoundBox |
| `AddUDAInfo` | 139 | 「사용자 정의 속성 (UDA)」 절 — 모델 전체 UDA 키를 훑으며 이 노드에 값이 있는 것만 넣는다. 키가 하나도 없으면 절 자체를 안 만든다. | UpdateAttributeTable (L57) | AddSectionHeader · SDK Object3D.UDA.Keys · UDA.FromIndex |
| `AddGeometryPropertyInfo` | 180 | 「지오메트리 속성」 절 — SDK 지오메트리 객체를 리플렉션으로 훑어 값이 있는 프로퍼티만 넣는다. 값이 하나라도 나올 때 비로소 절 헤더를 만든다. | UpdateAttributeTable (L60) | AddSectionHeader · SDK Object3D.GeometryProperty.FromIndex · System.Reflection |
| `AddSectionHeader` | 227 | 「━━ 이름 ━━」 회색 굵은 글씨 구분선 행을 한 줄 넣는다. 이 문자열이 나중에 UDA 행 판별의 표식이 된다. | Add* 4개 전부 | - |
| `ClearAttributeTable` | 238 | 표를 비우고 라벨을 안내 문구로 되돌리며 selectedAttributeNodeIndex를 -1로 만든다. | Object3D_OnObject3DSelected (선택 없을 때) · btnClearSelection_Click | - |

## UDA 편집

*부재 하나의 사용자 정의 속성을 손으로 추가·수정·삭제한다. 네 개 모두 같은 앞단 검사(부재 선택됐나 → 행 골랐나 → UDA 절 행 맞나)를 거치고, 끝나면 표를 새로 그린다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnUdaAdd_Click` | 364 | 「UDA 추가」 — 빈 다이얼로그를 띄워 Key/Value를 받고 SDK에 추가한 뒤 표를 새로 그린다. | Designer.cs L1122 | ShowUdaInputDialog · SDK Object3D.UDA.Add · UpdateAttributeTable |
| `btnUdaEdit_Click` | 390 | 「UDA 편집」 — 고른 행이 UDA 절 소속일 때만 진행한다. Key가 바뀌었으면 UpdateKey를 먼저 부르고, 그다음 Update로 값을 쓴다. | Designer.cs L1112 | IsUdaRow · ShowUdaInputDialog · SDK UDA.UpdateKey·UDA.Update · UpdateAttributeTable |
| `btnUdaDelete_Click` | 443 | 「UDA 삭제」 — UDA 절 행인지 확인하고 예/아니오를 한 번 물은 뒤 지운다. | Designer.cs L1102 | IsUdaRow · SDK UDA.Delete · UpdateAttributeTable |
| `btnUdaImportCSV_Click` | 485 | 「CSV 가져오기」 — Key,Value 두 칸짜리 CSV를 줄마다 읽어 UDA로 넣는다. 첫 줄에 key/value/속성이 들어 있으면 헤더로 보고 건너뛰고, Add가 실패하면 이미 있는 키로 보고 Update를 한 번 더 시도한다. 끝나면 성공·실패 건수와 오류 10건까지 보여준다. | Designer.cs L1092 | ParseCsvLine · SDK UDA.Add·UDA.Update · UpdateAttributeTable |
| `ShowUdaInputDialog` | 305 | Key/Value 두 칸 + 확인/취소 다이얼로그를 코드로 조립해 띄우고 (key, value) 튜플을 돌려준다. Key가 비면 경고 후 null. 편집 때는 keyReadOnly로 Key를 잠글 수 있다(현재 호출은 둘 다 false). | btnUdaAdd_Click (L372) · btnUdaEdit_Click (L414) | - |
| `IsUdaRow` | 344 | 그 행이 UDA 절에 속하는지 판별한다. 위로 거슬러 올라가 가장 가까운 「━━」 헤더를 찾아 이름이 「사용자 정의 속성 (UDA)」인지 본다 — 표에 구조 정보를 안 들고 있어서 텍스트로 되짚는 방식. | btnUdaEdit_Click (L405) · btnUdaDelete_Click (L458) | - |
| `ParseCsvLine` | 589 | CSV 한 줄을 따옴표를 고려해 쉼표로 쪼갠다. 따옴표 안의 쉼표는 구분자로 안 본다. | btnUdaImportCSV_Click (L528) | - |

## 선택 해제·CSV 내보내기

*탭 하단의 나머지 두 버튼.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `btnClearSelection_Click` | 248 | 3D 선택을 빈 목록으로 덮어 해제하고 표를 비운다. | Designer.cs L1142 | SDK Object3D.Select · ClearAttributeTable |
| `btnExportAttributeCSV_Click` | 257 | 지금 표에 보이는 No/Key/Value 전부를 UTF-8 CSV로 저장한다. 파일명은 Attributes_{노드인덱스}_{날짜시각}.csv이고 쉼표가 든 값은 따옴표로 감싼다. 구분선 행도 그대로 나간다. | Designer.cs L1132 | SaveFileDialog · StreamWriter |

## 다른 파일과의 연결

- selectedAttributeNodeIndex (Form1.cs L86) — 이 파일이 유일하게 쓰는 곳이고(3D 선택 때 채움, 해제 때 -1), Drawing2D.cs L55와 DrawingSheets.cs L478이 읽어 「도면 시트 이름을 무엇으로 할까」를 정한다. 이 파일이 밖으로 내보내는 값은 사실상 이것 하나다.
- Object3D_OnObject3DSelected — 정의는 여기, 구독은 BOM.cs L169. 이 파일 안에는 이벤트를 거는 코드가 없어 배선을 찾으려면 BOM.cs를 봐야 한다.
- UI 컨트롤 dgvAttributes·lblSelectedNode·btnUda* 6개는 Designer.cs가 만들고 Click을 배선한다. 이 파일의 다른 메서드는 전부 파일 안에서만 불린다 — 12개가 파일 내부 전용.
- SDK 의존 — Object3D.FromIndex / FromFilter / GetBoundBox / UDA.{Keys,FromIndex,Add,Update,UpdateKey,Delete} / GeometryProperty.FromIndex. 이 프로젝트에서 UDA 쓰기 API를 쓰는 곳은 여기뿐이다.
- 이 파일은 chainDimensionList·xraySelectedNodeIndices·osnapPoints 같은 도면 공유 필드를 전혀 건드리지 않는다. 4개 담당 파일 중 결합도가 가장 낮다.

---

# Models.cs

**259줄 · 메서드 13개 · 기반층**

> 13개 파일이 주고받는 데이터 모양을 한 곳에 모은 순수 데이터 클래스 10개 — 메서드는 생성자 3개뿐이고 로직이 전혀 없다.

## BOM·형상 데이터

*부재 하나와 거기 뚫린 구멍을 표현한다. Form1.BOM.cs가 채우고 가공도가 구멍 정보를 읽어 쓴다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `BOMData (class)` | 66 | 부재 1개 — Index·Name·회전각·중심 XYZ·bbox Min/Max XYZ·원반지름·용도·홀 목록·슬롯홀 목록. 18개 속성 | MfgDrawing 13 · Drawing2D 3 · BOM 2 · Dimensions 2 · DrawingSheets 1회 (Form1.cs:26 bomList의 원소) | - |
| `BOMData (생성자)` | 87 | Holes·SlotHoles 리스트와 HoleSize·SlotHoleSize 빈 문자열을 미리 만들어 둔다 — 호출부에서 null 체크를 안 하게 하려고 | new BOMData() 하는 모든 곳 | - |
| `HoleInfo (class)` | 99 | 원형 홀 — 지름·중심 XYZ·실린더 Body 인덱스·관통축 벡터·그 축을 어디서 얻었는지(ThroughAxisSource) | Form1.MfgDrawing.cs 8회 — 다른 파일은 안 씀 | - |
| `SlotHoleInfo (class)` | 113 | 슬롯홀 — 반지름·슬롯 길이·깊이·중심 XYZ·관통축. HoleInfo와 달리 길이/깊이가 따로 있다 | Form1.MfgDrawing.cs 8회 — 다른 파일은 안 씀 | - |

## 치수·투영 좌표계

*화면에 그릴 치수 한 줄과, 기울어진 어셈블리를 세계축에 맞춰 눕히는 임시 좌표계.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ChainDimensionData (class)` | 9 | 체인 치수 1줄 — 번호·축·뷰 이름·ViewDirection("X"/"Y"/"Z" 또는 "X,Y", 글로벌 X/Y/Z 버튼 필터 기준)·거리·시작/끝점·전체길이 여부. 여기에 스마트 필터용 판정 5개(Priority 1~10 · DisplayLevel · IsVisible · IsMerged · IsRequired)와 MemberIndices(선택 시 3D 강조·fit용)가 붙는다. 이 파일에서 가장 큰 클래스 | Dimensions 28 · DrawingSheets 7 · GlobalViews 4 · MfgDrawing 2회 (Form1.cs:192 chainDimensionList의 원소) | - |
| `DrawingReferenceFrame (internal sealed class)` | 214 | 제작도 4면도에서 기울어진 어셈블리를 세계축에 맞춰 투영하는 임시 로컬 좌표계 — X/Y/Z 단위벡터 + Origin + 로컬 Min/Max XYZ + 정렬 각도 + 출처 노드. 이 파일에서 유일한 internal(외부 어셈블리에 안 보임) | DrawingSheets 8 · Dimensions 4회 | - |

## 간섭·접합 데이터

*간섭검사가 뱉는 부재 쌍과, 설치도에서 선택 STRU가 바깥과 닿는 지점.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `ClashData (class)` | 128 | 간섭 부재 쌍 — Index1/2·Name1/2·XYZ 값·HotPoint 유무. 8개 속성으로 가장 단순 | Drawing2D 3 · Clash 2 · Dimensions 2 · DrawingSheets 1 · GlobalViews 1회 (Form1.cs:31 clashList와 :39 fabricationNeighborClashList의 원소) | - |
| `InstallationConnectionData (class)` | 146 | 설치도에서 선택 STRU와 외부 Part의 실제 접합 영역 — 대상/연결 Part·Body·Assembly 인덱스와 이름, A/B/C 라벨(같은 Body 쌍은 라벨 공유), 근접 폴백 여부, ContactPoints. ContactPoints는 접합선 시작/끝점이고 접합선이 없으면 HotPoint 1개만 담는다. 화면 치수점이 아니라 접합측 모서리를 고르는 내부 판정 자료다 | GlobalViews 9 · DrawingSheets 1회 | - |
| `InstallationConnectionData (생성자)` | 160 | ContactPoints 리스트를 미리 만든다 | new InstallationConnectionData() 하는 곳 | - |

## 도면 시트·표제부 데이터

*도면 한 장이 들고 있는 모든 것. 이 파일에서 가장 넓게 쓰이는 타입 묶음이고, 시트 단위 캐시 플래그가 붙어 있어 실행 흐름을 이해하는 핵심이다.*

| 메서드 | 줄 | 역할 | 불림 | 부름 |
|---|---|---|---|---|
| `DrawingSheetData (class)` | 169 | 도면 시트 1장 — 시트번호·기준부재 이름/인덱스·부재 인덱스/이름 목록·가공도 번호. PaintCode/PaintCode2는 같은 STRU의 모든 도면이 공유하는 PNT 계열 UDA로, null=아직 조회 안 함 / ""=조회했는데 값 없음을 구분한다(캐시 판정 기준, 조회 여부는 PaintCode로만 판단). PreparedDimensions+DimensionsPrepared / PreparedBomRows+BomPrepared는 시트별 사전 계산 캐시 쌍. InstallationContextIndices는 설치도 점선용 외부 부재로 #63부터 접합 Part 하나가 아니라 그 Part가 속한 STRU 전체의 BODY가 들어가며(못 찾으면 그 Part), 배율·화면 맞춤 기준에는 쓰지 않는다 | DrawingSheets 39 · MfgDrawing 11 · Clash 6 · Stru 6 · GlobalViews 4 · Drawing2D 2 · Dimensions 1회 — 7개 파일이 쓰는 최광역 모델 (Form1.cs:111 drawingSheetList의 원소) | - |
| `DrawingSheetData (생성자)` | 199 | 리스트 6개(MemberIndices·MemberNames·PreparedDimensions·PreparedBomRows·InstallationContextIndices·InstallationConnections)를 전부 미리 만든다 | Form1.DrawingSheets.cs가 시트 4종을 만들 때(:34·:117·:168·:182) | - |
| `DrawingBomRowData (class)` | 234 | 도면정보 탭 BOM 한 행의 사전 준비 데이터 — No·Item·Material·Size·Quantity·TotalWeight·Ma·Fa 8칸 전부 string. ListViewItem 자체를 안 들고 있어 여러 시트가 안전하게 재사용한다 | Form1.Clash.cs 5회 (DrawingSheetData.PreparedBomRows의 원소) | - |
| `RevisionEntry (class)` | 250 | 표제부 REV 이력 표 1행 — Rev·Date·Description·Drawn·Checked·Approved 6칸. Phase 1은 첫 기재행 1건만 쓴다 | Form1.ExcelTemplate.cs 5회 — 유일한 사용처 | - |

## 다른 파일과의 연결

- Models.cs:169 DrawingSheetData → 7개 파일이 씀 (DrawingSheets 39 · MfgDrawing 11 · Clash 6 · Stru 6 · GlobalViews 4 · Drawing2D 2 · Dimensions 1회). Form1.cs:111 drawingSheetList에 담겨 파일 사이를 건너다닌다
- Models.cs:9 ChainDimensionData → Form1.cs:192 chainDimensionList의 원소. Form1.Dimensions.cs(28회)가 만들고 DrawingSheets·GlobalViews·MfgDrawing이 읽는다
- Models.cs:66 BOMData → Form1.cs:26 bomList의 원소. Form1.BOM.cs:952가 Add하고 Form1.MfgDrawing.cs(13회)가 가장 많이 읽는다
- Models.cs:99 HoleInfo · :113 SlotHoleInfo → Form1.MfgDrawing.cs 전용 (각 8회). 가공도만 구멍을 다룬다
- Models.cs:146 InstallationConnectionData → Form1.GlobalViews.cs 전용에 가깝다(9회, DrawingSheets 1회). 설치도 접합 표시 경로
- Models.cs:234 DrawingBomRowData → Form1.Clash.cs 5회만. DrawingSheetData.PreparedBomRows를 채우는 쪽이 Clash.cs다
- Models.cs:250 RevisionEntry → Form1.ExcelTemplate.cs 5회만
- Models.cs:214 DrawingReferenceFrame → Form1.DrawingSheets.cs 8회 · Form1.Dimensions.cs 4회. 제작도 4면도 투영 경로에서만 산다
- Form1.cs:47 BodyBoundsData는 Models.cs가 아니라 Form1.cs 안의 private 중첩 struct다 — 데이터 모델이 두 곳에 나뉘어 있는 예외
- MfgViewPose(Form1.cs:158 _lastMfgViewPose의 타입)는 Models.cs가 아니라 별도 파일 A2Z/Models/MfgViewPose.cs에 있다 (MfgDrawing 21회 사용)

---

