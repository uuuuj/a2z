# 디버깅 학습 기록

> `Form1.Stru.cs:514` `btnExtractDrawingList_Click` 부터 디버거로 한 줄씩 따라가며 공부한 것.
> **셋으로 나눠 쌓는다** — ① 공부한 것(시간순) ② 변수 사전 ③ 메서드 사전
>
> 근거는 `파일:줄` 로 붙인다. 코드가 바뀌면 줄번호가 밀리므로 정리할 때 재검증한다.

---

# ① 공부한 것

## 2026-08-31

### 이벤트 핸들러의 매개변수 — `object sender` · `EventArgs e`

```csharp
private void btnExtractDrawingList_Click(object sender, EventArgs e)
//                                       ──┬──  ──┬──  ────┬───  ┬
//                                        타입    이름     타입   이름
```

- **`sender`** = 눌린 **버튼 객체 그 자체**. `object` 는 타입 이름일 뿐
- **`e`** = 그 이벤트에 딸려 온 정보
- 버튼 객체는 **창이 만들어질 때 이미 생성**돼 있다 (`Form1.Designer.cs:479~483`).
  누르는 순간 새로 만들어지는 게 아니라 **있던 객체를 넘겨준다**
- 객체는 복사되지 않고 **자리(참조)만 넘어간다** → `sender.Enabled` 를 바꾸면 원본 버튼이 바뀐다

**"보낸다"의 뜻** — 우편이 아니라 **함수를 부르며 인자로 건네는 것**.
우리가 `ProcessSingleStruFull(stru, saveDir)` 하는 것과 같고, **부르는 주체가 WinForms 일 뿐**이다.

**왜 `sender` 를 주나** — 「눌렸다」는 함수가 불린 사실 자체로 전달된다.
`sender` 는 **「어느 것이 눌렸나」**에 답하는 값이다. 버튼 여러 개를 한 함수에 걸었을 때 필요하다.

🔴 **실측 — 이 코드베이스는 전부 1:1 배선이다.** 한 핸들러가 여러 컨트롤에 걸린 경우 0건.
그래서 **`sender` 는 선언 줄 말고 사용 0건.** 핸들러 53개가 전부 받기만 한다.

### `Click` 의 `e` 는 빈 객체가 아니다

디버거로 보면 `e` 에 `X=92 Y=14 Button=Left` 가 들어 있다.
선언은 `EventArgs` 인데 **실제로 들어온 물건은 `MouseEventArgs`** — 자식 타입이라 그 자리에 들어간다.

- 좌표 기준은 화면이 아니라 **그 버튼의 왼쪽 위(0,0)**.
  `btnExtractDrawingList` 는 `120 × 28` (`Designer.cs:481`) 이라 `(92,14)` 는 버튼 안 오른쪽 3/4 지점
- **키보드로 누르면 다르다** — Tab 으로 포커스 후 Space/Enter 면 `e` 는 정보 없는 `EventArgs`
- 그래서 `(MouseEventArgs)e` 로 캐스팅하면 **키보드 입력에서 예외**가 난다.
  쓰려면 `e as MouseEventArgs` + null 검사

| 이벤트 | `e` 타입 | 들어 있는 것 | 이 코드의 사용처 |
|---|---|---|---|
| `Click` | `MouseEventArgs` 또는 `EventArgs` | 좌표·버튼 / 없음 | 안 씀 |
| `ItemCheck` | `ItemCheckEventArgs` | `e.Index` · `e.NewValue` | `Stru.cs:388` · `393` |
| `OnClashTestFinished` | `ClashEventArgs` | `e.ID` | `Clash.cs:1165` |
| `OnOsnapPickingItem` | `OsnapPickingItemEventArgs` | `e.Point` | `Drawing2D.cs:708` |

### 속성(property) 과 getter

**겉은 변수인데 실제로는 함수가 도는 것.** `MfgDrawing.cs:3634~3643` 에 필드와 속성이 나란히 있다.

```csharp
public double WeightedX;      // 필드 — 값이 든 상자
public double TotalLength;    // 필드

public MfgAxisVector Direction                       // 속성 — 읽는 순간 계산이 돈다
{
    get { return NormalizeMfgAxisVector(new MfgAxisVector(WeightedX, WeightedY, WeightedZ)); }
}
```

- **getter** = 읽을 때 도는 함수 (`get { }`) · **setter** = 넣을 때 도는 함수 (`set { }`)
- `Models.cs` 의 `{ get; set; }` 는 **자동 속성** — 컴파일러가 상자와 함수를 알아서 만든다. 사실상 필드
- **괄호가 구분점** — `()` 가 붙으면 메서드. 안 붙으면 필드 아니면 속성(선언을 봐야 구분)
- ⚠ **속성은 읽기만 해도 코드가 돈다.** 조사식에 넣으면 그 자리에서 실행된다
  (이 코드는 `Models.cs` 107개가 전부 자동 속성이고, 계산이 든 건 `MfgDrawing.cs` 3 · `DrawingSheets.cs` 5)

### SDK 담당자(매니저) 구조

```
vizcore3d  .  Model       .  IsOpen()
   필드         속성            ModelManager 의 메서드
```

**속성이 매니저 객체를 돌려주고, 그 객체가 메서드를 갖는다.** 세 칸이 각각 다른 물건이다.

| 속성 | 돌려주는 것 | 사용 횟수 | 무슨 일 |
|---|---|---|---|
| `.View` | View Manager | **168** | 카메라 · 시점 · 화면 |
| `.Object3D` | Object3D Manager | **145** | 부재 보이기/숨기기 · 선택 · 색 |
| `.Drawing2D` | Drawing2D Manager | 114 | 2D 도면 · PDF |
| `.Review` | Review Manager | 77 | 치수 · 풍선 |
| `.ShapeDrawing` | Shape Drawing Manager | 22 | 보조선 |
| `.Clash` | Clash Manager | 20 | 간섭검사 |
| `.Model` | Model Manager | 17 | 파일 열기 · 닫기 |
| `.GeometryUtility` | Geometry Utility Manager | 6 | Osnap 등 기하 |

`vizcore3d.Review.Measure.Clear()` 처럼 **매니저 밑에 또 매니저**가 온다.

### 디버거 사용법 — 이 프로젝트 기준

| 키 | 언제 |
|---|---|
| **F10** 프로시저 단위 | **기본.** 거의 항상 |
| **F11** 한 단계씩 | 줄에 **우리 함수**가 있을 때만 |
| **Shift+F11** 나가기 | 잘못 들어갔을 때 |

🔴 **SDK 안으로는 못 들어간다** — `lib/` 에 **PDB(디버그 심볼)가 없다.** DLL만 있다.
`vizcore3d.무엇()` 은 F11 을 눌러도 결과만 돌아온다.
→ 본문 줄의 74% 가 SDK 를 직접 만지므로, **코드의 3/4 은 결과만 보고 판단**해야 한다.

**「속성 및 연산자를 자동으로 건너뛰었습니다」 알림** — 오류가 아니다.
F11 이 속성 getter 로 들어가려다 VS 기본 설정(「속성 및 연산자 건너뛰기」)에 막힌 것.
알림은 「아니요」로 꺼도 되고, **설정 자체는 켜둔 채로 두는 게 낫다** (끄면 getter 마다 다 들어간다).

### 버튼 여러 개를 한 함수로 묶는 방법 — 이 코드는 이미 하고 있다

`sender` 로 분기하는 대신 **「얇은 핸들러 + 공용 함수 + enum」** 을 쓴다.

```csharp
// Form1.DrawingSheets.cs:1261 · 1269 · 1277 — 각각 4줄짜리 껍데기
private void btnExportFabricationSheets_Click(object sender, EventArgs e)
{ ExportSheetsByKind(DrawingSheetExportKind.Fabrication); }
private void btnExportAssemblySheets_Click(object sender, EventArgs e)
{ ExportSheetsByKind(DrawingSheetExportKind.Assembly); }
private void btnExportInstallationSheets_Click(object sender, EventArgs e)
{ ExportSheetsByKind(DrawingSheetExportKind.Installation); }

private void ExportSheetsByKind(DrawingSheetExportKind exportKind)   // :1286 — 진짜 일
```

| | `sender` 로 분기 | 지금 방식 (enum 인자) |
|---|---|---|
| 구분 기준 | 버튼 이름 **문자열** | **enum** |
| 오타·이름 변경 | 조용히 깨짐 (빌드는 통과) | **컴파일 오류** |
| 코드에서 직접 호출 | 가짜 `sender` 를 만들어야 함 | 그냥 부르면 됨 |
| 디버거 | 이름을 봐야 분기를 앎 | **인자만 보면 끝** |

세 번째가 실제 값을 낸다 — **일괄 출력이 버튼을 안 거치고 같은 기능을 쓴다.**
`sheetLabel.StartsWith("가공도")`(`Stru.cs:954`) 가 위험한 이유와 같은 얘기다.

**핸들러 크기 실측** — 왜 다섯을 하나로 못 묶나

| 핸들러 | 크기 | |
|---|---|---|
| `btnExportFabricationSheets_Click` | **4줄** | 껍데기 — 이미 통합됨 |
| `btnExportAssemblySheets_Click` | **4줄** | 껍데기 |
| `btnExportInstallationSheets_Click` | **4줄** | 껍데기 |
| `btnGenerateSheet2D_Click` | 55줄 | |
| `btnExportSheet2DPDF_Click` | 37줄 | |
| `btnMfgDrawingSheet_Click` (가공도) | **126줄** | 경로가 다름 — 「찍고 나서 치수」 |
| `btnExtractDrawingList_Click` (일괄) | **261줄** | 층이 다름 — 남을 *부르는* 쪽 |

> 🔑 **리팩토링 M2 의 본보기.** 「제작·조립·설치는 이미 이렇게 합쳐져 있다.
> 가시성 격리 10곳도 같은 방식으로 합치면 된다」 — 이 코드 안의 좋은 예라 반박이 어렵다.

### 재진입 가드 — 왜 버튼을 꺼도 플래그가 또 필요한가

`_p2aInProgress` 는 **「지금 일괄 출력이 도는 중인가」를 적어둔 쪽지 한 장**이다. 세 지점이 한 짝.

```
53   private bool _p2aInProgress = false;   선언 (프로그램 시작 = 안 돎)
517  if (_p2aInProgress) return;            읽기 — 이미 돌면 나간다
568  _p2aInProgress = true;                 쓰기 — 문을 잠근다
745  _p2aInProgress = false;                finally 안 — 문을 연다
```

**517 을 통과했다는 건 이미 `false` 였다는 뜻**이라, 568 에서 `true` 로 바꿔도 자기 자신은 안 걸린다.
이미 지나온 문이다. `true` 는 「멈춤」이 아니라 **「다음 사람이 못 들어옴」**이다.

**570 에서 버튼을 껐는데도 쪽지가 필요한 이유** — *이미 큐에 들어간 클릭* 때문이다.

```
09:00:00.0  클릭 → 핸들러 시작
09:00:00.1  사용자가 한 번 더 클릭        → 메시지 큐에 쌓임
09:00:00.2  570: 버튼 Enabled = false     ← 늦었다. 큐에 이미 하나 있다
09:00:05.0  669: Application.DoEvents()   → 큐에 있던 클릭이 지금 처리됨
                 → 핸들러 재진입 → 517 이 잡아낸다  ✋
```

버튼 끄기는 **앞으로의 클릭**을, 플래그는 **이미 줄 서 있는 클릭**을 막는다.
`Application.DoEvents()` 를 20곳에서 쓰는 이 코드에선 플래그가 없으면 뚫린다.

### 취소는 즉시 안 된다 — 체크포인트 방식

이 프로그램은 한 줄기로 돈다. 도면을 뽑는 동안 다른 일을 할 여유가 없어
**「누르면 바로 멈춤」을 만들 수가 없다.** 그래서 이렇게 한다.

| 순서 | 무슨 일 | 위치 |
|---|---|---|
| ① | [취소] 클릭 → **표시만 남긴다.** 아무것도 안 멈춤 | `Form1.cs:367` |
| ② | 본 작업은 계속 돈다. 곳곳에 **체크포인트**가 박혀 있다 | `Stru.cs` 에만 17개 (전체 21개) |
| ③ | 체크포인트에 닿으면 확인 → 걸려 있으면 **예외를 던진다** | `Form1.cs:461` |
| ④ | 던진 예외를 `catch` 가 받아 정리 | `Stru.cs:676` |

```
14:01:15  사용자가 [취소] 클릭
             → _cancelRequested = true 만 세우고 끝
             → 버튼이 "취소 요청됨" 으로 바뀌고 비활성화 (390)
             → 화면 문구: "현재 SDK 호출이 끝나는 즉시 안전하게 중단합니다" (392)
14:01:18  ★ 돌던 SDK 호출이 끝남
14:01:18  다음 체크포인트 도달 → 예외 → 676 에서 받음 → 중단
```

**누른 순간과 실제로 멈추는 순간 사이에 시차가 있다.** 그래서 저 안내 문구를 띄운다.
예외에 체크포인트 이름을 담아 던지므로(`throw new OperationCanceledException(checkpoint)`)
**로그를 보면 어디서 멈췄는지 알 수 있다** — `ex.Message` 가 곧 지점 이름.

### 568 과 569 는 다른 일이다

둘 다 「시작할 때 켜는 플래그」라 비슷해 보이지만 목적이 반대다.

| | `_p2aInProgress` (568) | `_cancelableOperationInProgress` (569 안) |
|---|---|---|
| 막는 것 | **남이 들어오는 것** | 아무것도 안 막음 |
| 켜면 생기는 일 | 517 에서 재진입 차단 | **[취소] 버튼이 화면에 뜸** |
| 쓰는 곳 | 일괄 출력 **1곳** | 일괄 출력·주치수·시트·가공도 **4곳 공용** |

`_p2aInProgress` 가 **「들어오지 마」**라면 `BeginCancelableOperation()` 은 **「나가고 싶으면 말해」**다.

---

# ② 변수 사전

## 공유 필드 (여러 파일이 함께 쓰는 것)

| 이름 | 선언 | 타입 | 무엇이 담기나 |
|---|---|---|---|
| `vizcore3d` | `Form1.cs:21` | `VIZCore3DControl` | **SDK 본체.** 모든 3D 작업의 창구 |
| `clbStruList` | `Designer.cs:1480` | `CheckedListBox` | **화면 좌측 STRU 목록.** 체크박스 달린 리스트박스 (437×186).<br>`Items` 전체 · `CheckedItems` 체크된 **글자** · `CheckedIndices` 체크된 **번호**<br>527 은 개수만 세니 `CheckedItems`, 535 는 캐시에서 꺼내야 해 `CheckedIndices` |
| `_p2aInProgress` | `Stru.cs:53` | `bool` | **일괄 출력이 도는 중인가 — 재진입 가드.** `_` 는 내부용 표시(관례), `P2a` 는 개발 단계 코드(T-064 P2a), `InProgress` 는 진행 중.<br>`517` 검사 → `568` 켬 → `745` 끔(finally). 버튼 비활성화만으론 `DoEvents` 로 뚫려서 이게 필요 |
| `_cancelRequested` | `Form1.cs:132` | `bool` | **사용자가 [취소]를 눌렀는가.** 누르면 이것만 `true` 가 되고 아무것도 안 멈춘다.<br>다음 체크포인트가 읽고 예외를 던진다 |
| `_cancelableOperationInProgress` | `Form1.cs:131` | `bool` | **지금 취소를 받을 수 있는 구간인가.** 켜져 있어야 [취소] 버튼이 화면에 보인다(388).<br>일괄 출력·주치수·시트·가공도 **4곳 공용** |
| `_lastCancellationCheckpoint` | `Form1.cs:133` | `string` | **마지막으로 로그를 남긴 체크포인트 이름.** 같은 지점이 반복 기록되는 것만 막는 용도(449) |
| `_struNodeCache` | `Stru.cs:42` | `List<Node>` | **화면 STRU 목록과 같은 순서**의 실제 노드.<br>목록은 글자만 갖고 있어 번호를 열쇠로 여기서 꺼낸다 |
| `drawingSheetList` | `Form1.cs:111` | `List<DrawingSheetData>` | 만들어진 도면 시트들 |
| `chainDimensionList` | `Form1.cs:192` | `List<ChainDimensionData>` | 뽑아낸 체인 치수들 |
| `xraySelectedNodeIndices` | `Form1.cs:76` | `List<int>` | 지금 작업 대상인 부재 번호들 |
| `_lastCollectedNodeOsnapMap` | `Form1.cs:141` | `Dictionary<int, List<…>>` | 부재별로 모아둔 Osnap 좌표 캐시 |
| `_udaValueCache` | `Form1.cs:150` | `Dictionary<(int,string), string>` | 부재 속성(SPREF·ORIENTATION) 캐시 |

## 이 함수 안에서만 쓰는 지역 변수

| 이름 | 줄 | 무엇이 담기나 |
|---|---|---|
| `checkedStrus` | `534` | **체크된 STRU 노드 목록.** 화면 순서 그대로. 이번 작업의 대상 전부 |
| `saveDir` | `545` | PDF 저장 폴더. `Application.StartupPath` + `Drawings`<br>= 실행 중인 exe 폴더 밑 (디버그면 `bin\Debug\Drawings`) |
| `ret` | `562` | 확인 팝업에서 사용자가 누른 버튼 (`DialogResult.Yes` 등) |
| `logoPath` | `633` | 도면 표제부에 넣을 회사 로고 파일 경로. 없으면 그 칸이 빈다 |
| `successCount` | `649` | 성공한 STRU 수 |
| `failCount` | `649` | 실패한 STRU 수 |
| `errors` | `650` | 실패 사유 목록 (`STRU이름: 메시지`) |
| `totalPdfCount` | `651` | **누적 PDF 장수.** `ProcessSingleStruFull` 이 돌려준 값을 더해 간다 |
| `cancelled` | `652` | 중간에 취소됐는가 |
| `s` | `659` | 루프 카운터 (0부터) |
| `stru` | `667` | **이번 회차에 처리할 STRU 노드 하나** |
| `allBodies` | `585` | 모델 전체의 BODY 노드 목록 (화면 초기화용) |

---

# ③ 메서드 사전

## 우리 코드

| 이름 | 정의 | 무엇을 하나 | 들어가 볼까 |
|---|---|---|---|
| `DiagLog` | `Form1.cs:238` | 진단 로그 한 줄 남기기 | ✗ |
| `BeginCancelableOperation` | `Form1.cs:420` | **취소 버튼을 켜는 스위치.** 본문 3줄 — 지난 취소 기록 지움 · 체크포인트 기억 지움 · 취소 가능 구간 켬.<br>첫 줄이 없으면 지난번 취소 흔적 때문에 새 작업이 시작하자마자 취소된다 | ✗ (읽음) |
| `EndCancelableOperation` | `Form1.cs:431` | **`Begin` 의 짝.** 세 필드를 지우고 오버레이에서 [취소] 버튼을 거둠. `Stru.cs:754` finally 안 | ✗ (읽음) |
| `ThrowIfCancellationRequested` | `Form1.cs:461` | **체크포인트.** 취소가 걸렸으면 `OperationCanceledException` 을 던지고, 아니면 그냥 지나감.<br>이름을 예외에 담아 던져서 로그로 지점을 알 수 있다 | ✗ (읽음) |
| `UpdateBusyOverlayContents` | `Form1.cs:379` | 오버레이 크기·문구·[취소] 버튼 표시를 현재 플래그에 맞춰 다시 그림 | ✗ |
| `ShowBusyOverlay` | `Form1.cs:315` | 진행창을 띄우고 문구를 갱신 | ✗ |
| `IsCancellationRequested` | `Form1.cs:444` | 취소를 눌렀는지 확인 (`bool` 반환) | ✗ |
| `ClearCanceledOperationArtifacts` | `BOM.cs:514` | 취소됐을 때 만들다 만 것들을 치움 | ✗ |
| `ResolveDrawingAssetPath` | `DrawingSheets.cs:3474` | 도면용 자원(로고 등) 파일 경로를 찾음 | ✗ |
| **`ProcessSingleStruFull`** | **`Stru.cs:790`** | **STRU 하나의 도면 4종을 전부 뽑는다. 여기가 본진** | **✅ 다음** |

## SDK (들어갈 수 없음 — PDB 없음)

| 호출 | 무엇을 시키나 |
|---|---|
| `vizcore3d.Model.IsOpen()` | 모델이 열려 있나 (`bool`) |
| `vizcore3d.BeginUpdate()` / `EndUpdate()` | 3D 화면 그리기 **중지 / 재개**. 여러 번 바꿀 때 깜빡임·느려짐 방지 |
| `vizcore3d.Object3D.GetPartialNode(a, p, b)` | 노드 목록. `(어셈블리, 파트, 바디)` 중 담을 것을 `true` 로.<br>**Partial = 정보를 일부만 채워서** 준다는 뜻 (인덱스·이름·유형만) |
| `vizcore3d.Object3D.Show(목록, 보임)` | 그 노드들을 보이게/숨기게 |
| `vizcore3d.Object3D.FromFilter(필터, 경로포함)` | 조건에 맞는 노드 목록. `ALL_INCLUDE_BODY` = 전체(BODY 포함) |
| `vizcore3d.Drawing2D.Object2D.DeleteAllObjectBy2DView()` | 2D 뷰의 객체 전부 삭제 |
| `vizcore3d.Review.Note.Clear()` / `Measure.Clear()` | 3D 풍선 / 치수 지우기 |
| `vizcore3d.ShapeDrawing.Clear()` | 보조선 지우기 |
| `vizcore3d.Drawing2D.Template.Set2DViewTemplateMark(a, b)` | 도면 양식에 로고 설정 |

## .NET 기본

| 이름 | 무엇을 하나 |
|---|---|
| `Path.Combine(a, b)` | 경로를 안전하게 이음 (OS별 구분자 처리) |
| `Application.StartupPath` | 실행 중인 exe 가 있는 폴더 |
| `Directory.Exists` / `CreateDirectory` | 폴더 있나 확인 / 만들기 |
| `MessageBox.Show(…)` | 팝업. **누른 버튼을 돌려준다** |
| `Application.DoEvents()` | **밀린 화면 갱신·클릭을 지금 처리.**<br>⚠ 이 사이에 사용자가 버튼을 또 누를 수 있다 |
| `GC.Collect()` | 안 쓰는 메모리를 지금 정리하라고 요청 |
| `Thread.Sleep(100)` | 0.1초 멈춤 |
