# 디버깅 문답 로그

> 코드를 따라가며 알아낸 것을 쌓아두는 곳. 나중에 주제별로 정리해
> [[디버거로 따라가기 — 도면 일괄 출력]] 으로 옮긴다.

## 무엇을 남기나

| ✅ 남긴다 | ✗ 안 남긴다 |
|---|---|
| **변수에 뭐가 담기나** — 실제 값·형태 | C# 문법 (`object` 가 뭔지 · `var` 가 뭔지) |
| **메서드가 뭘 하나** — 무엇을 받아 무엇을 만드나 | WinForms 일반 규칙 (`sender`·`e` 의 의미) |
| **어디로 이어지나** — 이 값이 다음에 어디서 쓰이나 | 언어·프레임워크 공통 지식 |
| **왜 이렇게 돼 있나** — 주석·커밋에 남은 사연 | |
| **실측** — 몇 건인지 세어본 것 | |

> 문법은 [[0 — 용어와 화면 읽는 법]] 에 이미 있거나 거기로 간다.
> 여기는 **이 프로젝트에 대해서만** 쌓는다.

**기록 규칙** — 답에는 **근거(`파일:줄`)** 를 붙인다. 추측이면 「추정」이라 표시한다.
코드가 바뀌면 줄번호가 밀리므로 정리할 때 재검증한다.

---

## 2026-08-31 · `btnExtractDrawingList_Click` 따라가기

시작 지점 `Form1.Stru.cs:514` · 디버거 F10 한 줄씩

### 실측 메모

- **`sender` 는 이 코드베이스에서 한 번도 안 쓰인다** — 선언 줄 말고 0건.
  핸들러 53개가 전부 받기만 한다. (`e` 는 `ItemCheckEventArgs` 등 정보가 있는 이벤트에서만 쓴다:
  `Stru.cs:388·393` · `Clash.cs:1165` · `Drawing2D.cs:708`)

### SDK 담당자(매니저) 구조 — `vizcore3d.` 다음 한 칸

`vizcore3d` 하나에 매니저가 여럿 달려 있다. **속성이 매니저 객체를 돌려주고, 그 객체가 메서드를 갖는다.**

```
vizcore3d  .  Model       .  IsOpen()
   필드         속성            ModelManager 의 메서드
```

| 속성 | 돌려주는 것 | 이 코드에서 쓴 횟수 | 무슨 일 |
|---|---|---|---|
| `.View` | View Manager | **168** | 카메라 · 시점 · 화면 |
| `.Object3D` | Object3D Manager | **145** | 부재 보이기/숨기기 · 선택 · 색 |
| `.Drawing2D` | Drawing2D Manager | 114 | 2D 도면 · PDF |
| `.Review` | Review Manager | 77 | 치수 · 풍선 같은 검토 표시 |
| `.ShapeDrawing` | Shape Drawing Manager | 22 | 보조선 등 |
| `.Clash` | Clash Manager | 20 | 간섭검사 |
| `.Model` | Model Manager | 17 | 파일 열기 · 닫기 |
| `.GeometryUtility` | Geometry Utility Manager | 6 | Osnap 등 기하 |

`vizcore3d.Review.Measure.Clear()` 처럼 **매니저 밑에 또 매니저**가 오기도 한다.

> 💡 디버깅 요령 — `vizcore3d.` 가 나오면 F10 으로 넘기고
> **「무슨 담당자에게 무엇을 시켰나」**만 적는다. `lib/` 에 PDB 가 없어 안으로 못 들어간다.

<!-- 여기부터 -->
