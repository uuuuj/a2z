# A2Z-HYI 프로젝트 — 작업 규칙

> 📌 **이 프로젝트의 작업 규칙은 [`CLAUDE.md`](./CLAUDE.md)가 정본입니다.**
> **작업 시작 전 `CLAUDE.md`를 먼저 읽으세요.**

---

## 왜 파일이 둘인가

도구마다 자동으로 읽는 파일 이름이 다릅니다 — Claude Code는 `CLAUDE.md`, Codex는 `AGENTS.md`.
그래서 진입점만 두 개 두고, **내용은 `CLAUDE.md` 한 곳에서만 관리**합니다.

> **2026-08-22 통합.** 그전까지 두 파일이 157줄 중 18줄만 다른 채 **이중 관리**되고 있었고,
> `AGENTS.md`가 낡아 **폐지된 `T-xxx` 추적 체계와 존재하지 않는 `.Codex/` 폴더**를 안내하고 있었습니다.
> 규칙이 갈라지면 두 도구가 서로 다른 기준으로 작업하게 되므로 정본을 하나로 합쳤습니다.

## 도구별로 다른 것 — 이것만 여기서 정합니다

**커밋 트레일러는 실제로 작업한 도구 이름으로 적습니다.**

```
Co-Authored-By: Codex <noreply@anthropic.com>
```

다른 도구 이름을 복사하면 나중에 누가 무엇을 했는지 추적이 안 됩니다.

## 협업 방식 (2026-08-22)

Claude Code와 Codex를 **병행 구동**하고, 결과는 **파일과 GitHub 이슈 코멘트로 왕복**합니다.
한쪽이 다른 쪽을 CLI로 호출하지 않습니다.

> 2026-07-24에 `codex_review.sh` 방식(외부 CLI 견제)을 시도했다가 폐기한 이력이 있습니다 (커밋 `8d16a80`).
> 사유가 *"병행 구동으로 전환해 외부 CLI 견제 미사용"* 이었고, 그 판단을 유지합니다.

**동시 작업 시 충돌 방지** — 코드는 양쪽 다 읽기만 하고, 산출물은 각자 다른 파일에 씁니다.
