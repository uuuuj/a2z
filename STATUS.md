# Status

> PC 간 작업 상태 동기화 파일.
> 작업 끝낼 때 `/wrapup` 입력하면 자동 갱신됨.
> 작업 시작할 때 SessionStart hook이 이 내용을 자동으로 보여줌.

## 마지막 작업
**2026-05-02 (오후) — 회사 doc 동기화 추가 3건 (T-049 + T-050 + T-052)**

- **T-049** (긴급중 3) — `main-dimension.md` Section 7.5 "치수 캐시 라이프사이클" 신설. `chainDimensionList` 단일 진실 공급원 + 4경로 mermaid + 표
- **T-050** (긴급하 1) — `Form1.BOM.cs` 초기화 핸들러에 `vizcore3d.View.MarineAxis.Visible = true` 1줄 추가. 3D 뷰 좌측하단 X/Y/Z triad 표시
- **T-052** (긴급하 3) — Sheet 1 포함부재 표기 "전체" → "1, 2, 3, ..., N". 일반 시트 분기와 통합

**오전 세션 누적** (커밋 `8081688`): T-046 확장 (모든 보조선 SOLID + 10mm gap), T-053 (SheetNumber 재채번), T-055/T-056 (Osnap·Z-MAX 검증 보고서)

## 진행 중 (WIP)
- **빌드 검증 완료 대기** — A2Z.exe 실행 중이라 dll lock으로 bin/Debug 복사 실패 (컴파일은 통과). exe 닫고 재빌드 필요
- **사용자 실기 검증** — 3D 뷰 축 표시기 (T-050) / Sheet 1 포함부재 셀 (T-052) / 보조선 10mm gap (T-046) / SheetNumber 재채번 (T-053)
- **회사 doc 답변 송부 대기** — T-055/T-056 검증 보고서 + T-049 캐시 라이프사이클 정리

## 다음에 할 것
1. 데스크톱 A2Z.exe 닫고 재빌드해서 컴파일 + 복사까지 완전 통과 확인
2. `/commit` 으로 변경사항 커밋·push (T-049/T-050/T-052)
3. 노트북에서 `git pull` 후 `.\scripts\check-build-env.ps1` 실행 → 환경 일치 확인
4. 회사 doc 답변 송부 (T-055/T-056/T-049 보고서 + 진행 결과)
5. 다음 작업 후보:
   - **T-006 2차** — 2D 도면 그리드 추가 실험 (IN_PROGRESS, 큰 작업, SDK 조사부터)
   - **T-038 → T-039 → T-040 → T-041** 사슬 — 모델 스케일·치수 재설계 (큰 작업, 일부 사용자 답변 필요)
   - 사용자 실기 후 발견 사항(보조선 gap 미세 조정, 축 표시기 위치/크기 조정 등)
6. (선택) `a2z/lib/` 잔재 폴더 처리 결정 — 백업 유지 vs 삭제

