# Status

> PC 간 작업 상태 동기화 파일.
> 작업 끝낼 때 `/wrapup` 입력하면 자동 갱신됨.
> 작업 시작할 때 SessionStart hook이 이 내용을 자동으로 보여줌.

## 마지막 작업 (PC: He0in-PC / 2026-08-21 12:59)

**평면·측면·정면도 카메라 부호 정합** (브랜치 `HYI`, issue #248)

- PDF 4면도 매핑을 평면도=`Z_PLUS`, 측면도=`X_PLUS`, 정면도=`Y_MINUS`로 정정하고 3D 미리보기·치수 경로도 같은 기준으로 통일
- 정면도 화면 right가 +X가 되므로 Y뷰 전용 모델 수평 이동·2단 치수 텍스트 부호 반전을 함께 제거. 각도 진단 투영축과 데이터 매핑 문서도 동일 기준으로 정리
- Release 별도 출력 빌드 성공. 기본 `bin\Release` Rebuild는 실행 중인 A2Z·원격 디버거 파일 잠금으로 최종 복사만 실패했으며 소스 컴파일은 통과
- 도면 라벨 `Looking Z/X/Y`의 ± 표기는 담당자 확인 전까지 기존 템플릿을 유지

### 직전 스틴트 (2026-07-27)

**SDK 1.0.26.727 도입 + 빈칸 선초기화 제거 + 라이선스 폴백**

- **빈칸 선초기화 제거** (issue #60) — 벤더 안내 반영. `ImportExcelWithData`는 `data`에 값이 있으면(`""`·`" "` 포함) 치환하고 값이 없을 때만 `{Input}`으로 남기는데, `{Input}`으로 남은 셀만 TextBox가 되고 `RemoveEmptyTemplateBorders`는 그 TextBox가 있어야 괘선을 지운다. 전 슬롯 선초기화 때문에 **괘선 제거가 통째로 무동작**이었음. 제작도(1~240)·가공도(1~245) 루프 모두 삭제
- **라이선스 서버 폴백** (issue #61) — 로컬(`127.0.0.1:8901`) 실패 시 사내(`60.100.164.177:8901`)로 자동 전환. 예외 발생해도 다음 후보 진행, 성공 서버를 기억해 30분 갱신에 재사용, 전부 실패 시에만 MessageBox(후보별 사유 표시)
- **제작도 ISO 풍선 미출력 진단 로그** (issue #62) — 원인 미규명. 풍선 경로에 로그가 없어 `P2 ISO 풍선 … notes=/visibleNodes=/visibleNotes=` + 2D 변환 결과 로그 추가

Debug·Release 양쪽 빌드 통과(에러 0), bin 3곳 모두 SDK 727 반영 확인.

### 직전 스틴트 (2026-07-26)
출장 사내 PC 실기 검증 3건 정상 확인 — 가공도 치수·풍선 종이 절대 정규화(T-089, #46), 장시간 작업 취소 응답성·진행률(T-091, #51), 설치도 연결부재·외부 Assembly 점선 표시(T-075). 개발 현황 Excel: 전체 85 / 완료 53 / 실기 검증 대기 0 / 생산 필수 미확인 22 (기준일 2026-07-26).

## 진행 중 (WIP)

- **[ ] issue #248 — 평면·측면·정면도 실기 확인**. View_2=평면도(시선 −Z), View_3=측면도(시선 −X), View_4=정면도(시선 +Y)와 치수·보조선 위치를 사내 PDF/미리보기에서 확인
- **[ ] issue #62 — 제작도 ISO 부재번호 풍선 미출력**. 원인 미규명, 진단 로그만 심어둔 상태. **다음 실행 로그가 있어야 진행 가능**
- **[ ] issue #60 실기 확인 3항목** — ①빈 BOM 행 괘선이 실제로 지워지는지 ②`{Input}` 글자가 PDF에 노출되지 않는지 ③PAINT/DP/TAG(165~169)·REV 첫 기재행(194~199) 괘선 보존이 유지되는지
- **[ ] issue #61 실기 확인** — 로컬 정상 / 로컬 차단 시 폴백 / 둘 다 불가 시 메시지 / 30분 갱신
- **최종 산출물 생성** (T-043, issue #9) — 제작도·가공도·설치도 최종 생산 PDF 3종 + 메뉴얼·룰북. **과제 완료 기준이라 최우선**
- **왼쪽 패널 재설계** (issue #25) — 사양서 확정본 작성 완료, **승인 대기·코드 미착수**
- STRU 일괄 도면 출력 P2 잔여(T-064), 치수 로직 4경로 통합 실기 확인(T-028·T-032), 2D 그리드 clip 2차(T-006·T-037·T-038), 가공도 카메라 회전 잔존 케이스(T-036), 엑셀 템플릿 PoC Step 2(T-012)

## 다음에 할 것 (새 세션 1번째)

1. **평면·측면·정면도 1회 출력·미리보기 확인** (#248) — 뷰 방향과 Y뷰 치수·보조선이 모델 반대편으로 가지 않는지 확인
2. **제작도 1회 출력 → 진단 로그 확인** (#62) — `P2 ISO 풍선` 줄에서 `notes` / `visibleNotes` 중 어디가 0인지로 세 단계 중 하나로 좁힘
3. **#60 실기 3항목 확인** — ③이 깨지면 1차 대응은 **보존 슬롯만 `" "`로 되돌리기**(제거 대상은 키 생략 유지)
4. **최종 생산 PDF 3종 생성·점검** — 외부 입력 없이 진행 가능한 유일한 최우선 항목
5. 업무 규칙 확정 요청 3건 / API 대기 3건 일정 재확인

## 메모

- **브랜치**: 작업 브랜치 = `HYI`. **push는 HYI에만.** 원격 유지: `main`·`CJH`·`KSH`·`HYI`
- **SDK**: `lib/`에 **1.0.26.727** (2026-07-27 배포). 수정 사항 = 설정창 범위 오류, 빈 셀 라인 제거 시 일부 라인 미표시. `lib/*.dll`은 gitignore이므로 **다른 PC에서는 DLL을 직접 받아 넣어야 함**. 벤더 전달 메일(`*.eml`)도 gitignore — 내용은 관련 issue에 요약
- ⚠ **선초기화 제거 부작용 감시** — 원래 목적이 미치환 `{Input_N}` 태그 노출 방지였음. PDF에 `{Input}` 글자가 보이면 즉시 롤백 후보
- **하드코딩 잔존**: 프로젝트명 `CEDAR FLNG`, 선박번호 `SN2688`, DP No `Test` — 정식 데이터 소스 확정 필요
- **BOM 임시값**: Q'TY 항상 1, MA 데이터행 L·요약행 F, FA 항상 F — 생산값 아님
- **상태 관리 출처 = GitHub 이슈** (2026-07-28 전환, `docs/tracking` 폐지). 상태 변경은 이슈 `상태:` 라벨로만 한다
- **`개발 현황.xlsx`는 이슈에서 생성되는 읽기 전용 뷰** — 손으로 고치면 다음 생성에 덮어써진다. 갱신: `python scripts/build_status_xlsx.py` (Excel + `docs/tracking-archive/issues.json` 백업 동시 생성)
- **폐지된 tracking 원본**은 [docs/tracking-archive/](docs/tracking-archive/README.md)에 읽기 전용 보관 — 이관 경위와 07-26 Excel 스냅샷 포함
- **데이터 매핑 계약**: [docs/기술 노트/데이터 매핑 기준.md](docs/기술%20노트/데이터%20매핑%20기준.md) — 뷰 매핑·BOM 출처·표제부 배선·슬롯 번호. Excel `요구사항·UDA` 시트 이관본
- **계획서 위치**: `docs/리팩토링/도면-출력-통합.md` (v3.1), `docs/리팩토링/가공도-템플릿-설계.md` (v2.1), `docs/리팩토링/왼쪽-패널-재설계.md` (승인 대기)
- **설명 자료**: `docs/장표/` HTML 8종 (메뉴얼 2 + 룰북 6), `docs/사용자-매뉴얼/`
