# A2Z-HYI 프로젝트 — 작업 규칙

이 파일은 **AI 코딩 도구가 이 프로젝트에서 작업할 때 항상 지켜야 할 규칙**을 담습니다.

> 🔑 **도구 무관 단일 정본입니다.** Claude Code는 `CLAUDE.md`를, Codex는 `AGENTS.md`를 자동으로 읽는데,
> `AGENTS.md`는 이 파일을 가리키는 포인터일 뿐입니다. **규칙은 여기 한 곳에서만 관리합니다.**
> (2026-08-22 통합 — 그전까지 두 파일이 89% 중복이었고 이미 어긋나 있었습니다)

---

## 프로젝트 요약
VIZCore3D.NET SDK 기반 **3D→2D 도면 자동화 WinForms 앱** (C# / .NET Framework 4.8).
Form1 partial class 8개 + Models.cs 구조. 상세는 [`docs/README.md`](./docs/README.md), [`docs/_pipeline.md`](./docs/_pipeline.md) 참고.

---

## 핵심 규칙

### R1. 코드 변경 시 문서 동기화 (필수)
**현행 코드를 설명하는 문서의 정본은 [`docs/코드분석/`](./docs/코드분석/README.md) 이다** (2026-08-22 신설).

`A2Z/Form1.*.cs` 를 고쳤으면:

1. **손으로 쓴 문서** — `docs/코드분석/파일별/{수정한 파일}.md`
   실행 순서·상태·알고리즘이 바뀌었으면 갱신. 줄 번호도 함께.
   계산 규칙 자체가 바뀌었으면 `docs/코드분석/알고리즘/` 도 확인
2. **자동 생성 문서** — 손으로 고치지 말고 재실행
   ```bash
   python docs/코드분석/generate.py
   ```
3. 새로 발견한 죽은 코드·중복·하드코딩은 `docs/코드분석/판정/` 에 기록

포매팅·주석만 고쳤으면 2번만 돌리면 된다.

> ⚠ **`docs/code-reference/` 와 `docs/기능/` 은 폐기 예정이다.** 갱신하지 말 것.
> 낡았고(75개 중 60개가 7월 이후 정지), 일부는 화면에 없는 죽은 핸들러를 설명하고 있다.
> `docs/코드분석/` 이 다 채워지면 정리한다.
>
> 이 규칙은 도구를 가리지 않는다. Claude Code에는 훅(`.claude/hooks/docs-sync-reminder.sh`)이
> 리마인더를 띄우지만 **Codex와 직접 편집에는 걸리지 않는다.** 훅은 보조 장치일 뿐이다.

### R2. 작업 시작 시 컨텍스트 로드
**상태 관리 정본은 GitHub 이슈다** (2026-07-28 전환, `docs/tracking` 폐지). 새 작업 시작 전 다음을 훑고 관련 내역이 있으면 브리핑:

```bash
gh issue list --label "상태: 개발 중" --state open        # 지금 이어갈 작업
gh issue list --label "상태: 실기 확인" --state open       # 실기 확인 대기
gh issue list --label "생산 필수" --state open            # 생산 차단 요소
```

- [`STATUS.md`](./STATUS.md) — PC 간 인수인계·다음에 할 것 (구 `sessions/` 역할을 대체)
- 담당자 피드백은 `출처: 담당자 피드백`, 내부 요청은 `출처: 내부 요청` 라벨로 구분

브리핑 예: "지난번에 #64 진행 중이었고, #120 새로 올라왔어요. 이어갈까요 아니면 새 작업부터 보실래요?"

### R3. 작업 완료 시 커밋 (도구 무관)
코드·문서 변경 후 커밋한다. 커밋 시 함께 하는 것:
- 커밋 메시지 생성 (증상·원인·조치·검증)
- **관련 이슈에 코멘트로 완료 내역 기록** + `상태:` 라벨 갱신 (완료면 이슈 닫기)
- 별도 CHANGELOG 파일은 쓰지 않는다 — **기록을 두 곳에 남기지 말 것**

> Claude Code에는 이 절차가 `/commit` 슬래시 커맨드로 묶여 있다. **Codex 등 다른 도구는 커맨드가 없으므로 위 항목을 직접 수행한다.**

### R4. 세션 종료 시 STATUS.md 갱신
비자명한 작업을 한 세션은 종료 전 [`STATUS.md`](./STATUS.md)를 직접 갱신한다. 마지막 작업·진행 중·다음에 할 것·메모를 남겨 다른 PC에서 이어받을 수 있게 한다. 짧은 질의응답/탐색 세션은 생략.

STATUS.md와 이슈 코멘트가 세션 기록의 정본이다. 별도 세션 요약 파일은 새로 만들지 않는다.
(과거 `docs/tracking-archive/tracking-원본/sessions/`에 쌓인 것은 **보관 이력**이며 더 추가하지 않는다.)

> ⚠ `/wrapup`·`/tracking` 슬래시 커맨드는 **존재하지 않는다** (2026-08-22 확인). 실재하는 것은 `/checkpoint`·`/commit` 둘뿐이고, 그마저 Claude Code에서만 동작한다.

### R5. 🔴 자동 push 금지 — 커밋까지만 자동
**push는 자동으로 하지 않는다. 사용자 승인 후에만 push한다.**

2026-08-04 사용자 지시로 자동 push·push 훅·시크릿 스캔이 전수 폐지됐고, 2026-08-20 폐지 유지로 재확정됐다. 이전 R5·R12의 "자동 push" 조항은 **사문화된 기록**이며 따르지 않는다.

- 커밋(L2)까지가 자동 한계. push(L3)는 매번 사용자 승인
- 🔴 **이 레포는 현재 PUBLIC이다.** 시크릿 스캔도 없어졌으므로 push 전 사람이 내용을 확인해야 한다 — 사내 자료(담당자 발언 원문, 부재번호, 협력사명, xlsx)가 그대로 공개된다
- `--force`, `main`/`master` 직접 push, 히스토리 변경은 승인이 있어도 별도 재확인

### R6. 언어 · 스타일
- 대화·커밋 메시지·문서: **한국어**
- 변수명·함수명: 기존 C# 코드 컨벤션(PascalCase/camelCase) 유지
- 주석: 필요한 곳만 (CLAUDE.md 기본 방침 준수)

### R7. 담당자 피드백은 원문 보존
사용자가 담당자 메시지를 공유하면 **이슈를 만들고 `출처: 담당자 피드백` 라벨**을 붙인다. 본문에 **원문을 인용 블록으로 그대로** 남길 것. 피드백 요약·의역 금지 (뉘앙스 유실 방지). 대응 작업이 여러 개로 갈리면 별도 이슈로 쪼개고 서로 링크한다.

### R8. 본인 요청은 맥락 중심 기록
사용자가 "나중에 바꾸고 싶다/개선하고 싶다" 등 본인 아이디어를 말하면 **이슈를 만들고 `출처: 내부 요청` 라벨**을 붙인다. 본문 필수 항목: **배경(왜)**, **기대효과**. 즉시 작업 지시라면 출처 라벨 없이 바로 작업 이슈로 만든다.

### R9. 훅 리마인더는 신호일 뿐
`A2Z/Form1.*.cs` Edit/Write 후 `[docs-sync-reminder]` 시스템 메시지가 주입되면, 이는 **R1 이행을 상기시키는 신호**일 뿐 맹목적으로 따르지 말 것. 실제 흐름 변경이 있었는지 판단 후 docs 갱신 여부 결정. 리팩토링·포매팅이면 주저 없이 생략.

### R10. SDK API는 반드시 `VIZCore3D.NET.xml`로 확인 (도구 무관)
**VIZCore3D SDK 멤버(메서드/프로퍼티/enum)를 코드에 쓰거나 문서에 적기 전, 레포 루트 `VIZCore3D.NET.xml`에서 존재·시그니처·공식 사용 패턴을 확인한다. 추측 금지. 확인 못 했으면 `(미확인)`을 붙여 적는다.**

Claude Code는 `sdk-verifier` 서브에이전트로 자동화돼 있다. **다른 도구는 `VIZCore3D.NET.xml`을 직접 grep한다.** 이미 코드베이스에 반복 사용 중인 익숙한 API(`Model.Open`, `View.FitToView` 등)는 생략 가능.

### R11. docs/ 대량 수정 후 링크 검증 (도구 무관)
다수의 `docs/**/*.md`를 수정·추가한 커밋 직전에는 링크 공백·파일 부재 문제를 검증한다. 한글·공백 파일명이 많아 렌더러에서 잘 깨진다.

Claude Code는 `md-link-checker` 서브에이전트를 쓴다. **다른 도구는 직접 링크를 뽑아 파일 존재를 확인한다.** 단일 문서 소폭 수정은 생략 가능.

### R12. 검증 사이클 — 커밋까지 자동, push는 알리고 승인받는다
사용자는 빌드 결과를 **사내·다른 PC**에서 실기 검증한다. push되지 않은 변경분은 사용자 손에 도달하지 못한다. 그래서 코드 변경(`A2Z/Form1.*.cs` 등) 후 검증 흐름으로 넘어가기 전 **커밋까지는 자동으로 마감**한다.

- **push는 자동으로 하지 않는다** (R5). 대신 "검증하려면 push가 필요합니다 — 할까요?"를 **먼저 말한다**. 사용자가 모른 채 기다리게 두지 말 것
- 승인받으면 push하고, 원격 경로를 보고한다
- 검증 결과 추가 변경 요청 시 같은 사이클 반복 (변경 → commit → 승인 → push → 다시 검증)

> 구 R12는 "사용자 컨펌 후 push 패턴 금지"라고 **승인을 건너뛰라고** 적혀 있었다. 2026-08-04 자동 push 폐지 결정과 정면 충돌하는 사문화 조항이라 2026-08-22에 뒤집었다.

### R13. 사용자 대면은 기능 이름으로, 내부 문서는 ID 병기
**사용자에게 말하거나 사용자가 읽을 자료(대화·표·HTML·PDF 등)를 만들 때**는 `T-xxx`·`FB-xxx`·`REQ-xxx`·`SDK-xxx` 같은 추적 ID와 `P1`·`p3-3` 같은 단계 코드를 **쓰지 않는다**. 사용자는 그 코드가 무슨 작업을 가리키는지 외우지 못해, 볼 때마다 매핑하는 비용이 든다. 대신 **무슨 기능이고 무슨 증상인지 바로 알 수 있는 말**로만 적는다. ID·단계 코드는 도구 내부 추적용으로만 보유.

- ❌ `T-005 진행 중` / `P3 검증 단계` / `p3-3 겹침`
- ✅ `치수를 부재 바깥쪽으로 빼는 작업 진행 중` / `사내에서 직접 출력해보며 확인하는 단계` / `치수 숫자끼리 겹치는 문제`

**내부 문서·커밋 메시지(커밋·이슈 코멘트·계획서·STATUS.md 등)**는 GitHub 이슈 번호를 ID로 쓴다. 번호만 단독으로 두지 말고 한 줄 요지를 같이 적는다 — `관련: #67 (BOM 요약행 T/W)`.

이슈 번호는 사용자도 같이 보는 공용 ID라 위 금지 대상이 아니다. 폐지된 `T-xxx`·`FB-xxx`·`REQ-xxx` 체계는 `docs/tracking-archive/`에만 남아 있으므로 새로 쓰지 않는다.

### R14. GitHub Issue 제목·상태 접두사 관리
열린 GitHub Issue 제목은 반드시 아래 상태 접두사 중 하나로 시작하고, 같은 이름의 `상태:` 라벨을 **정확히 하나만** 사용한다.

| 제목 접두사 | 상태 라벨 | 적용 기준 |
|---|---|---|
| `[API 확인]` | `상태: API 확인` | 필요한 기능을 지원하는 API의 존재·시그니처·동작을 먼저 확인해야 함 |
| `[API 요청 필요]` | `상태: API 요청 필요` | 새 API 추가 또는 기존 SDK 수정이 필요하지만 아직 요청하지 않음 |
| `[API 대기]` | `상태: API 대기` | API 요청을 전달했고 회신·수정본·배포를 기다림 |
| `[분석 필요]` | `상태: 분석 필요` | 재현 조건, 요구 기준, 정책 또는 구현 방법 분석이 먼저 필요함 |
| `[개발 대기]` | `상태: 개발 대기` | 요구사항이 정리되어 구현 착수만 기다림 |
| `[개발 중]` | `상태: 개발 중` | 현재 구현 중임 |
| `[실기 확인]` | `상태: 실기 확인` | 코드 반영 후 실제 모델·출력 검증을 기다림 |

- API 관련 기본 흐름: `API 확인 → API 요청 필요 → API 대기 → 개발 대기 또는 실기 확인`.
- API 확인 결과 기존 API로 구현 가능하면 `API 요청 필요`를 거치지 않고 `개발 대기`로 변경한다.
- 단순히 등록만 하고 구현하지 않은, 요구사항이 확정된 Issue는 `[개발 대기]`로 등록한다.
- 상태를 바꿀 때는 상태 라벨을 변경한다. GitHub Action이 제목 접두사를 같은 상태로 자동 동기화한다.
- 완료 상태는 `[완료]` 접두사를 만들지 않고 Issue를 Close하여 표현한다.
- Issue를 Close하면 GitHub Action이 상태 라벨과 기존 상태 접두사를 제거한다. 종료 Issue에는 활성 상태 라벨·접두사를 남기지 않는다.
- 신규 Issue는 `.github/ISSUE_TEMPLATE/`의 해당 양식을 우선 사용한다. CLI로 등록할 때도 접두사와 상태 라벨을 함께 지정한다.

---

## 파일 구조 개요

```
a2z-HYI/
├── A2Z/                 # 앱 소스 (Form1 partial 8개 + Models.cs)
├── docs/
│   ├── README.md        # 문서 진입점
│   ├── 기능/            # 버튼 단위 흐름 문서 (60+, 8개 카테고리)
│   ├── 기술 노트/        # 보조선·치수·Osnap 사양 등 기준 문서
│   ├── code-reference/  # 파일별 코드 레퍼런스 (9, 영어 유지)
│   ├── 사용자-매뉴얼/    # 담당자용 버튼 사용 가이드
│   ├── 장표/            # 보고·설명 HTML 장표
│   └── tracking-archive/ # 폐지된 tracking 원본 + 이슈 JSON 백업 (읽기 전용)
│       ├── FEEDBACK.md  # 담당자 피드백 inbox (FB-xxx)
│       ├── REQUESTS.md  # 본인 수정 요청 inbox (REQ-xxx)
│       ├── TASKS.md     # 할 일 (TODO/IN_PROGRESS/DONE, T-xxx)
│       ├── CHANGELOG.md # 완료 이력 (날짜 역순)
│       └── sessions/    # 세션별 작업 요약
├── .claude/
│   ├── settings.json    # 공유 설정 (PostToolUse 훅 등)
│   ├── commands/
│   │   ├── commit.md    # /commit — 통합 커밋 워크플로우
│   │   └── checkpoint.md # /checkpoint — 세션 요약 저장
│   └── hooks/
│       └── docs-sync-reminder.sh  # Form1.*.cs 수정 시 docs 동기화 리마인더
├── CLAUDE.md            # 작업 규칙 정본 (이 파일)
└── AGENTS.md            # → CLAUDE.md 포인터 (Codex용)
```

---

## 커밋 메시지 형식

```
{type}: {한 줄 요약, 50자 이내}

- 변경 상세 1
- 변경 상세 2
- 관련: #67 (BOM 요약행 T/W)   ← GitHub 이슈 번호 + 한 줄 요지

Co-Authored-By: <작업한 도구> <noreply@anthropic.com>
```

> ⚠ **`Co-Authored-By`는 실제로 작업한 도구 이름으로 적습니다.**
> Claude Code → `Claude Opus 5` / Codex → `Codex` 등.
> **다른 도구 이름을 그대로 복사하지 마세요.** 나중에 누가 무엇을 했는지 추적이 안 됩니다.
> ⚠ 폐지된 `FB-xxx`·`T-xxx`·`REQ-xxx`는 쓰지 않습니다 (`docs/tracking-archive/`에만 존재).

**type**: `feat` (신규 기능) / `fix` (버그 수정) / `docs` (문서만) / `refactor` (리팩토링) / `chore` (설정·빌드) / `style` (포매팅)
